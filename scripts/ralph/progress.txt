## Codebase Patterns
- 单一权威优先级：产品范围看 docs/PRD_v1.md，技术实现/schema/make target 看 docs/tech-spec.md，真实云资源/URL 看 docs/runbook/cloud-setup.md，开发红线看 AGENTS.md。冲突时按此优先级。
- Python 用 uv workspace：根 pyproject.toml 只声明 dev 工具（mypy/ruff/pytest）+ 引用本地成员 soniscope-worker，业务依赖（oss/pydantic/pyyaml/typer/nls）放 apps/worker/pyproject.toml。
- Worker 包采用 src 布局：apps/worker/src/soniscope_worker/，hatchling 打包，`[tool.hatch.build.targets.wheel] packages = ["src/soniscope_worker"]`。
- 顶层 Makefile 是唯一命令入口；所有命令用 `uv run ...` 执行，用户无需 cd 子目录。target 命名遵循 tech-spec §6.5。
- 质量门：`make typecheck`（mypy strict）、`make lint`（ruff，只查 apps/）、`make test`（pytest，testpaths=apps/worker/tests）。
- mypy/ruff/pytest 配置集中在根 pyproject.toml；mypy 用 files=apps/worker/{src,tests} + mypy_path + explicit_package_bases 适配 src 布局。
- 运行时数据目录与 repo 分离：$SONISCOPE_HOME（默认 ~/SoniScope），由 paths.py 解析；config.yaml、运行时目录、测试音频二进制均 gitignore，不进 git。
- 不要“修正”真实云资源里的拼写，尤其 issue-cedential-ottfirocds（少一个 r）。
- 遗留 scripts/test_asr.py 暂不纳入 lint（ruff 只查 apps/），由其归属 story 收口。
- scripts/ 下脚本「收口」模式（US-003 fetch_test_fixtures.py）：可测逻辑抽进 Worker 包（如 soniscope_worker/fixtures.py，自动被 mypy strict + ruff + pytest 覆盖），scripts/ 脚本作薄 CLU/IO 壳（OSS 下载等）复用之；脚本顶部 `sys.path.insert(0, REPO_ROOT/"apps/worker/src")` 后再 `from soniscope_worker... import`（带 `# noqa: E402`），使裸 `python3 scripts/xxx.py`（未装包）也能导入。
- fixture 校验：sha256 是唯一权威校验源（runbook §6），duration/codec 用 ffprobe 实测做 ±2s/容器编码的辅助校验；期望值数据驱动放 tests/audio/fixtures.manifest.json（duration_seconds 存 sha256 权威文件的实测时长，可能与文件名/runbook 标称值不同）。校验失败统一输出 fixtures.FIX_HINT 指向 runbook 第 6 节。
- 敏感字段脱敏：用 `MaskedSecret(SecretStr)` 子类重写 `_display()` 显示前后 4 位（`mask_secret`：>8 位显示首尾各 4，≤8 位整体打码）。Pydantic 自动把 str 验证成 MaskedSecret，业务侧用 `.get_secret_value()` 取明文，repr/str/日志只出脱敏摘要。
- make 命令优先做成 Worker CLI 子命令（typer），Makefile 只 `uv run python -m soniscope_worker <cmd>`；逻辑放 Python 便于 mypy/pytest 覆盖，CLI 失败用 `raise typer.Exit(code=1)` 保证非零退出。
- 配置加载：`load_config(path=None)` 默认走 `config_path()`，可注入路径便于测试；Pydantic ValidationError 用 `.errors()` 按 `type=="missing"` 汇总，一次性列全缺失字段（dotted loc）。
- mypy strict 下测试夹具参数用 `Mapping[str, object]`（covariant），不要用裸 `dict`/`dict[str, object]`（invariant 会报 arg-type）。
- 真实云端校验命令（live/verify-prep 类）模式：把"取数据"（IO/云 SDK）收敛到一个 `Probes` Protocol，纯校验逻辑 `check_*` 只对已取回的结构化 dataclass 做判断（无 IO，直接单测）；单测注入 `FakeProbes` 不触网，真实运行用 `RealProbes`（lazy import 云 SDK，缺失抛 `ProbeError`，由编排层 try/except 收敛为单项 fail）。云 SDK 无 stub：在根 pyproject 用 `[[tool.mypy.overrides]] module=["alibabacloud_oss_v2.*"] ignore_missing_imports=true`，lazy import 函数返回 `Any` 即可消除 attr-defined。
- 校验报告绝不打印 AK Secret：必填字段非空检查只列字段名不列值；用 `.get_secret_value()` 仅做"是否为空"判断后丢弃。单测专门断言 secret 明文不出现在报告里。
- 小程序（apps/miniprogram/）是 JS，不进 mypy/ruff（两者只覆盖 apps/ 下的 .py）。其静态检查走 Python CLI 子命令 `soniscope_worker.miniprogram_lint`（make lint 串联 `ruff check apps/` + `python -m soniscope_worker lint-miniprogram`），逻辑放 Worker 包内可被 mypy/ruff/pytest 覆盖；node v22 可用但未引入 npm/eslint。小程序合法域名/常量单一真实来源在 apps/miniprogram/config.js（US-012+ 一律 require 取用，勿散落硬编码）；脱敏日志在 apps/miniprogram/utils/logger.js（传对象自动按敏感键名脱敏）。WeChat DevTools 模拟器/真机类 AC 本沙箱无法自动验证，用 node --check + JSON.parse 做最大化静态验证后在进度里标注需人工复验。
- Worker 文件落盘遵守三段式协议（先临时→原子 os.replace→最后 .done）：临时文件名用 `<id>.part`（下载）/`<id>.wav.tmp`（转码）；inbox/tmp/fragments 同一文件系统保证 rename 原子。失败（探测/转码）只动 inbox（留档 inbox/failed/<id>.part + 清 .wav.tmp），**绝不创建 fragment 完成目录**，杜绝半成品完成态。本机 ffmpeg/ffprobe + tests/audio 真实 fixtures 实跑的端到端校验只放 make test-* target；Worker 标准化逻辑在 audio.py，可注入 probe/transcode，单测全程不触系统工具。
- ffmpeg 转码陷阱：输出文件名带 `.tmp`（如 `<id>.wav.tmp`）时 ffmpeg 无法按扩展名推断 muxer，必须显式 `-f wav`（否则报 "Error initializing the muxer ... Invalid argument"）；输入文件靠内容探测无需 `-f`。消费源文件（os.replace 移走 / unlink）前务必先算 original_sha256/size。
- 可注入默认值要 monkeypatch 友好：函数签名里需被单测替换的「默认依赖」（如 duration_probe/today/clock）不要在 def 处直接绑定模块级函数（`def f(probe=_probe)`，def-time 绑定后 monkeypatch 模块属性无效），而是默认 `None` 再在函数体首行 `probe = arg if arg is not None else _probe` call-time 解析。轮询/重试类逻辑统一注入 sleep/monotonic（假时钟 _Clock：monotonic 读 t 不自前进 + sleep 记录列表），用 side-effect 回调推进时钟做边界（超时/续签）确定性单测。

---

## 2026-06-27 00:20 - US-001
- 实现了什么：建立 monorepo 骨架与 Python Worker 基础。
  - 根 pyproject.toml：uv workspace（members=apps/worker），dev 工具组（mypy/ruff/pytest），mypy strict + ruff + pytest 配置集中于此；不直接声明 Worker 业务依赖。
  - apps/worker/pyproject.toml：包名 soniscope-worker，src 布局，hatchling 构建，依赖 typer，CLI 脚本入口 soniscope_worker.cli:app。
  - Worker 初始模块：__init__.py（__version__）、__main__.py（python -m soniscope_worker 入口）、cli.py（typer app，run/version 命令占位）、config.py（config_path 占位，完整 schema 留 US-002）、paths.py（soniscope_home/inbox/fragments/tmp 解析）。
  - 顶层 Makefile：install/check-config/init-dirs/worker-run/typecheck/lint/test + help；check-config、init-dirs 为占位（完整逻辑 US-002）。
  - apps/worker/tests/test_skeleton.py：5 个冒烟测试（版本、CLI run/version、SONISCOPE_HOME 解析与回退）。
  - .gitignore：补充 /SoniScope/、config.yaml、*.env（build/、.env、tests/audio/*.wav|m4a 原已忽略）。
  - uv.lock 由 make install (uv sync) 生成并纳入 git。
- 更改的文件：pyproject.toml、apps/worker/pyproject.toml、apps/worker/src/soniscope_worker/{__init__,__main__,cli,config,paths}.py、apps/worker/tests/test_skeleton.py、Makefile、.gitignore、uv.lock、scripts/ralph/prd.json（US-001 passes=true）。
- 验证：make typecheck（mypy 6 文件 0 错）、make lint（ruff apps/ 全过）、make test（5 passed）全部通过；`python -m soniscope_worker version/run` 正常；git check-ignore 确认 .venv 与测试音频不入库。
- **未来迭代的学习：**
  - 陷阱：首次 `uv sync` 较慢（解析 + 下载约十几分钟，受网络影响），后续走缓存很快；macOS 跨文件系统会有 reflink 警告，可 `export UV_LINK_MODE=copy` 抑制。
  - 陷阱：`ruff check .` 会扫到遗留 scripts/test_asr.py 报 E501 等 12 个错；本仓库 lint 故意只查 apps/，新代码放 apps/ 下即纳入检查。
  - 上下文：US-002 接手 config.py（Pydantic v2 schema + 脱敏加载 + 缺失字段一次列全）、check-config、init-dirs，并在 worker 依赖里加 pydantic/pyyaml；make target 命名见 tech-spec §6.5。
  - 上下文：mypy 用 strict，新增模块需完整类型注解；typer 命令函数返回 None 要显式标注。
---

## 2026-06-27 00:42 - US-002
- 实现了什么：配置 schema、脱敏加载器与运行时目录初始化。
  - config.py：Pydantic v2 schema（OSSConfig/PollConfig/TranscriberConfig/LocalConfig/SoniScopeConfig），覆盖 AC 列出的全部字段（含 transcriber.upload_mode、local.enabled）。`MaskedSecret(SecretStr)` 对 access_key_secret/appkey 脱敏（前后 4 位）。`load_config()` 文件缺失/非映射/字段缺失均抛 ConfigError；缺失字段一次性列全。`config_permission_is_600()` 权限检查。`masked_summary()` 输出脱敏摘要。
  - paths.py：新增 inbox_failed_dir()、runtime_dirs()、init_runtime_dirs()（幂等创建 inbox/、inbox/failed/、fragments/、tmp/）。
  - cli.py：新增 check-config（校验→脱敏摘要→600 权限警告，失败 exit 1）与 init-dirs 子命令。
  - Makefile：check-config / init-dirs 改为调用 `uv run python -m soniscope_worker <cmd>`。
  - 依赖：apps/worker 加 pydantic>=2、pyyaml>=6；根 dev 组加 types-pyyaml。
  - 测试：apps/worker/tests/test_config.py（19 个用例，含 mask、schema 覆盖、脱敏不泄漏、缺失字段全列、权限、幂等目录、CLI 成功/失败/init-dirs）。
- 更改的文件：apps/worker/pyproject.toml、pyproject.toml、apps/worker/src/soniscope_worker/{config,paths,cli}.py、Makefile、apps/worker/tests/test_config.py、uv.lock、scripts/ralph/prd.json（US-002 passes=true）。
- 验证：make typecheck（mypy 7 文件 0 错）、make lint（ruff 全过）、make test（19 passed）全部通过；手动用临时 SONISCOPE_HOME + config.yaml 跑 `check-config`（644 权限正确告警、密钥脱敏为 ossS...CDEF）与 `init-dirs`（四目录创建）通过。
- **未来迭代的学习：**
  - 陷阱：SecretStr 默认 repr 是全打码 `**********`；要前后 4 位需子类重写 `_display()`，不要直接改 `__repr__`。
  - 上下文：US-003 接手 scripts/fetch_test_fixtures.py（fixture 拉取/校验 sha256+duration+codec），sha256 基线见 AGENTS.md 测试音频表；ruff 仍只查 apps/，该脚本归属 US-003 时再决定是否纳入。
  - 上下文：config 的 transcriber 字段（appkey=1k8tqkjQsq65wp2m、provider=aliyun-nls、api_endpoint=cn-beijing）将被 US-025/US-026 的 Transcriber 工厂与 NLS 实现复用。
---

## 2026-06-27 01:05 - US-003
- 实现了什么：测试音频 fixture 拉取与校验脚本（sha256 + ffprobe duration ±2s + codec/容器）。
  - apps/worker/src/soniscope_worker/fixtures.py（新增，mypy strict + ruff + pytest 覆盖）：
    Fixture/Manifest/MediaInfo/VerifyResult 数据类；load_manifest()（缺失/非法抛 FixtureError）；
    sha256_of()（流式）；probe_media()（ffprobe -print_format json，不信任扩展名，ffprobe 缺失/失败抛 FixtureError）；
    codec_matches()（wav→format 含 wav 或 pcm_*；m4a→format 含 m4a/mp4 或 codec=aac）；
    verify_fixture()（只读：存在性→sha256→duration ±2s→codec；check_media=False 跳过 ffprobe）；
    常量 DURATION_TOLERANCE_SECONDS=2.0、FIX_HINT（指向 docs/runbook/cloud-setup.md 第 6 节）。
  - scripts/fetch_test_fixtures.py（重写为薄壳）：复用 fixtures.py 校验逻辑；保留 OSS 下载（lazy import）；
    --check 只校验不下载不修改；正常/--force 下载 .part→size+sha256 校验→原子 rename→ffprobe 复校；
    顶部 sys.path 插入 apps/worker/src，裸 python3 也能 import soniscope_worker.fixtures。
  - tests/audio/fixtures.manifest.json：duration_seconds 改为 sha256 权威文件的 ffprobe 实测值
    （20s.wav→24、54s.wav→54、25min.wav→1536、20s.m4a→24），加 _comment_duration 说明与标称值差异以实测为准。
  - apps/worker/tests/test_fixtures.py（新增 19 用例）：monkeypatch probe_media 打桩 ffprobe，
    覆盖 manifest 解析/缺失/非法、sha256、codec wav/m4a(aac)/容器/不匹配、duration 容差内外、
    缺失文件、check_media=False 跳过、FIX_HINT 指向 runbook 第 6 节。
- 更改的文件：apps/worker/src/soniscope_worker/fixtures.py（新）、scripts/fetch_test_fixtures.py（重写）、
  tests/audio/fixtures.manifest.json、apps/worker/tests/test_fixtures.py（新）、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 9 文件 0 错）、make lint（ruff apps/ 全过）、make test（38 passed）全部通过；
  `python3 scripts/fetch_test_fixtures.py --check` 对本地 4 个真实 fixture 全部「校验通过」exit=0
  （真实 ffprobe + sha256，m4a 经 codec=aac 路径识别通过）。
- **未来迭代的学习：**
  - 陷阱：sha256 权威文件实测时长与 AC/runbook 标称值不一致（sample-20s.wav 实为 24.02s、25min 实为 1536s）；
    sha256 是唯一权威校验源，duration 期望值应以实测值入 manifest，否则 --check 会误判权威文件不合格。
  - 陷阱：ffprobe format.duration 可能为 None（某些容器只有 stream.duration），probe_media 已回退到流时长并显式 None 兜底，
    否则 mypy 报 float(Any|None) arg-type。
  - 上下文：US-004 make verify-prep 的「F 块」fixture 校验可直接复用 soniscope_worker.fixtures.verify_fixture/load_manifest，
    无需重复实现 sha256/duration/codec 逻辑。
  - 上下文：scripts/ 脚本要进 pytest，把逻辑放 Worker 包内（testpaths=apps/worker/tests），脚本只留 IO 壳。
---

## 2026-06-27 01:40 - US-004
- 实现了什么：make verify-prep 一键校验 US-001 人工准备产物（Worker CLI 子命令 verify-prep）。
  - apps/worker/src/soniscope_worker/verify_prep.py（新增，mypy strict + ruff + pytest 覆盖）：
    Probes Protocol + RealProbes（真实探针）+ 纯校验逻辑 check_*：
    · check_env：Python>=3.11、SONISCOPE_HOME 可写、可用磁盘>=50GiB、ffmpeg/ffprobe 可执行（全 stdlib，真实可跑）。
    · check_config_security：config.yaml 权限=600 + 13 个必填字段非空（只列字段名，不打印任何值/AK Secret）。
    · check_oss_bucket：Bucket soniscope-audio 存在、region=cn-beijing、ACL=private（RealProbes 用 alibabacloud-oss-v2 lazy import 真实查 get_bucket_info）。
    · check_fc：两个 FC 公网 URL（含 issue-cedential 少一个 r 的真实拼写）urllib 真实可达且非 5xx。
    · check_sts_escape：4 个 STS 越权反例（越权 PutObject 其他 key / ListBucket / GetObject / 过期后 PutObject）全 Denied/Expired 才 pass（纯逻辑已实现+单测）。
    · check_nls_result：NLS 转写返回结构符合 tech-spec §3.4（segments 非空 + language/model/params_version/provider）。
    · 编排 run_checks/format_report/all_passed/run_verify_prep：逐项 try/except 收敛 ProbeError 为单项 fail；全绿打印 `✅ US-001 preparation verified. Ready for US-003+`，否则非零退出。
  - cli.py：新增 verify-prep 子命令（lazy import run_verify_prep，逐行 echo，typer.Exit(code)）。
  - Makefile：新增 verify-prep target（uv run python -m soniscope_worker verify-prep）+ .PHONY。
  - pyproject.toml：mypy overrides 让 alibabacloud_oss_v2.* ignore_missing_imports（云 SDK 无 stub）。
  - apps/worker/tests/test_verify_prep.py（新增 28 用例）：FakeProbes 注入全程不触网，覆盖每个 check_* 的 pass/fail、
    secret 不泄漏、ProbeError 收敛、缺失/权限错误 config、汇总与成功末行、CLI 非零退出、RepoLayout 路径。
- 更改的文件：apps/worker/src/soniscope_worker/{verify_prep.py(新),cli.py}、Makefile、pyproject.toml、
  apps/worker/tests/test_verify_prep.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 11 文件 0 错）、make lint（ruff apps/ 全过）、make test（66 passed）全部通过；
  `python -m soniscope_worker --help` 显示 verify-prep；CLI 在无 config 时优雅非零退出（不触网）。
  未跑：真实 `make verify-prep` 全链路（需真实 config.yaml 凭证 + 触网 OSS/FC/STS/NLS，本环境不具备）。
- **未来迭代的学习：**
  - 重要诚实声明：D 块（STS 越权反例）与 F 块（NLS 真实转写）的 RealProbes 目前抛 ProbeError 占位，
    真实云端执行分别依赖 US-007/US-008（issue-credential + STS 单 key policy）与 US-026（CloudSpeechTranscriber/alibabacloud-nls）。
    这两个 story 落地后，应把 RealProbes.sts_escape / nls_transcribe 接到各自已 live 验证过的实现上（纯校验逻辑 check_sts_escape/check_nls_result 已就绪、已单测，无需改）。
    在此之前，真实 `make verify-prep` 会在这两项报 FAIL 并指向对应 story —— 这是有意为之，避免在无法 live 验证时塞入未经验证的云代码。
  - 上下文：OSS bucket 探针与 fixture 校验（复用 soniscope_worker.fixtures.verify_fixture/load_manifest）已是真实可跑的；
    一旦本机装好 alibabacloud-oss-v2 且 config 有效，A/B/C/E/G 五块即可真实出结果。
  - 陷阱：ruff line-length=100 对 CJK 按显示宽度计数，含中文的长 fix_hint 容易超限；用括号续行或 if/else 表达式换行拆开。
---

## 2026-06-27 02:10 - US-004（第 2 次：修复 STS / NLS 真实 IO 占位）
- 背景：Validator 第 1 次判定 US-004 失败——RealProbes.sts_escape / nls_transcribe 是硬编码
  `raise ProbeError` 占位（指向 US-007/US-026），无论云端是否就绪都不执行真实云调用，
  导致 AC#4（STS 越权反例）/AC#6（NLS 真实转写）永远 FAIL、AC#9 末行不可达。
- 修复（apps/worker/src/soniscope_worker/verify_prep.py）：把两个占位替换为**真实 IO**，
  并按既有 Probes 模式把可测逻辑抽成纯函数（无 IO、已单测），IO 用 lazy import 云 SDK：
  · RealProbes.sts_escape：读 ALIYUN_DEPLOY_AK_ID/SECRET（缺则 ProbeError）→ 用部署凭证
    AssumeRole（alibabacloud-sts20150401 + tea-openapi）签发**单 object key、duration<=900s**
    的临时凭证 → 用临时凭证建 OSS client（带 security_token）跑 4 个越权反例：
    ①越权 PutObject 到其他 key ②ListObjectsV2 ③GetObject ④等到凭证过期后再 PutObject；
    每例用 `_run_oss_op` 捕获异常并 `_oss_error_code` 提取错误码，`is_denied` 判定是否如预期
    被拒（越权要 AccessDenied；过期要 ExpiredToken/等价过期或拒绝码）。AssumeRole/建 client
    失败收敛为 ProbeError（仅含异常类名，不泄漏明文）。
  · RealProbes.nls_transcribe：本地 fixture 存在性校验后，用 OSS 签名 URL（1 小时，
    `_presign_sample_url` 对 sample/sample-20s.wav presign）→ NLS 录音文件识别 filetrans
    POP API（aliyunsdkcore CommonRequest，SubmitTask+GetTaskResult 轮询，复用 scripts/test_asr.py
    基线）→ `nls_response_to_result` 把 Result.Sentences 映射为 transcript.json schema
    （segments[start/end/text]、language=zh、model/params_version/provider 取自 config）。
  · 新增纯函数 single_key_policy / is_denied / nls_response_to_result（§4.4 / §3.4），均已单测。
- 其它改动：pyproject.toml mypy overrides 增加 alibabacloud_sts20150401.* / alibabacloud_tea_openapi.*
  / aliyunsdkcore.*（lazy import 云 SDK 无 stub）。test_verify_prep.py 新增 8 用例：
  policy 单 key 无通配符、is_denied 分类、NLS 响应映射（通过/空 segments）、RealProbes 缺部署凭证
  →ProbeError、缺 SDK→ProbeError（不抛裸异常）、缺音频→ProbeError、STS duration<=900。
- 更改的文件：apps/worker/src/soniscope_worker/verify_prep.py、apps/worker/tests/test_verify_prep.py、
  pyproject.toml、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 11 文件 0 错）、make lint（ruff 全过）、make test（74 passed）全部通过。
  真实跑 `make verify-prep`（临时 config + 假部署凭证、本机未装云 SDK）：13/16 通过，STS/NLS/OSS
  三项 FAIL 均为「缺少依赖 …（环境）」——证明已是**真实 IO 路径**（STS 越过部署凭证门控进到
  AssumeRole 的 SDK lazy import；NLS 进到 OSS presign），不再是指向 US-007/US-026 的逻辑占位；
  报告 0 处密钥明文泄漏。未跑：真实云端全绿（需在 Worker 主机装 alibabacloud-oss-v2/
  alibabacloud-sts20150401/aliyun-python-sdk-core + 有效 AK/部署凭证，本沙箱不具备）。
- **未来迭代的学习：**
  - 云 SDK 一律 lazy import + 缺失抛 ProbeError（不进 hard deps），与已被验收接受的 OSS 探针一致；
    真实主机按 verify-prep 输出的 `uv add --package soniscope-worker <pkg>` 提示安装即可全绿。
  - STS 过期反例（AC#4 第 4 例）会真实 `time.sleep` 到凭证过期（AssumeRole 最短 900s）+缓冲，
    因此真实 `make verify-prep` 的 D 块至少耗时 ~15 分钟；这是 AC 要求的真实过期校验，单测用
    FakeProbes 不触发该 sleep。
  - NLS 录音文件识别用的是 filetrans POP API（aliyunsdkcore，version 2018-08-17，product
    nls-filetrans），不是 tech-spec §6.3 字面列的 alibabacloud-nls20180628（那是实时流式 SDK）；
    录音文件识别的可用基线脚本是 scripts/test_asr.py，US-026 的 CloudSpeechTranscriber 可复用本处
    nls_response_to_result 映射逻辑。
  - OSS v2 临时凭证客户端：StaticCredentialsProvider 必须带 security_token，否则 STS 凭证无效。
---

## 2026-06-27 02:45 - US-005
- 实现了什么：FC 3.0 函数工程目录约定 + 打包/备份/部署/回滚/日志脚本基线（make deploy-fc / rollback-fc / fc-logs）。
  - apps/fc/ 目录约定（AC#1）：issue_credential/ 与 verify_upload/ 各含占位 WSGI handler.py（真实逻辑 US-006/007/009）
    + requirements.txt 占位（注释行不触发依赖安装）+ apps/fc/README.md（云端名 kebab ↔ 代码目录 snake 映射表、真实 URL）。
  - apps/worker/src/soniscope_worker/fc_deploy.py（新增，mypy strict + ruff + pytest 覆盖）：沿用 verify_prep 的
    「纯逻辑 + IO Protocol」模式。
    · 纯逻辑（无 IO，直接单测）：source_dir_name（kebab→snake）、resolve_functions（None→两个函数 AC#6；未知报错）、
      function_url（含 issue-cedential 少一个 r 的真实拼写）、read_requirements、zip_dir（确定性 zip + sha256）、
      package_function（打包代码+运行依赖到 build/fc/<name>/ + 同名 .zip，AC#2）、find_latest_backup（按时间戳目录字典序）、
      curl_ok、format_deploy_log（函数名/zip sha256/上传耗时/curl 结果，AC#5）、format_report。
    · 编排：deploy_one（备份 best-effort → 打包 → 仅 update_code → curl 存活验证；首次部署无备份不阻断）、
      rollback_one（最新备份恢复 AC#7）、run_deploy/run_rollback/run_fc_logs。
    · IO 注入点 FcApi Protocol：download_code/env_var_names/install_deps/update_code/curl/fetch_logs；
      _write_backup 仅记录环境变量名、绝不记录值（AC#4 + 安全红线）。
    · RealFcApi（lazy import alibabacloud-fc20230330，缺 SDK/凭证抛 FcApiError）：update_code 只传 code、
      不传 environment_variables/triggers/运行规格（AC#3 不改环境变量/触发器/规格/URL）；fetch_logs 检查 SLS
      project/logstore，未配置时给明确诊断（AC#8）。
  - cli.py：新增 deploy-fc / rollback-fc / fc-logs 三个 typer 子命令（lazy import，typer.Exit(code)）。
  - Makefile：新增 deploy-fc（FUNCTION 可选，不传部署两个）、rollback-fc、fc-logs 三个 target + .PHONY。
  - pyproject.toml：mypy overrides 增加 alibabacloud_fc20230330.*（云 SDK 无 stub）。
  - apps/worker/tests/test_fc_deploy.py（新增 44 用例）：FakeFcApi 注入全程不触网，覆盖命名映射、resolve、
    打包（含 __pycache__ 排除 / 依赖安装 / 源码缺失）、备份只记名不记值、find_latest_backup、deploy_one
    四条路径（happy / 首次无备份 / 上传失败 / curl 5xx）、rollback、日志/报告渲染、run_* 入口与退出码。
- 更改的文件：apps/fc/{issue_credential,verify_upload}/{handler.py,requirements.txt}、apps/fc/README.md、
  apps/worker/src/soniscope_worker/{fc_deploy.py(新),cli.py}、Makefile、pyproject.toml、
  apps/worker/tests/test_fc_deploy.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 13 文件 0 错）、make lint（ruff apps/ 全过）、make test（110 passed，+44）全部通过；
  CLI --help 显示三命令；deploy-fc 未知函数 / rollback-fc 空 FUNCTION 均非零退出；build/ 已 gitignore。
  未跑：真实 make deploy-fc 全链路（需 ALIYUN_DEPLOY_AK_ID/SECRET + alibabacloud-fc20230330 + 触网，本沙箱不具备）。
- **未来迭代的学习：**
  - apps/fc/ 会被 ruff（ruff check apps/）扫描但**不**被 mypy（files=apps/worker/{src,tests}）检查；
    FC handler 占位代码要通过 ruff（E/F/I/UP/B），无需 mypy 注解，但建议仍写类型便于 US-006+ 接手。
  - FC SDK alibabacloud-fc20230330 仅部署脚本用、不随函数代码打包（tech-spec §6.4 依赖隔离）；lazy import + 缺失
    抛 FcApiError，与 verify_prep 的云 SDK 处理一致；真实主机按报错提示 uv add 即可。
  - US-008（issue-credential 云端联调）落地时：① 把 RealFcApi.fetch_logs 接到 aliyun-log-python-sdk 真实拉 SLS 日志
    （当前是诊断占位）；② make test-fc-live 复用本处 deploy_one/curl 存活验证。verify_prep 的 FC 块（check_fc）也可在
    函数真实部署后转绿。
  - US-006/007/009 实现 FC 真实逻辑时：替换 apps/fc/*/handler.py 占位，并在对应 requirements.txt 填真实依赖
    （非注释行会触发 package_function → api.install_deps 用 uv pip install --target 装进暂存目录一起打包）。
  - oss-delete-obj（US-010 首交付）/ show-oss-object（US-017 首交付）不属于 US-005，未实现。
---

## 2026-06-27 03:30 - US-006
- 实现了什么：FC 两个顶级 Web 函数（issue-credential / verify-upload）复用的共享鉴权 / 请求校验 / 安全日志模块 fc_shared，并把两个 handler.py 占位接入该模块。
  - apps/fc/shared/fc_shared/（新增包，mypy strict + ruff + pytest 全覆盖）：
    · errors.py：稳定错误码 INVALID_CODE(401)/OPENID_NOT_ALLOWED(403)/INVALID_REQUEST(400)/SERVER_MISCONFIGURED(500)；FcHttpError(status+error_code+安全 payload，绝不含敏感值)、FcConfigError(只列缺失变量名)。
    · env.py：DEFAULT_REQUIRED_VARS=OSS_BUCKET/OSS_REGION/OSS_ENDPOINT/WX_APPID/WX_APP_SECRET/OPENID_ALLOWLIST；load_env() 缺失一次列全（AC#3）；FcEnv frozen dataclass；parse_allowlist() 逗号分隔去空白去空项（AC#2）。
    · http.py：read_json_body（空/非法 JSON/非对象→400 INVALID_REQUEST，AC#5）、require_fields（缺字段/空串→400，0 视为存在）、json_response/error_response/status_line。
    · wechat.py：code_to_openid 调 jscode2session，任意失败（网络/errcode/缺 openid）统一 401 INVALID_CODE，不泄漏 code/secret/session_key（AC#1）；HTTP 拉取经 fetch 注入便于单测。
    · auth.py：authorize_request 编排「解析体→require_fields(含 code)→换 openid→check_allowlist」；不在 allowlist→403 OPENID_NOT_ALLOWED（AC#2）；返回 AuthContext(openid、openid_hash、body)。
    · audit.py：hash_openid（sha256 前 16）、is_sensitive（显式名 + 子串兜底 secret/token/appkey/api_key/session_key/password）、log_event（敏感字段 ***REDACTED***、None 省略、按名排序，print 到 stdout 供 FC 采集，AC#4）。
  - apps/fc/issue_credential/handler.py、apps/fc/verify_upload/handler.py：从 US-005 占位改为 import fc_shared；GET=存活探针，POST=load_env→authorize_request→脱敏 log_event；FcConfigError→500、FcHttpError→error_response；鉴权通过后返回占位（STS 签发=US-007、HeadObject=US-009）。
  - fc_deploy.py：package_function 新增 _vendor_shared，把 apps/fc/shared/fc_shared vendoring 到每个函数包根（部署后两函数都能 import fc_shared，AC#6）；共享包不存在时静默跳过（兼容单测临时仓库）。
  - pyproject.toml：mypy files/mypy_path、ruff src、pytest testpaths 加入 apps/fc/shared + apps/fc/tests；pytest pythonpath=apps/fc/shared 使 fc_shared 可被导入（非安装包）。
  - apps/fc/tests/test_fc_shared.py（26 用例）+ test_fc_handlers.py（12 用例，importlib 动态加载两 handler 参数化验证存活/500/400/403/200 五路径，并断言 mod.fc_shared is fc_shared）。
- 更改的文件：apps/fc/shared/fc_shared/{__init__,errors,env,http,wechat,auth,audit}.py(新)、apps/fc/{issue_credential,verify_upload}/handler.py、apps/worker/src/soniscope_worker/fc_deploy.py、pyproject.toml、apps/fc/tests/{test_fc_shared,test_fc_handlers}.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 22 文件 0 错）、make lint（ruff apps/ 全过）、make test（148 passed，+38）全部通过。未跑：真实云端（微信 jscode2session 需真实 AppSecret+code，OSS/STS 在 US-007/009 接入）。
- **未来迭代的学习：**
  - 两个 FC handler.py 文件同名，mypy 会模块名冲突——故 mypy files 只纳入 apps/fc/shared（fc_shared 包），不纳入 apps/fc/<fn>/handler.py；handler 仅受 ruff 检查 + pytest 用 importlib 以唯一模块名（fc_handler_<dir>）动态加载验证。
  - fc_shared 不是安装包，pytest 用 pyproject [tool.pytest.ini_options] pythonpath=["apps/fc/shared"] 让测试能 import fc_shared；部署时由 fc_deploy._vendor_shared vendoring 进函数包根（不走 pip/uv）。
  - US-007（issue-credential STS 签发）：在 handler 鉴权通过分支替换占位，复用 ctx.body 的 fragment_id/size + env 的 OSS_BUCKET/OSS_REGION；MAX_UPLOAD_BYTES/RAM_ROLE_ARN 等需新增 env 变量（按需扩展 load_env required 或单独读取）。US-009（verify-upload HeadObject）同理替换其占位分支。
  - 安全红线已落实：FcHttpError.payload 只含开发者自定义安全文案；audit.log_event 对 code/session_key/AK Secret/SecurityToken 强制脱敏（即便调用方误传）；env/config 缺失只列变量名不列值。新增 FC 逻辑沿用 log_event 记日志即自动脱敏。
---

## 2026-06-27 04:15 - US-007
- 实现了什么：FC issue-credential 的 STS 单 object key 凭证签发（tech-spec §4.1 / §4.4）。沿用 US-006 的「纯逻辑放 fc_shared（mypy strict + ruff + pytest 全覆盖）+ IO 用 Protocol 注入」模式；handler.py 仍只受 ruff + importlib pytest。
  - apps/fc/shared/fc_shared/sts.py（新增）：
    · 纯逻辑（无 IO，直接单测）：object_key_for（_FRAGMENT_ID_RE 解析 <YYYYMMDDTHHMMSS>_<4-8 deviceShortId>_<26 ULID>，datetime 校验日期合法，构造 recordings/<YYYY-MM-DD>/<fragment_id>.wav；格式/日期非法→400 INVALID_REQUEST，AC#2）、single_key_policy（精确 acs:oss:*:*:<bucket>/<object_key>，单条无通配符，AC#5）、parse_size（int/数字串→正整数，拒 bool/float/0/负/非数字→400）、check_size（>max→400 SIZE_EXCEEDED + limit_bytes/actual_bytes，AC#3）、credential_response（7 字段，AC#6）、常量 STS_MAX_DURATION_SECONDS=900（AC#4）。
    · IO 注入点 StsIssuer Protocol：assume_role(...)→StsCredential；RealStsIssuer（lazy import alibabacloud-sts20150401 + tea-openapi，endpoint sts.<region>.aliyuncs.com，role_session_name=soniscope-issue-credential，policy=json.dumps，duration<=900）；get_issuer() 工厂供 monkeypatch。
  - apps/fc/shared/fc_shared/errors.py：新增稳定错误码 SIZE_EXCEEDED(400)、STS_ISSUE_FAILED(500)。
  - apps/fc/shared/fc_shared/env.py：新增 ISSUE_CREDENTIAL_REQUIRED_VARS=(RAM_ROLE_ARN, ALIYUN_AK_ID, ALIYUN_AK_SECRET)、DEFAULT_MAX_UPLOAD_BYTES=52428800、StsEnv dataclass、load_sts_env()（缺必填→FcConfigError 列名；MAX_UPLOAD_BYTES 可选，缺失/非正整数回退默认）。verify-upload 不需 STS 变量，故不进共享 DEFAULT_REQUIRED_VARS。
  - apps/fc/shared/fc_shared/__init__.py：导出新符号 + 子模块 sts（handler 用 fc_shared.sts.get_issuer 便于打桩）。
  - apps/fc/issue_credential/handler.py：鉴权通过分支替换 US-006 占位为真实签发——load_env(shared)→authorize_request→load_sts_env→parse_size→check_size→object_key_for（这段顺序保证 invalid-body/allowlist 在加载 STS env 前先 400/403）；再单独 try 包 issuer.assume_role（任何异常→500 STS_ISSUE_FAILED，只记异常类名不泄漏明文）；成功 log_event(decision=ISSUED) + 返回 credential_response。
  - apps/fc/issue_credential/requirements.txt：非注释行填 alibabacloud-sts20150401 / alibabacloud-tea-openapi（deploy-fc package_function 会 install_deps 随函数打包）。
- 更改的文件：apps/fc/shared/fc_shared/{sts.py(新),errors.py,env.py,__init__.py}、apps/fc/issue_credential/{handler.py,requirements.txt}、apps/fc/tests/{test_sts.py(新),test_issue_credential.py(新),test_fc_handlers.py}、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 25 文件 0 错）、make lint（ruff apps/ 全过）、make test（189 passed，+41）全部通过。
  test_sts.py（34 用例，纯逻辑）+ test_issue_credential.py（8 用例，FakeIssuer monkeypatch fc_shared.sts.get_issuer：成功 7 字段、单 key policy+duration<=900、SIZE_EXCEEDED 不签发、非法 fragment_id 400、缺 STS env 500、伪造/缺 code 401/400 且无 STS 字段 AC#7、issuer 失败 500 不泄漏 ak-secret）。
  未跑：真实云端 make deploy-fc + make test-fc-live（需真实 wx.login code + ALIYUN_AK + AssumeRole 触网，本沙箱不具备）——属 US-008 范围。
- **未来迭代的学习：**
  - 陷阱：US-006 的 test_fc_handlers.py 通用 test_handler_authorized_path 参数化覆盖两个 handler 并断言 {"status":"authorized"}；US-007 后 issue-credential 该路径改为签发 STS，故把该测试改用 PLACEHOLDER_HANDLERS（排除 issue-credential），issue-credential 鉴权通过路径转由 test_issue_credential.py 专项覆盖。其余 4 个通用测试（imports/GET/missing-env-500/invalid-body-400/not-allowed-403）对两 handler 仍成立，因 handler 顺序为 load_env(shared)→authorize_request→load_sts_env，body/allowlist 错误在加载 STS env 前先返回。
  - 上下文：US-008（issue-credential 云端联调）复用本处：make deploy-fc FUNCTION=issue-credential + curl 存活（fc_deploy.deploy_one 已就绪）；make test-fc-live 跑 401/403/成功/越权 PutObject AccessDenied/过期 ExpiredToken/SIZE_EXCEEDED。RealStsIssuer 已是真实 AssumeRole 路径，部署后即可 live 验证；verify_prep 的 check_sts_escape 纯逻辑也可在此函数 live 后接到真实签发。
  - 上下文：STS region 取 env.oss_region（cn-beijing），STS endpoint=sts.<region>.aliyuncs.com，与 verify_prep._assume_role_single_key 一致；mypy override 已含 alibabacloud_sts20150401.* / alibabacloud_tea_openapi.*（US-004 已加），lazy import 函数返回 Any 即可。
  - 上下文：US-009（verify-upload HeadObject）将需 ALIYUN_AK_ID/SECRET（HeadObject 凭证），可仿 load_sts_env 加 verify-upload 专属 loader；不要把这些塞进共享 DEFAULT_REQUIRED_VARS。
---

## 2026-06-27 05:10 - US-008
- 实现了什么：issue-credential 云端联调与 STS 安全反例脚本 `make test-fc-live`（tech-spec §4.1/§4.4，AGENTS「单元测试 mock 云端，真实调用进 live target」）。沿用 verify_prep 的「纯断言逻辑 + IO Protocol」模式。
  - apps/worker/src/soniscope_worker/fc_live.py（新增，mypy strict + ruff + pytest 全覆盖）：
    · 纯断言逻辑（无 IO，直接单测）：assert_status_error（401 INVALID_CODE / 403 OPENID_NOT_ALLOWED + 拒绝响应里若混入 7 个 STS 字段判 FAIL 的安全检查，AC#2/#3）、assert_credential_complete（200 + 7 字段齐全非空，只展示 object_key 绝不展示 secret/token，AC#4）、assert_size_exceeded（400 SIZE_EXCEEDED + limit_bytes/actual_bytes，AC#8）、assert_escape_op（复用 verify_prep.is_denied 判定越权 AccessDenied / 过期 ExpiredToken，AC#5/#6/#7）、parse_issued_credential、other_recordings_key（越权目标 key）、make_fragment_id（合法 <YYYYMMDDTHHMMSS>_<device>_<26 ULID>）。
    · 编排 run_checks/_run_credential_path/all_passed/format_report：A 伪造 code→401；B allowlist 外 code→403（无 code 则 SKIP）；C-H allowlist 内 code→签发 STS→5 个越权/过期反例（put 其他 key / get / list / delete / 过期 put）；I size=60MB→SIZE_EXCEEDED（无 code 则 SKIP）。code 一次性，缺失场景 SKIP；ProbeError 收敛为单项 FAIL；报告绝不打印 AK Secret / SecurityToken。
    · IO 注入点 FcLiveProbes Protocol：call_issue_credential（urllib POST FC_ISSUE_URL）+ oss_escape_ops（复用 verify_prep._import_oss/_oss_sts_client/_run_oss_op/_seconds_until_expiry，STS 临时凭证带 security_token 跑反例，--skip-expiry 跳过 ≥900s 等待）。RealFcLiveProbes 构造无副作用、调用才触网，缺 SDK/不可达抛 ProbeError。
  - cli.py：新增 test-fc-live 子命令（--code / --code-not-allowed / --size-code / --skip-expiry，lazy import，typer.Exit(code)）。
  - Makefile：新增 test-fc-live target（CODE= CODE_NOT_ALLOWED= SIZE_CODE= SKIP_EXPIRY=1 透传）+ .PHONY。
  - apps/worker/tests/test_fc_live.py（新增 26 用例）：FakeProbes 注入全程不触网，覆盖每个 assert_* pass/fail、STS 字段泄漏检测、code 缺失 SKIP、--skip-expiry 时过期反例 SKIP、FC 不可达/探针错误收敛 FAIL、报告无 secret 明文、退出码、CLI skip-only exit0 / 失败 exit1、fragment_id 正则与唯一性。
  - 修复：上一轮遗留的 test_run_checks_skip_expiry 用 `"过期" in r.name` 过滤过期反例，但 expired_put 展示名按 AC#7 措辞为「等待超过 STS 有效期上限后 PutObject」（含「超过」非「过期」），导致空匹配 FAIL；改为按 ESCAPE_OPS 中 expired_put 的 display 精确定位，保持展示名与 AC 一致不改。
- 更改的文件：apps/worker/src/soniscope_worker/{fc_live.py(新),cli.py}、Makefile、apps/worker/tests/test_fc_live.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 27 文件 0 错）、make lint（ruff apps/ 全过）、make test（220 passed，+26）全部通过；CLI --help 显示 test-fc-live。
  未跑：真实 `make deploy-fc FUNCTION=issue-credential` + `make test-fc-live CODE=…`（需真实 DevTools wx.login 一次性 code + ALIYUN_AK + AssumeRole 触网 + 过期反例 ≥900s 等待，本沙箱不具备）。AC#1 部署后 curl 存活复用 US-005 deploy_one；AC#9 看日志复用 `make fc-logs FUNCTION=issue-credential`，本模块不重复实现。
- **未来迭代的学习：**
  - live target 模式：FC live 联调脚本复用 verify_prep 的 OSS STS 反例工具（_import_oss/_oss_sts_client/_run_oss_op/_seconds_until_expiry/is_denied），不重复实现；wx.login code 一次性，每个场景（成功/403/SIZE）各需独立 code，缺失即 SKIP 而非 FAIL，使无 code 的本地 CI 也能 exit 0。
  - 陷阱：越权/过期反例的展示名要与 AC 措辞一致（AC#7 是「等待超过…有效期上限」），测试里别用脆弱的子串（如「过期」）去匹配展示名；用 ESCAPE_OPS 的 (name, display) 元组精确定位。
  - 上下文：US-009（verify-upload HeadObject）落地后，US-010 的 make test-verify-upload 可仿本处 FcLiveProbes 模式：纯断言逻辑 + HTTP/OSS Probe 注入；verify_prep 的 check_fc / check_sts_escape 在 issue-credential 真实部署+live 验证后可接到真实签发转绿。

## 2026-06-27 06:00 - US-009
- 实现了什么：FC verify-upload 的 OSS HeadObject 上传确认接口（tech-spec §4.2）。沿用 US-007 的「纯逻辑放 fc_shared（mypy strict + ruff + pytest 全覆盖）+ IO 用 Protocol 注入」模式；handler.py 仍只受 ruff + importlib pytest。
  - apps/fc/shared/fc_shared/head.py（新增）：
    · 纯逻辑（无 IO，直接单测）：ObjectHead dataclass（exists/content_length/etag/last_modified）、verify_upload_result（§4.2 三态映射：不存在→OBJECT_NOT_FOUND；大小不一致→SIZE_MISMATCH+actual_size；一致→verified:true+etag/size/last_modified，成功响应严格 4 字段不夹带 AK）、is_not_found（从 OSS 异常提取 code/status_code，含 unwrap 内层，判定 NoSuchKey/NoSuchObject/404）。
    · IO 注入点 ObjectHeader Protocol：head_object(bucket/region/endpoint/ak_id/ak_secret/object_key)→ObjectHead；RealObjectHeader（lazy import alibabacloud-oss-v2，StaticCredentialsProvider+HeadObjectRequest，404→exists=False，其余异常上抛由 handler 收敛 500）；get_header() 工厂供 monkeypatch。
  - apps/fc/shared/fc_shared/errors.py：新增 HEAD_OBJECT_FAILED(500) 错误码 + OBJECT_NOT_FOUND/SIZE_MISMATCH reason 常量（注意：reason 是 200 响应体内业务原因，非 HTTP 错误码）。
  - apps/fc/shared/fc_shared/env.py：新增 VERIFY_UPLOAD_REQUIRED_VARS=(ALIYUN_AK_ID, ALIYUN_AK_SECRET)、VerifyEnv dataclass、load_verify_env()（HeadObject 读凭证；verify-upload 不需 STS AssumeRole 变量，故与 load_sts_env 各自独立、都不进共享 DEFAULT_REQUIRED_VARS）。
  - apps/fc/shared/fc_shared/__init__.py：导出 head 子模块 + ObjectHead/ObjectHeader/get_header/verify_upload_result/load_verify_env/VerifyEnv + 新错误码/reason 常量。
  - apps/fc/verify_upload/handler.py：鉴权通过分支替换 US-006 占位为真实校验——load_env(shared)→authorize_request→load_verify_env→parse_size(expected_size)→object_key_for（保证 invalid-body/allowlist 在加载 verify env 前先 400/403）；再单独 try 包 head_object（任何异常→500 HEAD_OBJECT_FAILED，只记异常类名不泄漏明文）；最后 verify_upload_result + log_event(verified/reason/elapsed_ms) + 200 返回。
  - apps/fc/verify_upload/requirements.txt：非注释行填 alibabacloud-oss-v2（deploy-fc package_function 会 install_deps 随函数打包）。
  - apps/worker/src/soniscope_worker/latency.py（新增，AC#7 P95 能力）：percentile（NIST 线性插值）、LatencyStats、summarize、format_latency_report（P95<=阈值 PASS，默认阈值 P95_THRESHOLD_SECONDS=1.0s，空样本判 FAIL）。供 US-010 的 make test-verify-upload 复用做 P95 输出与判定；本 story 只交付可复用且已单测的统计能力，不在此实现真实云端调用循环（属 US-010）。
- 更改的文件：apps/fc/shared/fc_shared/{head.py(新),errors.py,env.py,__init__.py}、apps/fc/verify_upload/{handler.py,requirements.txt}、apps/worker/src/soniscope_worker/latency.py(新)、apps/fc/tests/{test_head.py(新),test_verify_upload.py(新),test_fc_handlers.py}、apps/worker/tests/test_latency.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 32 文件 0 错）、make lint（ruff apps/ 全过）、make test（249 passed，+29）全部通过。
  test_head.py（14 用例，纯逻辑）+ test_verify_upload.py（7 用例，FakeHeader monkeypatch fc_shared.head.get_header：verified:true 4 字段、OBJECT_NOT_FOUND、SIZE_MISMATCH+actual_size、非法 fragment_id 400 不触达 HeadObject、缺 verify env 500、伪造 code 401 不暴露对象信息、HeadObject 异常 500 不泄漏 ak-secret）+ test_latency.py（9 用例）。
  test_fc_handlers.py：移除 PLACEHOLDER_HANDLERS 与 test_handler_authorized_path（verify-upload 自本 story 起鉴权通过即执行真实 HeadObject、不再返回占位 authorized；issue-credential 自 US-007 已如此），两 handler 共有的 imports/GET/缺 env 500/非法 body 400/allowlist 403 仍参数化覆盖。
  未跑：真实云端 make deploy-fc FUNCTION=verify-upload + make test-verify-upload（需真实 wx.login code + ALIYUN_AK + 构造 OSS 对象 + HeadObject 触网，本沙箱不具备）——属 US-010 范围。
- **未来迭代的学习：**
  - 上下文：US-010（verify-upload 云端闭环 make test-verify-upload）复用本处：① make deploy-fc FUNCTION=verify-upload + curl 存活（fc_deploy.deploy_one 已就绪）；② RealObjectHeader 已是真实 HeadObject 路径，部署后即可 live 验证；③ P95 统计用 soniscope_worker.latency.format_latency_report（已单测，阈值 1s），不要重复实现分位数；④ make oss-delete-obj（US-010 首交付）构造 OBJECT_NOT_FOUND 场景。
  - 陷阱：reason 常量（OBJECT_NOT_FOUND/SIZE_MISMATCH）是 verified:false 的 200 响应体字段，不是 FcHttpError 错误码；HeadObject 真正失败（非 404）才是 500 HEAD_OBJECT_FAILED。verify-upload 成功响应严格 4 字段（verified/etag/size/last_modified），单测专门断言不夹带 AK/token。
  - 陷阱：当一个 handler 从「占位 authorized」升级为真实业务，要同步把 test_fc_handlers.py 里基于占位响应的参数化测试（PLACEHOLDER_HANDLERS / authorized_path）收窄或移除，否则空 parametrize 或断言 status==authorized 会失败/误导；真实路径转到该 handler 的专项测试文件。
  - 上下文：verify-upload HeadObject 用 FC 子账号长期 AK（ALIYUN_AK_ID/SECRET，与 issue-credential 的 STS AssumeRole 凭证同名 env），不走 STS；OSS v2 client 建法见 verify_prep._oss_client（StaticCredentialsProvider + region + endpoint）。
---

## 2026-06-27 07:00 - US-010
- 实现了什么：verify-upload 云端闭环测试 `make test-verify-upload` + 仅测试用 `make oss-delete-obj`（tech-spec §4.2/§6.5）。沿用 fc_live 的「纯断言逻辑 + IO Protocol」模式。
  - apps/worker/src/soniscope_worker/oss_admin.py（新增，US-010 首交付 oss-delete-obj，US-017/US-029 复用）：
    · object_key_for（_FRAGMENT_ID_RE 解析 → recordings/<YYYY-MM-DD>/<id>.wav，格式/日期非法抛 OssAdminError）。
    · delete_allowed（删除门控：--yes 或 SONISCOPE_ALLOW_OSS_DELETE=1，AGENTS 安全红线）。
    · OssObjectStore Protocol（put/delete 注入点）+ RealOssObjectStore（复用 verify_prep._import_oss/_oss_client，用 config.yaml OSS AK，lazy import）。
    · run_oss_delete_obj（未授权拒绝 exit1 / 非法 fragment_id / 删除异常收敛只记类名不泄漏明文 / 成功）。仅测试用、绝不进 Worker 业务路径。
  - apps/worker/src/soniscope_worker/verify_upload_live.py（新增，make test-verify-upload）：
    · 纯断言 assert_auth_failure（400/401 + 鉴权失败不泄露对象信息 etag/size/last_modified/actual_size/verified=true，AC#6）、assert_verified_true（200+4 字段齐全+size 一致，AC#3）、assert_object_not_found（AC#4）、assert_size_mismatch（actual_size 真实大小，AC#5）、make_fragment_id（合法 fragment_id）。
    · 编排 run_checks：A 伪造 code→400/401（始终可跑）；B 上传对象+verify→true→清理；C 上传后删除→OBJECT_NOT_FOUND；D 100 字节 expected=200→SIZE_MISMATCH。每个真实 code 一次性，缺失即 SKIP（本地 CI exit 0）。
    · all_passed/format_report：无 FAIL + 至少跑一项 + P95 达标（复用 latency.format_latency_report，阈值 1s，AC#7）；绝不打印 AK Secret。
    · VerifyLiveProbes Protocol + RealVerifyUploadProbes（urllib POST FC_VERIFY_URL + RealOssObjectStore，缺失/不可达抛 ProbeError，构造无副作用调用才触网）。
  - cli.py：新增 oss-delete-obj（--fragment-id/--yes）+ test-verify-upload（--verified-code/--not-found-code/--mismatch-code）两个 typer 子命令。
  - Makefile：新增 oss-delete-obj（FRAGMENT_ID= YES=）+ test-verify-upload（VERIFIED_CODE= NOT_FOUND_CODE= MISMATCH_CODE=）target + .PHONY。
  - apps/worker/tests/test_oss_admin.py（14 用例）+ test_verify_upload_live.py（24 用例，FakeProbes 注入全程不触网）。
- 更改的文件：apps/worker/src/soniscope_worker/{oss_admin.py(新),verify_upload_live.py(新),cli.py}、Makefile、apps/worker/tests/{test_oss_admin.py(新),test_verify_upload_live.py(新)}、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 36 文件 0 错）、make lint（ruff apps/ 全过）、make test（287 passed，+38）全部通过；CLI --help 显示两命令；oss-delete-obj 未授权时拒绝并 exit 1。
  未跑：真实 make deploy-fc FUNCTION=verify-upload + make test-verify-upload CODE=…（需真实 wx.login 一次性 code + ALIYUN_AK + 构造 OSS 对象 + HeadObject 触网，本沙箱不具备）。AC#1 部署 curl 存活复用 US-005 deploy_one；AC#8 日志复用 make fc-logs FUNCTION=verify-upload。
- **未来迭代的学习：**
  - AC#9「Worker 源码中不存在 DeleteObject 调用」的口径：按 AGENTS.md 权威解读为 **Worker 业务路径**（轮询/下载/转写/落盘，US-021+）绝不删除；oss-delete-obj 是**仅测试用**能力，按既有「make 命令做成 Worker CLI 子命令」的 codebase pattern 落在 worker 包内（便于 mypy/ruff/pytest 覆盖），但重度标注仅测试用 + --yes/env 双重门控。delete_object 当前只出现在 3 个 test/live 模块（oss_admin / verify_upload_live / fc_live），无业务路径调用。US-029 的 verify-oss-retention 扫日志校验也以此为准。
  - 陷阱：ruff line-length=100 按 CJK 显示宽度计数，SKIP 文案里塞 `make test-verify-upload XXX=…` 容易超限；用「缺一次性 wx.login code」类短文案即可。
  - 上下文：US-017 的 make show-oss-object（首交付）可复用 oss_admin.object_key_for + RealOssObjectStore 模式，扩展 HeadObject 读取（exists/size/etag/last_modified/元数据）；US-029 复用 oss-delete-obj 不重复实现。
  - 上下文：verify-upload live 探针 RealOssObjectStore 用 config.yaml 长期 OSS AK 上传/删除测试对象，与 FC verify-upload 的 HeadObject（FC 子账号 AK）是两套凭证；本地 CI 无 config/无 code 时全 SKIP（伪造 code 一项也会因 FC 不可达 FAIL，故仅在能触网时跑）。
---

## 2026-06-27 08:00 - US-011
- 实现了什么：微信小程序项目骨架 apps/miniprogram/ + 环境配置 + Python 静态检查（make lint 覆盖小程序源码）。
  - apps/miniprogram/ 骨架（微信开发者工具可直接「导入项目」打开）：
    · project.config.json：appid=wx3f973c7297728b0c（真实 AppID，AC#1）、miniprogramRoot=./、setting.urlCheck=true。
    · app.json：pages=[pages/index/index, pages/uploads/uploads]（首页+上传列表，AC#4）、window、tabBar（两 tab 切换）、sitemapLocation。
    · app.js / app.wxss / sitemap.json：入口（onLaunch/onError 用脱敏 logger）+ 全局样式 + sitemap。
    · config.js（合法域名单一真实来源，AC#2/#3）：FC_ISSUE_CREDENTIAL_URL=https://issue-cedential-ottfirocds.cn-beijing.fcapp.run（保留少一个 r 的真实拼写，绝不修正）、FC_VERIFY_UPLOAD_URL、OSS_UPLOAD_URL=https://soniscope-audio.oss-cn-beijing.aliyuncs.com；REQUEST/UPLOAD_LEGAL_DOMAINS；CHUNK_MAX_DURATION_SECONDS=600（tech-spec §3.1 前端常量）。
    · utils/logger.js（AC#5）：createLogger/redact/maskValue，对 access_key_secret/access_key_id/app_secret/security_token/session_key/api_key/token/password 等敏感键名递归脱敏（前后 4 位，≤8 位整体打码），非敏感字段（如 object_key）原样保留；info→console.log、warn、error。
    · pages/index/{index.js,.json,.wxml,.wxss}（录音首页骨架，圆形按钮占位，录音逻辑留 US-012）+ pages/uploads/{...}（上传列表骨架，空态文案，八状态留 US-019）。
    · README.md：DevTools 导入步骤 + 服务器合法域名配置表（与 config.js 一致）。
  - apps/worker/src/soniscope_worker/miniprogram_lint.py（新增，mypy strict + ruff + pytest 覆盖，AC#6）：
    纯函数 check_project_config（appid 校验）/ check_app_pages（pages 非空+含必需页面+四件套齐全）/ check_domains（三域名齐全 + 检测 issue-cedential 被错误修正成 issue-credential 即报错）/ scan_hardcoded_secrets（LTAI AK ID 前缀 + 敏感键赋字面量正则）；
    编排 run_checks（遍历源码：全量 JSON 可解析 + 硬编码密钥扫描）/ format_report / run_lint_miniprogram（返回 (行, 退出码)，default_miniprogram_root=parents[4]/apps/miniprogram）。
  - cli.py：新增 lint-miniprogram 子命令（lazy import，typer.Exit(code)）。
  - Makefile：lint target 在 ruff 之后追加 `uv run python -m soniscope_worker lint-miniprogram`（AC#6）；.PHONY 加 lint-miniprogram。
  - .gitignore：忽略 apps/miniprogram/project.private.config.json、miniprogram_npm/、node_modules/。
  - apps/worker/tests/test_miniprogram_lint.py（新增 23 用例）：tmp 树构造合规/各类不合规小程序，覆盖每个 check_* pass/fail、域名错误修正检测、硬编码密钥检测、logger 正则不被误判、真实 apps/miniprogram 骨架通过校验。
- 更改的文件：apps/miniprogram/**（全新骨架）、apps/worker/src/soniscope_worker/{miniprogram_lint.py(新),cli.py}、Makefile、.gitignore、apps/worker/tests/test_miniprogram_lint.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 38 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（310 passed，+23）全部通过；
  node --check 5 个 .js + JSON.parse 5 个 .json 全部 OK（AC#7 语法层面无报错）；node 实跑 logger.redact 确认凭证字段脱敏为前后 4 位、object_key 原样（AC#5）。
  未跑：微信开发者工具模拟器真机打开首页/真机预览（本沙箱无 WeChat DevTools；AC#7「模拟器打开首页无 JS 初始化报错」需人工在 DevTools 复验）。
- **未来迭代的学习：**
  - 小程序源码（apps/miniprogram/）不进 mypy/ruff（mypy files=apps/worker/{src,tests}+apps/fc/shared；ruff check apps/ 只对 .py 生效）；JS 静态检查走 Python 的 miniprogram_lint（CLI 子命令），与「make 命令做成 Worker CLI 子命令」codebase pattern 一致，可被 mypy/ruff/pytest 覆盖。node v22 在本机可用，但未引入 npm/eslint 依赖（避免 CI 体积与脆性）。
  - 合法域名单一真实来源放 apps/miniprogram/config.js；US-012+（录音/上传/verify）一律 require('../../config') 取 FC/OSS URL，不要散落硬编码。issue-cedential 少一个 r 的拼写已在 config.js + miniprogram_lint.MISCORRECTED_ISSUE_URL 双向锁定，改动时勿“修正”。
  - 脱敏 logger 在 utils/logger.js：US-012+ 打印 STS 凭证/上传响应时用 createLogger(scope).info({...}) 传对象即自动脱敏；不要 console.log 直接打印含 secret/token 的对象。
  - WeChat DevTools 相关 AC（模拟器打开无报错、真机预览）本沙箱无法自动验证，已用 node --check（语法）+ JSON.parse（配置）做最大化静态验证；真机/模拟器项需人工复验。
  - default_miniprogram_root 用 Path(__file__).resolve().parents[4]/apps/miniprogram，与 fc_deploy/verify_prep 的 parents[4] 仓库根定位一致。
---

## 2026-06-27 09:00 - US-012
- 实现了什么：小程序首页录音开始/停止 + mm:ss 计时 + 停止后生成保留原始格式的本地草稿（tech-spec §5.1/ADR-1）。
  - apps/miniprogram/utils/audio.js（新增纯函数模块，不依赖 wx 运行时，便于 node --check 与后续单测复用）：
    · formatDuration（秒→mm:ss，floor + 负值兜底）；extensionOf（路径取小写扩展名）；
    · detectOriginalFormat（优先临时文件扩展名，扩展名缺失/不可识别时回退录音请求格式，再回退 unknown）；
      KNOWN_FORMATS=wav/mp3/aac/m4a/amr/pcm/silk/ogg。
    · objectKeyDate（本地 <YYYY-MM-DD>）、fragmentTimestamp（本地 <YYYYMMDDTHHMMSS>）、toIso（ISO8601 带本地时区偏移）；
    · previewFragmentId（US-012 仅预览：deviceShortId+ULID 用 pending 占位，US-015 正式生成）；
    · buildObjectKeyPreview（recordings/<date>/<id>.wav，始终 .wav 目标扩展名，OSS_OBJECT_KEY_EXT 来自 config.js）；
    · buildDraftManifest（US-012 范围草稿：recorded_at/duration_seconds/temp_file_path/audio.original_format/audio.size_bytes/object_key_preview；session_id/chunk_seq/sha256 等留 US-015）。
  - apps/miniprogram/pages/index/index.js：从骨架升级为真实录音逻辑——onLoad 取 wx.getRecorderManager() 并 onStart/onStop/onError 绑定；onTapRecord 切换开始/停止；_startRecording 用 format=mp3 + duration=600000ms（长录音分片留 US-016）start()，setInterval 每秒按 Date.now() 实时差刷新 mm:ss；_onRecordStop 用 res.tempFilePath/duration/fileSize 构造草稿、setData(draft) 并 logger.info 打印 original_format/duration/temp_file_path/object_key_preview；_onRecordError 清计时+toast；onUnload 清计时防泄漏。
  - apps/miniprogram/pages/index/index.wxml：草稿确认前用 hint，停止后 wx:if=draft 显示原始格式/时长/目标对象 key 预览 + “前端不转码”说明（试听/重录/上传留 US-014）。
  - apps/miniprogram/pages/index/index.wxss：新增 .draft/.draft-row(--mono)/.draft-note/.hint 样式（圆形录音按钮 + active 红色已在 US-011）。
- 更改的文件：apps/miniprogram/utils/audio.js（新）、apps/miniprogram/pages/index/{index.js,index.wxml,index.wxss}、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 38 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（310 passed，无回归）全部通过；node --check 两个 .js 通过；node 实跑 audio.js 纯函数确认 formatDuration（00:00/00:03/01:15）、detectOriginalFormat（mp3 路径=mp3、无扩展名回退 aac、空=unknown）、buildObjectKeyPreview/.buildDraftManifest（object key 为 recordings/<date>/<id>.wav、original_format=mp3、duration_seconds=3.2）正确。
  未跑：微信开发者工具模拟器录音 3 秒（AC#7）/ 真机授权录音权限完整流程（AC#8）——本沙箱无 WeChat DevTools/真机，需人工在 DevTools + 真机 vConsole 复验 original_format 日志。
- **未来迭代的学习：**
  - 小程序录音用纯函数（utils/audio.js）+ Page 薄壳分层：可静态校验/可单测的逻辑（时长格式化、格式探测、object key/草稿构造）抽进 utils/，Page 只做 wx API 绑定与 setData；与 fc_shared/verify_prep 的「纯逻辑 + IO 注入」一脉相承，但小程序侧 IO 是 wx.getRecorderManager()，无法在沙箱跑。
  - RecorderManager 回调（onStart/onStop/onError）应在 onLoad 一次性绑定，不要每次 start 重复绑定（会叠加多次触发）；计时用 Date.now() 差值而非 tick 累加，避免 setInterval 漂移；onUnload 必须 clearInterval 防泄漏。
  - US-012 的 fragment_id 是 pending 占位（仅 object key 预览用），US-015 才正式生成 device_short_id（4-8 字符持久化）+ ULID 并写 manifest 草案的 session_id/chunk_seq/chunk_total/sha256 等；US-014 接草稿确认态（试听/重录/删除/保存并上传）会复用本处 buildDraftManifest 产出的 draft。
  - original_format 探测口径：前端只能拿临时文件扩展名（AC#5「扩展名不可靠时用探测结果」）；微信端无 ffprobe，真正的格式探测在 Worker（US-022 ffprobe）。前端 detectOriginalFormat 已对 mp3/aac/m4a/amr/wav 等做扩展名→回退请求格式两级兜底。
---

## 2026-06-27 10:00 - US-013
- 实现了什么：小程序录音中断保护与草稿恢复提示（AC#1-#7；锁屏/来电/切后台中断自动停止并落盘草稿，回前台提示保留/丢弃/继续新录）。
  - apps/miniprogram/utils/draft.js（新增纯逻辑模块，复用 audio.buildDraftManifest）：STATUS_DRAFT_INTERRUPTED='draft_interrupted' + 标签 '草稿（被中断保存）'（AC#3）；INTERRUPT_DRAFT_STORAGE_KEY 单槽位（AC#6 天然去重）；RECOVERY_MESSAGE（AC#4 措辞与 PRD 一致）；buildInterruptedDraft（叠加 status/status_label/interrupted/interrupt_reason，保证含 duration_seconds）；buildRecoveryPrompt；dedupeInterruptedDraft（单槽位最后状态胜出）。
  - apps/miniprogram/pages/index/index.js：onLoad 绑定 recorder.onInterruptionBegin/onInterruptionEnd（AC#1）+ Page onHide 作为等价切后台中断（AC#1）+ onShow 读 storage 展示恢复提示（AC#4）；_handleInterruption(reason)（录音中触发自动 recorder.stop，_interruptHandled 同一次录音去重 AC#2/#6）；_onRecordStop 按 _interrupting 分流：中断走 _saveInterruptedDraft（buildInterruptedDraft→wx.setStorageSync 落盘 AC#2，duration 缺失时回退计时差 AC#3），普通走 US-012 草稿路径；onKeepDraft/onDiscardDraft/onRestartRecording 三动作（AC#5：保留草稿/清文件+记录/清理并重新录），_cleanupDraftFile 用 wx.getFileSystemManager().unlink best-effort，_clearInterruptStorage 清槽位。_startRecording 重置去重标志。
  - apps/miniprogram/pages/index/index.wxml：新增 recovery 提示面板（message + 保留/丢弃/继续新录三按钮）；draft 标题改用 status_label（中断草稿显示「草稿（被中断保存）」）；hint 用 wx:elif 避免与 recovery 同时出现。
  - apps/miniprogram/pages/index/index.wxss：新增 .recovery/.recovery-msg/.recovery-actions/.recovery-btn（keep 蓝/discard 红/restart 绿）。
  - apps/miniprogram/test/interruption.test.js（新增，node 内置 test runner + 手写 wx/Page mock，9 用例）：覆盖中断回调注册（AC#1）、中断时停止录音（AC#2）、落盘+标记+含时长（AC#2/#3）、连续两次中断去重（AC#6）、onHide 切后台中断、未录音空操作、onShow 恢复提示（AC#4）、保留/丢弃/继续新录三迁移（AC#5）、dedupe 纯函数。
  - apps/worker/tests/test_miniprogram_js.py（新增）：pytest 子进程跑 `node --test`，把小程序 JS 单测纳入 make test 统一质量门（node 缺失则 skip）。
- 更改的文件：apps/miniprogram/utils/draft.js(新)、apps/miniprogram/pages/index/{index.js,index.wxml,index.wxss}、apps/miniprogram/test/interruption.test.js(新)、apps/worker/tests/test_miniprogram_js.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 39 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（311 passed，+1 即 JS 包装测试，内部跑 9 个 node 子测试全过）全部通过；node --check 三个改动 .js 通过；node --test 直接跑 9/9 通过。
  未跑：微信开发者工具模拟器 / 真机锁屏中断完整流程（AC#8）——本沙箱无 WeChat DevTools/真机，需人工在 DevTools + 真机 vConsole 复验「锁屏后解锁草稿存在、时长接近、无未捕获异常」。
- **未来迭代的学习：**
  - 小程序 JS 单测方案：用 node v22 内置 `node --test` + 手写 wx/Page mock（test_miniprogram_js.py 以 pytest 子进程纳入 make test）。关键陷阱①该 node 版本 `node --test <目录>` 会把目录当模块加载报 MODULE_NOT_FOUND，必须传显式 *.test.js 文件清单（Python 端 glob）；②Page 测试 harness：global.Page 捕获配置对象、global.wx 注入 mock、delete require.cache 后 require 重新执行；实例化用 Object.assign + 深拷贝 data + setData 浅合并。
  - 陷阱：Page._startTimer 用真实 setInterval，测试里走「继续新录」分支后录音态会留下未清的 interval 挂住 node 事件循环（命令疑似卡死/后台化）；凡测试结束仍处录音态，必须显式 inst.onUnload() 清 timer。中断/停止路径已在 _handleInterruption/_stopRecording 里 _clearTimer，无需额外处理。
  - 中断去重口径（AC#6）：双保险——_interruptHandled 标志（同一次录音内第二次中断 no-op）+ INTERRUPT_DRAFT_STORAGE_KEY 单槽位（后到草稿覆盖前一份）；_startRecording 重置标志，使新录音可再次响应中断。
  - 上下文：US-014（草稿确认态：试听/重录/删除/保存并上传）会复用本处 draft（含 status_label）与 recovery 流程；中断草稿与普通草稿都进入同一草稿确认态。onShow 恢复提示与 US-014 的草稿确认 UI 需协调（本期 recovery 面板与 draft 卡片并存）。
  - 上下文：中断「本地落盘」本期 = wx.setStorageSync 单槽位；真正的草稿/Fragment 持久化与上传队列在 US-015/US-017 落地，届时草稿存储模型可能从单槽位升级为按 fragment_id 的列表。
---

## 2026-06-27 11:00 - US-014
- 实现了什么：小程序草稿确认态（停止录音后先进入确认态：试听 / 暂停 / 重录 / 删除 / 保存并上传），满意后晋升为「待上传」上传队列项（AC#1-#6）。
  - apps/miniprogram/utils/upload_queue.js（新增纯逻辑模块，复用 codebase「纯函数 utils + Page 薄壳」分层）：
    · STATUS_QUEUED/STATUS_UPLOADING + STATUS_TEXT（待上传 / 上传中，tech-spec §6.7 八状态前两态；其余 US-019）+ statusText()。
    · UPLOAD_QUEUE_STORAGE_KEY='soniscope:upload_queue'（uploads 页读取渲染的本地落盘槽位）。
    · fragmentIdFromObjectKey（从 recordings/<date>/<id>.wav 反推 id，去目录去扩展名；US-014 复用草稿 object key 预览作展示/去重键，US-015 落地正式 fragment_id 后替换）。
    · buildQueuedFragment（草稿→队列项：fragmentId/status/statusText/durationSeconds/originalFormat/objectKeyPreview/tempFilePath）。
    · appendQueuedFragment（同一 fragmentId 已存在则不重复追加，AC#5 去重双保险）。
  - apps/miniprogram/pages/index/index.js：在 US-012/013 草稿基础上加草稿确认态动作——
    · data 加 playing；onLoad 初始化 this._audio=null。
    · onTapPlay（AC#2）：懒创建 wx.createInnerAudioContext()，绑定 onEnded/onError 复位 playing，设 src=draft.temp_file_path 后 play()，setData playing=true。onTapPause（AC#2）：pause() + playing=false。
    · onReRecord（AC#3）：_destroyAudio + _cleanupDraftFile + 清中断槽位 + 清草稿 → _startRecording() 重新录音。
    · onDeleteDraft（AC#4）：_destroyAudio + _cleanupDraftFile + 清中断槽位 + 清草稿回初始态，不入队、不生成 Fragment。
    · onTapSaveUpload（AC#5）：draft.frozen 守卫防重复点击；buildQueuedFragment→appendQueuedFragment→wx.setStorageSync 落盘；冻结草稿（Object.assign frozen:true，禁用确认态按钮）；toast。
    · _destroyAudio（stop+destroy best-effort，复位 playing）；_startRecording/onUnload 调 _destroyAudio。
  - apps/miniprogram/pages/index/index.wxml：draft 卡片加 draft-actions（!draft.frozen && !recovery 时显示 5 个按钮：试听/暂停 wx:if 切换、重录、删除、保存并上传）+ draft.frozen 时显示「已加入上传队列」提示。
  - apps/miniprogram/pages/index/index.wxss：新增 .draft-actions/.draft-btn/.draft-btn--primary/.draft-btn--danger/.draft-frozen。
  - apps/miniprogram/pages/uploads/uploads.js：onShow 读 UPLOAD_QUEUE_STORAGE_KEY 渲染队列项（uploads.wxml 已用 item.fragmentId/item.statusText，无需改）。
  - apps/miniprogram/test/draft_confirm.test.js（新增，node --test + 手写 wx/Page mock，含 createInnerAudioContext mock，10 用例）：草稿确认态、试听创建+播放、暂停、播放结束复位、重录清理+重录、删除清理不入队、保存并上传冻结+落盘、重复点击去重、buildQueuedFragment、appendQueuedFragment 去重。
- 更改的文件：apps/miniprogram/utils/upload_queue.js(新)、apps/miniprogram/pages/index/{index.js,index.wxml,index.wxss}、apps/miniprogram/pages/uploads/uploads.js、apps/miniprogram/test/draft_confirm.test.js(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 39 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（311 passed，JS 包装测试内部 node --test 18/18：9 中断 + 9 草稿确认）全部通过；node --check 4 个改动/新增 .js 通过。
  未跑：微信开发者工具录音 5 秒试听播放/重录删除文件消失/保存上传列表出现记录（AC#6）+ 真机三分支切换（AC#7）——本沙箱无 WeChat DevTools/真机，需人工在 DevTools + 真机 vConsole 复验。
- **未来迭代的学习：**
  - 上传队列本地落盘单一真实来源 utils/upload_queue.js（UPLOAD_QUEUE_STORAGE_KEY）：US-014 只落「待上传」；US-015 把队列项 fragmentId 换成正式 fragment_id + manifest 草案；US-017 静默登录/STS/OSS 直传把 queued→uploading→pending_verify；US-018 verify；US-019 八状态全量渲染与折叠。uploads 页 onShow 读队列，新增状态时扩展 STATUS_TEXT 即可。
  - 草稿确认态与 US-013 中断恢复提示并存协调：draft-actions 用 `wx:if="{{!draft.frozen && !recovery}}"` 门控——recovery 提示（保留/丢弃/继续新录）显示时不出确认态按钮；用户「保留」后 recovery=null，中断草稿即进入同一确认态可试听/上传。
  - innerAudioContext 懒创建于 onTapPlay 并存 this._audio；重录/删除/保存上传/_startRecording/onUnload 都要 _destroyAudio（stop+destroy）防止播放泄漏与残留；测试里凡留录音态（onReRecord 后）必须 inst.onUnload() 清计时器+音频，避免 node 事件循环挂起（沿用 US-013 陷阱）。
  - 防重复 Fragment 双保险：Page 侧 draft.frozen 守卫（按钮 wx:if 隐藏 + 函数早返回）+ upload_queue.appendQueuedFragment 按 fragmentId 去重；onDiscardDraft（US-013 中断「丢弃」）与 onDeleteDraft（US-014 确认态「删除」）是两个独立 handler，勿混用。
---

## 2026-06-27 12:00 - US-015
- 实现了什么：小程序保存并上传前生成稳定 device_short_id、全局唯一 fragment_id、原始音频 sha256，并把将传给 Worker 的 OSS 用户自定义元数据草案备齐（tech-spec §3.1/§3.2/§3.3）。沿用「纯函数 utils + Page 薄壳」分层。
  - apps/miniprogram/utils/sha256.js（新增，纯函数）：无依赖纯 JS SHA-256，sha256Hex 接受 ArrayBuffer/TypedArray/number[]/UTF-8 字符串→小写 hex；64-bit 长度填充用 hi/lo 拆分避免 32-bit 溢出。wasm-crypto 优化（AGENTS 建议）留作后续性能项。
  - apps/miniprogram/utils/ulid.js（新增，纯函数）：26 字符 Crockford base32 ULID（10 时间 + 16 随机），字符集 [0-9A-Z] 满足 FC 正则 [0-9A-Za-z]{26}；monotonicFactory 单调工厂——时间不前进时递增随机段，保证同毫秒/同秒连续生成不同 ULID（AC#3）。time/rng 可注入做确定性单测。
  - apps/miniprogram/utils/device.js（新增）：device_short_id 6 字符（4-8 区间）字母数字；ensureDeviceShortId(storage, rng) 读 storage→非法/缺失则生成并持久化（幂等，AC#1，冷启动不变）；isValidDeviceShortId/DEVICE_SHORT_ID_RE 与 FC 正则一致。storage IO 与纯生成分离（运行时传 wx，测试传 mock）。
  - apps/miniprogram/utils/audio.js：新增 FRAGMENT_ID_RE（镜像 fc_shared/sts.py _FRAGMENT_ID_RE）、buildFragmentId(recordedAt, deviceShortId, ulid)、buildUploadManifestDraft（AC#4 全字段：fragment_id/session_id/chunk_seq/chunk_total(null)/device_id/recorded_at/duration_seconds/audio.{original_format,size_bytes}/upload.original_sha256/object_key/temp_file_path）、buildOssMetadata（AC#6：7 个 x-oss-meta-*；非分片 chunk-total 映射为 "0"，manifest 内为 null，遵 §3.2）。
  - apps/miniprogram/utils/upload_queue.js：buildQueuedFragment(draft, manifest?, ossMetadata?) 向后兼容——传 manifest 时用正式 fragment_id/object_key 并把 manifest+ossMetadata 挂到队列项（供 US-017 直传）；不传退回 US-014 行为（object key 反推）。
  - apps/miniprogram/pages/index/index.js：onLoad 建单调 ULID 生成器 this._nextUlid + ensureDeviceShortId(wx)；onTapSaveUpload 改为生成 deviceShortId/sessionId/fragmentId + 计算 sha256（_computeOriginalSha256 用 wx.getFileSystemManager().readFileSync→sha256Hex，读不到返回空串不阻断）→ buildUploadManifestDraft + buildOssMetadata → 入队；冻结草稿写回 fragment_id；vConsole 日志打印 fragmentId/deviceShortId/sessionId/sha256Prefix（AC#8）。
  - apps/miniprogram/app.js：onLaunch ensureDeviceShortId(wx) 首启持久化并存 globalData.deviceShortId（AC#1）。
  - apps/miniprogram/test/ids.test.js（新增，node --test，13 用例）：device 持久化/非法重生成/注入 rng；ulid 26 字符；fragment_id 正则；同毫秒唯一；manifest 字段完整；OSS meta 全字段 + chunk-total=0；sha256 空/已知向量（abc）+ 与 node crypto 随机字节对照（Uint8Array & ArrayBuffer）；Page 集成保存并上传生成 fragment_id+sha256+manifest、device_short_id 复用、同秒两条不同 fragment_id 同 device。
- 更改的文件：apps/miniprogram/utils/{sha256.js(新),ulid.js(新),device.js(新),audio.js,upload_queue.js}、apps/miniprogram/pages/index/index.js、apps/miniprogram/app.js、apps/miniprogram/test/ids.test.js(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 39 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（311 passed；JS 包装测试内部 node --test 31/31：13 ids + 9 草稿 + 9 中断）全部通过；8 个改动/新增 .js 全部 node --check OK；sha256 已对照 node crypto 验证正确（含空输入 e3b0c4… 与 "abc" ba7816… 向量）。
  未跑：微信开发者工具连续录两条短录音并保存上传、vConsole 显示两个不同 fragment_id 且 device_short_id 一致（AC#8 真机/模拟器项）——本沙箱无 WeChat DevTools/真机，需人工复验（集成测试已在 node 侧等价验证同秒两条不同 fragment_id + device 一致）。
- **未来迭代的学习：**
  - fragment_id/device_short_id 正则的单一真实来源有两处需同步：前端 audio.FRAGMENT_ID_RE / device.DEVICE_SHORT_ID_RE 必须与 FC 的 fc_shared/sts.py _FRAGMENT_ID_RE（deviceShortId [A-Za-z0-9]{4,8}、ULID [0-9A-Za-z]{26}）一致，否则前端生成的 id 会被 FC object_key_for 400。改一处务必核对另一处。
  - 同秒唯一性靠 ULID 单调（monotonicFactory），不靠时间前缀（fragment 时间前缀只到秒，同秒必然相同）；session_id 与 fragment ULID 都从同一 this._nextUlid 取，单调保证彼此不同。US-016 会把 session_id 提前到录音开始（本期单条在保存时生成 chunk_seq=1/chunk_total=null）。
  - OSS meta 与 manifest 的 chunk_total 语义不同：manifest 非分片存 null，OSS x-oss-meta-chunk-total 存 "0"（§3.2 约定，Worker HeadObject 读回 0→manifest null）。buildOssMetadata 已做该映射，勿在 Page 侧另写。
  - sha256 纯 JS 实现已对照 node crypto 验证；大文件（25min wav）主线程计算可能卡顿，AGENTS 建议 wasm-crypto，属后续性能优化；前端读字节用 wx.getFileSystemManager().readFileSync(path)→ArrayBuffer，sha256Hex 直接吃 ArrayBuffer。
  - 日志安全：logger SENSITIVE_KEY_RE 不匹配 sha256/session_id/fragment_id/deviceShortId（无 secret/token/api_key/...key 子串），可安全打印；仍只打 sha256 前 12 位前缀（非密钥但避免冗长）。
---

## 2026-06-27 13:00 - US-016
- 实现了什么：小程序长录音自动分片与 session 聚合元数据（tech-spec §3.1）。超过分片阈值（CHUNK_MAX_DURATION_SECONDS=600s）的录音自动切成多个 Fragment，整段共享 session_id，每片独立 fragment_id、chunk_seq 从 1 递增，最终停止后回填 chunk_total。沿用「纯函数 utils + Page 薄壳」分层。
  - apps/miniprogram/utils/chunking.js（新增纯逻辑模块）：shouldRotateChunk(chunkElapsedSeconds>=600，AC#2/#5)、createRecordingSession(sessionId)（AC#1）、addChunk(session,record)→分配 chunk_seq 从 1 递增（AC#3）、chunkCount、resolveChunkTotal(count>1?count:null)、backfillChunkTotal(manifests)→把片数回填到所有分片 manifest（单片回填 null，AC#4）。CHUNK_MAX_DURATION_SECONDS 复用 config.js 常量。
  - apps/miniprogram/pages/index/index.js：
    · onLoad 新增会话状态 _session/_chunkStartedAt/_sessionElapsedMs/_userStopping/_rotating。
    · _startRecording：录音开始即 createRecordingSession(this._nextUlid()) 分配 session_id（AC#1）；MAX_RECORD_DURATION_MS 改为 CHUNK_MAX_DURATION_SECONDS*1000。
    · _startNextChunk：分片旋转后沿用同一 session 立即 start 下一片（AC#2）。
    · 计时器（_startTimer）每秒计算「当前片已录秒数 + 已完成片累计时长」，显示总时长，并调 _maybeRotateChunk(chunkElapsed)：到阈值 _rotating 守卫下 stop 当前片。
    · _stopRecording 设 _userStopping=true，区分「最终停止」与「分片边界自动 stop」。
    · _onRecordStop 重构为路由：!interrupted && !userStop → 分片边界（addChunk + _startNextChunk）；interrupted → _finalizeInterrupted；userStop → _finalizeStopped。_captureChunkRecord 归一化 res（recordedAt 用该片 _chunkStartedAt）。
    · _finalizeStopped：单片（chunkCount<=1）走 US-012/014 单草稿确认态（buildDraftManifest，chunk_total=null，行为不变）；多片走 _buildSessionDraft(false) 长录音聚合草稿（is_long/chunk_total/session_id/总时长）。
    · _finalizeInterrupted：单片沿用 US-013 单槽位中断草稿落盘（buildInterruptedDraft，行为不变）；多片落盘长录音中断草稿。
    · onTapSaveUpload：draft.is_long && 多片 → _saveLongRecording(deviceShortId)：逐片 buildFragmentId（独立 ULID）+ _computeOriginalSha256 + buildUploadManifestDraft（共享 sessionId、chunk_seq 已分配、chunk_total 暂 null）→ backfillChunkTotal 回填（AC#4）→ 逐片 buildOssMetadata + buildQueuedFragment + appendQueuedFragment 落盘；单片走原 US-015 路径（sessionId 改取 this._session.sessionId）。
    · _cleanupDraftFile：长录音重录/删除时清理该 session 下所有分片临时文件。
  - apps/miniprogram/pages/index/index.wxml：新增 wx:if=draft.is_long 的「长录音：N 段」展示行。
  - apps/miniprogram/test/chunking.test.js（新增，node --test，8 用例）：纯函数（阈值分界/session 分配/chunk_seq 递增/resolveChunkTotal/backfillChunkTotal）+ Page 集成（录音开始分配 26 字符 session_id 且分片不变；25 分钟切 3 片 chunk_seq 1/2/3 共享 session、总时长 1500s、单片≤605s、确认入队 3 条独立 fragment_id+chunk_total=3+OSS meta；未达阈值单条 chunk_total=null/OSS chunk-total=0；长录音重录清理全部分片临时文件）。
- 更改的文件：apps/miniprogram/utils/chunking.js(新)、apps/miniprogram/pages/index/{index.js,index.wxml}、apps/miniprogram/test/chunking.test.js(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 39 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（311 passed；JS 包装测试内部 node --test 39/39：8 chunking + 13 ids + 9 草稿 + 9 中断）全部通过；node --check chunking.js / index.js OK。
  未跑：微信开发者工具 / 真机录制 25 分钟生成 3 条 Fragment、三条共享 session_id、chunk_seq 1/2/3、chunk_total 3、拼接总时长约 25 分钟（AC#7/#8 真机项）——本沙箱无 WeChat DevTools/真机，需人工复验；Page 集成测试已在 node 侧用 _maybeRotateChunk 模拟分片边界做等价验证。
- **未来迭代的学习：**
  - 录音状态机路由口径（US-016）：onStop 用「!interrupted && !userStop」判定分片边界——既非用户最终停止也非中断的 onStop（计时器旋转 stop 或微信 RecorderManager 到达 duration 上限 600000ms 自停）一律视为分片边界，落片后自动 start 下一片。_userStopping 在 _stopRecording 设，_interrupting 在 _handleInterruption 设，两者在 _onRecordStop 顶部读取后立即复位。
  - session_id 分配时机从 US-015 的「保存时生成」提前到「录音开始」（AC#1）：单条录音 onTapSaveUpload 改取 this._session.sessionId（回退 _nextUlid 兜底）；不影响 US-015 同秒两条 fragment_id 唯一性（仍靠单调 ULID）。
  - 分片边界用计时器（每秒）判断 chunkElapsed>=600 触发，配合 stop→start 极小间隙单片实际 ≤601s（≤605s 满足 AC#5）；微信 recorder duration 上限恰为 600000ms，故即使计时器漏触发，recorder 自停也会被 onStop 分片边界路由兜住。
  - 长录音确认态复用单草稿 UI：_buildSessionDraft 产出 is_long/chunk_total/session_id/聚合总时长 + 最后一片 temp_file_path（供试听）；真实分片记录始终以 this._session.chunks 为权威（含 Date 类型 recordedAt），draft 仅作展示，避免 setData 序列化 Date 问题。onTapSaveUpload 多片分支读 this._session.chunks 而非 draft。
  - 测试模拟分片：Page 集成测试调 inst._maybeRotateChunk(600) 触发 stop，再 recorder.handlers.stop(res) 走分片边界路由 → addChunk + _startNextChunk（recorder.start++）；最终 inst.onTapRecord() 触发 _stopRecording 收尾。每次 _startTimer 起真实 setInterval，最终停止后已 _clearTimer，无需额外 onUnload，但测试结尾仍 onUnload 兜底防事件循环挂起（沿用 US-013/014 陷阱）。
---

## 2026-06-27 14:00 - US-017
- 实现了什么：小程序上传队列、静默登录、获取单文件 STS 与 OSS 直传（tech-spec §3.2/§4.1），
  以及 make show-oss-object（US-017 首交付）与 make test-sts-escape（AC#8）。沿用「纯函数 utils + Page 薄壳」
  与「纯逻辑 + IO 注入 Probe」分层。
  - apps/miniprogram/utils/sha256.js：重构出 hashWords 内核，新增 sha256Bytes（32 字节 digest）/ bytesToHex /
    导出 toBytes，供 HMAC 复用；sha256Hex 行为不变。
  - apps/miniprogram/utils/hmac.js（新，纯函数）：hmacSha256/hmacSha256Hex（复用 sha256Bytes 内核）+ base64Encode
    （微信无 btoa，纯实现）。已对照 node:crypto 标准向量与随机字节校验。
  - apps/miniprogram/utils/oss_sign.js（新，纯函数）：buildPostObjectForm —— OSS V4 PostObject 表单签名
    （OSS4-HMAC-SHA256，policy base64 + 派生签名密钥链 aliyun_v4→date→region→oss→aliyun_v4_request）。
    注入 now 确定性产出；object_key 用 FC 返回值（AC#4），全部 x-oss-meta-* 进表单条件与字段（AC#5）。
    签名已与独立 node:crypto V4 计算逐字节对照一致。
  - apps/miniprogram/utils/upload_queue.js：补全 tech-spec §6.7 八状态常量与中文文案（pending_verify=待 verify、
    manual_retry=待人工重传、upload_failed=上传失败、verified=上传成功、manual_verify=待人工 verify）；
    新增 updateQueueItem（按 fragmentId 不可变更新，自动同步 statusText）。八态全量渲染/折叠仍属 US-019。
  - apps/miniprogram/utils/uploader.js（新，纯逻辑 + 注入 deps）：classifyFcResponse（200+7 字段→凭证；非 200→
    取 body.error 稳定错误码，AC#3）、RETRY_DELAYS_MS=[5s,15s,45s]/MAX=3、uploadFragment 编排——
    onStatus(uploading)→login→requestSts→FC 非 200 立即待人工重传记错误码（AC#1/#3）→buildPostObjectForm→
    uploadFile 带进度回调，OSS 非 2xx/网络错误按退避重试最多 3 次（共 4 次尝试）仍失败→待人工重传，
    2xx→待 verify（AC#6/#7）。绝不打印 secret/token（仅记 object_key/状态/错误码）。
  - apps/miniprogram/config.js：新增 OSS_REGION='cn-beijing'（V4 签名所需）。
  - apps/miniprogram/pages/uploads/uploads.js：onShow 自动驱动 queued 项上传——_processQueue（_processing 锁防并发、
    wx.getNetworkType 离线则跳过保持 queued）→ _uploadOne 构造真实 wx 适配器（wx.login/wx.request POST
    FC_ISSUE_CREDENTIAL_URL/wx.uploadFile OSS_UPLOAD_URL + onProgressUpdate）交给 uploader.uploadFragment；
    onStatus/onProgress 即时 updateQueueItem 落盘 + setData。uploads.wxml/wxss 增加进度% 与红色错误码展示。
  - apps/worker/src/soniscope_worker/oss_admin.py：OssObjectStore 协议新增 head_object；ObjectStat dataclass
    （exists/size/etag/last_modified/metadata）+ RealOssObjectStore.head_object（404→不存在，读 metadata 用户元数据）
    + _is_not_found + format_object_stat + run_show_oss_object（存在/不存在均 exit 0；非法 id/缺依赖/HeadObject 异常 exit 1）。
  - apps/worker/src/soniscope_worker/sts_escape.py（新，make test-sts-escape）：assert_escape_denied（纯，复用
    verify_prep.is_denied）+ StsEscapeProbes Protocol + RealStsEscapeProbes（无 CODE 时用部署凭证 AssumeRole 单 key
    签发「等价测试凭证」，有 CODE 时走 FC issue-credential）→ put_object 越权写另一个 key → 断言 AccessDenied。
    缺凭证 SKIP（本地 exit 0），真实环境跑真实反例。复用 fc_live.IssuedCredential/other_recordings_key/make_fragment_id。
  - cli.py：新增 show-oss-object（--fragment-id）+ test-sts-escape（--code 可选）。
  - Makefile：新增 show-oss-object（FRAGMENT_ID=）+ test-sts-escape（CODE= 可选）target + .PHONY。
  - miniprogram_lint.py：硬编码密钥扫描豁免 test/ 目录（单测夹具刻意用假密钥验证脱敏/上传链路；生产源码仍严扫）。
  - 测试：apps/miniprogram/test/{oss_sign.test.js(8),uploader.test.js(13)}（node --test，对照 node:crypto）；
    apps/worker/tests/{test_oss_admin.py(+10 head/show),test_sts_escape.py(新 10)}。
- 更改的文件：apps/miniprogram/utils/{sha256,hmac(新),oss_sign(新),upload_queue,uploader(新)}.js、
  apps/miniprogram/config.js、apps/miniprogram/pages/uploads/{uploads.js,uploads.wxml,uploads.wxss}、
  apps/miniprogram/test/{oss_sign,uploader}.test.js(新)、
  apps/worker/src/soniscope_worker/{oss_admin,sts_escape(新),cli,miniprogram_lint}.py、Makefile、
  apps/worker/tests/{test_oss_admin,test_sts_escape(新)}.py、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 41 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、
  make test（327 passed，+16）全部通过；node --test 全量 60/60（含 +21 新：8 oss_sign + 13 uploader）；
  CLI --help 显示两命令；test-sts-escape 无凭证优雅 SKIP exit 0。OSS V4 签名已与 node:crypto 独立计算逐字节对照一致。
  未跑：微信开发者工具录 5 秒保存上传 + make show-oss-object 核对 size（AC#10 真机/模拟器项，需真实 config.yaml
  + 触网 OSS）；真实 FC 部署后 wx.login code 联调；真实 test-sts-escape（需部署 AK + OSS SDK 触网）——本沙箱不具备，
  需人工在 DevTools/真机 + 部署主机复验。
- **未来迭代的学习：**
  - OSS 直传走「PostObject 表单上传」而非 PUT：wx.uploadFile 只能 POST multipart/form-data，故用 OSS V4
    PostObject（policy + OSS4-HMAC-SHA256 签名），不是带 Authorization 头的 PUT。表单字段在 oss_sign.buildPostObjectForm
    统一产出（key/policy/x-oss-signature-version/x-oss-credential/x-oss-date/x-oss-security-token/x-oss-signature/
    success_action_status + 全部 x-oss-meta-*），喂给 wx.uploadFile 的 formData。HMAC-SHA256 纯 JS 复用 sha256 内核。
  - 签名正确性已用 node:crypto 独立重算 V4 链对照（test/oss_sign.test.js）；本沙箱无真实 OSS，真机首次直传若 403
    多半是 region/时间/policy 条件不匹配，按 oss_sign 的条件逐项核对。
  - uploads 页 _processQueue 用 _processing 锁防并发：onShow 会触发一次未 await 的 _processQueue（持锁），
    测试里若再显式 await _processQueue() 会因持锁早返回而拿到中间态——测试应直接 await 单次 _processQueue()，
    不要叠加 onShow()（沿用 US-013/014 的 Page harness：global.Page 捕获 + delete require.cache + setData 浅合并）。
  - 错误码语义（AC#3）：FC 非 200 立即 待人工重传（4xx 鉴权/配额/参数不重试，AGENTS 错误处理表）；OSS 非 2xx/网络
    才走 5s/15s/45s 退避（最多 3 次，共 4 次尝试）。uploader 把这两类区分开：requestSts 失败/分类失败→manual_retry，
    uploadFile 失败→退避循环。
  - show-oss-object（US-017 首交付）扩展了 oss_admin.OssObjectStore 协议加 head_object，所有实现/Fake 必须同步加
    （test_oss_admin.FakeStore 已加，否则 mypy 结构化协议报缺方法）；US-029 verify-oss-retention/list-oss-objects 可复用
    RealOssObjectStore + object_key_for，不重复实现。
  - test-sts-escape 默认用部署凭证 AssumeRole 单 key 出「等价测试凭证」（无需一次性 wx.login code，部署主机可常态跑），
    传 CODE= 才走 FC 真实链路；与 fc_live 的越权反例共用 verify_prep._oss_sts_client/_run_oss_op/is_denied。
  - miniprogram_lint 硬编码密钥扫描已豁免 test/ 目录：单测需用 access_key_secret/security_token 等真实字段名构造夹具，
    生产源码（utils/pages/app.js）仍严扫；新增小程序单测放 test/ 下即可用假密钥。
---

## 2026-06-27 15:00 - US-018
- 实现了什么：小程序上传后 verify 回执、48 小时本地保留与手动删除二次确认（tech-spec §4.2/§6.7，AGENTS 错误处理表）。沿用「纯函数 utils + Page 薄壳」与「纯逻辑 + IO 注入」分层。
  - apps/miniprogram/utils/verify.js（新，纯逻辑 + 注入 deps）：classifyVerifyResponse（200 verified:true→verified；200 verified:false→unverified+reason OBJECT_NOT_FOUND/SIZE_MISMATCH；5xx→retryable；4xx→fatal）；VERIFY_RETRY_DELAYS_MS=[5s,15s,45s]/MAX=3；verifyFragment 编排——每次尝试 login（一次性 code）→requestVerify(code,fragment_id,expected_size，AC#1)→verified:true 写 verifiedAt+STATUS_VERIFIED（AC#2）；unverified→STATUS_MANUAL_RETRY+reason（AC#3）；4xx→立即 STATUS_MANUAL_VERIFY 不重试；网络/5xx 按退避重试 3 次仍失败→STATUS_MANUAL_VERIFY（AC#4）。绝不打印敏感字段。
  - apps/miniprogram/utils/retention.js（新，纯函数）：RETENTION_HOURS=48；canAutoDelete（仅 verified+未清理+verifiedAt 距今>=48h，AC#5；verifiedAt==null/'' 视为缺失，但允许 0 这类合法 epoch）；selectAutoDeletable；needsDeleteConfirmation（非 verified 需确认，AC#7）；DELETE_CONFIRM_MESSAGE（PRD/AC#7 措辞）。
  - apps/miniprogram/pages/uploads/uploads.js：onShow 改为 _autoCleanup→_loadQueue→_driveQueue（先 _processQueue 上传，再 _processVerifyQueue verify，串行）；新增 _processVerifyQueue（_verifying 锁，pending_verify→_verifyOne）、_verifyOne（构造 wx 适配器 + _wxRequestVerify POST FC_VERIFY_UPLOAD_URL）、onTapReVerify（AC#9 手动重新 verify）、onTapDelete+_deleteItem（AC#7 needsDeleteConfirmation→wx.showModal 二次确认；verified 直接删）、_autoCleanup（selectAutoDeletable→unlink 本地文件+标记 localDeleted，保留队列记录与 OSS 对象，AC#5）、_unlinkFile（wx.getFileSystemManager().unlink best-effort）。
  - apps/miniprogram/pages/uploads/uploads.wxml/wxss：item 改为竖排（main/meta/actions），展示 reason、本地缓存已清理 note；新增「重新 verify」（pending_verify/manual_verify）与「删除」按钮。
  - apps/miniprogram/test/verify.test.js（新，node --test，25 用例）：classifyVerifyResponse 四类；verifyFragment verified/OBJECT_NOT_FOUND/SIZE_MISMATCH/5xx 退避 3 次（waits=[5s,15s,45s]）/网络错误/第 3 次成功/4xx 不重试；retention canAutoDelete 48h 分界/未 verified 超 7 天不删/localDeleted/缺 verifiedAt、selectAutoDeletable、needsDeleteConfirmation；Page harness（mock wx 含 getFileSystemManager/showModal，request 按 URL 路由 verify-upload）：pending_verify→verified 落盘、重新 verify→manual_retry+OBJECT_NOT_FOUND（AC#9）、_autoCleanup 48h 删/不删/未 verified 不删、手动删除确认/取消/verified 免确认。
- 更改的文件：apps/miniprogram/utils/{verify.js(新),retention.js(新)}、apps/miniprogram/pages/uploads/{uploads.js,uploads.wxml,uploads.wxss}、apps/miniprogram/test/verify.test.js(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 41 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（327 passed，JS 包装测试内部 node --test 85/85：+25 verify）全部通过；node --check 4 个改动/新增 .js 通过。
  未跑：微信开发者工具上传短录音成功后状态为上传成功；删除 OSS 对象后点击重新 verify→待人工重传+OBJECT_NOT_FOUND（AC#9 真机/模拟器项，需真实 FC 部署 + wx.login code + 触网 OSS）——本沙箱无 WeChat DevTools/真机，需人工复验（Page 集成测试已在 node 侧等价验证 verify 成功/OBJECT_NOT_FOUND/48h 清理/手动删除确认）。
- **未来迭代的学习：**
  - verify 与 upload 在 uploads 页拆成两条独立循环：_processQueue（仅 queued→上传，行为不变，保 US-017 测试绿）+ _processVerifyQueue（pending_verify→verify）。关键：onShow 走 _driveQueue 串行跑两者，但 US-017 既有测试只 `await _processQueue()`，故上传循环绝不能内联 verify，否则那条「queued→待 verify」断言会被 verify 续跑覆盖成 verified/manual。新增自动后续动作时，优先拆成独立 driver 而非内联进既有循环。
  - verified_at 落在队列项 top-level `verifiedAt`（ms epoch，retention 权威读取），概念上对应 manifest.upload.verified_at；队列项是本地状态权威（沿用 US-014/017 denormalized 字段约定），不做 manifest 嵌套写（updateQueueItem 是浅合并）。
  - 退避/重试三态语义（与 uploader 一致但分工不同）：FC/verify 的 4xx 立即失败（uploader→manual_retry；verify→manual_verify，因上传已成功只是 verify 失败）；网络/5xx 才退避 5s/15s/45s（最多 3 次，共 4 次尝试）。verify 失败 3 次→manual_verify（待人工 verify），verified:false 业务原因→manual_retry（待人工重传）——两者状态不同，勿混。
  - 陷阱：canAutoDelete 不能用 `if (!verifiedAt) return false` 做缺失判断（会误杀 epoch 0）；用 `== null || === ''` 判缺失 + Number.isFinite 判数值。真实 verifiedAt 来自 Date.now() 永不为 0，但单测用小值要注意。
  - 自动清理只删本地音频缓存（unlink tempFilePath）并打 localDeleted 标记，保留队列记录（列表仍显示「上传成功」+「本地缓存已清理」），OSS 对象永不删除（AGENTS 红线）；US-019 八状态全量渲染/折叠时复用 localDeleted 展示。
  - Page harness mock wx 新增 getFileSystemManager().unlink 与 showModal（按 scenario.confirmDelete 决定 confirm）；request 用 `url.indexOf('verify-upload')` 路由区分 issue-credential 与 verify-upload 两个 FC 调用。
---

## 2026-06-27 07:00 - US-019
- 实现了什么：上传列表页八种状态全量渲染、顶部离线积压提示、长录音折叠卡片与手动重传入口（tech-spec §6.7 / ADR-4）。沿用小程序「纯逻辑模块 + Page 薄 IO 壳」分层。
  - apps/miniprogram/utils/uploads_view.js（新增，纯函数，node --test 覆盖）：
    · 状态归类：BACKLOG_STATUSES（queued/upload_failed/manual_retry/manual_verify，AC#4 计数集）、FAILED_STATUSES（红色标记/聚合失败）、MANUAL_RETRY_STATUSES（upload_failed/manual_retry，AC#2/#3）、RE_VERIFY_STATUSES（pending_verify/manual_verify/verified）；canManualRetry/canReVerify/isFailed/isBacklog。
    · buildBanner(queue, nowMs)（AC#4）：有积压时返回「未上传 N 条，距离最早录音已 X 小时」；N 按单 chunk 计数；X=最早积压 manifest.recorded_at 到 now 的整小时（hoursBetween 向下取整不为负，earliestBacklogMs 仅算积压项）。
    · buildCards(queue)（AC#6/#7/#8）：按 manifest.session_id 折叠——同 session ≥2 chunk → session 卡片（chunk 按 chunk_seq 升序、summaryText="mm:ss · N 段"、aggregateStatus：任一失败→"X / N 失败"、全 verified→"已完成"、其余→"M / N 已完成"）；单条/无 session_id → single 卡片。toChunkView 统一产出 statusText/durationText/canManualRetry/canReVerify/isFailed 等。
  - apps/miniprogram/utils/upload_queue.js：导出 STATUS_DRAFT 常量（八态齐全）。
  - pages/uploads/uploads.js：data 由 items 改为 { cards, banner, expanded }；_render(queue) 统一经 buildCards/buildBanner 渲染（_writeQueue/_loadQueue 复用）；新增 onToggleSession（AC#8 展开/收起）、onTapManualRetry（AC#2/#3：清 errorCode/reason、状态回 queued 重置重试计数 → _uploadOne → 若 pending_verify 再 _verifyOne；只影响该条不触其它 chunk）。
  - pages/uploads/uploads.wxml：重写为 banner + single 卡片 + session 折叠卡片（展开后逐 chunk 显示 chunk_seq/状态/独立手动重传+重新 verify+删除按钮）。
  - pages/uploads/uploads.wxss：新增 .banner（红色积压提示）、.item--failed/.item-status--failed（红色标记）、.session/.session-* 折叠卡片样式、.item-btn--retry。
  - test/uploads_view.test.js（新增 17 用例）：八态文案、归类、countBacklog/hoursBetween/buildBanner、buildCards 单条/折叠/排序/聚合三态/多 session 顺序；Page harness：手动重传跑到 verified、onShow 渲染折叠卡片+banner、onToggleSession 切换。
- 更改的文件：apps/miniprogram/utils/{uploads_view.js(新),upload_queue.js}、pages/uploads/{uploads.js,uploads.wxml,uploads.wxss}、test/uploads_view.test.js(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 41 文件 0 错）、make lint（ruff + miniprogram lint 全过）、make test（pytest 327 passed）全部通过；node --test test/*.test.js（102 passed，+17）。
  未自动验证（需人工）：AC#9 DevTools 构造本地状态看八态文案、AC#10 真机飞行模式离线积压与红色提示、网络恢复自动流转——本沙箱无 WeChat DevTools/真机，已用 node Page harness + mock wx 最大化覆盖。
- **未来迭代的学习：**
  - 离线 → queued（待上传/离线排队）的状态在 US-014/US-017 已落地（保存即 queued，onShow 离线时 _processQueue 跳过、网络恢复自动上传）；US-019 只负责「展示 + 手动重传」，无需改入队逻辑。
  - 折叠依据是 manifest.session_id（US-016 长录音各 chunk 共享）；单条录音 session_id 也存在但只有一片，故按「≥2 chunk 才折叠」判定。
  - 陷阱：本环境 Write 工具偶发把结尾标记（</content> / </invoke>）写进文件尾，导致 node --check 报 "Invalid regular expression: missing /"；新建文件后用 tail/grep 自检结尾再跑测试。
  - 上下文：上传列表渲染统一走 uploads.js `_render`（buildCards/buildBanner）；后续若加状态/筛选，改 uploads_view.js 纯函数即可，Page 不动。
---

## 2026-06-27 08:00 - US-020
- 实现了什么：小程序开发者故障注入菜单（tech-spec §6.1）——仅非 production 可见、运行时切换的三个开关，用于真机构造 FC 失效、网络离线、verify 失败异常路径。沿用「纯逻辑 utils + Page 薄 IO 壳」与「ENV 门控」。
  - apps/miniprogram/utils/fault_injection.js（新，纯函数 + 注入 storage deps，node --test 覆盖）：
    · 三个开关常量 FAULT_FC_URL_BROKEN='mock-fc-url-broken' / FAULT_NETWORK_OFFLINE='mock-network-offline' / FAULT_VERIFY_FAIL='mock-verify-fail' + FAULT_SWITCHES（含 label/hint）+ FAULT_STORAGE_KEY='soniscope:fault_injection'。
    · 纯逻辑：isDevEnv（env!=='production'，AC#1）、normalizeFaults（未知键丢弃、布尔化、默认全关）、isEnabled/setFault/toggleFault（不可变）、buildSwitchViews（name/label/hint/enabled 视图模型）。
    · loadFaults/saveFaults（注入 {env,getStorage,setStorage}）：统一 ENV 门控——production 读永远返回全关、写直接忽略（即便菜单已不可见也安全兜底，AC#1）。
  - apps/miniprogram/pages/dev/dev.{js,wxml,wxss,json}（新页面）：devEnv 据 config.ENV 判定；非 prod 渲染三个开关卡片，bindtap onToggleSwitch 运行时 toggleFault+saveFaults 落盘并 _refresh（AC#5）；production 分支只显示「生产环境不可用」兜底。
  - apps/miniprogram/app.json：注册 pages/dev/dev（miniprogram_lint 会校验四件套齐全，已补全）。
  - apps/miniprogram/pages/uploads/uploads.js：新增 _faults()（loadFaults，受 config.ENV 门控）；onLoad setData devMenuVisible=isDevEnv（AC#1 入口）；onTapDevMenu→wx.navigateTo /pages/dev/dev；三处适配器接入开关效果：
    · _isOnline：mock-network-offline → 直接 resolve(false)，queued 项保持待上传（AC#3）。
    · _wxRequestSts：mock-fc-url-broken → reject，FC 请求强制失败（AC#2）→ uploader 走 FC_UNREACHABLE → 待人工重传。
    · _wxRequestVerify：mock-fc-url-broken → reject；mock-verify-fail → resolve {statusCode:200,data:{verified:false,reason:'OBJECT_NOT_FOUND'}}（AC#4）→ verify 走 unverified → 待人工重传。
  - apps/miniprogram/pages/uploads/uploads.wxml/wxss：顶部加 wx:if=devMenuVisible 的「⚙ 开发者故障注入」入口（AC#1）。
  - apps/miniprogram/test/fault_injection.test.js（新，15 用例）：纯逻辑全覆盖（isDevEnv/normalize/toggle/buildSwitchViews/loadFaults·saveFaults 的 prod 门控）；dev 页 harness（dev 环境三开关切换落盘、production 菜单不可用且切换不落盘——用 mutate config.ENV 再 finally 还原）；uploads 页 harness（devMenuVisible+navigateTo、mock-network-offline 保持 queued、关开关后自动上传、mock-fc-url-broken→待人工重传、关后手动重传成功、mock-verify-fail→待人工重传+OBJECT_NOT_FOUND）。
- 更改的文件：apps/miniprogram/utils/fault_injection.js(新)、apps/miniprogram/pages/dev/dev.{js,wxml,wxss,json}(新)、apps/miniprogram/app.json、apps/miniprogram/pages/uploads/{uploads.js,uploads.wxml,uploads.wxss}、apps/miniprogram/test/fault_injection.test.js(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 41 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed，新 dev 页四件套通过 check_app_pages）、make test（pytest 327 passed）全部通过；node --test apps/miniprogram/test/*.test.js 117 passed（+15）；node --check 全部新增/改动 .js 通过；app.json/dev.json JSON 可解析。
  未自动验证（需人工 WeChat DevTools/真机）：AC#6 打开 mock-fc-url-broken 真机录音自动重试 3 次失败→列表变红→关闭后手动重传成功；AC#7 打开 mock-network-offline 真机进入离线排队→关闭后自动恢复——本沙箱无 DevTools/真机，已用 node Page harness + mock wx 等价覆盖三个开关效果与 ENV 门控。
- **未来迭代的学习：**
  - ENV 门控单一来源是 config.js 的 ENV（当前 'development'）；故障注入读写都过 fault_injection.loadFaults/saveFaults 的门控，production 强制全关 + 不落盘，无需在每个调用点重复判 env。
  - 新增小程序页面必须：① app.json pages 注册；② 四件套（.js/.json/.wxml/.wxss）齐全——否则 miniprogram_lint.check_app_pages 报「缺少 X 文件」使 make lint 失败。
  - dev 页 production 分支测试用「mutate 已 require 的 config 对象的 ENV 属性 + finally 还原 + delete require.cache[CONFIG]」：因 dev.js 在函数内动态读 config.ENV（非模块加载时缓存），mutate 即生效；同理可测任何依赖 config.ENV 的 production 路径。
  - 故障注入效果落在 uploads 页的三个请求适配器（_isOnline/_wxRequestSts/_wxRequestVerify），不改 uploader/verify 纯逻辑，保 US-017/018 既有测试绿；mock-fc-url-broken 同时作用于 issue-credential 与 verify-upload 两类 FC 请求（reject）。
  - 陷阱：navigateTo 目标 dev 页不是 tabBar 页（tabBar 只有 index/uploads），故用 wx.navigateTo 而非 switchTab。
---

## 2026-06-27 16:00 - US-021
- 实现了什么：Worker OSS 轮询、HeadObject 元数据读取与安全下载（tech-spec §3.2/§5；Worker 主流水线入口）。沿用既有「纯逻辑（无 IO，直接单测）+ IO 用 OssSource Protocol 注入」分层，单测注入 FakeSource 全程不触网。
  - apps/worker/src/soniscope_worker/poller.py（新增，mypy strict + ruff + pytest 全覆盖）：
    · 纯逻辑：fragment_id_from_key（OSS key↔fragment_id 往返校验 object_key_for(fid)==key，一并验格式/日期合法/路径日期一致，AC#1；非 .wav/非 recordings/ 前缀/非法 id/日期不一致均 None）、date_of、fragment_dir/done_marker_path/part_path（本地路径推导）、normalize_metadata（小写+去 x-oss-meta- 前缀，兼容 SDK 两种返回）、metadata_to_draft（x-oss-meta-* → ManifestDraft：session_id/chunk_seq/chunk_total/recorded_at/duration_seconds/original_format/original_sha256；chunk-total "0"→None 遵 §3.2，AC#5）、plan_downloads（list 结果按 .done 拆 to_download/skipped_done/ignored_keys，AC#2）、check_scan_intervals（相邻扫描间隔 ±5s 容差校验，AC#8）。
    · IO 注入点 OssSource Protocol：**只暴露 list_recordings/head_metadata/download，刻意不含任何 delete 方法**（AGENTS 红线 AC#7）。RealOssSource（lazy import OSS SDK，复用 verify_prep._import_oss/_oss_client，用 config.yaml OSS 只读 AK）：list_objects_v2 分页列举 recordings/ 前缀、head_object 读 metadata、get_object_to_file 下载；构造时加载 config（缺失抛 ConfigError）、调用才触网。
    · process_plan（AC#3/#4）：下载到 inbox/<fragment_id>.part → sha256_of 计算 → head_metadata 读 x-oss-meta-sha256 比对；不一致 part.unlink（删**本地** .part 非 OSS）返回 sha256_mismatch 等下一轮重下；下载/读元数据异常收敛为 error（不中断整轮）。
    · cleanup_parts（AC#6）：删 inbox 残留 *.part（崩溃残留），下一轮重下，不污染 fragments/。
    · poll_once（列举→计划→逐个处理，已 .done 跳过不下载不转写）；poll_loop（启动先 cleanup_parts→每 interval 扫描；注入 sleep/monotonic/on_scan/max_iterations/stop 便于确定性单测与时间戳采集）。
    · run_worker_run（make worker-run：读 config interval 无限轮询，config 缺失打印诊断返回）；run_test_poll_interval（make test-poll-interval：读 config interval、--expected 断言、跑 N 轮校验扫描间隔；缺 config/SDK 优雅 SKIP exit 0，AC#8）。
  - cli.py：run 子命令从占位改为调 run_worker_run；新增 test-poll-interval（--expected 默认 30 / --iterations 默认 3）。
  - Makefile：worker-run 已存在（指向 run）；新增 test-poll-interval target（EXPECTED= ITERATIONS=）+ .PHONY。
  - apps/worker/tests/test_poller.py（新增 26 用例，FakeSource 内存 dict 全程不触网）：覆盖 key 解析五分支、元数据归一化/映射（含 chunk-total 0→None、OSS 前缀键、非法值→None）、plan_downloads 三分类、process_plan（sha 匹配下载/sha 不匹配删 .part/下载异常收敛/无 sha 元数据仍下载）、cleanup_parts、poll_once（.done 跳过零下载 + 新对象下载）、poll_loop（注入时钟跑 3 轮+启动清 .part+末轮不 sleep+stop 回调）、check_scan_intervals 容差/不足、run_test_poll_interval（pass/expected 不符 FAIL/无 config SKIP）、**安全断言 OssSource/RealOssSource 无任何 delete 方法（AC#7）**。
- 更改的文件：apps/worker/src/soniscope_worker/{poller.py(新),cli.py}、Makefile、apps/worker/tests/test_poller.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 43 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（356 passed，+26）全部通过；CLI --help 显示 test-poll-interval；`run` 在无 config 时优雅打印诊断返回（不触网）；grep 确认 poller.py 业务路径无任何 delete_object/DeleteObject 调用（仅红线注释提及）。
  未跑：真实 make worker-run / make test-poll-interval 全链路（需真实 config.yaml OSS 只读 AK + alibabacloud-oss-v2 + 触网列举/下载，且 test-poll-interval 真实需等 ~30s×3 轮，本沙箱不具备）——AC#1/#6/#8 真实云端项需在 Worker 主机复验。
- **未来迭代的学习：**
  - Worker 主流水线入口在 poller.py：US-022（ffprobe 格式检测/WAV 直通/转码）接 process_plan 下载成功后的 .part → audio.wav；US-023（启动恢复扫描）扩展 cleanup_parts（当前只清 *.part）到 *.wav.tmp / tmp/*.transcript.json.tmp + fragments 目录状态恢复；US-024（manifest schema）落盘 ManifestDraft（本处只产内存草稿，已含 OSS 元数据映射字段）；US-027（完整流水线）把「下载→标准化→转写→manifest→.done」串起并实现「.done 不重下」幂等（US-021 standalone 每轮对无 .done 对象都会重下，是有意为之，整合在 US-027）。
  - OSS key↔fragment_id 用 object_key_for 往返校验（fragment_id_from_key）：复用 oss_admin.object_key_for（_FRAGMENT_ID_RE + 日期合法性），一次校验格式/日期/路径一致，避免再写一份正则。改 fragment_id 正则务必同步 fc_shared/sts.py、前端 audio.FRAGMENT_ID_RE、oss_admin。
  - x-oss-meta-chunk-total 语义：OSS 端非分片存 "0"，metadata_to_draft 映射为 manifest chunk_total=None（与前端 buildOssMetadata 镜像，§3.2）；chunk_total<=0 一律 None。
  - 安全红线落实方式：OssSource Protocol **不定义** delete 方法（结构化协议层面禁止），RealOssSource 不实现 delete_*；单测断言 `dir(OssSource)` 公开成员恰为 {list_recordings, head_metadata, download} + RealOssSource 无 delete 属性。删除能力只在 oss_admin（仅测试用，双门控）。US-029 verify-oss-retention 扫日志也以「业务路径无 DeleteObject」为准。
  - .part 是下载中间态：sha256 不符删 .part（本地，非 OSS）等下一轮重下（AC#4）；崩溃残留 .part 启动 cleanup_parts 清理重下（AC#6）。「删 .part」与「删 OSS 对象」是两件事，前者随处可做、后者业务路径绝不做。
  - 轮询循环可测性与守护进程健壮性：poll_loop 注入 sleep/monotonic/on_scan/max_iterations/stop——单测用假时钟跑固定轮数+采集时间戳验间隔，真实 run_worker_run 用默认 time.sleep/time.monotonic 无限循环。**poll_loop 把每轮 poll_once 包在 try/except 里**：单轮扫描失败（如 list_recordings OSS 不可达）只记日志不杀死守护进程，下一轮重试——故 make test-poll-interval 即便 OSS 暂不可达也能维持 ~30s 节奏（每轮记错误后继续）；本地无 config 仍优雅 SKIP exit 0。process_plan 自身另有 try/except 收敛单对象失败为 error，两层互补。
---

## 2026-06-27 17:00 - US-022
- 实现了什么：Worker 音频格式检测、WAV 直通与非 WAV 转码（tech-spec §5.1/§3.3/§3.5）。沿用「纯逻辑（无 IO，直接单测）+ IO 用可注入 callable（probe/transcode）」分层，单测注入 fake 全程不触 ffmpeg/ffprobe。
  - apps/worker/src/soniscope_worker/audio.py（新增，mypy strict + ruff + pytest 覆盖）：
    · 纯逻辑：is_wav(MediaInfo)（format_name 含 wav 或流编码 pcm_*，与 fixtures.codec_matches 一致，不信扩展名，AC#1）、format_label（非 WAV 原始格式标签兜底：aac→m4a / 首个 codec / 容器名，仅元数据缺 original_format 时用）。
    · ffmpeg_to_wav（IO）：ffmpeg -i src -vn -ac 1 -ar 16000 -c:a pcm_s16le -f wav dest（ASR 友好 16k 单声道 PCM）；ffmpeg 缺失/转码失败抛 AudioToolError。**关键：输出文件名为 <id>.wav.tmp，.tmp 扩展名无法让 ffmpeg 推断 muxer，必须显式 -f wav**。
    · standardize(part, fragment_id, fragments_root, inbox_root, failed_root, original_format?, log, probe, transcode)→StandardizeResult（核心）：① 由 object_key_for 解析 date（非法 fragment_id→failed）；② 消费 part 前先算 original_sha256/original_size_bytes（保证非 WAV 路径两 sha 都非 null，AC#6）；③ ffprobe 探测（FixtureError→留档 inbox/failed/ +failed，不建 fragment 目录，AC#7）；④ is_wav→**直通**：mkdir fragment 目录+os.replace(part→audio.wav)，bytes 不变故 audio.sha256==original_sha256、size 相等（AC#2/#5）；⑤ 非 WAV→**转码**：先写 inbox/<id>.wav.tmp→os.replace→audio.wav，删 part，audio.sha256 真实重算（AC#3/#4/#6）；转码失败删 .wav.tmp 半成品+留档 inbox/failed/+不建 fragment 目录（AC#7/#10）。
    · make test 入口 run_test_wav_passthrough / run_test_audio_transcode_to_wav / run_test_transcode_fail：用真实 tests/audio fixtures + ffmpeg/ffprobe 在 tempfile 工作区端到端跑 + 断言（passthrough sha 相等 / transcode sha 不同且 audio.wav 可被 ffprobe 识别为 WAV / 损坏音频留档未污染 fragments）；缺 fixture 或 ffmpeg 不可用优雅 SKIP exit 0。
  - cli.py：新增 test-wav-passthrough / test-audio-transcode-to-wav / test-transcode-fail 三子命令（lazy import，typer.Exit(code)）。
  - Makefile：新增同名三 target + .PHONY。
  - apps/worker/tests/test_audio.py（新增 20 用例，FakeProbe/fake transcode 不触 ffmpeg）：is_wav/format_label 全分支、直通 rename 保 bytes+sha 相等+part 移走、original_format 探测兜底、转码产物 sha 不同+tmp 在 inbox+清理、探测失败留档+不建目录、转码失败留档+清 .wav.tmp+不建目录、缺 part/非法 fragment_id→failed、ok 属性、run_test_* 缺 fixture SKIP（monkeypatch _fixture_path）。
- 更改的文件：apps/worker/src/soniscope_worker/{audio.py(新),cli.py}、Makefile、apps/worker/tests/test_audio.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 45 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（375 passed，+20）全部通过；**本机 ffmpeg/ffprobe + 真实 fixtures 实跑三 make target 全绿**：test-wav-passthrough（sample-20s.wav 直通，audio.sha256==original b07dee76…）、test-audio-transcode-to-wav（sample-20s.m4a→WAV，original d3d28661… ≠ audio 32ebf875…，ffprobe 识别为 WAV）、test-transcode-fail（损坏字节探测失败留档 inbox/failed/ 未污染 fragments/）。
- **未来迭代的学习：**
  - ffmpeg 转码输出到 ``<id>.wav.tmp``（三段式协议要求临时文件）时，**必须显式 ``-f wav``**——ffmpeg 默认按输出扩展名选 muxer，``.tmp`` 无法识别会报「Error initializing the muxer ... Invalid argument」。输入 ``.part`` 则靠内容探测无需 ``-f``。
  - standardize 的「消费前先算 original_sha256/size」很关键：直通时 os.replace 把 part 移走、转码时 part 被删，之后再算就拿不到原始字节了；先算保证两路径 original_* 都真实非 null（§3.3 AC#5/#6）。
  - 三段式 + 失败隔离：直通/转码成功才 mkdir fragment 目录（os.replace 原子落 audio.wav）；任何失败（探测/转码）都只动 inbox（留档 inbox/failed/<id>.part + 清 .wav.tmp），**绝不创建 fragment 目录**，故崩溃/失败永不产生半成品完成态（US-023 启动恢复扫描会清 inbox/*.wav.tmp，与此互补）。
  - 本 story 只做「.part→audio.wav」一步，未接入 poller.poll_once（那是 US-027 完整流水线）。US-024 落 manifest 时直接用 StandardizeResult 的 audio_format/original_format/audio_sha256/audio_size_bytes/original_sha256/original_size_bytes（字段已按 §3.3 manifest.audio/upload 对齐）。
  - 单测 hermetic 原则：standardize 默认 probe=probe_media/transcode=ffmpeg_to_wav，单测一律注入 fake 不触系统工具；真实 ffmpeg/ffprobe 验证只放 make test-* target（本机实跑），与 AGENTS「单测 mock 外部依赖、真实调用进 live/test target」一致。
---

## 2026-06-27 18:00 - US-023
- 实现了什么：Worker 启动恢复扫描（§3.6 三段）与统一原子写入工具（§3.5），含 make test-crash-recovery / simulate-worker-crash。沿用「纯逻辑（无 IO，直接单测）+ IO 注入」分层，单测全程不触系统工具/云端。
  - apps/worker/src/soniscope_worker/recovery.py（新增，mypy strict + ruff + pytest 全覆盖）：
    · 原子写入工具（§3.5）：atomic_write_text（同目录 tempfile.mkstemp 临时文件→os.replace，失败清临时文件）、atomic_write_json（manifest.json 等）、write_transcript_json（**临时文件刻意写 tmp/<id>.transcript.json.tmp** 再 rename，使转写中断时第二段扫描能据此清理）、transcript_txt_from_segments（segments[].text 顺序拼接）、create_done_marker（**最后创建、0 字节空文件**，AC#6）。
    · finalize_fragment（转写落盘收尾，US-027 复用）：严格顺序 transcript.json（经 tmp）→ transcript.txt → .done 最后；任一步失败不创建 .done，杜绝半成品完成态。转写内容由调用方注入（make test 用确定性 stub，真实 NLS 转写器在 US-026）。
    · 三段恢复扫描：recover_inbox（清 *.wav.tmp + *.part，AC#1/#2）、recover_tmp（清 *.transcript.json.tmp，AC#3）、classify_fragment_dir/scan_fragments（fragments/<date>/<id> 判定 done/pending/empty：有 .done→跳过、无 .done+有 audio.wav→转写未完、无 audio.wav→空目录，AC#4）；recover() 编排（remove_empty_dirs 可选删空目录）+ RecoveryReport 汇总；recover_runtime() 对真实 $SONISCOPE_HOME 扫描（Worker 启动调用）。
    · run_test_crash_recovery（make target，自包含 tempdir）：构造 audio.wav 就绪但缺 transcript.json/.done 的转写中断现场 + inbox/tmp 全部中间态残留 → recover() 清理并判 pending → finalize_fragment 重转写补齐 transcript.json/.done（AC#7）。run_simulate_worker_crash（操作真实 $SONISCOPE_HOME，inbox_root/fragments_root 可注入便于单测）：CASE=missing-done 删 .done（AC#8）、CASE=stale-part 生成残留 .part（AC#9），未知 CASE / 缺 FRAGMENT_ID / 非法 id / 找不到目录均非零退出。
  - cli.py：新增 test-crash-recovery、simulate-worker-crash（--case/--fragment-id）两子命令。
  - poller.py run_worker_run：启动 poll_loop 前调 recover_runtime(log)（lazy import），把 §3.6 恢复扫描接入真实 Worker 启动（poll_loop 内既有 cleanup_parts 保留，US-021 测试不受影响）。
  - Makefile：新增 test-crash-recovery、simulate-worker-crash（CASE= FRAGMENT_ID=）两 target + .PHONY。
  - apps/worker/tests/test_recovery.py（新增 26 用例）：原子写入（无临时残留/JSON 往返/transcript 经 tmp/txt 拼接/.done 0 字节/finalize 全产物+done last）、inbox/tmp 清理（含缺目录安全、保留无关文件）、fragments 分类五分支（done/pending/empty/非法 id/日期不一致）、scan_fragments、recover 编排（清理+分类+summary、删空目录/默认忽略）、run_test_crash_recovery pass、simulate 六路径（未知 CASE/缺 id/非法 id/missing-done 删标记+无目录/stale-part 生成可被清理）。
- 更改的文件：apps/worker/src/soniscope_worker/{recovery.py(新),cli.py,poller.py}、Makefile、apps/worker/tests/test_recovery.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 47 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（401 passed，+26）全部通过；**实跑 make test-crash-recovery 全绿**（清理三类中间态残留 + 判 pending + 重转写补齐 transcript.json/txt/.done）；make simulate-worker-crash 未知 CASE 正确非零退出；CLI --help 显示两新命令。
  未跑：真实 $SONISCOPE_HOME 上 make simulate-worker-crash CASE=missing-done/stale-part FRAGMENT_ID=<真实 id> 后重启 Worker 的端到端恢复（需真实 fragment 目录 + 真实 OSS 重下 + US-026/027 真实转写器补齐 .done）——本沙箱无真实运行时数据与云端，simulate 的故障注入逻辑已单测覆盖、test-crash-recovery 已用确定性 stub 等价验证恢复→重转写→.done 全链路。
- **未来迭代的学习：**
  - 三段式协议的「临时文件位置」有讲究：transcript.json 的临时文件**必须**写 tmp/<id>.transcript.json.tmp（不是 fragment 目录内同级 tmp），因为这正是 §3.6 第二段「转写中断」的恢复信号；manifest.json / transcript.txt 用同目录 sibling 临时文件即可。recovery.write_transcript_json 已封装此约定，US-024/027 落 transcript 一律走它，勿自行 open+write。
  - .done 创建顺序是硬约束：finalize_fragment 内严格 transcript.json → transcript.txt → create_done_marker 最后；.done 必须 0 字节（create_done_marker 用 `open(path,'wb')` 空写）。US-027 串完整流水线时，manifest 终稿 + transcript 都落盘后才调 create_done_marker。
  - 恢复扫描入口已接入 run_worker_run（recover_runtime），但「重转写 pending fragments」的循环编排留给 US-027（poll_once 目前只下载，不标准化/转写）。US-027 把 process_plan 下载 → audio.standardize → finalize_fragment 串起，并对 recover() 返回的 pending 列表补转写。finalize_fragment 与 audio.StandardizeResult 字段已就绪可直接组装。
  - simulate-worker-crash 是「故障注入器」而非「恢复器」：它只删 .done / 造残留 .part，真正的修复由重启后的 recover_runtime + 主循环完成。单测注入 inbox_root/fragments_root 即可在 tempdir 验证注入效果，不碰真实 $SONISCOPE_HOME。
  - mypy strict 下 atomic_write_json 的 obj 参数用 Any（json.dumps 接受任意），dataclass 字段用 list[FragmentState]=field(default_factory=list)；tempfile.mkstemp 返回 (fd, str)，用 os.fdopen(fd,...) 包装写入避免 fd 泄漏。
---

## 2026-06-27 19:00 - US-024
- 实现了什么：manifest.json schema 组装、transcript.json/txt 落盘与 Fragment 完整性检查（tech-spec §3.3/§3.4/§3.5）。沿用「纯逻辑（无 IO，直接单测）+ make test 端到端用真实 fixtures」分层。
  - apps/worker/src/soniscope_worker/manifest.py（新增，mypy strict + ruff + pytest 全覆盖）：
    · 纯逻辑：device_id_of（从 fragment_id 中间段解析 deviceShortId，先经 object_key_for 校验格式/日期，非法抛 ManifestError）、build_manifest（按 §3.3 字段来源拼 manifest dict：fragment_id/device_id 从 id 解析；session_id/chunk_seq/chunk_total/recorded_at/duration_seconds/audio.original_format/upload.original_sha256 来自 ManifestDraft(OSS 元数据)；audio.sha256/size_bytes/format + upload.original_size_bytes 来自 StandardizeResult；upload.* 时间戳/transcription.* 流程填入。original_format 元数据缺失时用 std 探测兜底）、transcript_json_from_result（只留 segments/language/model/params_version/provider，**剔除内存态 duration**，§3.4）、manifest_without_timestamps（深拷贝剔除 TIMESTAMP_FIELD_PATHS：upload.uploaded_at/verified_at + transcription.started_at/completed_at/elapsed_seconds，供 idempotent 比对）。
    · 数据类 UploadInfo（uploaded_at/verified_at/verify_method=fc-head-object）、TranscriptionInfo（started_at/completed_at/elapsed_seconds/transcriber/model/params_version/provider/upload_mode）。
    · 落盘 write_fragment_outputs（§3.5 三段式，复用 recovery 原子写入）：① atomic_write_json manifest.json → ② write_transcript_json（经 tmp/<id>.transcript.json.tmp）→ ③ atomic_write_text transcript.txt（segments[].text 顺序拼接）→ ④ create_done_marker **.done 最后、0 字节**。任一步失败不创建 .done。
    · make test 入口 run_test_fragment_integrity（AC#7：sample-20s.wav 走 audio.standardize 直通 → 组装 manifest → 落盘 → 断言 audio.wav/manifest.json/transcript.json/transcript.txt/.done 五产物齐全 + .done 0 字节 + transcript.json 无 duration + transcript.txt 派生正确 + device_id=devm01）、run_test_manifest_idempotent（AC#8：同一 WAV 跑两次、刻意用不同时间戳/计时，断言 manifest_without_timestamps 两次完全一致且时间戳确实不同）；缺 fixture 优雅 SKIP exit 0。
  - cli.py：新增 test-fragment-integrity / test-manifest-idempotent 两子命令（lazy import，typer.Exit(code)）。
  - Makefile：新增同名两 target + .PHONY。
  - apps/worker/tests/test_manifest.py（新增 17 用例，全内存构造 StandardizeResult/ManifestDraft 不触系统工具/云端）：device_id_of 三分支、build_manifest 顶层/audio/upload/transcription 块 + sha 一致性直通(相等)/转码(不同非 null) + original_format 兜底、transcript_json_from_result 剔 duration/缺 segments、manifest_without_timestamps 只剔时间戳+不改原对象+跨时间戳差异相等、write_fragment_outputs 五产物+.done 0 字节+txt 拼接+tmp 清理、两 make 入口缺 fixture SKIP。
- 更改的文件：apps/worker/src/soniscope_worker/{manifest.py(新),cli.py}、Makefile、apps/worker/tests/test_manifest.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 49 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（418 passed，+17）全部通过；**本机真实 fixtures + ffmpeg/ffprobe 实跑两 make target 全绿**：test-fragment-integrity（sample-20s.wav 直通 audio.sha256=b07dee76… 与 runbook 基线一致，五产物齐全）、test-manifest-idempotent（两次不同时间戳但剔除后 manifest 完全一致、audio.sha256 两次相同）。
- **未来迭代的学习：**
  - manifest 字段来源三分割（§3.3「字段来源」务必照搬，别凭空填）：① fragment_id/device_id 从 id 解析（device_id=fragment_id.split("_")[1]，先 object_key_for 校验）；② session_id/chunk_seq/chunk_total/recorded_at/duration_seconds/audio.original_format/upload.original_sha256 来自 OSS 元数据（poller.ManifestDraft，已含全部映射）；③ audio.sha256/size/format + upload.original_size_bytes 来自 audio.StandardizeResult（直通时 audio.sha256==original_sha256，转码时不同且都非 null）。US-027 串流水线时直接组装这三者即可，勿重复造字段。
  - transcript.json **不落盘 duration**（§3.4 硬约束）：TranscriptResult 内存含 duration（时长），但 transcript.json 只写 segments/language/model/params_version/provider；用 transcript_json_from_result 过滤，US-026/027 落 transcript 一律走它。transcript.txt 走 recovery.transcript_txt_from_segments（segments[].text 顺序拼接）。
  - idempotent 比对剔除的「时间戳字段」是 upload.uploaded_at/verified_at + transcription.started_at/completed_at/elapsed_seconds（见 TIMESTAMP_FIELD_PATHS）；**recorded_at 不剔**（来自固定 OSS 元数据，是真实幂等字段，跟 audio.sha256 一样应跨运行相同）。新增随运行变化的字段务必加进 TIMESTAMP_FIELD_PATHS，否则 test-manifest-idempotent 会误判。
  - 落盘顺序硬约束（§3.5）：write_fragment_outputs 内 manifest.json → transcript.json（经 tmp）→ transcript.txt → .done 最后（0 字节）。audio.wav 由上游 audio.standardize 落盘，本函数只校验存在；US-027 把 standardize→build_manifest→write_fragment_outputs 串起，真实转写结果替换本处 _stub_transcript_result。
  - 本 story 只做「manifest/transcript 组装+落盘」单元，未接入 poller.poll_once 主循环（那是 US-027）。make test 用 audio.standardize 真实跑 .part→audio.wav 直通验证端到端，转写结果用确定性 stub（真实 NLS 在 US-026）。
---

## 2026-06-27 05:30 - US-025
- 实现了什么：Transcriber 抽象接口、工厂方法与本地占位实现（§5.3）。
  - apps/worker/src/soniscope_worker/transcriber.py（新增，mypy strict + ruff + pytest 全覆盖）：
    · Segment dataclass（start/end/text + as_dict）；TranscriptResult dataclass（segments/language/model/params_version/provider/duration，含 transcript_json() 派生五字段落盘 dict 剔除 duration、as_result_dict() 含 duration 兼容 manifest.transcript_json_from_result）。
    · Transcriber Protocol（@runtime_checkable，方法签名 transcribe(fragment_id, audio_path: Path, oss_key) -> TranscriptResult，§5.3）。
    · CloudSpeechTranscriber（持 config，transcribe 占位 raise NotImplementedError 指向 US-026——真实 NLS 调用属 US-026，避免未 live 验证塞云代码）。
    · WhisperLocalTranscriber（占位，transcribe raise NotImplementedError 提示「本期不部署本地 Whisper」，AC#5）。
    · create_transcriber(config) 工厂：cloud-speech/whisper-local 分发；未知 name 抛 TranscriberError 并列出 SUPPORTED_TRANSCRIBERS（AC#6）。业务侧只依赖 Transcriber 接口（AC#7）。
  - apps/worker/tests/test_transcriber.py（新增 11 用例）：工厂返回 cloud/whisper 实例且 isinstance(Transcriber)、未知 name 报错含支持值、whisper-local transcribe 抛 NotImplementedError、cloud transcribe 占位指向 US-026、transcript_json 剔除 duration + 字段映射、as_result_dict 含 duration、与 manifest.transcript_json_from_result 兼容、Segment.as_dict。
- 更改的文件：apps/worker/src/soniscope_worker/transcriber.py（新）、apps/worker/tests/test_transcriber.py（新）、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 51 文件 0 错）、make lint（ruff apps/ + 小程序 lint 全过）、make test（429 passed，+11）全部通过。
- **未来迭代的学习：**
  - 陷阱：测试构造 TranscriberConfig 时，appkey/access_key_secret 字段类型是 MaskedSecret，直接传 str 会被 mypy 报 arg-type；用 TranscriberConfig.model_validate({...}) 让 Pydantic 在运行时把 str 强转成 MaskedSecret（与 load_config 路径一致），mypy 也满意。
  - 上下文：US-026 的 CloudSpeechTranscriber 应在本处骨架上接入真实 NLS（复用 verify_prep.nls_response_to_result 的 Sentences→segments 映射 + scripts/test_asr.py 的 filetrans POP 基线）；返回 TranscriptResult 后用 .transcript_json()/.as_result_dict() 落盘。manifest.transcript_json_from_result 已能接受含 duration 的 dict。
  - 上下文：业务流程（US-027 集成流水线）只 import create_transcriber + Transcriber/TranscriptResult，不要直接 import 具体 provider 类（AC#7 已用 isinstance(Transcriber) 断言守住）。
---

## 2026-06-27 20:00 - US-026
- 实现了什么：阿里云 NLS 云端转写器 oss-url / direct 两模式（tech-spec §5.2/§3.4/§6.8）。沿用「纯逻辑（无 IO，直接单测）+ IO 用 NlsBackend Protocol 注入 + 假时钟」分层，单测全程不触网/不调 ffprobe/不调云 SDK。
  - apps/worker/src/soniscope_worker/nls.py（新增，mypy strict + ruff + pytest 全覆盖）：
    · 纯逻辑：is_retryable_status（0=网络/5xx→可重试，4xx→否）、estimate_cost_yuan（runbook §5.3 费率 2.5 元/小时）、nls_result_to_transcript（filetrans Result.Sentences BeginTime/EndTime ms→s + language=zh + duration 取本地实测，§3.4）、transcript_text（拼 segments[].text 供基线比对）、build_cost_log（§6.8 asr_call_completed 九字段）、flash_to_filetrans_shape（极速版 flash_result.sentences begin_time/end_time/text → filetrans 形状，两模式共用同一映射）、DailyCounter（当日累计调用/时长，跨日归零、仅内存）。
    · IO 注入点 NlsBackend Protocol：presign_oss_url / submit_oss_url / poll_task / transcribe_direct。
    · 编排 transcribe_via_nls：oss-url→_run_oss_url（presign→submit→轮询；now-url_issued_at>=50min 自动续签+重提交，日志「重新签发」，AC#3）；direct→日志 mode=direct-upload + transcribe_direct（AC#4）；两路结果统一经 nls_result_to_transcript；完成后 counter.record + log(json 成本日志，AC#7）。_with_retries：NlsTranscribeError.retryable 才按 RETRY_DELAYS_SECONDS=[5,15,45] 退避最多 3 次（共 4 次尝试），4xx 立即抛（AC#5）。duration_probe/today 改 call-time 解析（默认 None→模块级函数）便于 monkeypatch。
    · RealNlsBackend（lazy import 云 SDK，复用 verify_prep._import_oss/_oss_client/_import_nls_core）：presign_oss_url（load_config 取 OSS 段，presign 1 小时，AC#2）；submit_oss_url/poll_task（filetrans SubmitTask/GetTaskResult，复用 verify_prep NLS_FILETRANS_* 常量）；transcribe_direct（极速版 FlashRecognizer：CreateToken→nls-gateway POST 音频字节→flash_to_filetrans_shape）；_classify_exception（读 http_status/error_code，5xx/网络→retryable，4xx→否，只记类名不泄漏明文）。
    · make test 入口 run_test_transcribe_oss_url/direct/perf：用真实 config + 真实 OSS sample/ + fixtures 端到端跑，断言 mode 日志 + §5.4 基线主干（_BASELINE_KEYWORDS 含「选择」「优先级」）/ direct 主干一致 / perf 端到端耗时 <= PERF_THRESHOLD_SECONDS=120s（P-01）。缺 config/SDK/ffprobe/网络优雅 SKIP exit 0。
  - transcriber.py：CloudSpeechTranscriber 从 US-025 占位（抛 NotImplementedError）改为委托 nls.transcribe_via_nls（持 DailyCounter 跨调用累计；backend 可注入、缺省 RealNlsBackend；log 缺省 print）。
  - cli.py：新增 test-transcribe-oss-url / test-transcribe-direct / test-transcribe-perf 三子命令。
  - Makefile：新增同名三 target + .PHONY。
  - apps/worker/tests/test_nls.py（新增 22 用例，FakeBackend + _Clock 假时钟）：纯逻辑全分支、oss-url happy+成本日志、QUEUEING/RUNNING→SUCCESS 轮询、续签（推时钟过 50min→presign/submit 各 2 次+日志「重新签发」）、非成功状态抛错、direct 模式日志+映射、5xx 退避[5,15]后成功、4xx 不重试、retryable 耗尽 3 次、RealNlsBackend._classify_exception 三类、presign 缺 SDK 抛 ProbeError、三 make 入口缺 config SKIP。test_transcriber.py：占位测试改为「委托后端」测试（monkeypatch nls._probe_duration）。
- 更改的文件：apps/worker/src/soniscope_worker/{nls.py(新),transcriber.py,cli.py}、Makefile、apps/worker/tests/{test_nls.py(新),test_transcriber.py}、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 53 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（451 passed，+22）全部通过；CLI --help 显示三命令；make test-transcribe-oss-url 在无 config 时优雅 SKIP exit 0。
  未跑：真实 make test-transcribe-{oss-url,direct,perf} 全链路（需真实 config.yaml NLS appkey/AK + alibabacloud-oss-v2/aliyun-python-sdk-core + 触网 OSS presign/NLS 提交轮询，本沙箱不具备）——AC#8/#9/#10 真实云端项需在 Worker 主机复验；编排/重试/续签/映射/成本日志逻辑已用 FakeBackend + 假时钟全单测覆盖。
- **未来迭代的学习：**
  - oss-url 与 direct 两模式响应形状不同（filetrans Result.Sentences[BeginTime/EndTime/Text] vs 极速版 flash_result.sentences[begin_time/end_time/text]）：在 RealNlsBackend.transcribe_direct 里用 flash_to_filetrans_shape 归一化为 filetrans 形状，使编排层 nls_result_to_transcript 一份映射通吃两模式。新增 provider 时也应在后端归一化，而非在映射层分叉。
  - direct 模式（方案 B 降级）真实走的是 NLS「录音文件识别极速版 FlashRecognizer」（nls-gateway-<region>.aliyuncs.com/stream/v1/FlashRecognizer，二进制 POST + X-NLS-Token），不是 filetrans（filetrans 只接 file_link URL，不支持 multipart）；Token 经 CreateToken（nls-meta.cn-shanghai.aliyuncs.com，version 2019-02-28，仅上海）。oss-url（方案 A 首选）走 filetrans 异步 SubmitTask/GetTaskResult。这两个真实 API 本沙箱无法 live 验证。
  - 续签判定用注入 monotonic 的「now - url_issued_at >= RESIGN_THRESHOLD_SECONDS(3000)」：单测用 _Clock（monotonic 读 t 不自前进 + sleep 记录），在 on_poll 回调里手动把 clock.t 推过 50min 触发续签，断言 presign/submit 各 2 次。轮询循环可测性沿用 poller.poll_loop 的注入时钟模式。
  - transcribe_via_nls 的 duration_probe/today 默认 None→call-time 解析模块级函数（_probe_duration/_today_iso），而非 def-time 绑定默认值；否则 monkeypatch 模块级函数对已绑定默认无效（test_transcriber 委托测试踩过）。需要单测 monkeypatch 的可注入默认一律用此 None→call-time 模式。
  - 成本日志（§6.8）当日累计计数由 CloudSpeechTranscriber 实例持有的 DailyCounter 跨 transcribe 调用累加，跨日自动归零、不持久化（重启归零）；US-027 串流水线时一个 Transcriber 实例贯穿整轮即可天然累计。
  - 错误分类红线：NlsTranscribeError.error_code 只含稳定错误码/HTTP_<code>/异常类名，绝不含 AK/appkey/token 明文；RealNlsBackend._classify_exception 用 getattr 读 http_status/error_code 容错（POP SDK 用 get_http_status()/get_error_code()，urllib 用 .code）。
---

## 2026-06-27 21:00 - US-027
- 实现了什么：集成 Worker 轮询到落盘完整处理流水线（tech-spec §1.1/§3.5/§3.6/§3.7）。把 US-021~026 的各单元串成一条幂等流水线。沿用「IO 用 OssSource/Transcriber 注入 + 纯逻辑可单测」分层。
  - apps/worker/src/soniscope_worker/pipeline.py（新增，mypy strict + ruff + pytest 全覆盖）：
    · process_part（从下载好的 .part 跑完整一条，AC#1 严格顺序）：standardize（.part→audio.wav）→ 写 manifest 初稿（transcription.started_at 已填、completed_at 待回填）→ 调 Transcriber → 写 transcript.json（经 tmp）+ transcript.txt → 回填 manifest.transcription 终稿 → **最后**创建 0 字节 .done。任一阶段失败都不创建 .done、返回 FragmentResult(failed, stage)、日志带 fragment_id+失败阶段（AC#2）。可注入 now_iso/monotonic/probe/transcode 便于单测。
    · process_pending（启动恢复重转写，§3.6 第三段）：对「有 audio.wav 无 .done」的 Fragment 读已有 manifest 初稿 → 转写现有 audio.wav → 回填 transcription + .done；缺 manifest 初稿（更早崩溃）跳过留待 OSS 轮询重下补全（对象永不删除）。
    · run_pipeline_once（一轮）：list → plan_downloads（已 .done 跳过，AC#3）→ 逐对象 process_plan（下载+sha256 校验）→ process_part；用 seen 集合对同 object key 在一轮内只处理一次（AC#4 不产生重复目录）。
    · run_pipeline_loop（完整主循环）：启动先 recover()（清 inbox/tmp 残留）+ 对 pending 列表 process_pending 补齐，随后每 interval 跑 run_pipeline_once；单轮异常只记日志不杀进程（AC#5 按硬盘文件状态恢复，不依赖内存）。
    · run_worker_pipeline（make worker-run 真实入口）：读 config → RealOssSource + create_transcriber → run_pipeline_loop。
    · make test 入口：run_test_download_interrupt（AC#6 自包含：FixtureSource 首次下载写部分字节后抛错→残留 .part→recover 清理→第二轮完整下载跑通五产物；stub 转写器+真实 ffprobe 直通）、run_test_no_redownload（AC#7 跑两轮，断言第二轮 .done 跳过、download 计数==1）、run_test_transcribe（AC#8 真实 Transcriber+config，oss_key=sample/sample-20s.wav 让 oss-url NLS 拉取真实 sample，校验五产物+transcript.txt 含 §5.4 基线「选择/优先级」；缺 config/ffprobe/SDK/网络优雅 SKIP）。
  - poller.run_worker_run：从「仅下载 poll_loop」改为委托 pipeline.run_worker_pipeline（完整流水线）；poll_loop/poll_once 保留不动（make test-poll-interval 仍用，US-021 测试不受影响）。
  - cli.py：新增 test-download-interrupt / test-no-redownload / test-transcribe 三子命令。
  - Makefile：新增同名三 target + .PHONY。
  - apps/worker/tests/test_pipeline.py（新增 13 用例，fake probe 直通+stub 转写器+内存 OssSource，全程不触网/ffprobe/云端）：完整成功五产物+transcript.txt 拼接+transcript.json 剔 duration+oss_key 传递、manifest.transcription 字段+计时（注入 now_iso/monotonic）+直通 sha 相等、转写失败不创建 .done（初稿在但 completed_at 空）、标准化失败留档 inbox/failed 不建目录、幂等跳过 download 计数 1、同 key 去重单目录、sha 不符不 .done 且删 .part、process_pending 补齐/缺 manifest 跳过/转写失败不 .done、loop 恢复 pending+清残留 .part、loop list 异常续跑、三 make 入口缺 fixture SKIP。
- 更改的文件：apps/worker/src/soniscope_worker/{pipeline.py(新),poller.py,cli.py}、Makefile、apps/worker/tests/test_pipeline.py(新)、scripts/ralph/{prd.json,progress.txt}。
- 验证：make typecheck（mypy 55 文件 0 错）、make lint（ruff apps/ 全过 + miniprogram lint passed）、make test（464 passed，+13）全部通过；**本机真实 ffprobe + 真实 fixtures 实跑两 make target 全绿**：test-download-interrupt（中断残留 .part→recover 清理→重下完成五产物）、test-no-redownload（download 计数 1，第二轮 .done 跳过）；make test-transcribe 在无 config 时优雅 SKIP exit 0；`python -m soniscope_worker run` 无 config 优雅诊断返回。
  未跑：真实 make test-transcribe 全链路（需真实 config.yaml NLS appkey/AK + alibabacloud-oss-v2/aliyun-python-sdk-core + 触网拉取 sample 转写，本沙箱不具备）——AC#8 真实云端项需 Worker 主机复验；完整编排/幂等/去重/失败隔离/恢复逻辑已用 fake 全单测覆盖。
- **未来迭代的学习：**
  - 流水线落盘顺序是 AC#1 硬约束：audio.wav → manifest 初稿（started_at 填、completed_at 空）→ transcribe → transcript.json/txt → manifest 终稿（回填 transcription）→ .done 最后。process_part 没复用 manifest.write_fragment_outputs（那个一次性写 manifest+transcript+done），而是拆成「初稿 atomic_write_json → 转写 → transcript 落盘 → 终稿 atomic_write_json → create_done_marker」以插入转写步骤并满足「初稿在转写前」。
  - 幂等只看 .done（§3.7）：plan_downloads 的 done_check=done_marker_path.exists()。pending（无 .done 有 audio.wav）有两条恢复路径——① 启动 recover 后 process_pending 直接转写现有 audio.wav（省一次下载+ASR）；② 即便不处理，下一轮 OSS 轮询也会因无 .done 重下重跑（对象永不删除）。两者都正确，process_pending 是优化路径。
  - 同 object key 一轮内去重用 run_pipeline_once 的 seen 集合（AC#4）：plan_downloads 对同 key 会产出两个 PollPlan（同 fid），seen 保证只处理一次，不产生重复目录。跨轮去重靠 .done。
  - poller.run_worker_run 已改为委托 pipeline.run_worker_pipeline；US-028（retranscribe CLI / file lock 防并发）应在 pipeline 之上加显式重转入口（--force/--upgrade/--all-from），并用 file lock 保证主轮询 process_part 与 retranscribe 不并发转同一 fragment_id（§3.7）。manifest.transcription 已记录 started_at/completed_at/elapsed_seconds/transcriber/model/params_version/provider/upload_mode 八字段（US-028 AC#2 可直接复用 _transcription_block / TranscriptionInfo）。
  - make test-transcribe 用真实 Transcriber 但 oss_key 传 runbook 真实 sample 对象（sample/sample-20s.wav），使 oss-url 模式 NLS 能拉到音频；fragment_id 用合法合成值（standardize 据此推 date/目录）。这是「真实云调用 + 本地 fixture 喂 standardize」的混合验证模式，沿用 nls.run_test_transcribe_* 的 SKIP 约定（缺 config/SDK/网络/ffprobe → exit 0）。
---
