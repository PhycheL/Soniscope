---
phase: quick-260706-7sy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/aliyun-oss-fc-ram-experience.md
autonomous: true
requirements: [QUICK-260706-7SY]
must_haves:
  truths:
    - "docs/aliyun-oss-fc-ram-experience.md 存在,是一本面向未来项目的阿里云 OSS/FC/RAM 经验手册(playbook),按决策/模式/踩坑组织,而非按本项目模块组织"
    - "文档包含约定的 7 个部分:引言、总纲(通用模式)、OSS 篇、FC 3.0 篇、RAM/STS 篇、跨领域踩坑总表、可复用资产索引"
    - "素材中的 file:line 证据在正文中保留;三份报告间的重复内容(issue-cedential 拼写坑、MaskedSecret、Protocol 注入模式等)已去重合并,各自只出现在最合适的一处"
    - "成本数据(OSS 0.12 元/GB/月、NLS 2.5 元/小时无免费额度)、region 一致性经验(同地域内网下载免费)、NLS Token 仅 cn-shanghai 等花钱买来的经验全部保留"
    - "文档不含任何真实凭证明文(符合项目秘密红线;仅允许出现 LTAI 前缀模式等扫描规则描述)"
  artifacts:
    - "docs/aliyun-oss-fc-ram-experience.md"
  key_links:
    - "/private/tmp/claude-501/-Volumes-Data-ProjectCode-my-soniscope/4ac5feac-90eb-49fb-9972-05be5d236df5/scratchpad/aliyun-research-material.md(三份并行调研报告,事实已核实)→ docs/aliyun-oss-fc-ram-experience.md 正文(重组+去重关系,事实不得增删改)"
---

<objective>
将三份已核实的并行代码调研报告(FC 3.0 / RAM-STS / OSS)重组沉淀为一本面向未来项目的阿里云使用经验手册 `docs/aliyun-oss-fc-ram-experience.md`。

Purpose: SoniScope 项目在阿里云 OSS/FC/RAM 上积累了大量真金白银换来的决策、模式与踩坑经验,目前散落在代码、runbook 与调研报告中;本任务把它们沉淀为可复用的 playbook,让"下一个要用阿里云 OSS/FC/RAM 的项目"直接受益。

Output: `docs/aliyun-oss-fc-ram-experience.md`(新建,唯一产品文件)。

风格对齐 `docs/audit-methodology.md`(方法论沉淀类文档):中文正文、来源说明置于标题下、原则先行 + 案例引用式表述、表格与代码块并用。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/private/tmp/claude-501/-Volumes-Data-ProjectCode-my-soniscope/4ac5feac-90eb-49fb-9972-05be5d236df5/scratchpad/aliyun-research-material.md
@docs/audit-methodology.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 撰写 docs/aliyun-oss-fc-ram-experience.md(阿里云 OSS/FC/RAM 经验手册)</name>
  <files>docs/aliyun-oss-fc-ram-experience.md</files>
  <action>
    读取写作素材(绝对路径,worktree 内亦可读):
    `/private/tmp/claude-501/-Volumes-Data-ProjectCode-my-soniscope/4ac5feac-90eb-49fb-9972-05be5d236df5/scratchpad/aliyun-research-material.md`
    该文件含三份并行调研报告(报告一 FC 3.0、报告二 RAM/STS、报告三 OSS),全部带 file:line 证据,事实已核实,可直接引用;禁止顺序拼接,必须重组结构并去重。

    同时读取 `docs/audit-methodology.md` 作为风格样板(方法论沉淀类文档:标题下来源说明引言块、章节编号、原则+案例式表述、表格速查、代码块示例)。

    撰写新文件 `docs/aliyun-oss-fc-ram-experience.md`,遵守以下要求:

    **文档定位(最重要):** 面向未来项目的经验手册(playbook),读者是"下一个要用阿里云 OSS/FC/RAM 的项目"。组织方式按"决策/模式/踩坑",不按 SoniScope 模块;SoniScope 的具体文件与数字作为"本项目实践/案例"式引用出现,文档必须自包含。

    **语言与风格:** 中文正文,英文术语/标识符保留原文(STS、AssumeRole、PostObject、Protocol、fc_shared 等);主标题下加一行来源说明(提炼自 SoniScope 项目对阿里云 OSS/FC/RAM 的实际使用,整理日期 2026-07-06)。

    **结构(7 部分,锁定):**
    1. **引言** — 经验来源(SoniScope:小程序直传 OSS + FC 3.0 网关 + 本地 Worker 的个人级流水线)与适用范围(个人/小团队规模、Python + 小程序 JS 双栈、按量付费)。
    2. **总纲:贯穿三个服务的通用模式** — 纯逻辑+Protocol 注入+lazy import(无凭证/无 SDK 时 CI 优雅 SKIP);runbook 作为真实云资源的单一真实来源;最小权限(一职能一子账号、两级收窄);防御性脱敏(黑名单+子串兜底比"约定不打印"可靠、MaskedSecret、备份只记名不记值)。MaskedSecret、Protocol 注入等在三份报告中重复出现的内容只在本节完整展开,后续各篇仅引用。
    3. **OSS 篇** — SDK client 构造(load_default + StaticCredentialsProvider + region/endpoint 都要设、get_bucket_info 的 oss- 前缀归一化坑);API 清单与分页 list(continuation_token 双保险、getattr 防御式取值);presign 续签(3600s 有效期 vs NLS 轮询超 50 分钟需重签重提,"长任务+短时效签名 URL"模式);错误码多路兜底(code/error_code/Code 多候选 + 递归 unwrap + 文本兜底、HeadObject 不存在抛异常需 catch 判 NoSuchKey/404);小程序 V4 PostObject 签名全部 6 步(UTC 时间戳格式、credential 串、HMAC 派生链、policy conditions 逐项、签 base64 后的 policy、formData 字段清单)与纯 JS 密码学原语自实现;x-oss-meta-* 元数据契约(7 字段、null↔"0" 映射坑、normalize_metadata 容错读回、key 规则 4 处独立实现+round-trip 校验模式);结构性防删除(Protocol 只暴露 list/head/download、hasattr 断言、删除三重门禁、retention 静态扫描只匹配真实调用形态)。
    4. **FC 3.0 篇** — 无 service 层的影响(URL 形态变化、无服务级共享环境变量需每函数各填一遍);Custom Runtime + 约 35 行零依赖 WSGI 适配层(ThreadingWSGIServer + daemon_threads、端口 fallback 链 FC_SERVER_PORT→PORT→9000、GET 作存活探针);共享库 vendor 进 zip 而非 pip 安装(解决 FC 无 Layer 便捷共享、handler.py 同名致 mypy 排除的坑);部署工程化全流程(确定性 zip+sha256、uv pip install --target、备份只记环境变量名、deploy_one 编排、按时间戳回滚、_redact_error_text 脱敏、412/CAExited 判失败、app.py 必须在包根);anonymous 触发器+应用内 openid allowlist 鉴权(三步走 authorize_request、稳定错误码表、缺环境变量一次性列名不列值、不用 FC 执行角色而显式读环境变量 AK 的取舍);OSS 事件触发器要点(事件 JSON 非普通 HTTP、至少一次投递必须幂等、失败 Destination、脚本只更新代码不建函数/触发器)。
    5. **RAM/STS 篇** — 子账号切分逻辑(一职能一子账号表:soniscope-fc / soniscope-local-reader / soniscope-asr / soniscope-uploader-role、信任主体精确到单个子账号的信任链闭环、部署账号与运行时账号绝不复用);两级权限收窄(角色策略限前缀 + AssumeRole inline policy 收到单 key,"角色宽+会话窄");单 key policy 模板(Resource 单条无通配符、仅 oss:PutObject、fragment_id 正则+日期合法性校验防路径穿越、900s、policy 以 JSON 字符串传入);凭证三处出现(security_token 必须同时进 policy conditions、表单字段、且 secret 参与 HMAC 派生;object_key 必须用 FC 返回值前端不许拼接);反例测试与泄漏检测(sts_escape 越权 PutObject 断言 AccessDenied、fc_live 5 类越权/过期反例、拒绝响应含凭证字段即判泄漏的断言模式、wx.login code 一次性坑);凭证管理纪律(MaskedSecret 引用总纲、config.yaml chmod 600、LTAI 前缀硬编码扫描、ADR-8 安全约束驱动架构决策案例)。
    6. **跨领域踩坑总表** — 把三份报告的全部踩坑点汇总为一张速查表,列:现象 / 原因 / 对策 / 证据。必含:issue-cedential 拼写坑(阿里云分配的 url-id 拼错但绝不能"修")、region 带 oss- 前缀、HeadObject 抛异常、null↔"0" 映射、presign 过期需重签重提、app.py 不在包根、handler.py 同名 mypy 冲突、412/CAExited、部署/运行时账号混淆、角色最大会话时长≥请求值、wx.login code 一次性、security_token 三处缺一不可、object_key 必须用 FC 返回值、错误码提取多路兜底、NLS Token 仅 cn-shanghai、跨文档 story 编号以代码 docstring 为准。踩坑在各篇正文与总表可并存(总表是速查入口),但同一坑的展开解释只在一处。
    7. **可复用资产索引** — 本仓库中可直接搬运的文件清单表(文件路径 / 用途 / 搬运注意),至少含:apps/fc/shared/app.py(WSGI 适配层)、fc_shared/audit.py(脱敏日志)、fc_shared/errors.py(稳定错误码模式)、fc_shared/sts.py(单 key STS)、fc_deploy.py(部署工程化)、oss_sign.js + hmac.js + sha256.js(V4 PostObject 签名整套)、uploader.js(退避重试编排)、poller.py(分页 list + round-trip 校验 + OssSource Protocol)、miniprogram_lint.py(硬编码 AK 扫描)、sts_escape.py / fc_live.py / verify_upload_live.py(反例测试)、config.py MaskedSecret。

    **内容红线:**
    - 素材中的 file:line 证据保留在正文(可信度来源);事实不得增删改,只做重组、去重与表述打磨。
    - 成本数据(OSS 存储 0.12 元/GB/月、上传流入免费、同地域内网下载免费所以 OSS/FC/Worker region 必须一致、NLS 2.5 元/小时无免费额度)必须保留。
    - 全文不得出现任何真实凭证值或密钥明文;素材中本就只有模式与规则描述,转写时不得额外引入示例凭证。素材中出现的账号 UID 与 appid 属公开标识符,可按素材原样保留。

    这是唯一的产品文件;不修改任何其他文件。
  </action>
  <verify>
    <automated>test -f docs/aliyun-oss-fc-ram-experience.md && for s in "引言" "总纲" "OSS 篇" "FC 3.0 篇" "RAM/STS 篇" "跨领域踩坑总表" "可复用资产索引"; do grep -q "$s" docs/aliyun-oss-fc-ram-experience.md || { echo "缺少章节: $s"; exit 1; }; done && grep -q "cn-shanghai" docs/aliyun-oss-fc-ram-experience.md && grep -q "0.12" docs/aliyun-oss-fc-ram-experience.md && grep -q "issue-cedential" docs/aliyun-oss-fc-ram-experience.md && [ "$(grep -cE 'LTAI[0-9A-Za-z]{16,}' docs/aliyun-oss-fc-ram-experience.md)" -eq 0 ] && echo OK</automated>
  </verify>
  <done>docs/aliyun-oss-fc-ram-experience.md 存在;7 个约定部分齐全;file:line 证据保留;成本/region/NLS Token region 等经验保留;issue-cedential 等踩坑进入总表;三报告重复内容已去重(MaskedSecret、Protocol 注入、issue-cedential 各只完整展开一次);无真实凭证明文。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 调研素材 → 公开文档 | 素材含云资源标识与安全机制描述,写入 docs/ 后随仓库可见 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-260706-7sy-01 | Information Disclosure | docs/aliyun-oss-fc-ram-experience.md | high | mitigate | 转写红线:不引入任何真实凭证/密钥明文;verify 门禁 grep 真实 AK 形态(LTAI+16 位以上字母数字)必须 0 命中 |
| T-quick-260706-7sy-02 | Information Disclosure | 账号 UID / appid / bucket 名等标识符 | low | accept | 公开标识符非秘密,素材与现有 runbook/CLAUDE.md 中已存在,按原样保留 |
</threat_model>

<verification>
- `test -f docs/aliyun-oss-fc-ram-experience.md` 且 7 个章节关键词全部命中
- `grep -cE 'LTAI[0-9A-Za-z]{16,}' docs/aliyun-oss-fc-ram-experience.md` 为 0(无真实长期 AK 形态字符串)
- 抽查:cn-shanghai(NLS Token region 坑)、0.12(成本数据)、issue-cedential(拼写坑)均在文档中
</verification>

<success_criteria>
- 文档以 playbook 视角组织(决策/模式/踩坑),未来项目读者无需了解 SoniScope 即可套用
- 三份报告内容全部覆盖且去重合并,file:line 证据保留
- 花钱买来的经验(成本、region 一致性、NLS Token region)完整保留
- 工作树除 docs/aliyun-oss-fc-ram-experience.md 外零改动
</success_criteria>

<output>
完成后创建 `.planning/quick/260706-7sy-oss-fc-ram-docs-aliyun-oss-fc-ram-experi/260706-7sy-SUMMARY.md`
</output>
