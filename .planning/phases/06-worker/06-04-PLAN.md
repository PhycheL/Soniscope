---
phase: 06-worker
plan: 04
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/worker/src/soniscope_worker/cli.py
  - apps/worker/src/soniscope_worker/audio.py
  - Makefile
autonomous: true
requirements: [WKR-03]
must_haves:
  truths:
    - "Operator can run `make quarantine-list` to see isolated fragments and `make clear-quarantine` to release them (WKR-03, D-07/D-08)"
    - "_archive_failed docstring reflects new isolation semantics — archiving does not itself block retry, threshold quarantine does (D-11)"
  artifacts:
    - "apps/worker/src/soniscope_worker/cli.py (quarantine-list + clear-quarantine commands)"
    - "Makefile (quarantine-list + clear-quarantine targets + .PHONY entries)"
    - "apps/worker/src/soniscope_worker/audio.py (_archive_failed docstring corrected)"
  key_links:
    - "CLI commands delegate to quarantine.run_quarantine_list / run_clear_quarantine (already unit-tested in Plan 01)"
    - "Each make target maps to one CLI subcommand (repo convention)"
  prohibitions:
    - "Must NOT change retranscribe.py semantics — quarantine CLI is orthogonal (D-07)"
    - "Must NOT change _archive_failed behavior — docstring text only (D-11)"
    - "Must NOT put isolation logic in cli.py — commands are thin delegates over quarantine runners"
---

<objective>
接线 operator 暴露面。cli.py 加 `quarantine-list` 与 `clear-quarantine` 两个薄委托子命令(沿用既有 `(lines, code)` + `typer.echo`/`typer.Exit` 惯例,委托到 Plan 01 已单测的运行器)。Makefile 加对应两个目标 + `.PHONY` 条目。audio.py 修正 `_archive_failed` docstring 的「不再重试」错误表述(D-11,仅注释,不改行为)。

Purpose: WKR-03 的 operator「一个看、一个清」暴露面(D-08),make 目标映射 CLI 子命令(仓库惯例);D-11 docstring 与实现语义一致。
Output: 两个 CLI 命令 + 两个 make 目标 + docstring 修正。
</objective>

<execution_context>
@/Users/bemied/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Users/bemied/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-worker/06-CONTEXT.md
@.planning/phases/06-worker/06-RESEARCH.md
@.planning/phases/06-worker/06-VALIDATION.md
@apps/worker/src/soniscope_worker/quarantine.py
</context>

<artifacts_this_phase_produces>
This plan adds CLI commands `quarantine-list` and `clear-quarantine` in `cli.py`, two matching
Makefile targets (+ `.PHONY` entries), and corrects the `_archive_failed` docstring in `audio.py`.
Consumes Plan 01's `quarantine.run_quarantine_list` / `run_clear_quarantine`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: cli.py 两个委托子命令 quarantine-list / clear-quarantine</name>
  <files>apps/worker/src/soniscope_worker/cli.py</files>
  <read_first>
    - apps/worker/src/soniscope_worker/cli.py (retranscribe/deploy-fc 委托模式:73-120 — lazy-import 运行器 + `for line in lines: typer.echo(line, err=code != 0)` + `raise typer.Exit(code=code)`;typer.Option 用法)
    - apps/worker/src/soniscope_worker/quarantine.py (run_quarantine_list / run_clear_quarantine 签名 — Plan 01 产出)
    - 06-RESEARCH.md §Code Examples "CLI 子命令(委托模式)"(两命令目标形态 + flag)
  </read_first>
  <action>
    加 `@app.command(name="quarantine-list")` def `quarantine_list() -> None`:lazy-import `run_quarantine_list`,`lines, code = run_quarantine_list()`,`for line in lines: typer.echo(line, err=code != 0)`,`raise typer.Exit(code=code)`。docstring 引用 WKR-03/D-08。
    加 `@app.command(name="clear-quarantine")` def `clear_quarantine(fragment_id: str = typer.Option("", "--fragment-id", help="要解除隔离的 fragment_id"), all_: bool = typer.Option(False, "--all", help="清空整个隔离账本")) -> None`:lazy-import `run_clear_quarantine`,`lines, code = run_clear_quarantine(fragment_id=fragment_id or None, clear_all=all_)`,同款 echo + Exit。docstring 引用 WKR-03/D-07。
    命令是薄委托,隔离逻辑不落 cli.py。全量注解;不新增测试(运行器逻辑已在 Plan 01 test_quarantine.py -k cli 覆盖;此处靠 typecheck/lint + 运行器测试保证)。
  </action>
  <verify>
    <automated>uv run python -m soniscope_worker --help 2>&1 | grep -E "quarantine-list|clear-quarantine" && make typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `cli.py` contains `@app.command(name="quarantine-list")` and `@app.command(name="clear-quarantine")`
    - `clear_quarantine` exposes `--fragment-id` and `--all` options and calls `run_clear_quarantine(fragment_id=..., clear_all=...)`
    - Both commands delegate via lazy-import to `quarantine` runners (no isolation logic in cli.py — grep confirms `is_quarantined`/`record_failure` absent from cli.py)
    - `uv run python -m soniscope_worker --help` lists both commands (exit 0)
    - `make typecheck` exits 0 and `make lint` exits 0
  </acceptance_criteria>
  <done>两个 CLI 子命令注册、委托到已测运行器、--help 可见;mypy/ruff 绿。</done>
</task>

<task type="auto">
  <name>Task 2: Makefile 两个目标 + .PHONY;audio.py _archive_failed docstring 修正(D-11)</name>
  <files>Makefile, apps/worker/src/soniscope_worker/audio.py</files>
  <read_first>
    - Makefile (retranscribe:119-120 / oss-delete-obj:63-64 目标形态 + FRAGMENT_ID 传参;.PHONY:6-19)
    - 06-RESEARCH.md §Code Examples "Makefile 目标"(quarantine-list / clear-quarantine 目标 + ALL/FRAGMENT_ID 传参 + .PHONY 提醒)
    - apps/worker/src/soniscope_worker/audio.py (_archive_failed:134 docstring「不再重试」误述 — D-11 修正目标)
    - .planning/audit/findings/code.md §F-CODE-02(docstring 与实现相悖的原始判定)
  </read_first>
  <action>
    Makefile 加两个目标(带 `##` 帮助注释,shell 到 `uv run python -m soniscope_worker <subcommand>`):
    `quarantine-list`:`uv run python -m soniscope_worker quarantine-list`。
    `clear-quarantine`:`uv run python -m soniscope_worker clear-quarantine $(if $(strip $(FRAGMENT_ID)),--fragment-id $(FRAGMENT_ID),) $(if $(strip $(ALL)),--all,)`(FRAGMENT_ID=<id> 单条;ALL=1 全清)。
    把 `quarantine-list clear-quarantine` 两个目标名加入 `.PHONY`(Makefile:6-19)。
    audio.py `_archive_failed`(:134)docstring 修正:去掉「不再重试」的错误表述,改为准确描述 — 留档本身不阻止重下重试,真正阻止重试的是达阈值后的隔离(引用 D-11)。仅改 docstring 文本,函数体行为不变(禁改 `os.replace` 留档逻辑)。
  </action>
  <verify>
    <automated>make -n quarantine-list >/dev/null && make -n clear-quarantine FRAGMENT_ID=x ALL=1 >/dev/null && make lint</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` contains a `quarantine-list:` target and a `clear-quarantine:` target
    - Both target names appear in `.PHONY`
    - `make -n quarantine-list` and `make -n clear-quarantine FRAGMENT_ID=x ALL=1` dry-run successfully (exit 0)
    - `_archive_failed` docstring in audio.py no longer states archiving means no retry; it references D-11 isolation semantics instead
    - `_archive_failed` function body is unchanged (only docstring text edited)
    - `make lint` exits 0
  </acceptance_criteria>
  <done>两个 make 目标 + .PHONY 就位、dry-run 通过;_archive_failed docstring 与隔离语义一致(仅注释),行为不变。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI/make → ledger | `clear-quarantine FRAGMENT_ID=<id>` 的 fid 是外部输入(校验在 Plan 01 运行器) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-10 | Tampering/Input | clear-quarantine FRAGMENT_ID | low | mitigate | CLI 薄委托,fid 校验在 Plan 01 run_clear_quarantine(object_key_for 往返);cli.py 不含隔离逻辑。 |
| T-06-11 | Information Disclosure | quarantine-list output | low | accept | 列表仅打印 fid/reason/attempt/时间戳(账本内容),无秘密面(账本本身不存秘密,Plan 01 保证)。 |
</threat_model>

<verification>
- `uv run python -m soniscope_worker --help` 列出 quarantine-list / clear-quarantine
- `make -n quarantine-list` 与 `make -n clear-quarantine FRAGMENT_ID=x ALL=1` dry-run 通过
- `make typecheck` + `make lint` 通过
- audio.py `_archive_failed` docstring 修正,函数体不变(既有 audio 测试保持绿)
</verification>

<success_criteria>
- operator 可用 `make quarantine-list` 看隔离清单、`make clear-quarantine` 解除(WKR-03,D-07/D-08)
- 每个 make 目标映射一个 CLI 子命令(仓库惯例);CLI 是薄委托
- `_archive_failed` docstring 与新隔离语义一致(D-11),retranscribe.py 语义不变(D-07)
</success_criteria>

<output>
Create `.planning/phases/06-worker/06-04-SUMMARY.md` when done
</output>