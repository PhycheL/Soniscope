---
phase: 06-worker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/soniscope_worker/config.py
  - apps/worker/tests/test_config.py
  - apps/worker/tests/test_recovery.py
autonomous: true
requirements: [WKR-02]
must_haves:
  truths:
    - "Threshold is configurable in config.yaml via PollConfig.max_fragment_failures, pydantic-validated, default 3 (WKR-02, D-04)"
    - "Existing config.yaml without the field still loads (backward compatible via default value)"
    - "Startup recovery scan does not delete inbox/failed/ledger.json (state safety)"
  artifacts:
    - "apps/worker/src/soniscope_worker/config.py (PollConfig.max_fragment_failures + masked_summary line)"
    - "apps/worker/tests/test_config.py (extended)"
    - "apps/worker/tests/test_recovery.py (extended)"
  key_links:
    - "PollConfig.max_fragment_failures default 3 aligns with RETRY_DELAYS_SECONDS (max 3 attempts)"
  prohibitions:
    - "Must NOT make the field required — existing config.yaml files must still load (default 3)"
---

<objective>
给 `PollConfig` 新增 `max_fragment_failures: int = 3`(D-04:达此失败次数即隔离,默认对齐既有 3 次重试约定),pydantic 校验,`masked_summary` 加一行打印。补 `test_config.py`(默认值/显式值/校验/summary 行)与 `test_recovery.py`(启动恢复扫描不误删 `ledger.json`,状态安全)。

Purpose: 阈值是隔离判据的唯一配置真相源(WKR-02);默认值保证既有 `config.yaml` 无此字段仍能加载。恢复扫描非回归确认新账本文件安全。
Output: `config.py` 阈值字段 + summary 行;两个既有测试文件扩展。
</objective>

<execution_context>
@/Users/bemied/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Users/bemied/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-worker/06-CONTEXT.md
@.planning/phases/06-worker/06-RESEARCH.md
@.planning/phases/06-worker/06-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan adds `PollConfig.max_fragment_failures: int = 3` and a `masked_summary` line in
`apps/worker/src/soniscope_worker/config.py`. No new module/symbol beyond that field.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PollConfig.max_fragment_failures + masked_summary 行 + config 测试</name>
  <files>apps/worker/src/soniscope_worker/config.py, apps/worker/tests/test_config.py</files>
  <read_first>
    - apps/worker/src/soniscope_worker/config.py (PollConfig:51 定义、masked_summary:85、_collect_validation_errors:109)
    - apps/worker/tests/test_config.py (既有默认值/校验/`test_secret_not_leaked_in_repr_and_summary` 用例模式)
    - 06-RESEARCH.md §Code Examples "PollConfig 加阈值字段"(向后兼容说明)
  </read_first>
  <behavior>
    - Test 默认值: 构造 PollConfig(interval_seconds=…) 不传 max_fragment_failures → 值为 3
    - Test 显式值: 传 max_fragment_failures=5 → 值为 5
    - Test 向后兼容: 既有 config.yaml 结构(poll 段仅 interval_seconds)仍能 load_config 成功,poll.max_fragment_failures==3
    - Test 校验: 非法值(如字符串/负数按既有校验策略)触发 pydantic 校验错误,经 _collect_validation_errors 汇总
    - Test summary 行: masked_summary() 输出含一行包含 `max_fragment_failures`
  </behavior>
  <action>
    在 `PollConfig`(config.py:51)加字段 `max_fragment_failures: int = 3`,中文注释引用 D-04(达此失败次数即隔离,默认对齐 RETRY_DELAYS_SECONDS 3 次)+ D-05(所有隔离类失败共用同一阈值,不按类型分开)。有默认值 → 不是 required,既有 config 不 break。
    在 `masked_summary()`(config.py:85)的 `[poll]` 段 `interval_seconds` 行后加一行 `f"  max_fragment_failures = {self.poll.max_fragment_failures}"`(该字段非秘密,直接打印)。
    在 `test_config.py` 补上述用例;沿用既有 `_write_config` helper 与断言风格,测试函数 `-> None`。
    若需要下界校验(如 `>= 1`),用 pydantic `Field(default=3, ge=1)` 并加对应校验测试;是否加下界由实现者按 pydantic 惯例判断并在注释说明。
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_config.py -k "max_fragment or summary or default" -x</automated>
  </verify>
  <acceptance_criteria>
    - `config.py` PollConfig contains `max_fragment_failures: int = 3`
    - `PollConfig(interval_seconds=1).max_fragment_failures == 3` (test-verified default)
    - Loading a config.yaml whose `poll` section omits the field succeeds with value 3 (test-verified backward compat)
    - `masked_summary()` output contains a line with `max_fragment_failures` (test-verified)
    - `pytest apps/worker/tests/test_config.py -k max_fragment` exits 0
    - `make typecheck` exits 0
  </acceptance_criteria>
  <done>阈值字段默认 3、可配、pydantic 校验、summary 打印,既有 config 向后兼容;test_config.py 覆盖且绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 恢复扫描不误删 ledger.json 非回归测试</name>
  <files>apps/worker/tests/test_recovery.py</files>
  <read_first>
    - apps/worker/src/soniscope_worker/recovery.py (recover_inbox:196, _remove_by_suffix:181 — 按后缀非递归清理 inbox/ 顶层)
    - apps/worker/tests/test_recovery.py (既有 recover 用例的目录构造 + 断言模式)
    - 06-RESEARCH.md §Runtime State Inventory("恢复扫描交互(必须验证)"结论:ledger.json 后缀不匹配、inbox/failed/ 是子目录非递归不触及)
    - apps/worker/src/soniscope_worker/paths.py (inbox_failed_dir:83)
  </read_first>
  <behavior>
    - Test ledger 安全: 在 inbox/failed/ 放一个 ledger.json(含任意内容)→ 跑 recover(recover_inbox 或恢复入口)→ 断言 ledger.json 仍存在且内容不变
    - (可选)Test inbox 顶层清理仍生效: inbox/ 顶层的 *.part / *.wav.tmp 仍被清理,证明恢复行为整体未变(D-10 邻域非回归)
  </behavior>
  <action>
    在 `test_recovery.py` 增用例:构造运行时目录,在 `inbox/failed/` 写 `ledger.json`(如 `{"x": {"attempt": 3}}`),同时在 `inbox/` 顶层放一个 `*.part` 诱饵,调既有恢复函数,断言 (1) 顶层 `.part` 被清、(2) `inbox/failed/ledger.json` 原样保留。用既有测试的目录 fixture 与注入方式,不改 recovery.py 源码(本任务纯新增断言,验证既有行为)。
    测试函数 `-> None`;不触网、不触真实 HOME。
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_recovery.py -k ledger -x</automated>
  </verify>
  <acceptance_criteria>
    - `test_recovery.py` contains a test asserting `inbox/failed/ledger.json` survives a recover pass
    - The test also confirms a top-level `inbox/*.part` decoy IS removed (recovery behavior unchanged)
    - `pytest apps/worker/tests/test_recovery.py -k ledger` exits 0
    - No change to `recovery.py` source (grep confirms recovery.py not in this task's diff)
    - `make typecheck` exits 0
  </acceptance_criteria>
  <done>新账本文件在启动恢复扫描后安全存活,既有 inbox 清理行为未变;test_recovery.py 覆盖且绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.yaml → worker | 阈值配置来自 chmod 600 config.yaml,pydantic 校验后使用 |
| disk (inbox/failed/) → recovery scan | 恢复扫描遍历 inbox/,新账本文件须不被误删 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Tampering | PollConfig.max_fragment_failures | low | mitigate | pydantic 校验 + 默认 3;非法值汇总报错(_collect_validation_errors),不静默接受。 |
| T-06-06 | Denial of Service (data loss) | recovery scan vs ledger.json | low | accept | recover 按后缀非递归清理 inbox/ 顶层,ledger.json 在子目录且后缀不匹配 — 测试断言存活。既有行为,无需改 recovery.py。 |
</threat_model>

<verification>
- `uv run pytest apps/worker/tests/test_config.py apps/worker/tests/test_recovery.py -x` 全绿
- `make typecheck` 通过;`make lint` 通过
- 既有 `test_config.py` 其余用例(含 secret 不泄漏)保持绿(向后兼容)
</verification>

<success_criteria>
- `max_fragment_failures` 默认 3、可配、pydantic 校验、summary 打印、既有 config 向后兼容
- 启动恢复扫描不误删 `inbox/failed/ledger.json`(状态安全,重启不丢隔离)
</success_criteria>

<output>
Create `.planning/phases/06-worker/06-02-SUMMARY.md` when done
</output>
