---
phase: 06-worker
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - apps/worker/src/soniscope_worker/poller.py
  - apps/worker/src/soniscope_worker/pipeline.py
  - apps/worker/tests/test_poller.py
  - apps/worker/tests/test_pipeline.py
autonomous: true
requirements: [WKR-01, WKR-02, WKR-03]
must_haves:
  truths:
    - "Per-fragment failure history persists for sha256_mismatch + download error + STAGE_STANDARDIZE failures counted at run_pipeline_once (WKR-01, criterion 1)"
    - "After threshold, the same OSS object is excluded from downloads on subsequent polls via plan_downloads → skipped_quarantined — bounded, no re-download (WKR-02, criterion 2)"
    - "The threshold-crossing round emits exactly one alert line with fragment_id + reason + attempt + next-action (WKR-03, criterion 3)"
    - "Existing .done idempotent-skip path stays green and always precedes the quarantine branch (criterion 4, D-10)"
  artifacts:
    - "apps/worker/src/soniscope_worker/poller.py (ScanPlan.skipped_quarantined + plan_downloads quarantine_check param)"
    - "apps/worker/src/soniscope_worker/pipeline.py (run_pipeline_once/loop ledger read + increment + alert + clear-on-success; max_fragment_failures threaded from config)"
    - "apps/worker/tests/test_poller.py (extended)"
    - "apps/worker/tests/test_pipeline.py (extended)"
  key_links:
    - "run_pipeline_once reads ledger once/round, injects quarantine_check lambda over is_quarantined, increments on failure, saves ledger"
    - "run_worker_pipeline threads cfg.poll.max_fragment_failures into run_pipeline_loop → run_pipeline_once"
  prohibitions:
    - "STAGE_TRANSCRIBE / STAGE_MANIFEST_* failures must NOT increment the ledger (D-09) — only sha256_mismatch/error (pre-fragment) + STAGE_STANDARDIZE"
    - "The .done skipped_done branch must remain unchanged and always precede the skipped_quarantined branch (D-10)"
    - "The isolation predicate must NOT be re-implemented in the poller loop — call quarantine.is_quarantined via the injected quarantine_check"
    - "Counting must land in pipeline.run_pipeline_once, NOT poller.poll_once (no production caller — Pitfall 1)"
---

<objective>
把隔离机制接入生产运行路径。poller: `ScanPlan` 加 `skipped_quarantined` 字段,`plan_downloads` 加 `quarantine_check: Callable[[str], bool] | None = None` 参数(与既有 `done_check` 同构,默认 None 向后兼容),`.done` 分支永远先于隔离分支(D-10)。pipeline: `run_pipeline_once`/`run_pipeline_loop` 加 `max_fragment_failures` 参数,每轮读账本注入 `quarantine_check`,在消费端对 `sha256_mismatch`/`error`/`STAGE_STANDARDIZE` 失败递增账本、跨阈值那一次发告警、成功清条目;`run_worker_pipeline` 从 `cfg.poll.max_fragment_failures` 透传。

Purpose: 关闭 F-CODE-02 无界重下循环(WKR-02),记录失败历史(WKR-01),跨阈值告警(WKR-03),严守 D-09 边界与 D-10 幂等红线。隔离持久保留、不自动过期(D-06):账本超阈值即持续排除,唯一出口是 operator 的 clear-quarantine(Plan 04)。
Output: poller/pipeline 改动 + 两个既有测试文件扩展(含多轮失败隔离、D-09 边界、D-10 非回归)。
</objective>

<execution_context>
@/Users/bemied/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Users/bemied/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-worker/06-CONTEXT.md
@.planning/phases/06-worker/06-RESEARCH.md
@.planning/phases/06-worker/06-VALIDATION.md
@apps/worker/src/soniscope_worker/quarantine.py
</context>

<artifacts_this_phase_produces>
This plan adds `ScanPlan.skipped_quarantined: list[str]` and `plan_downloads(..., quarantine_check=...)`
in `poller.py`; adds `max_fragment_failures` parameter to `run_pipeline_once` and `run_pipeline_loop`
and ledger read/increment/alert/clear wiring in `pipeline.py`. Consumes Plan 01's `quarantine.py`
symbols (`load_ledger`, `save_ledger`, `record_failure`, `clear_entry`, `is_quarantined`,
`quarantine_alert_line`, reason constants) and Plan 02's `PollConfig.max_fragment_failures`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: poller — ScanPlan.skipped_quarantined + plan_downloads quarantine_check 参数</name>
  <files>apps/worker/src/soniscope_worker/poller.py, apps/worker/tests/test_poller.py</files>
  <read_first>
    - apps/worker/src/soniscope_worker/poller.py (ScanPlan:169, plan_downloads:177-214, fragment_id_from_key:47 — done_check 同构改造源)
    - apps/worker/tests/test_poller.py (既有 plan_downloads 用例的 listing 构造 + done_check 注入模式)
    - 06-RESEARCH.md §Architecture Patterns "Pattern 1: 注入式跳过集成"(目标签名 + .done 优先顺序)
    - apps/worker/src/soniscope_worker/quarantine.py (is_quarantined — 判据来源,poller 只注入不重实现)
  </read_first>
  <behavior>
    - Test 隔离排除: quarantine_check 对某 fid 返回 True → 该 fid 进 skipped_quarantined,不进 to_download
    - Test .done 优先: 同一 fid 既 .done 又「超阈值」→ 走 skipped_done(D-10:.done 分支永远先于隔离分支),不进 skipped_quarantined
    - Test 向后兼容: 不传 quarantine_check(默认 None)→ 行为与既有完全一致,skipped_quarantined 为空,既有 plan_downloads 用例全绿
    - Test 正常下载: quarantine_check 返回 False 的 fid 正常进 to_download
  </behavior>
  <action>
    `ScanPlan`(poller.py:169)加字段 `skipped_quarantined: list[str] = field(default_factory=list)`(放在 skipped_done 后)。
    `plan_downloads`(poller.py:177)签名加 keyword-only `quarantine_check: Callable[[str], bool] | None = None`(默认 None 向后兼容)。循环内顺序严格为:`fragment_id_from_key` → None 则 ignored_keys;`done_check` True 则 skipped_done + continue(D-10 幂等永远优先,不动既有分支);`quarantine_check is not None and quarantine_check(fid)` True 则 skipped_quarantined + continue;否则 to_download。
    poller 不重实现判据:隔离决策由调用方注入的 `quarantine_check`(Wave 2 pipeline 用 `lambda fid: is_quarantined(...)` 封装)。中文注释引用 WKR-02/D-10。
    在 `test_poller.py` 补上述用例(注入假的 quarantine_check 闭包 + 假 done_check),测试函数 `-> None`。既有 `run_worker_run` 的日志行若打印 skipped 数量,可顺带加隔离数(可选,不强制)。
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_poller.py -k "quarantine or plan_downloads or done" -x</automated>
  </verify>
  <acceptance_criteria>
    - `poller.py` ScanPlan contains `skipped_quarantined: list[str]`
    - `plan_downloads` signature contains `quarantine_check: Callable[[str], bool] | None = None`
    - plan_downloads returns ScanPlan with `skipped_quarantined` populated when quarantine_check returns True (test-verified)
    - A fid that is both .done and over-threshold lands in `skipped_done`, NOT `skipped_quarantined` (D-10 order test-verified)
    - Calling plan_downloads without quarantine_check reproduces prior behavior (existing test_poller.py cases stay green)
    - `pytest apps/worker/tests/test_poller.py -k quarantine` exits 0
    - `make typecheck` exits 0
  </acceptance_criteria>
  <done>plan_downloads 支持注入式隔离跳过,.done 分支优先不变,向后兼容;test_poller.py 覆盖且绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline — 每轮读账本注入 + 失败递增 + 跨阈值告警 + 成功清条目 + config 透传</name>
  <files>apps/worker/src/soniscope_worker/pipeline.py, apps/worker/tests/test_pipeline.py</files>
  <read_first>
    - apps/worker/src/soniscope_worker/pipeline.py (run_pipeline_once:375-442 消费端、run_pipeline_loop:445-508、run_worker_pipeline:511-544、STAGE_*/STATUS_* 常量:72-83、process_part 返回 FragmentResult.stage/ok:92-104)
    - apps/worker/tests/test_pipeline.py (既有 run_pipeline_once 多轮/`.done`/FakeSource 用例模式)
    - 06-RESEARCH.md §Code Examples "计数递增 + 告警" + "隔离跳过注入" + Pitfall 1(改 pipeline 非 poller)/Pitfall 4(成功清)/Pitfall 5(恰跨阈值告警一次)
    - apps/worker/src/soniscope_worker/quarantine.py (load_ledger/save_ledger/record_failure/clear_entry/is_quarantined/quarantine_alert_line/REASON_*)
  </read_first>
  <behavior>
    - Test 多轮累积隔离(奈奎斯特核心): FakeSource 每轮 list 同一 key + 强制下载失败(error 或损坏 body),连跑 threshold+1 轮 run_pipeline_once → 账本 attempt 逐轮 +1;第 threshold 轮末该 fid attempt==threshold;第 threshold+1 轮 to_download 为空(进 skipped_quarantined)、下载调用次数不再增(证明无界重下被止住)
    - Test 告警恰一次: 恰好在 attempt==threshold 的那一轮 log 收到一条含 fragment_id/reason/attempt/next 的告警行;之前的轮次不发告警
    - Test D-09 边界(计入): sha256_mismatch → 递增;download error → 递增;STAGE_STANDARDIZE 失败 → 递增
    - Test D-09 边界(不计入): STAGE_TRANSCRIBE 失败(fragment 目录已建)→ 账本 attempt 不变;STAGE_MANIFEST_* 失败 → attempt 不变
    - Test 成功清条目: fid 失败 2 次(未达阈值 3)后第 3 轮成功(FragmentResult.ok)→ 账本无该 fid 残留(防未来误隔离,Pitfall 4)
    - Test D-10 非回归: 已 .done 的 fragment 仍走 skipped_done,不因隔离逻辑改变(既有 test_pipeline `-k done` + make test-no-redownload 绿)
    - Test config 透传: run_worker_pipeline / run_pipeline_loop 把 max_fragment_failures 传到 run_pipeline_once(可用较小阈值注入验证隔离点随之移动)
  </behavior>
  <action>
    `run_pipeline_once`(pipeline.py:375)加 keyword-only 参数 `max_fragment_failures: int = 3`。函数体:每轮开头 `ledger = load_ledger(failed_root)`;`plan_downloads(...)` 调用点加 `quarantine_check=lambda fid: is_quarantined(int(ledger.get(fid, {}).get("attempt", 0)), threshold=max_fragment_failures)`(不动既有 done_check 分支);日志行加隔离计数 `隔离 {len(plan.skipped_quarantined)}`。
    消费端(pipeline.py:407-441)增计数:定义局部 `_record_and_maybe_alert(fid, reason)` — 调 `record_failure(ledger, fid, reason, now=<iso>)`,若返回 entry 的 attempt == max_fragment_failures 则 `log(quarantine_alert_line(fid, reason, attempt))`(恰跨阈值一次,Pitfall 5),然后 `save_ledger(failed_root, ledger)`。
    D-09 边界严格:`outcome.status != "downloaded"`(sha256_mismatch/error 前置失败)→ 用 `REASON_SHA256_MISMATCH`/`REASON_DOWNLOAD_ERROR` 递增;`process_part` 返回 `FragmentResult.status == STATUS_FAILED and stage == STAGE_STANDARDIZE` → 用 `REASON_STANDARDIZE_FAILED` 递增;`stage in (STAGE_TRANSCRIBE, STAGE_MANIFEST_DRAFT, STAGE_MANIFEST_FINAL, STAGE_TRANSCRIPT)` 的 failed **不递增**(D-09,fragment 目录已建,NLS 有自带重试);`FragmentResult.ok` → `clear_entry(ledger, fid)` + `save_ledger`(Pitfall 4)。
    `run_pipeline_loop`(pipeline.py:445)加同名参数并透传给 `run_pipeline_once`;`run_worker_pipeline`(pipeline.py:511)从 `cfg.poll.max_fragment_failures` 取值透传(与 `interval` 同源)。now iso 用既有 `now_iso` 注入通道(缺省时的默认取现有惯例)。
    在 `test_pipeline.py` 补上述用例(复用既有 FakeSource / stub transcriber,注入小阈值 + 可捕获 log 列表),测试函数 `-> None`,不触网。
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_pipeline.py -k "quarantine or accumulate or alert or standardize or transcribe or success or done" -x</automated>
  </verify>
  <acceptance_criteria>
    - `run_pipeline_once` signature contains `max_fragment_failures: int`
    - run_pipeline_once reads ledger via `load_ledger` and passes a `quarantine_check` lambda to `plan_downloads` (grep -v '^#' | grep -c 'quarantine_check' ≥ 1 in pipeline.py)
    - Multi-round test: same fid failing threshold+1 rounds → attempt increments each round, round threshold+1 has empty to_download and no further download calls (test-verified, WKR-02 core)
    - Alert fires exactly once at attempt==threshold with 4 elements (test-verified, WKR-03)
    - A STAGE_TRANSCRIBE failure leaves ledger attempt unchanged (D-09 test-verified)
    - A STAGE_STANDARDIZE failure increments the ledger (D-09 test-verified)
    - Success (FragmentResult.ok) clears the fid entry (Pitfall 4 test-verified)
    - `run_worker_pipeline` threads `cfg.poll.max_fragment_failures` into the loop (source-verified)
    - `make test-no-redownload` exits 0 (D-10 non-regression)
    - `pytest apps/worker/tests/test_pipeline.py -k done` exits 0 (D-10)
    - `make typecheck` exits 0
  </acceptance_criteria>
  <done>生产路径 run_pipeline_once 记录/隔离/告警接线完成,D-09 边界与 D-10 幂等红线经测试锁定,config 阈值透传;test_pipeline.py 覆盖且绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OSS listing → pipeline | 每轮 list_recordings 的对象集是外部输入;隔离判定决定是否下载 |
| disk ledger → pipeline | 每轮 load_ledger 读入账本(损坏由 Plan 01 load_ledger 降级) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Denial of Service (bandwidth/loop) | run_pipeline_once unbounded redownload | medium | mitigate | 达阈值 fid 进 skipped_quarantined,不再下载 — 无界循环止住(WKR-02,多轮测试验证)。此为本 phase 关闭 F-CODE-02 的核心缓解。 |
| T-06-08 | Information Disclosure | alert line at threshold crossing | low | mitigate | quarantine_alert_line 仅结构化非敏感字段(fid/reason/attempt/next),不回显下载 body 或秘密。 |
| T-06-09 | Tampering (false isolation) | occasional-failure fid | low | mitigate | 成功清条目(clear_entry),避免偶发失败累积到误隔离(Pitfall 4)。 |
</threat_model>

<verification>
- `uv run pytest apps/worker/tests/test_pipeline.py apps/worker/tests/test_poller.py -x` 全绿
- `make test-no-redownload` 绿(D-10 幂等非回归,成功标准 #4 显式要求)
- `make test` 全套绿(含 test_config/test_recovery/test_quarantine 回归)
- `make typecheck` + `make lint` 通过
</verification>

<success_criteria>
- 生产路径(run_pipeline_once)对前置失败递增账本、超阈值从下载集排除、跨阈值告警一次
- D-09 边界:仅 sha256_mismatch/error/STAGE_STANDARDIZE 计入;transcribe/manifest 阶段不计入
- D-10 红线:.done 幂等跳过路径不变,skipped_done 永远先于 skipped_quarantined
- config 阈值透传生产主循环
</success_criteria>

<output>
Create `.planning/phases/06-worker/06-03-SUMMARY.md` when done
</output>
