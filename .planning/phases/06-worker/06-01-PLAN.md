---
phase: 06-worker
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/worker/src/soniscope_worker/quarantine.py
  - apps/worker/tests/test_quarantine.py
autonomous: true
requirements: [WKR-01, WKR-02, WKR-03]
must_haves:
  truths:
    - "Per-fragment failure history persists for sha256_mismatch + download error + standardize failures (WKR-01, criterion 1)"
    - "Alert/diagnostic output contains fragment_id + reason + attempt count + next-action in fixed order (WKR-03, criterion 3)"
    - "The isolation predicate is a decoupled pure function importable and unit-tested independently of plan_downloads"
  artifacts:
    - "apps/worker/src/soniscope_worker/quarantine.py"
    - "apps/worker/tests/test_quarantine.py"
  key_links:
    - "quarantine.save_ledger delegates to recovery.atomic_write_json (D-03, no hand-rolled temp/rename)"
    - "run_clear_quarantine validates FRAGMENT_ID via oss_admin.object_key_for round-trip (V5 input validation)"
  prohibitions:
    - "is_quarantined must NOT be hard-coded into the poller download loop — it is a standalone pure function taking only (attempt, threshold)"
    - "save_ledger must NOT hand-roll temp/rename — must reuse recovery.atomic_write_json"
    - "load_ledger must NOT propagate parse exceptions — corrupt ledger degrades to {} (DoS mitigation)"
---

<objective>
新建 `quarantine.py` 模块:失败计数账本的纯逻辑(schema、`is_quarantined` 判据、`record_failure`/`clear_entry` read-modify-write、告警行组装)+ IO(`load_ledger`/`save_ledger` 复用 `recovery.atomic_write_json`)+ 两个 CLI 运行器(`run_quarantine_list`/`run_clear_quarantine`)。同时建 `test_quarantine.py` 覆盖全部纯逻辑、账本累积、告警四要素、损坏账本降级、磁盘重读(重启存活)与 CLI 增删。

Purpose: 这是本 phase 唯一的全新机制代码(RESEARCH「唯一真新代码」)。账本用单一 JSON 落 `inbox/failed/ledger.json`(D-01),状态从磁盘推导、重启不丢隔离(D-02,非进程内计数)。隔离判据写成与工作集解耦的纯函数(D-10 + deferred FC 迁移复用要求),供 Wave 2 的 poller/pipeline 注入。
Output: `quarantine.py`(公开符号见 Artifacts 段)+ `test_quarantine.py`。
</objective>

<execution_context>
@/Users/bemied/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Users/bemied/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-worker/06-CONTEXT.md
@.planning/phases/06-worker/06-RESEARCH.md
@.planning/phases/06-worker/06-VALIDATION.md
</context>

<artifacts_this_phase_produces>
This plan creates the module `apps/worker/src/soniscope_worker/quarantine.py` with public symbols:
`is_quarantined`, `record_failure`, `clear_entry`, `load_ledger`, `save_ledger`, `ledger_path`,
`quarantine_alert_line`, `run_quarantine_list`, `run_clear_quarantine`, and constants
`LEDGER_FILENAME`, `QUARANTINE_ALERT_PREFIX`, `NEXT_ACTION_HINT`, `REASON_SHA256_MISMATCH`,
`REASON_DOWNLOAD_ERROR`, `REASON_STANDARDIZE_FAILED`. Plus new test file `apps/worker/tests/test_quarantine.py`.
(Downstream drift-verification excludes these newly-created symbols.)
</artifacts_this_phase_produces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: quarantine.py 纯逻辑 + 账本 IO + 告警组装(含失败测试先行)</name>
  <files>apps/worker/src/soniscope_worker/quarantine.py, apps/worker/tests/test_quarantine.py</files>
  <read_first>
    - apps/worker/src/soniscope_worker/recovery.py (atomic_write_json:63 — 账本原子写复用源)
    - apps/worker/src/soniscope_worker/paths.py (inbox_failed_dir:83 — 账本目录解析)
    - 06-RESEARCH.md §Code Examples "Pattern 2/3/4"(is_quarantined / load_ledger / save_ledger / quarantine_alert_line 目标签名)
    - 06-RESEARCH.md §Common Pitfalls (Pitfall 2 损坏账本降级、Pitfall 4 成功清条目、Pitfall 5 阈值语义 >= )
    - apps/worker/src/soniscope_worker/audit.py (模块 docstring + `# ── 段落名 ─────` 分节 + UPPER_SNAKE 常量惯例)
  </read_first>
  <behavior>
    - Test is_quarantined: attempt=threshold-1 → False;attempt=threshold → True;attempt=threshold+1 → True;attempt=0,threshold=3 → False(边界锁定 D-05/Pitfall 5,`>=` 语义)
    - Test record_failure: 首次记录 fid → 返回 entry attempt=1、reason=给定值、first_seen==last_seen;同 fid 再记 → attempt=2、first_seen 不变、last_seen 更新;reason 更新为最新失败原因
    - Test 多轮累积(accumulate): 同 fid 连续记 N 次 → attempt==N(账本是 dict,in-memory 累积语义)
    - Test clear_entry: 存在的 fid → 删除后 fid 不在账本;不存在的 fid → 幂等无异常
    - Test load_ledger/save_ledger 往返(磁盘重读/重启存活): save 后 load 回读值相等;隔离态 attempt>=threshold 重读后仍成立
    - Test 损坏账本降级(corrupt): 写入非法 JSON 文本 → load_ledger 返回 {} 不抛异常;写入 JSON 数组(非 dict)→ 返回 {}
    - Test 告警四要素(alert): quarantine_alert_line 输出含固定顺序子串 `fragment_id=` / `reason=` / `attempt=` / `next=`,且以 QUARANTINE_ALERT_PREFIX 开头
    - Test 常量存在: LEDGER_FILENAME == "ledger.json";REASON_* 三个常量值稳定
  </behavior>
  <action>
    先在 `test_quarantine.py` 写上述 RED 测试(全部先失败),再实现 `quarantine.py`。
    模块首行 `from __future__ import annotations`;模块 docstring 注明 purpose + 关联发现 F-CODE-02 + WKR-01/02/03 + invariant(账本即隔离依据,单一真相源)。
    定义常量: `LEDGER_FILENAME = "ledger.json"`;`QUARANTINE_ALERT_PREFIX = "[quarantine]"`;`NEXT_ACTION_HINT`(中文,提示 `make clear-quarantine FRAGMENT_ID=<id>` 解除后下一轮重下);`REASON_SHA256_MISMATCH = "sha256_mismatch"`、`REASON_DOWNLOAD_ERROR = "error"`(对齐 ObjectOutcome.status)、`REASON_STANDARDIZE_FAILED = "standardize_failed"`。
    纯逻辑函数(无 IO):`def is_quarantined(attempt: int, *, threshold: int) -> bool`(实现 `attempt >= threshold`,per D-05/Pitfall 5);`def record_failure(ledger: dict[str, dict[str, object]], fragment_id: str, reason: str, *, now: str) -> dict[str, object]`(read-modify:attempt 自增、reason 覆盖为最新、first_seen 仅首次写、last_seen 每次写,返回该 entry);`def clear_entry(ledger: dict[str, dict[str, object]], fragment_id: str) -> None`(幂等删)。
    IO 函数:`def ledger_path(failed_root: Path) -> Path`(返回 `failed_root / LEDGER_FILENAME`);`def load_ledger(failed_root: Path) -> dict[str, dict[str, object]]`(文件不存在→{};`json.loads` 包 `try/except (OSError, json.JSONDecodeError)` 降级 {};非 dict→{} — Pitfall 2 DoS 缓解);`def save_ledger(failed_root: Path, ledger: dict[str, dict[str, object]]) -> None`(调 `recovery.atomic_write_json`,per D-03,禁自造 temp/rename)。
    告警组装(纯):`def quarantine_alert_line(fragment_id: str, reason: str, attempt: int) -> str`,固定顺序输出 `{QUARANTINE_ALERT_PREFIX} fragment_id=… reason=… attempt=… next=…`(D-08,便于 grep + pytest 断言)。告警行只含结构化非敏感字段,绝不回显秘密(CLAUDE.md 红线)。
    全量类型注解(mypy --strict);测试函数 `-> None`;Chinese 注释引用 D-NN/AC。`run_quarantine_list`/`run_clear_quarantine` 在 Task 2 加。
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_quarantine.py -k "is_quarantined or record or accumulate or alert or corrupt or success_clears or ledger" -x</automated>
  </verify>
  <acceptance_criteria>
    - `quarantine.py` 首行为 `from __future__ import annotations`
    - `quarantine.py` contains `def is_quarantined(attempt: int, *, threshold: int) -> bool`
    - `quarantine.py` contains `def save_ledger(` whose body calls `atomic_write_json` (grep -v '^#' | grep -c atomic_write_json ≥ 1)
    - `load_ledger` catches `(OSError, json.JSONDecodeError)` and returns `{}` on corrupt input (test-verified)
    - `LEDGER_FILENAME == "ledger.json"` asserted in a test
    - `quarantine_alert_line(...)` output contains substrings `fragment_id=`, `reason=`, `attempt=`, `next=` in that order (test-verified)
    - `is_quarantined` is importable without importing `poller` (test imports only `soniscope_worker.quarantine`)
    - `pytest apps/worker/tests/test_quarantine.py -k is_quarantined` exits 0
    - `make typecheck` exits 0
  </acceptance_criteria>
  <done>纯判据、账本 read-modify-write、原子写 IO、损坏降级、告警四要素、磁盘重读全部有 pytest 覆盖且绿;mypy --strict 通过。</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: quarantine.py CLI 运行器 run_quarantine_list / run_clear_quarantine(含失败测试先行)</name>
  <files>apps/worker/src/soniscope_worker/quarantine.py, apps/worker/tests/test_quarantine.py</files>
  <read_first>
    - apps/worker/src/soniscope_worker/retranscribe.py (run_retranscribe:280 — `(lines, code)` tuple 运行器 + `emit`/lazy-import config/paths 模式)
    - apps/worker/src/soniscope_worker/oss_admin.py (object_key_for:37, OssAdminError:33 — FRAGMENT_ID 往返校验)
    - apps/worker/src/soniscope_worker/paths.py (inbox_failed_dir:83)
    - 06-RESEARCH.md §Security Domain(V5 输入校验:clear-quarantine 非法 FRAGMENT_ID 经 object_key_for 往返)
    - quarantine.py(Task 1 已实现的 load_ledger/save_ledger/clear_entry/is_quarantined)
  </read_first>
  <behavior>
    - Test cli list(空): 账本空 → run_quarantine_list 返回 (lines, 0),lines 含「无隔离」类提示
    - Test cli list(有隔离项): 账本含一条 attempt>=threshold 的 fid → 输出行含该 fragment_id + reason + attempt;未达阈值的 fid 不列入隔离清单
    - Test cli clear 单条: 账本含 fid A、B → run_clear_quarantine(fragment_id=A) 后账本仅剩 B,返回 code 0
    - Test cli clear --all: 账本含多条 → run_clear_quarantine(clear_all=True) 后账本为空,返回 code 0
    - Test cli clear 非法 fid: run_clear_quarantine(fragment_id="not-a-valid-id") → 非零 code + 错误行,账本不变(object_key_for 往返拒绝)
    - Test cli clear 参数缺失: 既无 fragment_id 也无 clear_all → 非零 code + 参数错误提示
  </behavior>
  <action>
    先补 RED 测试(-k cli),再实现两个运行器。签名对齐 retranscribe 惯例、返回 `tuple[list[str], int]`、`emit = log if log is not None else lines.append`,lazy-import `paths.inbox_failed_dir`。
    `def run_quarantine_list(*, threshold: int | None = None, failed_root: Path | None = None, log: Callable[[str], None] | None = None) -> tuple[list[str], int]`:读账本 → 用 `is_quarantined` 过滤 attempt>=阈值者 → 每条打印 fragment_id/reason/attempt/first_seen/last_seen;threshold 缺省时 lazy 读 `config.load_config().poll.max_fragment_failures`(config 不可读则降级默认 3 或打印诊断非零退出,与 retranscribe 缺 config 同款)。`failed_root` 参数供单测注入(不触真实 $SONISCOPE_HOME)。
    `def run_clear_quarantine(*, fragment_id: str | None, clear_all: bool, failed_root: Path | None = None, log: Callable[[str], None] | None = None) -> tuple[list[str], int]`:既无 fragment_id 也非 clear_all → 参数错误非零;fragment_id 给定先 `object_key_for(fragment_id)` 往返,`OssAdminError` → 打印非法并非零退出(不改账本);合法则 `clear_entry` + `save_ledger`;`clear_all` → 置空账本 + save。成功打印解除提示(下一轮重下)。
    可选:read-modify-write 竞态取 RESEARCH Pitfall 3 方案(a)接受竞态,在 clear 输出注一句「若下一轮仍被跳过请重跑」即可,不引锁(个人规模,窗口极小)。
    全量注解;测试注入 `failed_root=tmp_path`、`log=lines.append`,不触盘真实 HOME、不触网。
  </action>
  <verify>
    <automated>uv run pytest apps/worker/tests/test_quarantine.py -k cli -x</automated>
  </verify>
  <acceptance_criteria>
    - `quarantine.py` contains `def run_quarantine_list(` returning a `tuple[list[str], int]`
    - `quarantine.py` contains `def run_clear_quarantine(` returning a `tuple[list[str], int]`
    - `run_clear_quarantine(fragment_id="not-a-valid-id", clear_all=False)` returns non-zero code and leaves the ledger unchanged (test-verified via `object_key_for` round-trip)
    - `run_clear_quarantine(fragment_id=None, clear_all=False)` returns non-zero code (missing-arg guard)
    - `run_quarantine_list` lists only fids with `attempt >= threshold` (test-verified)
    - Tests inject `failed_root=tmp_path` and never touch real `$SONISCOPE_HOME` or the network
    - `pytest apps/worker/tests/test_quarantine.py -k cli` exits 0
    - `make typecheck` exits 0 and `make lint` exits 0
  </acceptance_criteria>
  <done>两个运行器有 pytest 覆盖(空/有项/单删/全删/非法 fid/缺参),FRAGMENT_ID 往返校验生效,mypy/ruff 绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disk → worker process | `inbox/failed/ledger.json` 内容不可信(可能被手改/半写),读入即穿越 |
| operator CLI → ledger | `clear-quarantine FRAGMENT_ID=<id>` 的 fid 是外部输入 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Denial of Service | load_ledger (corrupt/malicious ledger.json) | medium | mitigate | `load_ledger` catches `(OSError, json.JSONDecodeError)` and non-dict payloads, degrades to `{}` — never kills the daemon. Test-verified. |
| T-06-02 | Tampering/Input | run_clear_quarantine (FRAGMENT_ID) | low | mitigate | Validate fid via `oss_admin.object_key_for` round-trip before mutating ledger (same guard as retranscribe / oss-delete-obj). |
| T-06-03 | Information Disclosure | quarantine_alert_line | low | mitigate | Alert carries only fragment_id/reason/attempt/next-action structured fields — never secrets or raw body (CLAUDE.md secrets red line). |
| T-06-04 | Denial of Service (disk) | ledger growth | low | accept | Success clears entry (Plan 03) + clear-quarantine operator exit bound size; personal-scale entry count is tiny. |
</threat_model>

<verification>
- `uv run pytest apps/worker/tests/test_quarantine.py -x` 全绿(纯判据、record、accumulate、alert、corrupt、cli、success_clears、ledger 磁盘重读)
- `make typecheck`(mypy --strict on apps/worker/src + tests)通过
- `make lint`(ruff E,F,I,UP,B line-100)通过
</verification>

<success_criteria>
- `quarantine.py` 提供全部 Artifacts 段列出的公开符号,含解耦纯判据 `is_quarantined`
- `test_quarantine.py` 覆盖 WKR-01(history/accumulate)、WKR-02(is_quarantined 边界)、WKR-03(alert 四要素 + cli list/clear)、损坏降级、磁盘重读、成功清条目
- 账本写盘走 `recovery.atomic_write_json`;FRAGMENT_ID 经 `object_key_for` 校验;告警不含秘密
</success_criteria>

<output>
Create `.planning/phases/06-worker/06-01-SUMMARY.md` when done
</output>
