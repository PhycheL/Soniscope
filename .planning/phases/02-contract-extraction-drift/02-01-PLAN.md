---
phase: 02-contract-extraction-drift
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/CONTRACT-MATRIX.md
autonomous: true
requirements: [CONTRACT-01]
must_haves:
  truths:
    - "CONTRACT-MATRIX.md 存在,含 D-01 三组分节骨架(OSS 数据面 / HTTP 契约 / 镜像常量)与往返校验、样本清单、普查、收尾四个占位章节"
    - "组① OSS 数据面全部逐字段行落格:key 族(fragment_id 正则、日期合法性校验、object key 模板、key 目录日期来源、key→fragment_id 反推、.wav 扩展名)+ 7 个 x-oss-meta-* 字段 + chunk_total 语义映射 + recorded-at 值格式"
    - "每个非 n/a 格标注 agree/diverge/absent 状态并附 path:line @ 5927f36 行号证据,agree 格同样带行号(D-11)"
    - "n/a 格写一句结构性理由代替行号(D-03)"
    - "全部证据出自 git show 5927f36:<path>,零 diff 验证通过"
  artifacts:
    - ".planning/audit/CONTRACT-MATRIX.md"
  key_links:
    - "矩阵每格行号可用 git show 5927f36:<path> 复核命中对应代码"
    - "判定列留待 02-04 回填(只建列不填四类标签)"
---

<objective>
创建 `.planning/audit/CONTRACT-MATRIX.md`(D-09:矩阵独立成文件)并完成 D-01 组①(OSS 数据面)的逐字段静态抽取:每个契约要素一行(D-02),FC(`fc_shared`)/ Worker / 小程序(utils)严格三列(D-04),每格独立标注 agree/diverge/absent/n/a + `path:line @ 5927f36` 行号证据(D-11,agree 格也要行号;D-03,n/a 格写结构性理由)。

Purpose: 组① 是 HYP-13(fragment_id ↔ object_key 契约三处重复实现)的核心证据面,是本阶段一切判定的地基。
Output: `.planning/audit/CONTRACT-MATRIX.md`(骨架 + 组① 全部行,约 15 行矩阵)。

**秘密红线(CHARTER,每个任务生效):** 引用疑似秘密类证据只写 `path:line @ 5927f36` + 模式名,绝不复制值本体——哪怕是测试假值以外的任何真实值。`.planning` 提交即永久入库。
**取证纪律(D-05):** 一切行号与代码片段出自 `git show 5927f36:<path>` 或 `git grep -n <pat> 5927f36 -- apps/`,禁止读工作树文件充当证据。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/audit/CHARTER.md
@.planning/phases/02-contract-extraction-drift/02-CONTEXT.md
@.planning/phases/02-contract-extraction-drift/02-RESEARCH.md
@.planning/phases/02-contract-extraction-drift/02-PATTERNS.md
</context>

## Artifacts this phase produces

| Artifact | Plan | 说明 |
|----------|------|------|
| `.planning/audit/CONTRACT-MATRIX.md` | 02-01 新建;02-02/03/04 追加 | 漂移矩阵 + 往返校验记录 + 样本清单附录 + 普查章节 + 零 diff 收尾记录 |
| `.planning/audit/findings/contract.md` | 02-04 追加 | F-CON-NN 发现条目(九字段 schema) |
| `.planning/audit/CONTRACT-TEST-RECIPE.md` | 02-04 条件新建(D-15) | 黄金样本跨语言契约测试设计配方(仅设计不实现) |
| scratchpad harness(`harness.py`/`harness.js` + 基线导出树) | 02-03 临时物 | 不入仓库,不提交 |

<tasks>

<task type="auto">
  <name>Task 1: 建矩阵骨架并抽取组① key 族六行</name>
  <files>.planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/CHARTER.md(证据格式 `path:line @ 5927f36`、短 SHA 规则、零 diff 命令)
    - .planning/phases/02-contract-extraction-drift/02-PATTERNS.md(文件头三行模式、矩阵格证据格式、文档尾模式——照抄结构)
    - .planning/phases/02-contract-extraction-drift/02-RESEARCH.md『契约现场勘察』组① 表(锚点行号起点)
    - `git show 5927f36:apps/fc/shared/fc_shared/sts.py`(_FRAGMENT_ID_RE :28-32、object_key_for :46-59)
    - `git show 5927f36:apps/worker/src/soniscope_worker/oss_admin.py`(正则 :24-27、object_key_for :37-50)
    - `git show 5927f36:apps/worker/src/soniscope_worker/poller.py`(fragment_id_from_key :47-60、.wav endswith :52)
    - `git show 5927f36:apps/miniprogram/utils/audio.js`(FRAGMENT_ID_RE :95-97、buildObjectKeyPreview :103-105、require config :10)
    - `git show 5927f36:apps/miniprogram/utils/upload_queue.js`(fragmentIdFromObjectKey :36-44)
    - `git show 5927f36:apps/miniprogram/config.js`(OSS_OBJECT_KEY_EXT 常量)
  </read_first>
  <action>
    用 Write 工具新建 `.planning/audit/CONTRACT-MATRIX.md`(实现 D-09)。文件头照 02-PATTERNS.md 的三行模式:`# 契约漂移矩阵` 标题 + `**Created:** 2026-07-05` + 基线引用行(短 SHA `5927f36`,全 SHA 声明指向 CHARTER)。章节骨架(Claude's discretion 采用按契约域分节,防 RESEARCH Pitfall 8 行爆炸):
    1. `## 判定标准与负面清单` — 写明格子状态词表(agree / diverge / absent=应参与未实现 / n/a=结构性不适用,per D-03);写明"diverge 指语义分歧,不是字面差异"(RESEARCH Pitfall 5);列负面清单:DNF-01~04 已裁定故意设计不立分歧、不引入 `docs/fc-transcribe-design.md` 目标态对照(CHARTER 排除项)。
    2. `## 组① OSS 数据面`(本任务与 Task 2 填)
    3. `## 组② 小程序↔FC HTTP 契约`(02-02 填,留节标题)
    4. `## 组③ 两侧镜像常量`(02-02 填,留节标题)
    5. `## 重复逻辑普查`(02-02 填,留节标题)
    6. `## 往返校验结论`(02-03 填,留节标题)
    7. `## 附录:往返校验样本清单`(02-03 填,留节标题)
    8. `## 收尾:零 diff 验证与对账`(02-04 填,留节标题)

    组① 表列式:`| 契约要素 | FC (fc_shared) | Worker | 小程序 (utils) | 判定 |`(D-04 严格三列实现 + 判定列;判定列本计划一律填 `待判定`,四类标签与 F-CON 链接由 02-04 回填,per D-12)。本任务抽取 key 族六行,每格 = 状态词 + `path:line @ 5927f36`(D-11):
    - 行 1 fragment_id 格式正则:FC `sts.py:28-32` / Worker `oss_admin.py:24-27` / 小程序 `audio.js:95-97` — 逐字符对照三处正则字面,状态按语义一致性判。
    - 行 2 fragment_id 日期合法性校验:FC 与 Worker 正则命中后的 `datetime()` 合法性校验 vs 小程序无此校验 — 以 git show 展开三处校验逻辑逐行对照后落状态。
    - 行 3 object key 模板 `recordings/<YYYY-MM-DD>/<id>.wav`:FC `sts.py:46-59` / Worker `oss_admin.py:37-50` / 小程序 `audio.js:103-105`。
    - 行 4 key 目录日期来源:FC 与 Worker 取自 fragment_id 前缀 vs 小程序 `buildObjectKeyPreview` 日期取自 `recordedAt` 本地时区(两个独立入参)— 单独成行。
    - 行 5 key → fragment_id 反推:FC 列 n/a(理由:FC 只正向签发,不反推)/ Worker `poller.py:47-60`(往返校验式)/ 小程序 `upload_queue.js:36-44`(fragmentIdFromObjectKey,纯字符串切割)。
    - 行 6 `.wav` 固定扩展名:FC `sts.py:59` / Worker `poller.py:52` / 小程序 `config.js` 的 OSS_OBJECT_KEY_EXT(由 `audio.js:10` require)。
    上述行号是 RESEARCH 勘察起点,落格前必须用 git show 逐一复核,以复核后的实际行号为准;发现勘察行号有偏差以实际为准并如实记录。每行下可附一段简短证据摘录(从 git show 提取,≤3 行);不复制任何疑似秘密值。
  </action>
  <verify>
    <automated>test -f .planning/audit/CONTRACT-MATRIX.md && [ "$(grep -o '@ 5927f36' .planning/audit/CONTRACT-MATRIX.md | wc -l)" -ge 12 ] && grep -q '## 组① OSS 数据面' .planning/audit/CONTRACT-MATRIX.md && grep -q '## 收尾' .planning/audit/CONTRACT-MATRIX.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audit/CONTRACT-MATRIX.md` 存在,含 8 个章节标题(判定标准/组①/组②/组③/普查/往返校验结论/样本清单附录/收尾)
    - 组① 表含 6 行 key 族契约要素,每个非 n/a 格有状态词 + `path:line @ 5927f36` 引用(`grep -o '@ 5927f36' | wc -l` ≥ 12)
    - FC 列的 key 反推格为 n/a 且写有一句结构性理由,无行号(D-03)
    - 判定标准章节含 agree/diverge/absent/n/a 四个状态词定义与 DNF 负面清单
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>矩阵骨架成形,组① key 族 6 行三列证据齐备,判定列统一为待判定</done>
</task>

<task type="auto">
  <name>Task 2: 抽取组① 元数据九行(7 个 x-oss-meta-* 字段 + chunk_total 语义 + recorded-at 格式)</name>
  <files>.planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(Task 1 产出的当前状态)
    - `git show 5927f36:apps/miniprogram/utils/audio.js`(buildOssMetadata 写入端 :158-170、toIso :75-86)
    - `git show 5927f36:apps/worker/src/soniscope_worker/poller.py`(META_* 常量 :33-40、normalize_metadata :85-93、manifest 映射 :132+、ManifestDraft docstring)
    - `git show 5927f36:apps/worker/src/soniscope_worker/pipeline.py`(元数据消费触点,配合 RESEARCH 勘察"x-oss-meta 触点分布")
    - .planning/phases/02-contract-extraction-drift/02-RESEARCH.md(Pitfall 5:chunk_total 故意不对称;组① 勘察表)
  </read_first>
  <action>
    在组① 节续写 9 行(D-02 逐字段,每个元数据字段独立一行):
    - 行 7-13:7 个 `x-oss-meta-*` 字段各一行 — session-id、chunk-seq、chunk-total、recorded-at、duration、original-format、sha256。每行:小程序列 = `audio.js:162-168` 区间内该键的写入行号;Worker 列 = `poller.py` 的 META_* 常量行 + normalize_metadata/manifest 映射行(可写多个行号);FC 列 = 判定点——FC 不读写 meta,逐字段判 n/a(结构性不参与)还是 absent(应参与而未实现,覆盖洞候选,per D-03);此判定本任务先按静态事实落格(FC 代码是否有任何 meta 触点用 `git grep -n 'x-oss-meta' 5927f36 -- apps/fc/` 核实),是否构成覆盖洞的四类归类留给 02-04。
    - 行 14:chunk_total 语义映射(小程序 manifest `null` → OSS meta `"0"` → Worker manifest `None`)— 小程序 `audio.js:157` 注释 + Worker `poller.py` ManifestDraft docstring 两侧均声明该约定;按 Pitfall 5 规则,字面异/语义同应标 agree 并在格内注明映射约定行号,不得机械判 diverge。
    - 行 15:recorded-at 值格式 — 小程序 `audio.js:75-86`(toIso,本地时区偏移 ISO 8601)vs Worker 映射端对该值的解析/透传方式;FC 列同上判 n/a 或 absent。
    所有行号先经 git show 复核再落格。测试文件中对这些字段的断言(如 `apps/miniprogram/test/` 内 META 常量)不占列,但可在格内以括注补一条辅助证据(D-04:常量被测试锁定)。
  </action>
  <verify>
    <automated>[ "$(grep -c 'x-oss-meta-' .planning/audit/CONTRACT-MATRIX.md)" -ge 7 ] && [ "$(grep -o '@ 5927f36' .planning/audit/CONTRACT-MATRIX.md | wc -l)" -ge 30 ] && grep -q 'chunk-total' .planning/audit/CONTRACT-MATRIX.md && grep -q 'recorded-at' .planning/audit/CONTRACT-MATRIX.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - 组① 节含 session-id/chunk-seq/chunk-total/recorded-at/duration/original-format/sha256 七个字段行(`grep -c 'x-oss-meta-'` ≥ 7)
    - chunk_total 语义映射行的状态判定遵循"语义分歧才是 diverge"规则,格内注有两侧映射约定行号(audio.js:157 与 poller.py ManifestDraft docstring 的实际行号)
    - FC 列逐字段标 n/a 或 absent,并附一句理由;n/a/absent 的裁决有 `git grep 'x-oss-meta' 5927f36 -- apps/fc/` 结果支撑(命中/未命中记入格内或行下注)
    - 全文 `@ 5927f36` 出现次数 ≥ 30
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>组① 全部 15 行落格完毕,元数据逐字段三列证据齐备,FC 列 n/a/absent 双语义区分落地</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 基线代码 → 审计文档 | 从 git show 摘录代码片段进 `.planning`,可能携带敏感字面量 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | CONTRACT-MATRIX.md 证据摘录 | high | mitigate | CHARTER 秘密红线:疑似秘密只写 path:line + 模式名,绝不复制值本体;摘录前人工过目 |
| T-02-02 | Tampering | apps/ scripts/ docs/ 基线 | medium | mitigate | 取证只用 git show/git grep(只读);每任务收尾跑 `git diff --stat 5927f36 -- apps/ scripts/ docs/` 必须为空 |
| T-02-SC | Tampering | 包安装供应链 | low | accept | 本阶段零安装零新依赖(RESEARCH Package Legitimacy Audit:不适用),无安装面 |
</threat_model>

<verification>
- `.planning/audit/CONTRACT-MATRIX.md` 存在且组① 15 行齐备,`grep -o '@ 5927f36' | wc -l` ≥ 30
- 零 diff:`git diff --stat 5927f36 -- apps/ scripts/ docs/` 空输出
- `git status --porcelain` 仅见 `.planning/` 路径变更
- 人工抽查:任取 3 个格的 `path:line @ 5927f36`,用 `git show 5927f36:<path> | sed -n '<line>p'` 复核命中(02-VALIDATION.md Manual-Only 项)
</verification>

<success_criteria>
- 矩阵骨架 8 章节成形,组① OSS 数据面逐字段抽取完成(CONTRACT-01 组① 部分)
- 每格状态 + 行号证据符合 D-02/D-03/D-04/D-11,判定列留待 02-04
- 仓库零污染
</success_criteria>

<output>
Create `.planning/phases/02-contract-extraction-drift/02-01-SUMMARY.md` when done
</output>
