---
phase: 02-contract-extraction-drift
plan: 2
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .planning/audit/CONTRACT-MATRIX.md
autonomous: true
requirements: [CONTRACT-01, CONTRACT-03]
must_haves:
  truths:
    - "组② HTTP 契约逐字段行落格:issue-credential 请求 3 字段、响应 7 字段、verify-upload 请求/响应字段全集、7 个错误码字符串、2 个 verify reason,每行 FC 侧与小程序侧行号证据齐备,Worker 列 n/a(结构性理由)"
    - "组③ 镜像常量行落格:重试节奏 5/15/45、最多 3 次、50MB 上限、600s 分片阈值、STS 900s"
    - "普查章节完成:9 项候选逐项核实结论(含'已检查,无新发现'显式行)+ 系统扫描命令原文与输出存档;实现命中入矩阵新行,测试命中记为格内辅助证据"
    - "重复债务线索标注移交 Phase 3(D-14)"
  artifacts:
    - ".planning/audit/CONTRACT-MATRIX.md"
  key_links:
    - "普查命中的契约承载逻辑 → 矩阵对应组新行(D-14)"
    - "uploader.js 错误码分支实态(RESEARCH Open Question 1)在矩阵行落定 agree/absent/n/a"
---

<objective>
完成 D-01 组②(小程序↔FC HTTP 契约)与组③(两侧镜像常量)的逐字段静态抽取(D-02/D-11 同 02-01),并完成 CONTRACT-03 重复逻辑普查(D-13 双保险:候选清单逐项核实 + git grep 系统扫描存档;D-14:命中重复入矩阵、债务判断移交 Phase 3)。

Purpose: 组②③ 补齐矩阵全部行;普查回答"三处之外还有没有契约重复实现",并给 RPT-08 提供可复核的"已检查,无新发现"记录。
Output: CONTRACT-MATRIX.md 组②③ 节 + 普查节(约 25 行矩阵新增)。

**秘密红线(CHARTER):** 疑似秘密类证据只写 `path:line @ 5927f36` + 模式名,绝不复制值本体。
**取证纪律(D-05):** 一切证据出自 `git show 5927f36:<path>` / `git grep -n <pat> 5927f36 -- apps/`,禁止读工作树。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/audit/CHARTER.md
@.planning/phases/02-contract-extraction-drift/02-CONTEXT.md
@.planning/phases/02-contract-extraction-drift/02-RESEARCH.md
@.planning/phases/02-contract-extraction-drift/02-PATTERNS.md
@.planning/phases/02-contract-extraction-drift/02-01-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Plan | 说明 |
|----------|------|------|
| `.planning/audit/CONTRACT-MATRIX.md` | 02-01 新建;02-02/03/04 追加 | 漂移矩阵 + 往返校验记录 + 样本清单附录 + 普查章节 + 零 diff 收尾记录 |
| `.planning/audit/findings/contract.md` | 02-04 追加 | F-CON-NN 发现条目(九字段 schema) |
| `.planning/audit/CONTRACT-TEST-RECIPE.md` | 02-04 条件新建(D-15) | 黄金样本跨语言契约测试设计配方(仅设计不实现) |
| scratchpad harness | 02-03 临时物 | 不入仓库,不提交 |

<tasks>

<task type="auto">
  <name>Task 1: 组② HTTP 契约逐字段抽取(含 verify-upload 字段全集展开与 uploader.js 错误码分支裁决)</name>
  <files>.planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(02-01 产出;组② 节标题与表列式沿用组①)
    - .planning/phases/02-contract-extraction-drift/02-RESEARCH.md(『契约现场勘察』组② 表;Open Questions 1/2)
    - `git show 5927f36:apps/fc/issue_credential/handler.py`(请求字段解析入口)
    - `git show 5927f36:apps/fc/shared/fc_shared/sts.py`(parse_size :74-90、credential_response :102-114)
    - `git show 5927f36:apps/fc/verify_upload/handler.py` 与 `git show 5927f36:apps/fc/shared/fc_shared/head.py`(verify-upload 请求/响应字段全集——RESEARCH Open Question 2 点名逐行展开)
    - `git show 5927f36:apps/fc/shared/fc_shared/errors.py`(错误码 :13-19、reason :23-25)
    - `git show 5927f36:apps/miniprogram/utils/uploader.js`(请求组装、classifyFcResponse 全文、object_key 消费 :102 附近 AC#4)
    - `git show 5927f36:apps/miniprogram/utils/verify.js`(REASON_* :20-21、重试与响应消费分支)
  </read_first>
  <action>
    在组② 节落格(表列式与组① 相同;Worker 列本组全部 n/a,每格写一句结构性理由如"Worker 不参与 HTTP 契约",per D-03/D-04):
    - issue-credential 请求字段 3 行:code、fragment_id、size — FC 侧 = handler.py 解析行 + sts.py parse_size;小程序侧 = uploader.js 请求组装处该字段的写入行。
    - issue-credential 响应字段 7 行:以 `sts.py:102-114`(credential_response)实际返回的字段名为准逐字段一行;小程序侧 = uploader.js 对每个字段的消费行号(未消费的字段如实标注,消费方式差异落状态)。object_key 字段行需注明 AC#4 语义(小程序用 FC 返回值、不由前端拼接)——这是往返链关键环,行下注 02-03 将做执行佐证。
    - verify-upload 请求字段与响应字段:先用 git show 展开 handler.py + head.py 逐行确定字段全集(请求含 code/object_key/size 类字段以实际代码为准;响应含 verified/reason 等以实际代码为准),每字段一行;小程序侧 = verify.js 对应组装/消费行。
    - 错误码字符串 7 行:INVALID_CODE、OPENID_NOT_ALLOWED、INVALID_REQUEST、SIZE_EXCEEDED、SERVER_MISCONFIGURED、STS_ISSUE_FAILED、HEAD_OBJECT_FAILED(FC 侧 `errors.py:13-19`)。小程序侧裁决 RESEARCH Open Question 1:以 uploader.js classifyFcResponse 全文对照,确认小程序是按错误码字符串分支还是按 statusCode 段分支;逐码落 agree/absent/n/a 状态。若查明 CLAUDE.md"uploader.js branches on the same strings"声明与实态不符,在行下注一条移交线索(Phase 4 DOC 维度),不在本矩阵立 DOC 判断。
    - verify reason 2 行:OBJECT_NOT_FOUND、SIZE_MISMATCH(FC 侧 `errors.py:23-25` / 小程序侧 `verify.js:20-21`)。
    所有行号先经 git show 复核。测试断言不占列,可作格内括注辅助证据(D-04)。
  </action>
  <verify>
    <automated>for c in INVALID_CODE OPENID_NOT_ALLOWED INVALID_REQUEST SIZE_EXCEEDED SERVER_MISCONFIGURED STS_ISSUE_FAILED HEAD_OBJECT_FAILED OBJECT_NOT_FOUND SIZE_MISMATCH; do grep -q "$c" .planning/audit/CONTRACT-MATRIX.md || exit 1; done && grep -q 'credential_response\|issue-credential 响应' .planning/audit/CONTRACT-MATRIX.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - 组② 节含:请求字段 3 行、响应字段 7 行、verify-upload 请求/响应字段行(字段全集以 handler.py + head.py 实际代码为准)、错误码 7 行、reason 2 行
    - 9 个错误码/reason 字符串全部出现在矩阵中(自动检查逐一 grep 命中)
    - uploader.js 错误码分支实态有明确落格结论(agree/absent/n/a 之一),且以 classifyFcResponse 的 git show 行号为证
    - Worker 列全部为 n/a + 结构性理由,无行号
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>组② 全部逐字段行落格,Open Question 1/2 有行号级答案</done>
</task>

<task type="auto">
  <name>Task 2: 组③ 镜像常量行抽取</name>
  <files>.planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(当前状态)
    - `git show 5927f36:apps/worker/src/soniscope_worker/nls.py`(RETRY_DELAYS_SECONDS :45 及最大尝试次数常量)
    - `git show 5927f36:apps/miniprogram/utils/uploader.js`(RETRY_DELAYS_MS :28-29、MAX_UPLOAD_RETRIES)
    - `git show 5927f36:apps/miniprogram/utils/verify.js`(VERIFY_RETRY_DELAYS_MS :16-17)
    - `git show 5927f36:apps/fc/shared/fc_shared/env.py`(DEFAULT_MAX_UPLOAD_BYTES :41)
    - `git show 5927f36:apps/miniprogram/config.js`(CHUNK_MAX_DURATION_SECONDS :22-23)
    - `git show 5927f36:apps/fc/shared/fc_shared/sts.py`(STS_MAX_DURATION_SECONDS :24-25)
  </read_first>
  <action>
    在组③ 节落格(本组是"两侧镜像"对照,列式仍用三列;不参与的列标 n/a + 理由):
    - 重试节奏 5/15/45 行:Worker `nls.py:45`(秒)/ 小程序 uploader.js:28-29(毫秒)与 verify.js:16-17(毫秒)— 注意 JS 有两份独立常量,格内两处行号都要列;FC 列 n/a(FC 无重试表)或按实态判。
    - 最多 3 次重试行:两侧最大尝试次数常量对照(nls.py 与 uploader.js 的对应常量,以 git show 实际为准)。
    - 上传大小上限 50MB 行:FC `env.py:41`(DEFAULT_MAX_UPLOAD_BYTES = 52428800);小程序侧是否存在对应常量/上传前预检用 `git grep -nE '52428800|MAX_UPLOAD' 5927f36 -- apps/miniprogram/` 核实,命中则落行号,未命中则此格判 absent(应参与未实现,覆盖洞候选)或 n/a,理由写明裁决依据;Worker 列按实态判 n/a。
    - 分片阈值 600s 行:小程序 `config.js:22-23`;FC/Worker 侧是否有感知用 `git grep -n '600' 5927f36 -- apps/fc/ apps/worker/src/` 结合语义人工筛选核实(数字 600 命中须人工排除无关值)。
    - STS 时长 ≤900s 行:FC `sts.py:24-25`;小程序对 expiration 的消费方式(uploader.js 对凭证过期字段的处理)落格。
    所有行号先经 git show 复核;grep 核实命令与结论写入行下注(为 Task 3 普查存档预铺)。
  </action>
  <verify>
    <automated>grep -q '52428800' .planning/audit/CONTRACT-MATRIX.md && grep -q 'CHUNK_MAX_DURATION_SECONDS' .planning/audit/CONTRACT-MATRIX.md && grep -q 'RETRY_DELAYS' .planning/audit/CONTRACT-MATRIX.md && grep -q 'STS_MAX_DURATION_SECONDS' .planning/audit/CONTRACT-MATRIX.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - 组③ 节含 5 行:重试节奏、最多 3 次、50MB 上限、600s 分片阈值、STS 900s
    - 重试节奏行同时引用 uploader.js 与 verify.js 两份 JS 常量的行号
    - 50MB 行的小程序格有明确 absent/n/a 裁决 + grep 核实依据
    - 4 个常量标识符(52428800/CHUNK_MAX_DURATION_SECONDS/RETRY_DELAYS/STS_MAX_DURATION_SECONDS)在矩阵中可 grep 命中
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>组③ 5 行落格,矩阵三组主体抽取完毕(合计约 35-45 行,落在 D-02 预估区间)</done>
</task>

<task type="auto">
  <name>Task 3: 重复逻辑普查——候选清单逐项核实 + 系统扫描存档(CONTRACT-03,D-13/D-14)</name>
  <files>.planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(当前状态)
    - .planning/phases/02-contract-extraction-drift/02-RESEARCH.md(『普查候选与扫描词』节:9 项候选定位表 + 5 条扫描命令原文)
    - .planning/audit/HYPOTHESES.md(HYP-03 sha256、HYP-13 契约三处重复——普查结论关联挂钩)
    - .planning/phases/02-contract-extraction-drift/02-PATTERNS.md(『机械对账/可复核完成判定模式』与『命令存档模式』——普查章节照此格式)
  </read_first>
  <action>
    在 `## 重复逻辑普查` 节完成 D-13 双保险,格式照 02-PATTERNS.md 的命令存档模式(fenced bash + 行内中文注释)与机械对账模式:
    ① 候选清单逐项核实(9 项,来自 D-13 + RESEARCH 定位表):sha256(utils/sha256.js vs Worker hashlib 用法 vs poller.py 比对逻辑,挂 HYP-03)、日期格式 YYYY-MM-DD(audio.js objectKeyDate vs sts.py/oss_admin.py 前缀拼接 vs poller.py date_of)、ULID/fragment_id 生成(utils/ulid.js 唯一生成端,FC/Worker 只解析不生成→n/a 素材)、错误码字符串(已入组②,此处只记指针)、重试表(已入组③,记指针)、大小上限(已入组③,记指针)、HMAC/OSS V4 签名(utils/hmac.js + utils/oss_sign.js,Worker 侧 SDK 内置→n/a 素材)、配置解析(Worker config.py pydantic vs 小程序 config.js 常量 vs FC env.py——三种机制解析同族值)、第四处 key 反推(upload_queue.js fragmentIdFromObjectKey,已入组① 行 5,记指针)。每项给出结论:是否为"承载契约的跨语言重复实现";是→拆矩阵新行入对应组做语义对照(D-14),否/已覆盖→写"已检查,无新发现"或指针行。
    ② 系统扫描:执行 02-RESEARCH.md『普查候选与扫描词』节的 5 条 `git grep -nE ... 5927f36 -- apps/` 命令(关键词组:key/meta/fragment 族、sha256 族、重试与大小数值族、日期格式函数族、错误码字符串族;可按勘察扩充,扩充词一并存档),命令原文 + 输出(或输出摘要 + 命中计数)照命令存档模式归档进普查节。命中分两栏处理(RESEARCH Pitfall 7):实现代码命中且承载契约 → 矩阵新行;`apps/miniprogram/test/`、`apps/worker/tests/`、`apps/fc/tests/` 命中 → 不占列,记为对应矩阵格的"常量被测试锁定"辅助证据(D-04)。
    ③ 债务移交:普查中"重复实现本身是否构成技术债"不在本维度判断——每个重复点写一行移交记录(标注"移交 Phase 3 CODE 维度",per D-14)。
    ④ 完成判定:普查节末尾写机械对账行——扫描命令条数、总命中数、进矩阵新行数、测试辅助证据数、"无新发现"项数,数字可复算(这是 CONTRACT-03 的可验收完成判定,用户明确不接受主观"查过了")。
  </action>
  <verify>
    <automated>[ "$(grep -c 'git grep' .planning/audit/CONTRACT-MATRIX.md)" -ge 5 ] && grep -q 'fragmentIdFromObjectKey' .planning/audit/CONTRACT-MATRIX.md && grep -q 'HYP-03' .planning/audit/CONTRACT-MATRIX.md && grep -q '移交 Phase 3' .planning/audit/CONTRACT-MATRIX.md && grep -q '无新发现\|已检查' .planning/audit/CONTRACT-MATRIX.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - 普查节含 9 项候选,每项有核实结论(新行/指针/已检查无新发现 三态之一)
    - ≥5 条 `git grep ... 5927f36` 扫描命令原文归档,每条附输出或命中计数
    - 测试目录命中未被立为矩阵列,以"测试锁定"括注形式挂到对应格
    - 每个重复实现点有"移交 Phase 3 CODE 维度"记录行
    - 普查节末尾有机械对账行(命令数/命中数/新行数/辅助证据数/无新发现数)
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>CONTRACT-03 普查完成且完成判定可复核;矩阵行集合封版,待 02-03 执行佐证与 02-04 判定</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 基线代码 → 审计文档 | git show/grep 摘录进 `.planning`,可能携带敏感字面量 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | 普查扫描输出存档 | high | mitigate | 扫描输出入档前过目,疑似秘密只记 path:line + 模式名(CHARTER 红线);错误码/常量为公开标识符,可整档 |
| T-02-02 | Tampering | apps/ scripts/ docs/ 基线 | medium | mitigate | 只用 git show/git grep 只读取证;每任务收尾零 diff 命令必须为空 |
| T-02-SC | Tampering | 包安装供应链 | low | accept | 零安装零新依赖,无安装面 |
</threat_model>

<verification>
- 矩阵三组主体行齐备(约 35-45 行),9 个错误码/reason 与 4 个常量标识符可 grep 命中
- 普查节:≥5 条扫描命令存档 + 机械对账行
- 零 diff:`git diff --stat 5927f36 -- apps/ scripts/ docs/` 空输出;`git status --porcelain` 仅见 `.planning/`
- 人工抽查:任取组②③ 各 2 格行号,git show 复核命中
</verification>

<success_criteria>
- CONTRACT-01 矩阵行抽取全量完成(三组 + 普查新行)
- CONTRACT-03 普查完成,结论(含"已检查,无新发现")与扫描命令可复核存档
- 仓库零污染
</success_criteria>

<output>
Create `.planning/phases/02-contract-extraction-drift/02-02-SUMMARY.md` when done
</output>
