---
phase: 02-contract-extraction-drift
plan: 4
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - .planning/audit/CONTRACT-MATRIX.md
  - .planning/audit/findings/contract.md
  - .planning/audit/CONTRACT-TEST-RECIPE.md
autonomous: true
requirements: [CONTRACT-02, CONTRACT-04]
must_haves:
  truths:
    - "矩阵每个 diverge/absent 格的判定列回填四类标签(良性/潜伏/活跃失配/覆盖洞)之一 + F-CON-NN 链接(D-12:矩阵只标类)"
    - "每条分歧无论类别均有一条 F-CON 发现(D-10),九字段齐全,Postel 生产者-消费者宽严分析写在证据/修复建议字段内,严重度按 良性→INFO/LOW、潜伏→MEDIUM 起、活跃失配→HIGH 起 映射"
    - "存在非良性分歧则 CONTRACT-TEST-RECIPE.md 成文(D-16 五要素);全为良性则矩阵显式记录'无需配方'(D-15)"
    - "收尾章节记录零 diff 验证命令输出(空)与机械对账等式(diverge/absent 格数 = F-CON 条数等)"
  artifacts:
    - ".planning/audit/CONTRACT-MATRIX.md"
    - ".planning/audit/findings/contract.md"
    - ".planning/audit/CONTRACT-TEST-RECIPE.md(条件产出)"
  key_links:
    - "矩阵 diverge/absent 格 ↔ F-CON 条目双向引用(D-09 证据与判断分离)"
    - "F-CON 关联字段挂 HYP-13(核心假设)及相关 HYP(sha256 行挂 HYP-03)"
    - "配方黄金样本集 = 02-03 样本清单(D-07 → D-16 复用链)"
---

<objective>
完成本阶段的判定与收尾:遍历矩阵全部分歧格做四类归类(良性/潜伏/活跃失配/覆盖洞)并回填判定列(D-12),每条分歧按 CHARTER 九字段 schema 写 F-CON 发现进 `findings/contract.md`(D-09/D-10,Postel 分析住发现);按 D-15 触发线条件产出 `CONTRACT-TEST-RECIPE.md`(D-16 深度)或显式记录"无需配方";最后跑零 diff 验证并写收尾对账章节。

Purpose: 把 02-01~03 收集的证据转化为可直接驱动修复里程碑的分级发现(成功判据 3/5),并以机械对账封版整个 Phase 2。
Output: 矩阵判定列 + F-CON 台账条目 + 条件配方 + 收尾章节。

**判定纪律:** 负面清单前置——DNF-01~04 已裁定故意设计不立 F-CON;chunk_total `null`↔`"0"`↔`None` 是文档化约定(字面异/语义同);不引入 `docs/fc-transcribe-design.md` 目标态对照;"diverge 指语义分歧"。严重度理由固定格式 `影响:…;可能性:…`,禁任何数值评分;工作量只用 S/M/L/XL(跨三处同步的契约变更 = L 档 CHARTER 明文示例)。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/audit/CHARTER.md
@.planning/audit/DO-NOT-FIX.md
@.planning/audit/HYPOTHESES.md
@.planning/phases/02-contract-extraction-drift/02-CONTEXT.md
@.planning/phases/02-contract-extraction-drift/02-RESEARCH.md
@.planning/phases/02-contract-extraction-drift/02-PATTERNS.md
@.planning/phases/02-contract-extraction-drift/02-03-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Plan | 说明 |
|----------|------|------|
| `.planning/audit/CONTRACT-MATRIX.md` | 02-01 新建;02-02/03/04 追加 | 漂移矩阵 + 往返校验记录 + 样本清单附录 + 普查章节 + 零 diff 收尾记录 |
| `.planning/audit/findings/contract.md` | 02-04 追加 | F-CON-NN 发现条目(九字段 schema) |
| `.planning/audit/CONTRACT-TEST-RECIPE.md` | 02-04 条件新建(D-15) | 黄金样本跨语言契约测试设计配方(仅设计不实现) |
| scratchpad harness | 02-03 临时物 | 不入仓库,不提交 |

<tasks>

<task type="auto">
  <name>Task 1: 四类分歧判定回填矩阵 + F-CON 发现写入台账</name>
  <files>.planning/audit/CONTRACT-MATRIX.md, .planning/audit/findings/contract.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(02-03 后全量:三组矩阵行、普查节、往返校验结论与样本销号)
    - .planning/audit/findings/contract.md(F-CON-00 schema 示例与 `## 发现` 追加位置——示例保留不动)
    - .planning/audit/CHARTER.md(九字段 schema、严重度锚点表、S/M/L/XL 判定标准、评级理由格式)
    - .planning/audit/DO-NOT-FIX.md(DNF-01~04 负面清单——判定前置排除)
    - .planning/audit/HYPOTHESES.md(HYP-13 :124-129、HYP-03 :36-41——关联字段挂钩)
    - .planning/phases/02-contract-extraction-drift/02-PATTERNS.md(『本阶段特有的字段填法』与证据引用文风)
  </read_first>
  <action>
    ① 遍历矩阵全部状态为 diverge 或 absent 的格/行,先过负面清单(DNF-01~04、chunk_total 文档化约定、目标态排除——命中者在判定列标"负面清单排除 + DNF-NN"不立发现),其余逐条归入四类之一:良性(语义一致或无现实触发路径的字面/结构差异)、潜伏(当前参数/格式下不触发,变更即爆)、活跃失配(当前输入域内即产生行为分叉)、覆盖洞(应参与而未实现,从 absent 格直接读出,per D-03)。归类依据 = 静态行号对照(判据)+ 02-03 执行佐证(佐证)。
    ② 每条分歧(含良性,per D-10)在 `findings/contract.md` 的 `## 发现` 标题下追加一条 F-CON 发现,ID 从 `F-CON-01` 顺序编号,F-CON-00 示例保留。九字段逐一填写(CHARTER schema 固定顺序):严重度映射按 D-10(良性→INFO/LOW;潜伏→MEDIUM 起;活跃失配→HIGH 起——"活跃失配使上传对 Worker 永久不可见"是 CHARTER HIGH 明文锚点;覆盖洞按影响×可能性对号最接近锚点),理由固定格式 `影响:…;可能性:…`;证据字段 = `path:line @ 5927f36` + git show 摘录片段 + 反向引用矩阵行(组号+要素名,D-09)+ 完整 Postel 生产者-消费者宽严分析(谁严谁宽、失配方向、触发条件,D-12——分析住发现不住矩阵);修复建议一段可直接驱动修复里程碑;工作量 S/M/L/XL(跨 FC+Worker+小程序三处同步 = L);关联发现字段挂 HYP-13(全部契约行核心假设)、sha256 相关挂 HYP-03、普查移交线索注明 Phase 3;上线判定留空(Phase 5 填);状态 draft。
    ③ 矩阵判定列回填:每个已判格写四类标签 + `F-CON-NN` 链接(或"负面清单排除 + DNF-NN");agree 格判定列统一改为 `—`(无分歧无发现,行号证据即"已检查无发现"支撑,D-11)。
    ④ 顺带发现处置:判定过程中若撞见安全类问题,照常九字段进台账并加 `顺带发现(out-of-dimension)` 标注,不自动升级严重度(CHARTER 条款);任何发现只进台账,不改代码不动云端(无例外协议)。
  </action>
  <verify>
    <automated>[ "$(grep -c '^### F-CON-' .planning/audit/findings/contract.md)" -ge 1 ] && grep -q 'HYP-13' .planning/audit/findings/contract.md && grep -q 'Postel\|宽严' .planning/audit/findings/contract.md && grep -q 'F-CON-00' .planning/audit/findings/contract.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - 矩阵中不存在判定列仍为"待判定"的行(全部回填四类标签+F-CON 链接、负面清单排除注、或 agree 的 `—`)
    - `findings/contract.md` 中每条 F-CON 九字段齐全且顺序符合 CHARTER;严重度行含 `影响:` 与 `可能性:` 两段;无数值评分
    - 每条 F-CON 证据字段含矩阵行反向引用与 Postel 宽严分析(谁严谁宽、失配方向、触发条件三要素)
    - 至少一条发现关联 HYP-13;F-CON-00 示例未被删改
    - DNF-01~04 相关对照点未立 F-CON(判定列有排除注)
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>全部分歧完成四类判定并成 F-CON 发现,矩阵与台账双向可追溯</done>
</task>

<task type="auto">
  <name>Task 2: 条件产出黄金样本契约测试设计配方(或显式记录"无需配方")</name>
  <files>.planning/audit/CONTRACT-TEST-RECIPE.md, .planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/findings/contract.md(Task 1 后:四类分布——触发线裁决输入)
    - .planning/audit/CONTRACT-MATRIX.md(样本清单附录——配方黄金样本集来源;矩阵行——配方覆盖要素引用)
    - .planning/phases/02-contract-extraction-drift/02-PATTERNS.md(『CONTRACT-TEST-RECIPE.md』节:pytest→node 桥接模式全文摘录、node:test 骨架与黄金样本常量形态、配方文档结构)
    - `git show 5927f36:apps/worker/tests/test_miniprogram_js.py`(桥接先例——make 接入点设计蓝本)
    - `git show 5927f36:apps/miniprogram/test/oss_sign.test.js`(node:test 骨架与样本常量文风)
  </read_first>
  <action>
    ① 触发线裁决(D-15):检视 Task 1 四类分布——存在任一 潜伏/活跃失配/覆盖洞 即触发配方;仅有良性(或零分歧)不触发。裁决结论一句写进矩阵收尾章节。
    ② 若触发:用 Write 新建 `.planning/audit/CONTRACT-TEST-RECIPE.md`(文件头三行模式同矩阵:标题 + `**Created:**` + 基线引用行),内容按 D-16 五要素成节,深度须达"修复里程碑拿到即可写代码,不用再设计":
    - 黄金样本文件 schema 与存放位置:JSON 单一真值源,字段形态沿用 oss_sign.test.js 的 CRED/META 既有键名;给出建议仓库路径(纸面建议,本阶段不落文件,不违零 diff)。
    - 覆盖的契约要素:逐条引用矩阵行(组号+要素名+触发它的 F-CON ID),只覆盖非良性分歧涉及的要素 + 往返主链。
    - pytest 侧测试骨架伪代码:照 test_miniprogram_js.py 结构(subprocess 起 node、node 缺席 skip、显式文件清单),增加"读共享黄金样本 JSON → 逐样本断言 object_key_for/fragment_id_from_key 往返等式"一步。
    - node:test 侧测试骨架伪代码:照 oss_sign.test.js 文风(zero-dep CommonJS require、中文测试名、模块级样本常量改为读同一 JSON)。
    - make 接入点与验收标准:接入 `make test` 单一质量门的方式(经 pytest 桥接先例);验收标准 = 黄金样本全过 + 任一侧实现改动导致样本失配时测试失败。
    - 样本值:直接复用矩阵附录 S-NN 清单(D-07→D-16 复用链),逐样本注明来源 ID。
    ③ 若不触发:不建配方文件,在矩阵收尾章节显式记录一行"无需配方"及依据(全部分歧均为良性/零分歧,引用 F-CON 严重度分布),满足 CONTRACT-04 else 分支。
  </action>
  <verify>
    <automated>{ test -f .planning/audit/CONTRACT-TEST-RECIPE.md && grep -q 'S-0' .planning/audit/CONTRACT-TEST-RECIPE.md && grep -q 'make test' .planning/audit/CONTRACT-TEST-RECIPE.md; } || grep -q '无需配方' .planning/audit/CONTRACT-MATRIX.md</automated>
  </verify>
  <acceptance_criteria>
    - 二选一成立:CONTRACT-TEST-RECIPE.md 存在且含 D-16 五要素各自成节(黄金样本 schema 与位置/覆盖要素引矩阵行与 F-CON/pytest 骨架/node:test 骨架/make 接入点与验收标准),样本引用 S-NN ID;或矩阵收尾章节有"无需配方"显式行 + 依据
    - 若配方产出:pytest 骨架含 node 缺席 skip 与显式文件清单两个先例要点;覆盖要素每条带矩阵行引用与 F-CON ID
    - 配方为纯设计文档,未在 apps/ 落任何测试文件(零 diff 保障)
  </acceptance_criteria>
  <done>CONTRACT-04 完成:配方成文可直接开工,或"无需配方"有据可查</done>
</task>

<task type="auto">
  <name>Task 3: 阶段收尾——零 diff 验证记录与机械对账封版</name>
  <files>.planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(全量终稿前状态)
    - .planning/audit/findings/contract.md(F-CON 最终条数)
    - .planning/phases/02-contract-extraction-drift/02-PATTERNS.md(『机械对账』模式与『文档尾模式』——照抄格式)
  </read_first>
  <action>
    在 `## 收尾:零 diff 验证与对账` 节完成(格式照 HYPOTHESES.md 对账先例):
    ① 跑 `git diff --stat 5927f36 -- apps/ scripts/ docs/` 并把命令原文 + 实际输出(期望空)记录在案(CHARTER D-03 每阶段必跑必记);同时记录 `git status --porcelain` 检查结论(仅 .planning/ 变更)。
    ② 机械对账等式(每条给出可复算的计数命令 + 数字):矩阵总行数;`grep -o '@ 5927f36' .planning/audit/CONTRACT-MATRIX.md | wc -l` 证据引用总数;diverge/absent 判定格数 vs `grep -c '^### F-CON-' .planning/audit/findings/contract.md` 减 1(扣除 F-CON-00 示例)的对账等式(负面清单排除格单列计数);样本表行数 vs 销号数(应相等);普查候选 9 项 vs 结论数。任一等式不平须修复后再封版。
    ③ 成功判据自查清单:对照 ROADMAP Phase 2 的 5 条成功判据逐条打勾并注对应章节/文件。
    ④ 文档尾:`---` + 一行斜体摘要(文档名 + 日期 + 关键计数:矩阵行数、F-CON 条数、样本数、配方产出与否),照 Phase 1 产物文档尾模式。
  </action>
  <verify>
    <automated>grep -q '零 diff' .planning/audit/CONTRACT-MATRIX.md && grep -q '对账' .planning/audit/CONTRACT-MATRIX.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ] && [ -z "$(git status --porcelain | grep -v '.planning/')" ] && tail -3 .planning/audit/CONTRACT-MATRIX.md | grep -q '^\*'</automated>
  </verify>
  <acceptance_criteria>
    - 收尾章节含零 diff 命令原文与实际输出记录,且实际执行输出为空
    - 机械对账等式全部给出计数命令与数字且相互平衡(diverge/absent 格数 = F-CON 条数 - 1 - 负面清单排除数,以章节内实际等式为准)
    - 5 条成功判据逐条有对应章节指针
    - 文档尾有一行斜体摘要(含矩阵行数、F-CON 条数、样本数、配方产出与否)
  </acceptance_criteria>
  <done>Phase 2 产物封版:证据、判定、配方、零 diff 记录全部在案且可机械复核</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 基线代码 → 审计文档 | F-CON 证据摘录与配方伪代码进 `.planning` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Information Disclosure | F-CON 证据片段 / 配方样本 | high | mitigate | CHARTER 秘密红线:疑似秘密只写 path:line + 模式名;配方样本全部为合成值 |
| T-02-02 | Tampering | apps/ scripts/ docs/ 基线 | medium | mitigate | 配方仅纸面设计不落测试文件;收尾零 diff 命令输出记录在案 |
| T-02-05 | Repudiation | 判定不可复核 | medium | mitigate | D-11 agree 也带行号、机械对账等式、四类判定均链接 F-CON——判定全程留痕 |
| T-02-SC | Tampering | 包安装供应链 | low | accept | 零安装零新依赖,无安装面 |
</threat_model>

<verification>
- 矩阵无"待判定"残留;每条 diverge/absent 有 F-CON 链接;F-CON 九字段齐全
- 配方存在(含五要素)或"无需配方"显式记录,二者恰一
- 收尾章节零 diff 记录 + 对账等式平衡;`git status --porcelain` 仅 `.planning/`
- 人工抽查:任取 2 条 F-CON,复核证据行号命中且 Postel 分析含三要素
</verification>

<success_criteria>
- ROADMAP Phase 2 五条成功判据全部为 TRUE(矩阵、往返结论、四类判定+Postel、普查记录、配方/无需配方)
- CONTRACT-02/CONTRACT-04 完成;发现台账可直接喂 Phase 5 汇总
- 仓库零污染且有记录
</success_criteria>

<output>
Create `.planning/phases/02-contract-extraction-drift/02-04-SUMMARY.md` when done
</output>
