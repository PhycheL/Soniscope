---
phase: 02-contract-extraction-drift
plan: 3
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - .planning/audit/CONTRACT-MATRIX.md
autonomous: true
requirements: [CONTRACT-02]
must_haves:
  truths:
    - "样本清单附录写定:每个样本有 ID(S-NN)、值、类别、三处各自的预期行为;条目覆盖 D-07 全部类别(典型值、非法日期、闰日/跨年、跨时区、近午夜、deviceShortId 边界、ULID 边界、非 .wav key、chunk 后缀、空/畸形)"
    - "往返校验结论在案:FC object_key_for 签发的 key 能否被 Worker fragment_id_from_key 解析有逐样本明确记录(预期/实测/销号三元组)"
    - "执行佐证跑在基线导出树上(git archive 5927f36 → scratchpad),python 侧有 __file__ 来源断言,JS 时区敏感样本显式 TZ 且 TZ 记入佐证记录"
    - "执行结果只作佐证记入矩阵,不替代静态判据(D-05)"
  artifacts:
    - ".planning/audit/CONTRACT-MATRIX.md"
  key_links:
    - "样本清单 ↔ CONTRACT-TEST-RECIPE.md 黄金样本集复用链(D-07 → D-16)"
    - "可疑矩阵格(JS 无日期校验、第四处反推、object_key AC#4)← 执行佐证回填格内注"
---

<objective>
完成 CONTRACT-02 的往返校验执行佐证:先写定 D-07 样本清单(典型 + 边界,含预期行为),再按 D-06 把基线代码 `git archive 5927f36` 导出到 scratchpad,用仓库 `.venv` python 与系统 node 执行三处纯函数跑样本,逐项销号,产出往返校验结论章节。零云 IO(D-08:"FC 签发的 key"即本地执行 `fc_shared/sts.py::object_key_for`);执行只作佐证,静态对照仍是判据(D-05)。

Purpose: 用可复跑的执行证据回答成功判据 2("FC 签发的 object key 能否被 Worker `fragment_id_from_key` 解析"),并为可疑格子(JS 正则无日期校验、第四处无校验反推、跨时区日期错位)提供行为级佐证,喂 02-04 的四类判定。
Output: CONTRACT-MATRIX.md 的『往返校验结论』与『附录:往返校验样本清单』两节 + 相关矩阵格的佐证注。

**红线:** harness 只调用纯函数,样本全为合成数据(不含真实 openid/凭证);harness 脚本只写入 scratchpad,严禁落仓库(收尾零 diff + git status 双保险);佐证记录不复制任何疑似秘密值。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/audit/CHARTER.md
@.planning/phases/02-contract-extraction-drift/02-CONTEXT.md
@.planning/phases/02-contract-extraction-drift/02-RESEARCH.md
@.planning/phases/02-contract-extraction-drift/02-02-SUMMARY.md
</context>

## Artifacts this phase produces

| Artifact | Plan | 说明 |
|----------|------|------|
| `.planning/audit/CONTRACT-MATRIX.md` | 02-01 新建;02-02/03/04 追加 | 漂移矩阵 + 往返校验记录 + 样本清单附录 + 普查章节 + 零 diff 收尾记录 |
| `.planning/audit/findings/contract.md` | 02-04 追加 | F-CON-NN 发现条目(九字段 schema) |
| `.planning/audit/CONTRACT-TEST-RECIPE.md` | 02-04 条件新建(D-15) | 黄金样本跨语言契约测试设计配方(仅设计不实现) |
| scratchpad harness(`harness.py`/`harness.js` + 基线导出树) | 02-03 临时物 | 不入仓库,不提交 |

<tasks>

<task type="auto">
  <name>Task 1: 写定样本清单附录 + 基线导出 + python 侧往返校验(FC 签发 → Worker 解析)</name>
  <files>.planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(02-02 后的当前状态,尤其组① 各行的静态结论——样本预期须与之对表)
    - .planning/phases/02-contract-extraction-drift/02-RESEARCH.md(『D-07 样本清单素材』表——样本候选下限;『Code Examples』——导出与 harness 命令实测配方;『Common Pitfalls』1/2;Assumptions A1/A2)
    - `git show 5927f36:apps/miniprogram/utils/chunking.js`(chunk 后缀样本的 fragment_id 实际形态——RESEARCH Open Question 3,以实际产出定样本值)
    - `git show 5927f36:apps/fc/shared/fc_shared/__init__.py`(A2 冒烟 import 风险面)
  </read_first>
  <action>
    ① 在 `## 附录:往返校验样本清单` 节写定样本表,列式 `| ID | 样本值 | 类别 | 预期(FC) | 预期(Worker) | 预期(小程序) | 实测 | 销号 |`,样本 ID 用 `S-01`、`S-02`… 编号(行首格式固定为 `| S-`,供机械计数)。条目覆盖 RESEARCH『D-07 样本清单素材』全部 11 个类别(该表为下限,Claude's discretion 可加):典型值(仓库测试通用形态 fragment_id)、非法日期但正则可过(13 月 32 日)、闰日、跨年边界、跨时区(同一 recordedAt 在 Asia/Shanghai 与 America/New_York)、近午夜、deviceShortId 过短/过长/非法字符、ULID 25/27 字符与 Crockford 排除字符、非 .wav key、chunk 后缀(值以 chunking.js 实际产出为准)、空串/无分隔符/目录日期与前缀日期不一致的 key。每个样本的三处预期行为先从矩阵静态结论推导写死,再执行验证(先预期后实测,防结果倒灌预期)。
    ② 基线导出(D-06):在会话 scratchpad 建 `phase2-baseline` 目录,`git archive 5927f36 apps/worker/src apps/fc/shared apps/miniprogram/utils apps/miniprogram/config.js | tar -x -C <scratchpad>/phase2-baseline`(整树导出保 import/require 结构;禁止手抄函数体——手抄即篡改证据链)。
    ③ 冒烟 import(RESEARCH A2):先以 PYTHONPATH 指向导出树,用仓库 `.venv/bin/python` 冒烟 `import fc_shared` 与 `import soniscope_worker.poller`,确认 import 成功且无网络触发;任何 import 失败原样记录。
    ④ python harness(用 Write 写入 scratchpad,不落仓库):首部断言 `soniscope_worker.poller.__file__` 与 `fc_shared.sts.__file__` 以 scratchpad 前缀开头,断言失败即中止(Pitfall 1 兜底);随后逐样本执行:FC 路径 `fc_shared.sts.object_key_for(样本)` → Worker 路径 `soniscope_worker.poller.fragment_id_from_key(所得 key)` 往返等式,另执行 Worker `oss_admin.object_key_for(样本)` 与 FC 产出 key 对照;异常(如日期非法被拒)按类型与消息记录。运行命令形态:PYTHONPATH 前置导出树的 `apps/worker/src` 与 `apps/fc/shared` 两个目录,解释器用 `/Volumes/Data/ProjectCode/my_soniscope/.venv/bin/python`(RESEARCH 实测:poller import 链需 pydantic/yaml,必须 .venv)。
    ⑤ 每个 python 侧样本的实测结果回填样本表『实测』列并销号;`## 往返校验结论` 节先落 python 侧小结(FC 签发 key → Worker 解析:逐样本 可解析/被拒 及原因)。佐证记录注明"执行结果为佐证,判据以静态行号对照为准(D-05)"。
  </action>
  <verify>
    <automated>[ "$(grep -c '^| S-' .planning/audit/CONTRACT-MATRIX.md)" -ge 11 ] && grep -q 'fragment_id_from_key' .planning/audit/CONTRACT-MATRIX.md && grep -q '## 往返校验结论' .planning/audit/CONTRACT-MATRIX.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ] && [ -z "$(git status --porcelain | grep -v '.planning/')" ]</automated>
  </verify>
  <acceptance_criteria>
    - 样本表 ≥ 11 行(`grep -c '^| S-'` ≥ 11),每行预期三格在实测前已写定
    - python 侧样本的『实测』列全部回填,无空置;异常样本记录了异常类型
    - 往返校验结论节含 python 侧小结:每个样本"FC 签发 → Worker 可解析/被拒"的明确记录
    - harness 与导出树只存在于 scratchpad:`git status --porcelain` 无 `.planning/` 之外的路径
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>样本清单写定并完成 python 侧(FC↔Worker)往返执行销号,CONTRACT-02 主链有逐样本记录</done>
</task>

<task type="auto">
  <name>Task 2: node 侧执行佐证(小程序声部,含跨时区双跑)+ 往返校验结论成文</name>
  <files>.planning/audit/CONTRACT-MATRIX.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(Task 1 后状态:样本表与 python 侧小结)
    - .planning/phases/02-contract-extraction-drift/02-RESEARCH.md(『Code Examples』node harness 配方;Pitfall 2 时区污染)
    - scratchpad 导出树中的 `apps/miniprogram/utils/audio.js`、`apps/miniprogram/utils/upload_queue.js`、`apps/miniprogram/utils/ulid.js`、`apps/miniprogram/utils/chunking.js`(确认待调用纯函数的实际导出名与签名——以导出树为准)
  </read_first>
  <action>
    ① node harness(Write 写入 scratchpad):require 导出树内 utils 模块(导出保真了 `audio.js:10` 对 `../config` 的相对 require),逐样本执行小程序声部纯函数——`buildObjectKeyPreview`(key 生成预览)、fragment_id 构造函数(以 audio.js/ulid.js 实际导出名为准)、`upload_queue.js` 的 `fragmentIdFromObjectKey`(第四处反推)——并与 python 侧产出对照。
    ② 时区敏感样本(跨时区、近午夜、跨年)以 `TZ=Asia/Shanghai node <harness>` 与 `TZ=America/New_York node <harness>` 双跑(Pitfall 2),每条佐证记录必须写明 TZ;矩阵附录注明"JS 执行佐证反映指定 TZ 下的行为"。
    ③ 高价值对照点逐一记录:(a) 非法日期样本——FC/Worker 的 datetime 校验拒绝 vs JS 正则放行的实际行为;(b) 非 .wav key——`fragment_id_from_key` endswith 拒绝 vs `fragmentIdFromObjectKey` 照单全收;(c) 目录日期与前缀日期不一致的 key——Worker 往返等式是否拒绝、第四处是否放行;(d) 跨时区下 fragment_id 前缀日期与 key 目录日期是否错位。
    ④ 回填:JS 声部实测进样本表『实测』列并销号(全表销号完毕);相关矩阵格(组① 行 2/4/5 等)加执行佐证括注(佐证不改静态判据的状态词,per D-05——若执行结果与静态判定矛盾,原样记录矛盾供 02-04 裁决)。
    ⑤ `## 往返校验结论` 节成文:总结论(FC 签发的 key 在何种样本域内可被 Worker 解析、在何处分叉)+ 逐样本记录指针 + harness 复跑说明(导出命令、PYTHONPATH/TZ 运行形态、node 版本与 python 解释器路径),使记录满足 02-VALIDATION.md 的"harness 命令可重放"要求。
  </action>
  <verify>
    <automated>grep -q 'TZ=Asia/Shanghai' .planning/audit/CONTRACT-MATRIX.md && grep -q 'TZ=America/New_York' .planning/audit/CONTRACT-MATRIX.md && grep -q 'fragmentIdFromObjectKey' .planning/audit/CONTRACT-MATRIX.md && grep -q 'buildObjectKeyPreview' .planning/audit/CONTRACT-MATRIX.md && [ -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" ] && [ -z "$(git status --porcelain | grep -v '.planning/')" ]</automated>
  </verify>
  <acceptance_criteria>
    - 样本表全部行销号(『销号』列无空置)
    - 每条 JS 时区敏感佐证记录含显式 TZ 值;矩阵附录含 TZ 声明句
    - 往返校验结论节含总结论 + harness 复跑说明(命令形态、解释器路径、node 版本)
    - 高价值对照点 (a)-(d) 各有一条实测记录
    - 仓库零污染(零 diff 为空,status 仅 .planning/)
  </acceptance_criteria>
  <done>CONTRACT-02 往返校验结论在案且可复跑;样本清单成为 D-16 配方的现成黄金样本集胚胎</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 基线代码 → scratchpad 执行 | 导出的基线代码在本机以 .venv python / node 执行 |
| 执行输出 → 审计文档 | harness 输出转录进 `.planning` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03 | Denial of Service / 意外云 IO | harness import 链(fc_shared __init__ 全量导入) | medium | mitigate | D-08 零云 IO:只调用纯函数;A2 冒烟 import 先行;云 SDK 均 lazy import + .venv 兜底,不调用任何网络函数 |
| T-02-04 | Tampering | 仓库工作区被 harness 污染 | medium | mitigate | D-06:harness 与导出树只写 scratchpad;收尾 `git status --porcelain` 仅允许 .planning/ |
| T-02-01 | Information Disclosure | 佐证记录携带敏感值 | high | mitigate | 样本全为合成数据;记录只含合成样本与函数输出,不含真实 openid/凭证 |
| T-02-SC | Tampering | 包安装供应链 | low | accept | 零安装;解释器为仓库既有 .venv 与系统 node |
</threat_model>

<verification>
- 样本表 ≥ 11 行且全销号;往返校验结论节成文并含复跑说明
- 佐证记录含显式 TZ;来源断言(__file__ 前缀 scratchpad)在 harness 中存在并通过
- 零 diff:`git diff --stat 5927f36 -- apps/ scripts/ docs/` 空;`git status --porcelain` 仅 `.planning/`
</verification>

<success_criteria>
- CONTRACT-02 往返校验有逐样本"预期/实测/销号"记录与明确总结论
- 执行佐证全部跑在基线导出树上,可复跑,零云 IO,零仓库污染
- 可疑矩阵格获得行为级佐证注,供 02-04 四类判定使用
</success_criteria>

<output>
Create `.planning/phases/02-contract-extraction-drift/02-03-SUMMARY.md` when done
</output>
