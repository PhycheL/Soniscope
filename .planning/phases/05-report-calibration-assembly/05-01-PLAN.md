---
phase: 05-report-calibration-assembly
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/CALIBRATION.md
autonomous: false
requirements: [RPT-02, RPT-03, RPT-04]
must_haves:
  truths:
    - "CALIBRATION.md 存在并承载全部校准/并入/聚类/工作包/判定裁定;findings/*.md 相对基线零改动(只读封版)"
    - "每条调整含 ID、原值→终值、理由、CHARTER 锚点依据与批准记录"
    - "40 条真实发现每条有 BLOCKER/PRE-LAUNCH/POST-LAUNCH 三态之一的判定 + 准则条款引用 + 一句理由"
    - "用户已通过 checkpoint 对拟调整清单、真重复并入、判定准则与判定抽样作出批复,批复结果落账"
  artifacts:
    - path: ".planning/audit/CALIBRATION.md"
      provides: "校准台账:CAL-NN 调整条目、CL-NN 聚类划分、WP-NN 工作包划分与包级工作量档、判定准则全文、40 行逐条判定表、呈报与批准记录"
  key_links:
    - from: ".planning/audit/CALIBRATION.md"
      to: ".planning/audit/findings/contract.md"
      via: "逐条判定表与 CAL 条目引用 F-XX-NN ID,findings 封版只读不回写"
      pattern: "F-(CON|CODE|TOOL|DOC|TEST)-[0-9]{2}"
    - from: ".planning/audit/CALIBRATION.md"
      to: ".planning/audit/CHARTER.md"
      via: "每条调整的锚点依据指向 CHARTER 严重度锚点表或 S/M/L/XL 分档行"
      pattern: "CHARTER"
---

<objective>
完成本阶段仅有的两类被授权新判断并经用户批准落账:①跨维度严重度/工作量对齐校准(D-01/D-04)与真重复并入判定(D-08);②上线判定准则定稿与 40 条逐条判定(D-09/D-10/D-11/D-12)。同时完成根因聚类划分(D-05/D-06)与修复工作包划分 + 包级工作量重估(D-04)。全部裁定落入新建 `.planning/audit/CALIBRATION.md`(D-03),findings/*.md 封版不动。

Purpose: 后续报告组装(05-02/05-03)必须零新判断——本 plan 把所有判断前置、批准、落账,使每个报告表行字段有唯一确定来源。
Output: `.planning/audit/CALIBRATION.md`(终稿,含批准记录)。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-report-calibration-assembly/05-CONTEXT.md
@.planning/phases/05-report-calibration-assembly/05-RESEARCH.md
@.planning/phases/05-report-calibration-assembly/05-PATTERNS.md
</context>

## Artifacts this phase produces

本 phase(全部 3 个 plan)新建下列产物;plan-review 源锚定应将这些新符号排除出漂移核查:

- `.planning/audit/CALIBRATION.md` — 校准台账(本 plan 创建)
- `.planning/audit/REPORT.md` — 最终审计报告主文件(05-02 创建)
- `.planning/audit/REPORT-APPENDIX-A-traceability.md` — RPT-08 追溯映射附录(05-03 创建)
- `.planning/audit/REPORT-APPENDIX-B-clusters.md` — 聚类明细附录(05-03 创建)
- 新 ID 约定:`CAL-NN`(校准/并入调整条目)、`CL-NN`(根因聚类)、`WP-NN`(修复工作包)
- 新判定词表:`BLOCKER` / `PRE-LAUNCH` / `POST-LAUNCH`(逐条),`GO` / `CONDITIONAL GO` / `NO-GO`(总判定);准则条款编号 `B-1~B-3`、`P-1~P-3`、`PL-1`
- REPORT.md 固定章节锚:`## 方法声明`、`## 执行摘要`、`### 总体上线判定`、`### 存在级观察与范围外事项`、`## 上线判定准则`、`## 发现汇总表`、`## 修复工作包`、`## Do-NOT-fix 登记表`、`## 优点盘点`、`## 分维度置信声明`(五个子节 `### 置信·CON/CODE/TOOL/DOC/TEST`)、`## 修复里程碑移交物`、`## 附录索引`、`## 收尾验证`

<tasks>

<task type="auto">
  <name>Task 1: 逐条抽取 + 跨维度对齐扫描 + 聚类/工作包/判定草案,写出 CALIBRATION.md 呈报草稿</name>
  <files>.planning/audit/CALIBRATION.md</files>
  <read_first>
    - .planning/audit/findings/contract.md
    - .planning/audit/findings/code.md
    - .planning/audit/findings/toolchain.md
    - .planning/audit/findings/docs-config.md
    - .planning/audit/findings/test.md
    - .planning/audit/CHARTER.md(严重度锚点表约 :110-116、S/M/L/XL 分档表约 :124-131 —— 校准的唯一裁定基准,per D-01)
    - .planning/audit/DO-NOT-FIX.md(文件头/条目五字段格式类比)
    - .planning/audit/TEST-AUDIT.md(反向映射清单与成员归属等式 —— 聚类成员关系实证)
    - .planning/phases/05-report-calibration-assembly/05-PATTERNS.md(CALIBRATION.md 章节的字段模板)
  </read_first>
  <action>
    新建 `.planning/audit/CALIBRATION.md`,按 PATTERNS 文件头三件套(标题 + Created + 基线 `5927f36` 引用 + 自述段)开头;自述写明:依据 D-01~D-04/D-08 记录跨维度对齐调级、真重复并入与工作量重估,findings/*.md 封版不动、本文件是唯一调整记录载体(D-03),并注记"CHARTER 九字段 schema 字段 8/9(上线判定槽、draft→calibrated)的台账回填预期由本文件承载,D-03 压过 schema 字面预期"。

    第一步(抽取):以 `^### F-` 标题为唯一锚点(勿依赖上线判定槽文本——存在带注记 27 条/裸槽 18 条两种格式,RESEARCH Pitfall 1)逐条抽取 40 条真实发现(剔除 5 条 F-*-00 schema 示例)的 ID/维度/严重度/工作量/标题/修复建议位置/关联发现,现场 grep 复核严重度与工作量分布(RESEARCH 实测基准:MEDIUM 11 / LOW 26 / INFO 3;S 32 / M 7 / L 1 / XL 0;勿抄 CONTEXT.md 的"MEDIUM 19/LOW 32/INFO 5"笔误,RESEARCH Pitfall 2)。

    第二步(D-01 跨维度对齐扫描):按主题横向比对同类问题在不同维度的定级,仅对"同类问题不同级"条目拟调整到统一 CHARTER 锚点;单条原定级默认信任,不做全量复核。工作量 S/M/L/XL 同法(D-04)。每条拟调整写为 `### CAL-NN: <F-ID> <原值> → <终值>` 条目,五字段:调整类型(严重度对齐/工作量对齐/真重复并入)、原值→终值、理由(引同类条目 ID 对照)、锚点依据(指向 CHARTER 锚点表或分档表具体行)、批准记录(草稿阶段统一写"待 D-02 批量呈报")。零拟调整也是合法结果,此时该节写显式"扫描完成,无跨维度不一致"记录。

    第三步(D-08 真重复判定):候选起点为 RESEARCH 列出的重叠群;判定标准 = "两条 ID 实质描述同一缺陷且修复建议字段为同一修复动作"。注意 Pitfall 6:F-TEST-05/06/07 是"缺测试锁定"元发现,与其成员条目修复动作不同(补测试 vs 修代码),不是真重复,归聚类互指。真重复成立时选证据更完整者为主条,并入判定同样写为 CAL-NN 条目。

    第四步(D-05/D-06 聚类划分):新建 `## 根因聚类划分` 节,每簇 `### CL-NN: <根因一句话>`,字段 = 根因陈述 / 成员 F-ID 清单(引用不合并不退役)/ 证据锚(引 TEST-AUDIT 成员归属等式行、findings 关联发现字段等既有行)。起点四群:重试常量四落点(F-CODE-07 × F-TEST-05 × F-TOOL-08)、key 反推无校验(F-CON-03 × 相关条)、镜像常量注释同步(F-CON-05/06 × F-TOOL-08 × F-TEST-05)、静默失败面(F-CODE-02/03 × F-TEST-06)。预期 4~7 簇;无共同根因的孤条不强行入簇。

    第五步(D-04/D-06 工作包划分):新建 `## 修复工作包划分` 节,每包 `### WP-NN: <共同修复位置一句话>`,字段 = 成员 F-ID / 共同修复位置(取自各条修复建议字段)/ 包级工作量档(整体重估一个 S/M/L/XL 档,包内共修一处时总量可小于各条之和,注明重估理由)/ 依赖(包间先后关系)。INFO 与并入副条不进工作包(D-07/D-08)。包级重估并入 D-02 同批呈报。

    第六步(D-09/D-10 判定准则定稿 + 逐条套用):新建 `## 上线判定准则` 节,写入三级准则条款(条款编号供逐条引用):
    B-1 用户录音数据丢失或不可恢复(上线即有现实触发路径);B-2 秘密/凭证泄漏(超出 DNF-04 已裁定的受限爆炸半径);B-3 主链路(录音→上传→转写产出)对全部用户不可用 —— 命中任一为 BLOCKER。
    P-1 用户可感知的卡死态/无提示失败且非作者用户无法自救(D-10 语境:邀请制小范围真实用户,不会重录、不看日志;此类权重上调,如 F-CODE-06 uploading 死态);P-2 静默失败不可发现,排障需读代码或云端日志;P-3 运维者无法从日志/工件判断数据是否安全落地 —— 命中任一为 PRE-LAUNCH。
    PL-1 其余全部(含代码债、注释/文档漂移、缺测试锁定、开放注册级滥用/频控——D-10 明示不按公开口径拔高、INFO/acknowledge 条目)为 POST-LAUNCH。
    随后写 D-11 总判定推导规则:有 BLOCKER→NO-GO;无 BLOCKER 有 PRE-LAUNCH→CONDITIONAL GO(附 PRE-LAUNCH 必做清单);全 POST-LAUNCH→GO。
    然后新建 `## 逐条上线判定表`:40 行,列 = ID / 判定(BLOCKER|PRE-LAUNCH|POST-LAUNCH)/ 准则条款(B-1~PL-1)/ 一句理由;判定与严重度独立评(严重度≠紧迫度)。本表是 CALIBRATION.md 中唯一以 `| F-` 开行的表(供机械对账)。

    文件尾新建 `## 呈报与批准记录` 节,状态行写 `状态: 呈报待批`,并写尾部对账等式草稿(CAL 条数、CL 簇数、WP 包数、判定表 40 行)。全程只读 findings,不回写任何封版文件;证据一律 `path:line @ 5927f36` 格式,秘密类只写位置与模式名不复制值本体。
  </action>
  <verify>
    <automated>test -f .planning/audit/CALIBRATION.md && test $(grep -c '^| F-' .planning/audit/CALIBRATION.md) -eq 40 && grep -q '^## 上线判定准则' .planning/audit/CALIBRATION.md && grep -q '^## 根因聚类划分' .planning/audit/CALIBRATION.md && grep -q '^## 修复工作包划分' .planning/audit/CALIBRATION.md && grep -q '状态: 呈报待批' .planning/audit/CALIBRATION.md && test $(git status --porcelain .planning/audit/findings/ | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| F-' .planning/audit/CALIBRATION.md` = 40(逐条判定表全量,且为文件内唯一 `| F-` 开行表)
    - 判定表每行的判定列取值 ∈ {BLOCKER, PRE-LAUNCH, POST-LAUNCH},条款列取值 ∈ {B-1,B-2,B-3,P-1,P-2,P-3,PL-1}
    - 每条 `### CAL-` 条目含五字段:调整类型、原值→终值、理由、锚点依据、批准记录;锚点依据指向 CHARTER 锚点表或分档表具体行
    - `### CL-` 簇数在 4~7 之间,每簇有成员 F-ID 清单与既有台账证据锚
    - 每个 `### WP-` 包有成员、共同修复位置、包级工作量档与依赖字段;成员不含 INFO 条与并入副条
    - `git status --porcelain .planning/audit/findings/` 输出为空(封版未动,D-03)
    - 文件内不出现数值评分或小时估计(工作量仅 S/M/L/XL 档)
  </acceptance_criteria>
  <done>CALIBRATION.md 呈报草稿完整:拟调整清单、并入判定、聚类、工作包、判定准则与 40 行判定表齐备,状态为呈报待批,findings 封版零改动。</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: D-02/D-12 合并批量呈报 —— 用户批复校准调整与上线判定</name>
  <files>.planning/audit/CALIBRATION.md</files>
  <action>把 Task 1 草稿中的四组内容整理为一份批量呈报摘要直接呈现给用户(不新建文件,引用 CALIBRATION.md 草稿章节):①拟调整清单(每条 CAL-NN 的 ID、原级→新级、理由、锚点依据)②真重复并入判定 ③上线判定准则全文(B-1~PL-1 + D-11 推导规则)④判定抽样呈报——全部 BLOCKER 与 PRE-LAUNCH 条目、以及与严重度直觉相厄的条目(如 MEDIUM 却 POST-LAUNCH);POST-LAUNCH 大盘不逐条过(per D-12)。呈报中附一句确认项:CHARTER schema 字段 8/9 的台账回填预期由 CALIBRATION.md 承载、findings/*.md 不回写(RESEARCH 假设 A3)。等待用户批复后才允许 Task 3 落账(per D-02,不先斩后奏)。</action>
  <decision>批准或修改:①全部拟调整项(CAL-NN:ID、原级→新级、理由、锚点依据)②真重复并入判定 ③上线判定准则全文(B-1~PL-1 + D-11 推导规则)④判定抽样(全部 BLOCKER 与 PRE-LAUNCH 条目、以及与严重度直觉相厄的条目,如 MEDIUM 却 POST-LAUNCH);⑤顺带确认一句:CHARTER schema 字段 8/9 的台账回填预期由 CALIBRATION.md 承载、findings/*.md 不回写(RESEARCH 假设 A3)。</decision>
  <context>D-02(校准批量呈报一次批准)与 D-12(准则批准 + 抽样呈报)为 locked decisions,批复必须发生在落账之前,不可被 yolo/end-of-phase 模式吞掉(不先斩后奏)。两次呈报按 RESEARCH Pattern 2 合并为一次交互(判定与严重度独立评,无先后依赖)。POST-LAUNCH 大盘不逐条过,仅呈报非平凡项。DNF-04 归属已裁定维持(D-13),本次呈报不再请示该项。</context>
  <options>
    <option id="approve-all">
      <name>整批通过</name>
      <pros>一次交互完成 D-02 + D-12 双批准,直接进入落账</pros>
      <cons>不逐条留痕个别意见</cons>
    </option>
    <option id="approve-with-changes">
      <name>逐条批注后通过</name>
      <pros>可对个别 CAL/判定条目改判,批复意见逐条落入批准记录字段</pros>
      <cons>需要用户逐条给出改判意见</cons>
    </option>
    <option id="reject">
      <name>退回重扫</name>
      <pros>准则或调整口径方向性有误时重做 Task 1</pros>
      <cons>本 plan 重新执行</cons>
    </option>
  </options>
  <resume-signal>回复 approve-all / approve-with-changes(附逐条意见)/ reject</resume-signal>
  <verify>
    <automated>grep -q '状态: 呈报待批' .planning/audit/CALIBRATION.md</automated>
  </verify>
  <acceptance_criteria>
    - 用户已给出 approve-all / approve-with-changes / reject 之一的显式批复
    - 批复原文可供 Task 3 落入批准记录字段
  </acceptance_criteria>
  <done>用户对拟调整清单、并入判定、判定准则与判定抽样完成批复(D-02/D-12 双批准或退回)。</done>
</task>

<task type="auto">
  <name>Task 3: 按批复落账 CALIBRATION.md 终稿并提交</name>
  <files>.planning/audit/CALIBRATION.md</files>
  <read_first>
    - .planning/audit/CALIBRATION.md(Task 1 草稿现状)
    - .planning/audit/HYPOTHESES.md(尾部机械验证命令范式,对账等式照此照录)
  </read_first>
  <action>
    按用户批复更新 CALIBRATION.md:①每条 CAL-NN 的批准记录字段写终态("经 D-02 批量呈报,用户于 2026-07-05 批复:整批通过 / 逐条意见原文");被否决或改判的条目按批复修改终值并留痕原拟值;②`## 呈报与批准记录` 节状态行改为 `状态: 已批准落账`,记录呈报批次、日期、批复方式与判定准则批准结论(D-12);③尾部对账等式按终稿实跑照录(命令 + 实际输出 + ✓):CAL 条数 = `grep -c '^### CAL-'`、CL 簇数 = `grep -c '^### CL-'`、WP 包数 = `grep -c '^### WP-'`、判定表行数 = `grep -c '^| F-'` = 40、三态判定计数和 = 40;④文件尾加统一斜体总结行(文档名: 日期 + 关键计数摘要,仿 COVERAGE.md 尾注)。完成后按 GSD 流程提交(仅 .planning/ 路径)。
  </action>
  <verify>
    <automated>grep -q '状态: 已批准落账' .planning/audit/CALIBRATION.md && test $(grep -c '^| F-' .planning/audit/CALIBRATION.md) -eq 40 && test $(grep '^| F-' .planning/audit/CALIBRATION.md | grep -cE 'BLOCKER|PRE-LAUNCH|POST-LAUNCH') -eq 40 && test $(git status --porcelain .planning/audit/findings/ | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '状态: 已批准落账'` 命中;每条 CAL-NN 的批准记录字段不再是"待 D-02 批量呈报"
    - 判定表 40 行三态计数之和 = 40(`grep '^| F-' | grep -cE 'BLOCKER|PRE-LAUNCH|POST-LAUNCH'` = 40)
    - 尾部对账等式含实跑命令与输出照录,全部命中 ✓
    - `git status --porcelain .planning/audit/findings/` 为空;`git diff --stat 5927f36 -- apps/ scripts/ docs/ | wc -l` = 0
  </acceptance_criteria>
  <done>CALIBRATION.md 终稿含完整批准记录与对账等式,已提交;后续组装 plan 的每个判断类字段均有唯一来源。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| findings 证据 → 新建台账 | 封版台账中的证据引用被转写进新文件,可能带出秘密模式内容 |
| 本阶段写入面 → 审计基线 | 任何误写 apps/、scripts/、docs/ 即污染基线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | CALIBRATION.md 证据引用 | medium | mitigate | 证据一律只写 `path:line @ 5927f36` + 模式名,不复制值本体(CHARTER 秘密红线);阶段收尾(05-03)对 .planning/audit/ 全目录跑秘密反扫 |
| T-05-02 | Tampering | apps/ scripts/ docs/ 基线 | high | mitigate | 写入面严格限定 .planning/;每 task 验收含 `git status --porcelain` 复核;收尾跑 CHARTER 零 diff 命令 |
</threat_model>

<verification>
- CALIBRATION.md 存在且含批准记录终态;判定表 40 行三态全填
- findings/*.md 相对工作树零改动;apps/ scripts/ docs/ 相对 5927f36 零 diff
- 全部裁定(调级/并入/聚类/工作包/判定)在 CALIBRATION.md 有唯一记录,可被 05-02 机械引用
</verification>

<success_criteria>
- ROADMAP 成功判据 1 前半达成:去重、根因聚类与单一口径校准完成且调整有记录可查(RPT-02/RPT-04 的判断前置输入就绪)
- RPT-03 判定准则与 40 条判定经用户批准(D-09/D-10/D-11/D-12)
- "报告组装阶段不产生新判断"具备结构性保证(Pattern 1)
</success_criteria>

<output>
Create `.planning/phases/05-report-calibration-assembly/05-01-SUMMARY.md` when done
</output>
