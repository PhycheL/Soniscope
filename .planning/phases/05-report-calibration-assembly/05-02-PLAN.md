---
phase: 05-report-calibration-assembly
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .planning/audit/REPORT.md
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03, RPT-04, RPT-05, RPT-06, RPT-07, RPT-09]
must_haves:
  truths:
    - "REPORT.md 含一页执行摘要(审计缘由、范围、按严重度的发现计数、总体上线判定三档词)且无任何'待裁定'占位"
    - "40 条真实发现全量入汇总表,每行含终级严重度/维度/标题/工作量/上线判定/聚类/处置 + 一句概要,按严重度降序再工作量升序排序"
    - "修复工作包、DNF 登记表(4 条,DNF-04 记经用户裁定维持)、优点盘点、五维度置信声明章节齐备"
    - "报告不复制九字段全文或证据片段,详情一律链回封版 findings/*.md"
  artifacts:
    - path: ".planning/audit/REPORT.md"
      provides: "最终审计报告主文件:方法声明、执行摘要、判定准则、发现汇总表、工作包、DNF 表、优点盘点、置信声明、移交物"
      contains: "## 执行摘要"
  key_links:
    - from: ".planning/audit/REPORT.md"
      to: ".planning/audit/CALIBRATION.md"
      via: "终级严重度/判定/聚类/工作包字段逐一取自 CALIBRATION.md 已批准记录,零新判断"
      pattern: "CALIBRATION"
    - from: ".planning/audit/REPORT.md"
      to: ".planning/audit/findings/code.md"
      via: "每条发现详情链回封版台账(D-15),不复制证据片段"
      pattern: "findings/"
---

<objective>
从封版台账 + 已批准的 CALIBRATION.md 纯机械组装最终审计报告主文件 `.planning/audit/REPORT.md`(D-14 主报告 + 附录分文件形态;附录由 05-03 产出)。覆盖 RPT-01(执行摘要)、RPT-02(汇总表)、RPT-03(判定准则与判定列)、RPT-04(修复工作包)、RPT-05(DNF 登记表)、RPT-06(优点盘点)、RPT-07(置信声明)、RPT-09(中文正文 + 英文 ID/严重度术语)。组装零新判断:每个表行字段有唯一确定来源(findings 字段或 CALIBRATION.md 已批准记录)。

Purpose: 产出可直接驱动修复里程碑的 backlog 与排期清单。
Output: `.planning/audit/REPORT.md`(除附录索引与收尾验证章节外全部章节)。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-report-calibration-assembly/05-CONTEXT.md
@.planning/phases/05-report-calibration-assembly/05-PATTERNS.md
@.planning/phases/05-report-calibration-assembly/05-01-SUMMARY.md
</context>

## Artifacts this phase produces

见 05-01-PLAN.md 同名章节(phase 级清单)。本 plan 创建 `.planning/audit/REPORT.md` 及其固定章节锚:`## 方法声明`、`## 执行摘要`(含 `### 总体上线判定`、`### 存在级观察与范围外事项`)、`## 上线判定准则`、`## 发现汇总表`、`## 修复工作包`(`### WP-NN` 小节)、`## Do-NOT-fix 登记表`、`## 优点盘点`、`## 分维度置信声明`(`### 置信·CON` / `### 置信·CODE` / `### 置信·TOOL` / `### 置信·DOC` / `### 置信·TEST`)、`## 修复里程碑移交物`。

<tasks>

<task type="auto">
  <name>Task 1: REPORT.md 骨架 + 方法声明 + 判定准则章节 + 发现汇总表(RPT-02/RPT-03 核心 backlog)</name>
  <files>.planning/audit/REPORT.md</files>
  <read_first>
    - .planning/audit/CALIBRATION.md(终级严重度/工作量、并入判定、CL-NN 聚类、逐条判定表 —— 全部判断字段的唯一来源)
    - .planning/audit/findings/contract.md
    - .planning/audit/findings/code.md
    - .planning/audit/findings/toolchain.md
    - .planning/audit/findings/docs-config.md
    - .planning/audit/findings/test.md
    - .planning/audit/CHARTER.md(范围与方法声明章节、基线 SHA —— 方法声明的引用源)
    - .planning/phases/05-report-calibration-assembly/05-PATTERNS.md(章节类比与图例先行范式)
  </read_first>
  <action>
    新建 `.planning/audit/REPORT.md`,文件头三件套(标题 + Created + 基线 `5927f36`)。

    `## 方法声明` 写死四条(PATTERNS 指定):①"45 条目 = 40 真实发现 + 5 条 F-*-00 schema 示例,示例剔除出一切计数与表格";②排序规则:严重度降序(CRITICAL→HIGH→MEDIUM→LOW→INFO,空档照常在词表中声明)→ 同级内工作量升序(S→M→L→XL,backlog 语义先修便宜的)→ 按 ID 稳定排序;③终级取值规则:该 ID 在 CALIBRATION.md 有调整记录则用终级并标"经校准",无记录原级照抄;④CHARTER schema 字段 8/9 槽位由 CALIBRATION.md 承载的取舍注记(D-03)。另注记:报告不复制九字段全文与证据片段,详情链回封版 findings/*.md(D-15);禁数值评分与小时估计。

    `## 上线判定准则`:从 CALIBRATION.md 照搬已批准的三级准则全文(B-1~B-3 / P-1~P-3 / PL-1,含 D-10 语境说明)与 D-11 总判定推导规则(有 BLOCKER→NO-GO;无 BLOCKER 有 PRE-LAUNCH→CONDITIONAL GO;全 POST-LAUNCH→GO)。章节头用 `>` 引块注明"准则经 D-12 用户批准,见 CALIBRATION.md 批准记录"。

    `## 发现汇总表`(RPT-02+RPT-03):图例先行(仿 TEST-AUDIT 反向映射清单),40 行,列 = ID / 终级严重度(经校准者加标注)/ 维度 / 标题 / 工作量 / 上线判定 / 聚类(CL-NN 或『—』)/ 处置 / 一句概要。处置列取值:进工作包 WP-NN;并入 F-XX-NN 处理(D-08 副条);acknowledge 无需动作(D-07,INFO/良性行,自然沉底)。每行终级严重度、判定、聚类、处置取自 CALIBRATION.md,ID/维度/标题/工作量/概要机械抽取自 findings(以 `^### F-` 为锚,剔除 -00 示例)。本表是 REPORT.md 中唯一以 `| F-` 开行的表(其他章节的成员引用一律放单元格内或行内代码,供机械对账)。章节头 `>` 引块写"本表 40 行 = 45 条目 − 5 示例;终级含 N 条经校准,见 CALIBRATION.md"。
  </action>
  <verify>
    <automated>test -f .planning/audit/REPORT.md && test $(grep -c '^| F-' .planning/audit/REPORT.md) -eq 40 && test $(grep '^| F-' .planning/audit/REPORT.md | grep -cE 'BLOCKER|PRE-LAUNCH|POST-LAUNCH') -eq 40 && grep -q '^## 方法声明' .planning/audit/REPORT.md && grep -q '^## 上线判定准则' .planning/audit/REPORT.md && test $(git status --porcelain .planning/audit/findings/ | wc -l) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| F-' .planning/audit/REPORT.md` = 40,且判定三态计数和 = 40
    - 汇总表排序满足:严重度降序 → 同级工作量升序 → ID 稳定序(抽查 MEDIUM 段全部在 LOW 段之前、S 在 M 之前)
    - 每行有处置列取值 ∈ {WP-NN, 并入 F-XX-NN 处理, acknowledge};INFO 行全部为 acknowledge
    - 方法声明含 45=40+5 剔除注记、排序规则、终级取值规则、CHARTER 字段 8/9 取舍注记四条
    - 表内不含九字段全文或证据片段引文;详情以 findings/*.md 相对路径链回
  </acceptance_criteria>
  <done>REPORT.md 含方法声明、已批准判定准则与 40 行全量汇总表(修复里程碑 backlog 主体)。</done>
</task>

<task type="auto">
  <name>Task 2: 修复工作包 + DNF 登记表 + 优点盘点 + 置信声明 + 执行摘要(RPT-04/05/06/07/01)</name>
  <files>.planning/audit/REPORT.md</files>
  <read_first>
    - .planning/audit/REPORT.md(Task 1 现状)
    - .planning/audit/CALIBRATION.md(WP-NN 划分与包级档、判定结果 —— 总判定推导输入)
    - .planning/audit/DO-NOT-FIX.md(DNF-01~04 全文,RPT-05 直接来源)
    - .planning/audit/HYPOTHESES.md(7 处"RPT-06 优点候选"备注:HYP-03/04/08/09/10/16/19;存在级观察 5 条:HYP-01/11/18/20/21)
    - .planning/audit/COVERAGE.md(RPT-07 CODE/TOOL 规模数字与"无发现"行;补录优点的既有证据行)
    - .planning/audit/DOC-CLAIMS.md(RPT-07 DOC:23 对象 198 条四态,区分 agree 与"无法静态核实")
    - .planning/audit/TEST-AUDIT.md(RPT-07 TEST:41 模块 8/8 面 + 2 处显式无发现)
    - .planning/audit/CONTRACT-MATRIX.md(RPT-07 CON:agree 91/diverge 2/absent 10)
    - .planning/audit/CONTRACT-TEST-RECIPE.md(移交物引用)
  </read_first>
  <action>
    在 REPORT.md 追加以下章节:

    `## 修复工作包`(RPT-04):从 CALIBRATION.md 照搬 WP-NN 划分,每包 `### WP-NN` 小节:成员 F-ID(行内引用,不开 `| F-` 表行)、共同修复位置、包级工作量档(D-04 重估记录,注明与成员各档之和的关系)、包间依赖。包排序用序数规则并在节首声明:先按包内最高终级严重度降序,同级按包级工作量档升序(严禁折算数字比值,Out of Scope 禁数值评分)。

    `## Do-NOT-fix 登记表`(RPT-05):4 行表,列 = ID / 标注 / 一句理由 / 证据链接(链回 DO-NOT-FIX.md 条目);每行标注列为 `⚠ intentional — do not "fix"`;DNF-04 行加"经用户裁定维持(D-13),Phase 1 挂起事项就此闭环",不再请示。

    `## 优点盘点`(RPT-06):主体三来源(D-16):①HYPOTHESES.md 七处候选备注(HYP-03 纯 JS sha256 可辩护取舍 / HYP-04 deploy 工具覆盖完整 / HYP-08 双侧脱敏 MaskedSecret + is_sensitive+hash_openid / HYP-09 单键 STS + test-sts-escape 实测 / HYP-10 单线程文件状态机免锁 / HYP-16 OSS 备份 + retranscribe 可重建 / HYP-19 Transcriber/NlsBackend 双层 Protocol);②DNF 4 条(故意设计即优点);③REQUIREMENTS 点名例:MaskedSecret、单键 STS、`.done` 状态机(第三例补录时引 COVERAGE/CONCERNS 既有行)。每条优点 = 一句陈述 + 既有台账证据行号引用;允许从 COVERAGE/矩阵 agree 行补录明显遗漏项但必须引既有行号,不新采证。章节导语写明本节目的:防止修复里程碑误"优化"。

    `## 分维度置信声明`(RPT-07):五个子节,标题固定为 `### 置信·CON`、`### 置信·CODE`、`### 置信·TOOL`、`### 置信·DOC`、`### 置信·TEST`。每子节写:审到多深(底稿规模数字:CON=矩阵 103 格 agree 91/diverge 2/absent 10;CODE+TOOL=COVERAGE 63 对象 9/9 面、深挖 20 处;DOC=23 对象 198 条声明四态,agree 与"无法静态核实"分开陈述;TEST=41 模块 8/8 面 + 反向映射 22 行)、哪些区域仅轻度覆盖、"已检查无发现"的显式源引用;另加仪器证据段(scans/ 9 份,258 命中销号:确认 15/误报 243)。数字执行时从各底稿现场 grep 复核,勿盲抄。

    `## 修复里程碑移交物`:显式引用 CONTRACT-TEST-RECIPE.md(黄金样本跨语言契约测试设计配方)并标注移交 FUTURE-02;RPT-02 汇总表即 backlog、WP-NN 即阶段清单的使用说明一句。

    最后在方法声明之后、判定准则之前插入 `## 执行摘要`(RPT-01,一页):审计缘由与范围(引 CHARTER 范围与方法章节,含排除项);按终级严重度的发现计数(现场 grep findings 实测 + CALIBRATION 调整叠加,命令与结果照录,勿抄 CONTEXT 笔误);`### 总体上线判定`:按 D-11 从判定表机械推导三档词(GO / CONDITIONAL GO / NO-GO),附推导链一句(如"PRE-LAUNCH N 条、BLOCKER 0 条 → CONDITIONAL GO");若为 CONDITIONAL GO,必做清单直接引用全部 PRE-LAUNCH 条目 ID(不另立实体);结论必须已定,不留"待裁定"占位。`### 存在级观察与范围外事项`:逐条点名 HYP-01/11/18/20/21(FC 直转落差 XL 档存在级、章程范围外、两代 SDK 并存观察、转写消费 MVP 范围外)并回链 HYPOTHESES.md,防 RPT-08 断链(Pitfall 4);根因聚类叙事一段(引 CL-NN,回答"为什么会有这类问题",D-06 分析层喂摘要)。

    全文中文正文 + 英文 ID/严重度/判定词/工作量档(RPT-09)。

    <!-- planner-discipline-allow: 待裁定 -->
  </action>
  <verify>
    <automated>grep -q '^## 执行摘要' .planning/audit/REPORT.md && grep -q '^### 总体上线判定' .planning/audit/REPORT.md && grep -q '^### 存在级观察与范围外事项' .planning/audit/REPORT.md && test $(grep -c '^### 置信·' .planning/audit/REPORT.md) -eq 5 && test $(grep -c '^| DNF-' .planning/audit/REPORT.md) -eq 4 && test $(grep -c '^### WP-' .planning/audit/REPORT.md) -eq $(grep -c '^### WP-' .planning/audit/CALIBRATION.md) && awk '/^### 总体上线判定/{f=1;next} /^#/{f=0} f' .planning/audit/REPORT.md | grep -qE '\b(GO|CONDITIONAL GO|NO-GO)\b' && ! grep -q '待裁定' .planning/audit/REPORT.md</automated>
    <human-check>抽查 RPT-06 每条优点的台账行号引用真实存在且未新采证据;执行摘要严重度计数与汇总表逐级相符;总判定三档词与判定表推导一致(end-of-phase 验收)</human-check>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### 置信·' .planning/audit/REPORT.md` = 5;`grep -c '^| DNF-'` = 4;WP 小节数与 CALIBRATION.md 一致
    - `### 总体上线判定` 小节含三档词之一与 D-11 推导链,无"待裁定"字样占位
    - 执行摘要含审计缘由、范围(含排除项)、按严重度计数(附照录的 grep 命令与输出)
    - `### 存在级观察与范围外事项` 逐条含 HYP-01、HYP-11、HYP-18、HYP-20、HYP-21 五个 ID
    - DNF-04 行含"经用户裁定维持"表述;优点盘点覆盖 HYP-03/04/08/09/10/16/19 + DNF 4 条 + REQUIREMENTS 3 例且每条引既有台账行号
    - `git status --porcelain` 输出仅含 .planning/ 路径
  </acceptance_criteria>
  <done>REPORT.md 主体齐备(RPT-01~07 + 移交物 + RPT-09 语言约定),仅余附录索引与收尾验证章节待 05-03 补入。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| findings 证据 → REPORT.md | 组装时复制证据片段可能带出签名 URL/AK 模式内容 |
| 本阶段写入面 → 审计基线 | 任何误写 apps/、scripts/、docs/ 即污染基线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | REPORT.md 表行与链接 | medium | mitigate | D-15 禁止复制证据片段,只引 `path:line @ 5927f36` + 模式名;05-03 收尾秘密反扫全目录 |
| T-05-02 | Tampering | apps/ scripts/ docs/ 基线 | high | mitigate | 写入面限定 .planning/audit/REPORT.md;验收含 `git status --porcelain` 复核 |
</threat_model>

<verification>
- REPORT.md 全部主体章节存在,40 行主表 + 三态判定齐备,总判定三档词已定
- 每个判断类字段可回溯至 CALIBRATION.md 已批准记录(零新判断)
- findings/*.md 与 apps/ scripts/ docs/ 零改动
</verification>

<success_criteria>
- ROADMAP 成功判据 2、3(工作包部分)、4(DNF/优点/置信部分)达成
- RPT-01/02/03/04/05/06/07/09 全部有对应章节且机械可验收
</success_criteria>

<output>
Create `.planning/phases/05-report-calibration-assembly/05-02-SUMMARY.md` when done
</output>
