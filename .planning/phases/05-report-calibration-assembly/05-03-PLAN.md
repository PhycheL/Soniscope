---
phase: 05-report-calibration-assembly
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - .planning/audit/REPORT-APPENDIX-A-traceability.md
  - .planning/audit/REPORT-APPENDIX-B-clusters.md
  - .planning/audit/REPORT.md
autonomous: true
requirements: [RPT-08, RPT-09]
must_haves:
  truths:
    - "追溯映射附录含 29 行(25 HYP + 4 DNF)闭环 + 报告落点列 + 需求映射列 + '已检查,无发现'显式行,报告侧无断链"
    - "聚类附录 CL-NN 明细与 CALIBRATION.md 一致并带成员全覆盖对账等式"
    - "REPORT.md 收尾验证章节照录全套机械门禁(零 diff、计数等式、秘密反扫、写入面复核)且全部命中"
    - "apps/、scripts/、docs/ 相对 5927f36 零 diff 验证通过"
  artifacts:
    - path: ".planning/audit/REPORT-APPENDIX-A-traceability.md"
      provides: "RPT-08 可追溯映射表:发现↔CONCERNS 线索(HYP/DNF)↔需求 + 报告落点"
    - path: ".planning/audit/REPORT-APPENDIX-B-clusters.md"
      provides: "CL-NN 根因聚类明细与成员对账等式"
    - path: ".planning/audit/REPORT.md"
      provides: "附录索引与收尾验证章节(命令 + 实跑输出照录)"
      contains: "## 收尾验证"
  key_links:
    - from: ".planning/audit/REPORT-APPENDIX-A-traceability.md"
      to: ".planning/audit/HYPOTHESES.md"
      via: "29 条溯源闭环声明表机械转写 + 报告落点/需求映射两列"
      pattern: "HYP-"
    - from: ".planning/audit/REPORT.md"
      to: ".planning/audit/REPORT-APPENDIX-A-traceability.md"
      via: "附录索引章节相对路径链接"
      pattern: "REPORT-APPENDIX-A"
---

<objective>
产出两份附录(D-14 机械性长内容分文件):RPT-08 追溯映射表(附录 A)与 CL-NN 聚类明细(附录 B);在 REPORT.md 补入附录索引与收尾验证章节;实跑并照录全套机械门禁(零 diff、计数等式、秘密反扫、写入面复核),完成里程碑最后一个 phase 的机械收尾。

Purpose: RPT-08 追溯闭环("每条发现有下落、每条线索有去向、已检查无发现显式记录")与 ROADMAP 成功判据 5(零 diff 验证通过)。
Output: REPORT-APPENDIX-A-traceability.md、REPORT-APPENDIX-B-clusters.md、REPORT.md 收尾章节。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-report-calibration-assembly/05-CONTEXT.md
@.planning/phases/05-report-calibration-assembly/05-PATTERNS.md
@.planning/phases/05-report-calibration-assembly/05-02-SUMMARY.md
</context>

## Artifacts this phase produces

见 05-01-PLAN.md 同名章节(phase 级清单)。本 plan 创建 `.planning/audit/REPORT-APPENDIX-A-traceability.md`、`.planning/audit/REPORT-APPENDIX-B-clusters.md`,并向 REPORT.md 补入 `## 附录索引` 与 `## 收尾验证` 两章节。

<tasks>

<task type="auto">
  <name>Task 1: 附录 A — RPT-08 可追溯映射表(29 条闭环 + 报告落点 + 需求映射 + 显式无发现行)</name>
  <files>.planning/audit/REPORT-APPENDIX-A-traceability.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(§29 条溯源闭环声明表约 :287-321 —— exact 类比骨架;Known Bugs 显式无线索行约 :321)
    - .planning/audit/findings/contract.md(关联发现字段自由文本样例,约 :37,58,83)
    - .planning/audit/findings/code.md
    - .planning/audit/findings/toolchain.md
    - .planning/audit/findings/docs-config.md
    - .planning/audit/findings/test.md
    - .planning/REQUIREMENTS.md(traceability 表 —— 维度→需求固定映射源)
    - .planning/audit/HANDOFF-PHASE4.md(6 条移交销号,佐证引用)
    - .planning/audit/REPORT.md(章节锚现状,报告落点列引用)
  </read_first>
  <action>
    新建 `.planning/audit/REPORT-APPENDIX-A-traceability.md`,文件头三件套 + 自述(承载 RPT-08,数据源 = HYPOTHESES 29 行表机械转写 + findings 关联字段人工转写补边)。

    主表:在 HYPOTHESES.md 29 行骨架(ID / 状态 / 去向)上加两列——**报告落点**(该 ID 在 REPORT.md 的章节锚:落发现的填 `## 发现汇总表` 对应 F-ID 行;"RPT 呈现"类 5 条 HYP-01/11/18/20/21 填 `### 存在级观察与范围外事项`;DNF 4 条填 `## Do-NOT-fix 登记表`;RPT-06 候选另注 `## 优点盘点`)与**需求映射**(固定映射,无需逐条判断:CON↔CONTRACT-01~03、CODE↔AUDIT-01、TOOL↔AUDIT-02、DOC↔AUDIT-03、TEST↔AUDIT-04、HYP 闭环整体↔AUDIT-05;发现所在维度决定需求列)。行格式:HYP 行以 `| HYP-` 开行、DNF 行以 `| DNF-` 开行(机械对账锚)。

    "已检查,无发现"显式行:照录 HYPOTHESES.md Known Bugs 显式无线索行(不计入 29,单独小节陈述);另从 CONTRACT-MATRIX agree 行、COVERAGE"无发现"行、TEST-AUDIT 2 处显式无发现、DOC-CLAIMS agree 行各引一条代表性记录做"各维度已检查无发现"显式记录小节(只引既有行号,不新采证)。

    发现↔发现补边:findings 各条 `关联发现` 字段为自由文本(存在 `F-CON-02`、`关联线索: HYP-13`、`矩阵组① 行 4`、`D14-6` 等多种记法),人工转写为规整三列表(源 ID / 关系 / 目标引用),不做纯机械解析。

    尾部对账等式照录:`grep -c '^| HYP-'` = 25、`grep -c '^| DNF-'` = 4、25+4=29 ✓;文件尾斜体总结行。
  </action>
  <verify>
    <automated>test -f .planning/audit/REPORT-APPENDIX-A-traceability.md && test $(grep -c '^| HYP-' .planning/audit/REPORT-APPENDIX-A-traceability.md) -eq 25 && test $(grep -c '^| DNF-' .planning/audit/REPORT-APPENDIX-A-traceability.md) -eq 4 && grep -q '存在级观察与范围外事项' .planning/audit/REPORT-APPENDIX-A-traceability.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| HYP-'` = 25 且 `grep -c '^| DNF-'` = 4(29 条闭环全量)
    - 每行报告落点列非空;HYP-01/11/18/20/21 五行的落点均为 `### 存在级观察与范围外事项` 锚(Pitfall 4 防断链)
    - 需求映射列按固定映射填写,覆盖 CONTRACT-01~03 / AUDIT-01~05
    - 含"已检查,无发现"显式记录小节,五维度各至少一条既有行号引用
    - 发现↔发现补边表为规整三列表
  </acceptance_criteria>
  <done>附录 A 完成:29 条溯源在报告侧全部有落点,追溯链发现↔线索↔需求闭环,显式无发现记录在案。</done>
</task>

<task type="auto">
  <name>Task 2: 附录 B — CL-NN 聚类明细 + REPORT.md 附录索引</name>
  <files>.planning/audit/REPORT-APPENDIX-B-clusters.md, .planning/audit/REPORT.md</files>
  <read_first>
    - .planning/audit/CALIBRATION.md(`## 根因聚类划分` 节 —— 附录 B 的唯一判断来源)
    - .planning/audit/TEST-AUDIT.md(成员归属等式行约 :169 —— 对账等式范式)
    - .planning/audit/REPORT.md(现状,附录索引插入位置)
  </read_first>
  <action>
    新建 `.planning/audit/REPORT-APPENDIX-B-clusters.md`,文件头三件套 + 自述(D-05 聚类为叠加层:ID 全保留不合并不退役;D-06 聚类是分析层,与 WP-NN 执行层互指不强制对齐)。从 CALIBRATION.md 照搬每簇 `### CL-NN: <根因一句话>` 明细:根因陈述 / 成员 F-ID 清单 / 关联工作包(WP-NN 互指)/ 证据锚(既有台账行引用)。尾部写成员全覆盖对账等式(仿 TEST-AUDIT 成员归属等式:数字 + 分解 + ✓),含"未入簇孤条清单"显式记录(聚类层允许有未入簇发现)。

    然后在 REPORT.md 收尾验证章节之前的位置补入 `## 附录索引` 章节:相对路径链接 REPORT-APPENDIX-A-traceability.md(RPT-08 追溯映射)与 REPORT-APPENDIX-B-clusters.md(聚类明细),各附一句内容说明(D-14 主报告内链引用)。
  </action>
  <verify>
    <automated>test -f .planning/audit/REPORT-APPENDIX-B-clusters.md && test $(grep -c '^### CL-' .planning/audit/REPORT-APPENDIX-B-clusters.md) -eq $(grep -c '^### CL-' .planning/audit/CALIBRATION.md) && grep -q '^## 附录索引' .planning/audit/REPORT.md && grep -q 'REPORT-APPENDIX-A-traceability' .planning/audit/REPORT.md && grep -q 'REPORT-APPENDIX-B-clusters' .planning/audit/REPORT.md</automated>
  </verify>
  <acceptance_criteria>
    - 附录 B 的 `### CL-` 簇数与 CALIBRATION.md 完全一致,成员清单逐簇一致(照搬零改判)
    - 尾部成员全覆盖对账等式:全部入簇成员 + 未入簇孤条 = 40 ✓
    - 每簇有关联工作包互指字段(或显式『—』)
    - REPORT.md `## 附录索引` 含两个附录的相对路径链接
  </acceptance_criteria>
  <done>附录 B 与附录索引完成,聚类叠加层在报告侧可导航且与校准台账一致。</done>
</task>

<task type="auto">
  <name>Task 3: 收尾验证章节 — 全套机械门禁实跑照录 + 阶段收尾</name>
  <files>.planning/audit/REPORT.md</files>
  <read_first>
    - .planning/audit/REPORT.md(现状)
    - .planning/audit/HYPOTHESES.md(阶段收尾验证照录范式约 :334-399:命令 + 实际输出 + 期望值命中声明)
    - .planning/audit/COVERAGE.md(完成判定编号条目范式约 :130-155,含秘密反扫与 git status 补充核查先例)
    - .planning/audit/CHARTER.md(零 diff 写定命令与秘密红线条款)
  </read_first>
  <action>
    在 REPORT.md 文末新建 `## 收尾验证` 章节,逐条实跑下列门禁并照录(命令 + 实际输出 + 期望值命中 ✓,仿 HYPOTHESES 收尾范式):

    1. 零 diff(里程碑硬约束,CHARTER 写定命令):`git diff --stat 5927f36 -- apps/ scripts/ docs/` 期望空输出,配 `| wc -l` = 0。
    2. 发现底数等式:findings 五文件 `^### F-` 标题合计 = 45;剔除 -00 示例后 = 40。
    3. 报告主表等式:REPORT.md `^| F-` 行数 = 40;三态判定计数和 = 40。
    4. 溯源等式:HYPOTHESES `^### HYP-` = 25;DO-NOT-FIX `^### DNF-` = 4;附录 A `^| HYP-` + `^| DNF-` = 29。
    5. 校准等式:CALIBRATION.md CAL/CL/WP 计数与其尾部对账一致;报告 WP 小节数 = CALIBRATION WP 数;附录 B CL 数 = CALIBRATION CL 数。
    6. 严重度分布复算:findings 现场 grep 各级计数,叠加 CALIBRATION 调整后与执行摘要计数逐级相符。
    7. 秘密反扫(COVERAGE 完成判定第 9 条同款,范围 .planning/audit/ 全目录含本阶段全部新文件):期望零命中(exit 1);报告中只记命令与"零命中 ✓"结论。
    8. 写入面复核(Pitfall 7:零 diff 命令不保护根文件):`git status --porcelain` 输出仅含 .planning/ 路径,结论照录。

    任何等式不中即修组装侧(报告/附录),严禁改封版台账或 CALIBRATION.md 已批准记录。章节末尾写一句里程碑交付声明(报告 + 两附录 + CALIBRATION.md 四件套即本审计里程碑最终交付物,修复移交物为 CONTRACT-TEST-RECIPE.md),并加文件尾统一斜体总结行(文档名: 日期 + 40 条发现/总判定词/附录清单摘要)。完成后按 GSD 流程提交(仅 .planning/ 路径)。
  </action>
  <verify>
    <automated>grep -q '^## 收尾验证' .planning/audit/REPORT.md && test $(git diff --stat 5927f36 -- apps/ scripts/ docs/ | wc -l) -eq 0 && test $(grep -hc '^### F-' .planning/audit/findings/*.md | paste -sd+ - | bc) -eq 45 && test $(grep -h '^### F-' .planning/audit/findings/*.md | grep -vc '\-00:') -eq 40 && test $(grep -c '^| F-' .planning/audit/REPORT.md) -eq 40 && { grep -rE 'OSSAccessKeyId=[0-9A-Za-z]{8,}|Signature=[0-9A-Za-z%+/=]{16,}|LTAI[0-9A-Za-z]{10,}' .planning/audit/ >/dev/null; test $? -eq 1; }</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/ | wc -l` = 0(ROADMAP 成功判据 5)
    - 收尾验证章节含上述 8 项门禁的命令与实跑输出照录,全部命中 ✓
    - 秘密反扫对 .planning/audit/ 全目录零命中(exit 1)
    - `git status --porcelain` 输出仅含 .planning/ 路径
    - REPORT.md 与两附录均以中文正文 + 英文 ID/严重度/判定术语书写(RPT-09)
  </acceptance_criteria>
  <done>全套机械门禁实跑通过并照录进报告;最终审计报告四件套交付,里程碑最后一个 phase 机械收尾完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 台账证据 → 附录/报告 | 转写过程可能带出秘密模式内容并随提交永久入库 |
| 本阶段写入面 → 审计基线 | 任何误写 apps/、scripts/、docs/ 即污染基线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Information Disclosure | 附录 A/B 与 REPORT.md 收尾章节 | medium | mitigate | 只引 `path:line @ 5927f36` + 模式名;Task 3 秘密反扫 .planning/audit/ 全目录,期望零命中,结果照录 |
| T-05-02 | Tampering | apps/ scripts/ docs/ 基线 | high | mitigate | Task 3 实跑 CHARTER 零 diff 命令 + `git status --porcelain` 写入面复核,双结果照录进报告 |
</threat_model>

<verification>
- 附录 A 29 行闭环 + 报告落点全填;附录 B 与 CALIBRATION 一致;附录索引可导航
- 收尾验证 8 项门禁全部命中并照录
- 零 diff 通过、秘密反扫零命中、写入面仅 .planning/
</verification>

<success_criteria>
- RPT-08 追溯闭环达成(发现↔CONCERNS 线索↔需求,含显式无发现记录)
- ROADMAP 成功判据 4(映射表部分)与判据 5(语言 + 零 diff)达成
- 里程碑最终交付物四件套(REPORT.md + 附录 A/B + CALIBRATION.md)齐备
</success_criteria>

<output>
Create `.planning/phases/05-report-calibration-assembly/05-03-SUMMARY.md` when done
</output>
