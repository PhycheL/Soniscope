---
phase: 04-docs-config-test-audit
plan: 6
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/TEST-AUDIT.md
autonomous: true
requirements: [AUDIT-04]
must_haves:
  truths:
    - "TEST-AUDIT.md 骨架就绪:D-10 质量检查面清单定稿(8 面,各配锚点/信号列)、41 个测试模块的逐对象台账表、worktree 执行区备注"
    - "D-09 反向映射清单成表:22 条 F-*(F-CON-01~06 / F-CODE-01~08 / F-TOOL-01~08)+ 契约矩阵非 agree 关键行逐条在列,兜底列初判完成"
  artifacts:
    - ".planning/audit/TEST-AUDIT.md — 覆盖台账 + 反向映射清单(证据层,发现不入本文件)"
  key_links:
    - "反向映射清单兜底判定 → 04-08 F-TEST 缺口定级(参照原发现严重度)"
    - "8 面清单 → 04-07 逐模块普审的分母定义(已过面 N/8)"
---

<objective>
按 D-16(独立新文件)创建 `.planning/audit/TEST-AUDIT.md`:定稿 D-10 质量检查面清单(8 面)、建 41 个测试模块(pytest 31 + node:test 10)的逐对象台账骨架,并按 D-09 编制反向映射清单(Phase 2/3 全部 22 条 F-* 发现 + 契约矩阵关键行 → 现有测试兜底情况初判)。

Purpose: AUDIT-04 的『系统排查完成』必须可机械验收——分面分母、逐对象台账、逐条销号的反向映射三件套在证据收集前定稿,04-07/04-08 按格填账。
Output: TEST-AUDIT.md(骨架 + 反向映射清单)。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-docs-config-test-audit/04-PATTERNS.md
@.planning/audit/COVERAGE.md
@.planning/codebase/TESTING.md
</context>

## Artifacts this phase produces

见 04-01-PLAN.md 同名节(阶段全量清单)。本计划创建:`.planning/audit/TEST-AUDIT.md`。

<tasks>

<task type="auto">
  <name>Task 1: TEST-AUDIT.md 骨架(8 面清单 + 41 行逐对象台账)</name>
  <files>.planning/audit/TEST-AUDIT.md</files>
  <read_first>
    - .planning/audit/COVERAGE.md(:1-32 文件头/关注面清单/逐对象台账三段范式——『Don't Hand-Roll』明示只读仿写到新文件)
    - .planning/codebase/TESTING.md(测试地图:无 conftest、全手写 fake + DI、无覆盖率配置、JS 桥模式——分面定义的勘察底稿)
    - .planning/audit/CHARTER.md(严重度锚点,分面锚定用)
  </read_first>
  <action>
    创建 `.planning/audit/TEST-AUDIT.md`(D-16),含三段:

    1. 文件头:标题『TEST 覆盖台账与反向映射清单』+ `**Created:** 2026-07-05` + 基线行 + 角色说明:证据与判断分离,本文件只登记对象/深度/已过面/线索,发现正文入 findings/test.md(判断层);取证纪律(git show 口径,worktree 副本仅供执行);worktree 执行区备注:实跑证据见 `scans/gate-run-worktree.md` 与 `scans/coverage-*.md`(04-01/04-02 产出),重建命令 `git worktree add <scratchpad>/wt-5927f36 5927f36` + `uv sync --frozen`。
    2. 质量检查面清单(D-10 定稿,8 面——全阶段『已过面 N/8』的分母定义,表列:# | 关注面 | 锚点/关联 | 仪器辅助信号):
       - ①断言强度:结果值断言 vs 仅『不抛异常』/存在性弱断言 — 锚点:缺口面属 CHARTER LOW『测试覆盖缺口』类;
       - ②fake 与真实实现漂移风险:手写 fake(FakeSource/FakeApi 等)的行为是否有与真实 Protocol 实现对齐的锁定 — 关联:潜伏失配类;
       - ③隔离与状态泄漏:tmp_path/环境变量/wx storage mock 是否互不污染,无 conftest 下的重复 setup 惯例 — 信号:TESTING.md『无 conftest』;
       - ④契约常量锁定:RETRY_DELAYS_SECONDS/RETRY_DELAYS_MS、错误码字符串(INVALID_CODE 等)、object key 格式等镜像常量是否在双语言测试中被字面断言 — 关联:CONTRACT-MATRIX 组③、F-CON 系;
       - ⑤秘密泄漏断言:输出/日志/repr 无秘密断言的覆盖面 — 关联:CHARTER 秘密红线、test_config.py 泄漏断言先例;
       - ⑥静默 skip 路径:skipif/条件跳过使测试静默不跑 — 关联:D-11 门禁完整性输入,已知线索 test_miniprogram_js.py:24;
       - ⑦错误路径与边界覆盖:失败分支/异常路径是否有测试 — 关联:F-CODE 系脆弱区;
       - ⑧测试与生产耦合:私有函数直测、对内部实现细节的脆弱耦合 — 信号:重构脆性观察。
    3. 逐对象台账表(41 行,列:路径 | 行数 | 侧 | 深度 | 已过面 | 产出 | 备注):对象清单以 `git ls-tree -r --name-only 5927f36 apps/worker/tests apps/fc/tests apps/miniprogram/test` 过滤 `test_*.py` 与 `*.test.js` 枚举(预期 worker 24 + fc 7 + node 10 = 41,若实测计数不同以 ls-tree 为准并在头部注记);行数列用 `git show 5927f36:<path> | wc -l` 填;已过面列初始 `0/8`、产出列初始『-』(04-07 回填)。
  </action>
  <verify>
    <automated>test -f .planning/audit/TEST-AUDIT.md && grep -c '^| `apps/' .planning/audit/TEST-AUDIT.md && grep -c '/8' .planning/audit/TEST-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| `apps/' .planning/audit/TEST-AUDIT.md` = 41(逐对象台账行数与 ls-tree 枚举一致;若基线实测非 41 则以头部注记的实测数为准且计数相符)
    - 8 面清单表存在:`grep -c '契约常量锁定' 该文件` ≥ 1 且 `grep -c '静默 skip' 该文件` ≥ 1
    - 文件头含『发现正文入 findings/test.md』分层声明与 worktree 重建命令
    - `grep -c 'test_miniprogram_js.py' 该文件` ≥ 1(JS 桥在台账对象中)
  </acceptance_criteria>
  <done>台账骨架与 8 面分母定稿,04-07 可直接逐行填账</done>
</task>

<task type="auto">
  <name>Task 2: D-09 反向映射清单编制与兜底初判</name>
  <files>.planning/audit/TEST-AUDIT.md</files>
  <read_first>
    - .planning/audit/findings/contract.md(F-CON-01~06 全文——摘『应重点覆盖行为』与原严重度)
    - .planning/audit/findings/code.md(F-CODE-01~08 全文;:110 附近 F-CODE-07 条目内已含测试兜底证据行,可直接反查引用)
    - .planning/audit/findings/toolchain.md(F-TOOL-01~08 全文)
    - .planning/audit/CONTRACT-MATRIX.md(判定非 agree 的矩阵行与 §往返校验结论——关键行补充源)
  </read_first>
  <action>
    在 TEST-AUDIT.md 新增『反向映射清单(D-09)』节:

    1. 行集 = 22 条 F-*(F-CON-01~06、F-CODE-01~08、F-TOOL-01~08,逐条从三本台账摘录)+ CONTRACT-MATRIX 中判定为 diverge/absent/覆盖洞 但未立 F-CON 的关键行(与既有 F-CON 去重后追加,行 ID 用矩阵行号引用)。
    2. 列:条目 | 原严重度 | 应重点覆盖行为(一句,从发现的影响面反推) | 现有测试兜底(测试文件:行号 @ 5927f36 或『无』) | 缺口判定(参照原严重度 或 占位态『补证中』)。
    3. 兜底初判方法:对每条用 `git grep -n '<相关符号>' 5927f36 -- apps/worker/tests apps/fc/tests apps/miniprogram/test` 检索关联测试(符号取自原发现证据字段,如 F-CODE-01 的 process_plan、F-CON 系的 fragment_id/object key 函数名);发现正文中已含兜底证据的直接反查引用(如 F-CODE-07 在 findings/code.md 条目内已列 uploader.test.js:55-56 与 test_nls.py:401,449-450);静读即可定判的当场填终态,需 04-07 逐面普审佐证的填『补证中』。
    4. 定级规则预置在节首(供 04-08 引用):脆弱区无测试 → 缺口定级参照原发现严重度;无关联脆弱区的一般缺口 → CHARTER LOW 锚点(『lint/typecheck/测试覆盖缺口』类)。
    5. 节尾机械对账行:行总数 = 22 + 追加行数;终态/占位态计数。
  </action>
  <verify>
    <automated>grep -c 'F-CON-0\|F-CODE-0\|F-TOOL-0' .planning/audit/TEST-AUDIT.md && grep -c '反向映射' .planning/audit/TEST-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 反向映射节含全部 22 条 F-* 行:`grep -c '^| F-CON-\|^| F-CODE-\|^| F-TOOL-' .planning/audit/TEST-AUDIT.md` = 22(矩阵追加行另计)
    - 每行兜底列非空(有 `文件:行号 @ 5927f36`、『无』或占位态之一);抽查 F-CODE-07 行含 uploader.test.js 反查引用
    - 节首含定级规则两句(参照原严重度 / LOW 锚点)
    - 节尾机械对账行计数等式成立
  </acceptance_criteria>
  <done>22+N 行反向映射清单成表,初判尽量收敛,余项显式占位待 04-07 补证</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计产物 → 读者 | 反向映射引用 F-TOOL-05(已提交签名 URL)等秘密类发现时须防二次泄露 |
| 审计写入 → 封版产物 | findings 三本与 CONTRACT-MATRIX 为只读引用源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-13 | Information Disclosure | 反向映射证据字段 | medium | mitigate | 引用 test_asr.py:80 等秘密类位置时只写 `path:line @ 5927f36` + 模式名(HANDOFF 已示范该写法) |
| T-04-14 | Tampering | findings/contract.md、code.md、toolchain.md(封版) | medium | mitigate | 本计划 files_modified 仅 TEST-AUDIT.md;封版产物只读引用(D-16) |
</threat_model>

<verification>
- TEST-AUDIT.md 三段骨架 + 反向映射节成档,机械对账行闭合
- 封版产物零改动;`git diff --stat 5927f36 -- apps/ scripts/ docs/` 为空
</verification>

<success_criteria>
- AUDIT-04 的可机械验收框架就位:8 面分母、41 行台账、22+N 行反向映射
- 04-07(普审)与 04-08(定级立条)有明确的填账格与销号对象
</success_criteria>

<output>
完成后创建 `.planning/phases/04-docs-config-test-audit/04-06-SUMMARY.md`(记录占位态行数,供 04-07 销号核对)
</output>
