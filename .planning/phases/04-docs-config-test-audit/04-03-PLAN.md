---
phase: 04-docs-config-test-audit
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/DOC-CLAIMS.md
  - .planning/audit/findings/docs-config.md
autonomous: true
requirements: [AUDIT-03]
must_haves:
  truths:
    - "DOC-CLAIMS.md 骨架就绪:四态词表(agree/drift/dead-ref/无法静态核实)、负面清单(DNF 闭环 + 目标态排除)、四层覆盖总表全部对象在列"
    - "PRD_v1.md 与 tech-spec.md 两份深核文档的可核声明已抽取成清单并逐条四态销号,每条附文档侧与代码侧双行号证据 @ 5927f36"
    - "HYP-21(PRD 范围声明)与 HYP-16 文档口径半句的核对结论行已登记并附指针"
  artifacts:
    - ".planning/audit/DOC-CLAIMS.md — 销号清单底稿(骨架 + PRD/tech-spec 两节)"
    - ".planning/audit/findings/docs-config.md — 本计划批次 F-DOC 条目(若有发现级 drift)"
  key_links:
    - "DOC-CLAIMS 各结论行 → HYP-02/16/21 回填锚点(04-09 消费)"
    - "tech-spec §3.1 域名声明行 → config.js 深核互指(04-05 完成配置侧)"
---

<objective>
按 D-05(全量分层范围)/D-07(可核声明清单式深核)/D-16(独立新文件)创建 `.planning/audit/DOC-CLAIMS.md` 骨架(四态词表、负面清单、覆盖总表),并完成权威链头两份深核对象——`docs/v1.0.0 prd/PRD_v1.md`(869 行)与 `docs/v1.0.0 prd/tech-spec.md`(782 行)——的声明抽取与四态销号;发现级 drift 立 F-DOC 条目(AUDIT-03)。

Purpose: DOC 维度审计的骨架与最重两份文档先行,为 04-04(runbook)与 04-05(AGENTS/README/config 与收口)铺定格式与编号基准。
Output: DOC-CLAIMS.md(骨架 + 2 个深核节);findings/docs-config.md 本批次条目与批次导语。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-docs-config-test-audit/04-PATTERNS.md
@.planning/audit/CHARTER.md
@.planning/audit/CONTRACT-MATRIX.md
@.planning/audit/DO-NOT-FIX.md
</context>

## Artifacts this phase produces

见 04-01-PLAN.md 同名节(阶段全量清单)。本计划创建:`.planning/audit/DOC-CLAIMS.md`;续写:`.planning/audit/findings/docs-config.md` `## 发现` 节。

<tasks>

<task type="auto">
  <name>Task 1: DOC-CLAIMS.md 骨架(四态词表 + 负面清单 + 覆盖总表)</name>
  <files>.planning/audit/DOC-CLAIMS.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(:1-29 文件头/状态词表/负面清单三段范式——D-07 明示延续该范式)
    - .planning/audit/DO-NOT-FIX.md(DNF-01~04 条目,负面清单依据)
    - .planning/phases/04-docs-config-test-audit/04-PATTERNS.md(§DOC-CLAIMS 节的逐段仿写指引与专属增量三点)
  </read_first>
  <action>
    创建 `.planning/audit/DOC-CLAIMS.md`(D-16 独立新文件),含四段:

    1. 文件头:标题『DOC 声明核对清单』+ `**Created:** 2026-07-05` + `**基线:** \`5927f36\`(全 SHA 见 .planning/audit/CHARTER.md 审计基线章节)` + 角色说明段:本文档为 Phase 4 DOC 维度销号底稿(证据层),发现正文入 findings/docs-config.md(判断层);范围分层 per D-05,核对方式 per D-07,目标态排除 per D-06;全部证据出自 `git show 5927f36:<path>` / `git grep -n <pat> 5927f36`,禁止读工作树取证(CHARTER 取证纪律;PRD/tech-spec 路径含空格须引号)。
    2. 四态词表(结构照抄 CONTRACT-MATRIX 状态词表):`agree` = 文档声明与代码/配置实态语义一致(字面差异不算);`drift` = 声明与实态不一致且引用目标存在;`dead-ref` = 引用的路径/文件/命令/锚点在基线不存在;`无法静态核实` = 纯云端/平台侧事实(控制台配置、微信平台登记值),静态取证不可判——只标注不猜测。
    3. 负面清单(判定前置排除):①DNF-01~04 已裁定的故意设计命中只写『核实结论 + 引 DNF 条目闭环』,不立 F-DOC——`issue-cedential` 域名拼写(DNF-02)、`whisper-local` 桩(DNF-01)、handler mypy 豁免(DNF-03)点名在列;②`docs/fc-transcribe-design.md` 与 `docs/multi-user-design.md` 不做设计 vs 代码实态对照(CHARTER 排除项 + D-06),只审引用有效性与明显自相矛盾;③『文档滞后于目标态设计』不算 drift(Pitfall 8)。
    4. 覆盖总表(D-05 四层,每行:对象 | 层级 | 销号节 | 状态):深核 11 项(PRD_v1.md、tech-spec.md、cloud-setup.md、deployment-guide.md、fc-deploy.md、mvp-acceptance.md、AGENTS.md、README.md、apps/fc/README.md、apps/miniprogram/README.md、apps/miniprogram/config.js);普审 5 项(architecture-review-2026-07-02.md、transcribe-approach-comparison.md、docs/agents/ 的 domain.md/issue-tracker.md/triage-labels.md)+ 普审配置 2 项(project.config.json、app.json);只审引用与自洽 2 项(fc-transcribe-design.md、multi-user-design.md,状态列预置『目标态对照未审(章程排除)』标注);只记存在 3 组(docs/小程序原型/ 4 PNG、soniscope-mvp-architecture.drawio、docs/runbook/us-001-manual.html)。全部行状态列初始占位词统一为『待审』,由各计划销号后改为『已审无发现』或 F-DOC-NN/HYP-NN 指针(04-05 收口清零)。措辞硬约束:占位词『待审』只允许出现在覆盖总表表行的状态列单元格内,文件头/角色说明/四态词表/负面清单等任何说明性文字中不得出现该词——04-05 Task 3 以全文负向 grep 清零收口,说明文字外泄该词会导致 false-fail(与 04-02/04-08 的『措辞避开负向检索词』约束同款)。
  </action>
  <verify>
    <automated>test -f .planning/audit/DOC-CLAIMS.md && grep -c 'dead-ref' .planning/audit/DOC-CLAIMS.md && grep -c 'PRD_v1.md' .planning/audit/DOC-CLAIMS.md</automated>
  </verify>
  <acceptance_criteria>
    - 四态词表 4 行俱全:`grep -c 'agree\|drift\|dead-ref\|无法静态核实' .planning/audit/DOC-CLAIMS.md` ≥ 4
    - 负面清单含 `grep -c 'DNF-02' 该文件` ≥ 1 与 `grep -c '章程排除' 该文件` ≥ 1
    - 覆盖总表含全部 23 个对象行(11 深核 + 5 普审文档 + 2 普审配置 + 2 引用级 + 3 存在级组),抽查:`grep -c 'mvp-acceptance.md' 该文件` ≥ 1、`grep -c 'project.config.json' 该文件` ≥ 1、`grep -c 'drawio' 该文件` ≥ 1
    - 『待审』仅出现于覆盖总表表行:`grep -c '待审' .planning/audit/DOC-CLAIMS.md` 与 `grep -c '^|.*待审' 该文件` 输出相等(说明性文字中零出现)
    - 文件头含基线行与『禁止读工作树取证』纪律句
  </acceptance_criteria>
  <done>骨架四段齐备,后续三个 DOC 计划可直接按节填充销号</done>
</task>

<task type="auto">
  <name>Task 2: PRD_v1.md 深核声明清单与四态销号</name>
  <files>.planning/audit/DOC-CLAIMS.md</files>
  <read_first>
    - .planning/audit/DOC-CLAIMS.md(Task 1 骨架——按覆盖总表登记的销号节写入)
    - .planning/audit/HYPOTHESES.md(HYP-16 已回填条目的证据行,:159-166——文档口径半句核对的代码侧靠山;HYP-21 假设原文 :208-213)
    - .planning/audit/HANDOFF-PHASE4.md(DOC 节第 1 条:HYP-16 文档一致性半句移交原文与证据)
  </read_first>
  <action>
    用 `git show '5927f36:docs/v1.0.0 prd/PRD_v1.md'`(路径空格引号,Pitfall 6)通读并抽取『可与代码/配置对照的声明』成清单(编号 P-01 起),逐条四态销号,每条附文档侧与代码侧双行号证据 `@ 5927f36`:

    - 抽取粒度(D-07 + Claude 裁量):命令/路径/常量/接口声明逐条;叙事类按声明句合并;纯产品愿景叙事不成声明的不入清单。
    - 重点声明类:数值边界(上传上限 50 MB ↔ `fc_shared/env.py` MAX_UPLOAD_BYTES 默认 52428800;分片阈值 600 s ↔ `config.js` CHUNK_MAX_DURATION_SECONDS;重试 5/15/45 ↔ `nls.py` RETRY_DELAYS_SECONDS / `uploader.js` RETRY_DELAYS_MS)、流程步骤(录音→STS→上传→verify→Worker 转写)、产物形态(5 artifacts、transcript 字段)、验收口径。
    - HYP-21 专项核对:PRD 是否明示转写产物展示/日稿/LLM 润色为 MVP 范围外——找到范围声明的具体行号,与代码实态(无任何读取 UI)口径互证,登记结论行,判定态 + `→ HYP-21(04-09 回填锚点)`。
    - HYP-16 半句专项核对(HANDOFF DOC 第 1 条销号的一半):PRD 对单机单用户/Worker 离线滞留/本地盘权威存储边界的声明,与 HYP-16 既有代码侧证据(`poller.py:378-391`、`pipeline.py:15-18` 等,直接引 HYPOTHESES.md HYP-16 证据行,不重复采证)口径核对,登记结论行 + `→ HYP-16(半句;销号引 HANDOFF-PHASE4.md DOC 节第 1 条)`。tech-spec/runbook 侧的同口径核对由 Task 3 与 04-04 补齐。
    - 纯云端事实(如 Aliyun 侧配置数值)标『无法静态核实』。
    - 节尾机械对账行:P 条目总数 + 四态计数等式(仿 CONTRACT-MATRIX 完成判定节)。
  </action>
  <verify>
    <automated>grep -c '^| P-' .planning/audit/DOC-CLAIMS.md && grep -c 'HYP-21' .planning/audit/DOC-CLAIMS.md && grep -c 'HYP-16' .planning/audit/DOC-CLAIMS.md</automated>
  </verify>
  <acceptance_criteria>
    - PRD 节存在且清单行 `grep -c '^| P-' .planning/audit/DOC-CLAIMS.md` ≥ 10
    - 每条清单行含两处 `@ 5927f36` 证据(抽查任意 3 行)且判定列为四态之一
    - `grep -c 'HYP-21' 该文件` ≥ 1 且 `grep -c 'HYP-16' 该文件` ≥ 1(两条专项结论行在档)
    - PRD 节尾有四态计数等式行(计数之和 = 条目总数)
  </acceptance_criteria>
  <done>PRD 全部可核声明四态销号完毕,HYP-21/HYP-16 核对结论行落档待 04-09 引用</done>
</task>

<task type="auto">
  <name>Task 3: tech-spec.md 深核销号与本批次 F-DOC 立条</name>
  <files>.planning/audit/DOC-CLAIMS.md, .planning/audit/findings/docs-config.md</files>
  <read_first>
    - .planning/audit/findings/docs-config.md(F-DOC-00 schema 示例与 `## 发现` 写入位置 :21)
    - .planning/audit/findings/code.md(:23 批次导语 blockquote 先例与九字段实战条目)
    - .planning/audit/CHARTER.md(严重度锚点:MEDIUM『可诱发高危误操作的误导性文档』/ LOW『文档死链/路径失效』/ INFO『存在级观察』;九字段 schema :139-163)
  </read_first>
  <action>
    1. 用 `git show '5927f36:docs/v1.0.0 prd/tech-spec.md'` 抽取声明清单(编号 T-01 起)并四态销号,方法同 Task 2。重点:
       - §3.1 域名/URL 声明 ↔ `apps/miniprogram/config.js` 常量(FC 两 URL 含 `issue-cedential` 拼写、OSS 上传域名)——本任务销**文档侧**行并备注『配置侧对照行见 config.js 节(04-05)』互指;`issue-cedential` 命中走负面清单:核实结论 + 闭环 DNF-02,不立 F-DOC。
       - 架构/流程声明 ↔ 代码实态:7 阶段管线(`pipeline.py` STAGE_* 常量)、5 artifacts 完成态(audio.wav/manifest.json/transcript.json/transcript.txt/.done)、原子三步写协议(temp → rename → .done 最后)、错误码常量表(`fc_shared/errors.py`)、STS 单 key policy(`fc_shared/sts.py:62-73`,可直引 HYPOTHESES.md HYP-09 证据行)。
       - dead-ref 检查:tech-spec 内全部相对路径/文档引用经 `git ls-tree -r --name-only 5927f36` 存在性核对;旧路径命中登记 dead-ref + `→ HYP-02`(聚合立条留 04-05,本任务只登记)。
       - HYP-16 半句:tech-spec 对单机单用户容量边界的声明口径核对,登记结论行(方法同 Task 2)。
    2. 本批次发现立条(仅 PRD/tech-spec 两文档中判定为 drift 且达发现级的条目):写入 findings/docs-config.md `## 发现` 节之下——先批次导语 blockquote(『04-03 判定产物(PRD + tech-spec 深核):共 N 条发现——…;DNF 对照命中不立发现清单;dead-ref 登记 M 处待 04-05 聚合』),再逐条九字段(字段顺序照 F-DOC-00 示例;维度行固定 `- **维度:** 文档配置一致性 (DOC)`;严重度理由格式『严重度 — 影响:…;可能性:…』;状态 draft;上线判定留空)。编号从 F-DOC-01 起(F-DOC-00 为示例,立条前以 `grep -c '^### F-DOC-' findings/docs-config.md` 确认续接基数)。若无发现级 drift,批次导语显式写『0 条发现,显式无发现记录』。
    3. DOC-CLAIMS 覆盖总表:PRD_v1.md 与 tech-spec.md 两行状态列改为终态(『已审无发现』或指针)。
  </action>
  <verify>
    <automated>grep -c '^| T-' .planning/audit/DOC-CLAIMS.md && grep -c '04-03 判定产物' .planning/audit/findings/docs-config.md</automated>
  </verify>
  <acceptance_criteria>
    - tech-spec 节清单行 `grep -c '^| T-' .planning/audit/DOC-CLAIMS.md` ≥ 10,节尾四态计数等式在档
    - `grep -c 'DNF-02' .planning/audit/DOC-CLAIMS.md` ≥ 2(负面清单 + tech-spec 节核实结论行)
    - findings/docs-config.md 含 `04-03 判定产物` 批次导语(0 条发现时亦有显式记录)
    - 新立 F-DOC 条目(若有)九字段齐全:每条 `grep -A12 '^### F-DOC-0[1-9]'` 输出含 维度/严重度/证据/修复建议/工作量/关联发现/上线判定/状态 八个字段行
    - 覆盖总表 PRD/tech-spec 两行状态列非初始占位词
  </acceptance_criteria>
  <done>权威链头两文档销号完毕并立条;dead-ref 命中已登记指针待 04-05 聚合;编号基准与批次导语范式为后续计划钉定</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计产物 → 读者 | 台账若复制文档中的凭证类内容会二次泄露 |
| 审计写入 → 封版产物/被审源码 | 误写 docs/ 或封版 .planning 产物破坏零 diff 与封版纪律 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-06 | Information Disclosure | DOC-CLAIMS/findings 证据字段 | medium | mitigate | CHARTER 秘密红线:凭证模式只记 `path:line @ 5927f36` + 模式名,不复制值本体 |
| T-04-07 | Tampering | docs/、封版产物(CONTRACT-MATRIX 等) | high | mitigate | 本计划 files_modified 仅两个新写入点;纯静读取证(git show),不执行任何脚本;收尾零 diff 由 04-09 统一验证 |
</threat_model>

<verification>
- DOC-CLAIMS.md 骨架四段 + PRD/tech-spec 两节成档,每条销号行双侧行号证据
- findings/docs-config.md 批次导语与条目(或显式无发现)在档
- `git diff --stat 5927f36 -- apps/ scripts/ docs/` 为空
</verification>

<success_criteria>
- AUDIT-03 最重两份深核对象销号完毕,可机械验收(清单计数等式 + 覆盖总表状态)
- HYP-16 半句/HYP-21 的 DOC 侧结论锚点就位,04-09 回填可直接引用
</success_criteria>

<output>
完成后创建 `.planning/phases/04-docs-config-test-audit/04-03-SUMMARY.md`(记录 F-DOC 已用最大编号,供 04-04 续接)
</output>
