---
phase: 04-docs-config-test-audit
plan: 7
type: execute
wave: 2
depends_on: ["04-06"]
files_modified:
  - .planning/audit/TEST-AUDIT.md
autonomous: true
requirements: [AUDIT-04]
must_haves:
  truths:
    - "41 个测试模块(pytest 24+7、node:test 10)逐个按 8 面过完,台账行已过面列全部 8/8,线索登记入备注列"
    - "HYP-23 专项结论在档:fc/tests 对两个 handler.py 的错误码路径行为覆盖逐码登记,只验补偿充分性、不质疑 mypy 豁免(DNF-03 交叉引用)"
    - "HYP-24 专项证据在档:node 测试对 pages/ 的加载边界(uploader.test.js 加载 uploads.js;index.js 796 行胶水是否有任何测试加载)有明确登记"
    - "反向映射清单占位态全部补证销号"
  artifacts:
    - ".planning/audit/TEST-AUDIT.md — 41 行台账全量回填 + 反向映射终态"
  key_links:
    - "台账线索登记与反向映射终态 → 04-08 F-TEST 按面聚合立条"
    - "HYP-23/HYP-24 专项结论行 → 04-09 回填锚点"
---

<objective>
按 D-10 对全部 41 个测试模块逐面普审(8 面),回填 TEST-AUDIT.md 逐对象台账;完成 HYP-23(handler 行为测试补偿)与 HYP-24(pages 胶水层)两个专项的证据登记;把 04-06 反向映射清单的占位态行全部补证为终态(AUDIT-04)。

Purpose: 『每个测试模块逐面过并入覆盖台账』是 D-10 的硬要求;缺口候选线索在本计划收齐,判断(立条)留 04-08。
Output: TEST-AUDIT.md 台账全量回填、反向映射终态、两个专项结论行。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/audit/TEST-AUDIT.md
@.planning/codebase/TESTING.md
@.planning/phases/04-docs-config-test-audit/04-06-SUMMARY.md
</context>

## Artifacts this phase produces

见 04-01-PLAN.md 同名节(阶段全量清单)。本计划续写:`.planning/audit/TEST-AUDIT.md`(台账回填与反向映射终态)。

<tasks>

<task type="auto">
  <name>Task 1: apps/worker/tests 24 文件逐面普审</name>
  <files>.planning/audit/TEST-AUDIT.md</files>
  <read_first>
    - .planning/audit/TEST-AUDIT.md(8 面定义与台账行格式——严格按 04-06 定稿的分面过,不增删面)
    - .planning/audit/COVERAGE.md(:30-32 台账行备注列的证据行号与去向写法先例)
  </read_first>
  <action>
    对 `apps/worker/tests/` 全部 24 个 test_*.py 逐个用 `git show 5927f36:<path>` 读取并按 8 面过账(每文件一行回填:已过面 8/8;产出列『无缺口线索』或线索简述;备注列关键证据行号 @ 5927f36 与去向指针):

    - 面④契约常量锁定重点核对:test_nls.py 对 RETRY_DELAYS_SECONDS/RESIGN_THRESHOLD_SECONDS 的字面断言、test 系对错误码字符串/object key 格式(`recordings/<date>/<id>.wav`)的锁定情况——有锁定的记为反向映射兜底证据(行号级),无锁定的记缺口线索 `→ 04-08 候选面`。
    - 面⑥静默 skip:`git grep -n 'skipif\|skip(' 5927f36 -- apps/worker/tests` 全量命中逐处登记;test_miniprogram_js.py:24 的 node 缺失 skipif 已知线索登记 `→ D-11(04-08 三方对照)`。
    - 面②fake 漂移:FakeSource 等手写 fake 与真实 Protocol 实现(poller.py RealOssSource 等)的行为差异风险点抽查登记(不下判断,只记线索)。
    - 面①③⑤⑦⑧按面定义逐文件扫读登记;『已审无线索』也逐行显式落账(用户可机械验收要求)。
    - 备注列同步反填反向映射:普审中撞见的 F-* 兜底证据(测试文件:行号)补入反向映射对应行。
  </action>
  <verify>
    <automated>grep -c '8/8' .planning/audit/TEST-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - worker 侧 24 行台账已过面列全部 8/8(`grep -c '8/8'` 本任务后 ≥ 24)
    - `grep -c 'skipif' .planning/audit/TEST-AUDIT.md` ≥ 1(面⑥全量命中登记在档)
    - `grep -c 'RETRY_DELAYS' 该文件` ≥ 1(面④契约常量核对在档)
    - 每行产出/备注列非空(『无缺口线索』亦显式)
  </acceptance_criteria>
  <done>worker 侧 24 文件逐面过完,线索与兜底证据双向登记</done>
</task>

<task type="auto">
  <name>Task 2: apps/fc/tests 7 文件逐面普审与 HYP-23 专项</name>
  <files>.planning/audit/TEST-AUDIT.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(HYP-23 假设原文 :224-230——含『仅验证行为测试补偿充分,不质疑豁免本身』边界与 DNF-03 交叉引用条款)
    - .planning/audit/DO-NOT-FIX.md(DNF-03 条目 :28-35——豁免裁定原文)
  </read_first>
  <action>
    1. 对 `apps/fc/tests/` 全部 7 个 test_*.py 按 8 面过账(方法同 Task 1)。
    2. HYP-23 专项(可机械销号的逐码登记):以 `fc_shared/errors.py` 的稳定错误码全集为行(INVALID_CODE、OPENID_NOT_ALLOWED、SIZE_EXCEEDED、STS_ISSUE_FAILED、OBJECT_NOT_FOUND、SIZE_MISMATCH、SERVER_MISCONFIGURED,以 `git show 5927f36:apps/fc/shared/fc_shared/errors.py` 实际枚举为准),逐码检索 fc/tests 中的行为覆盖(`git grep -n '<错误码>' 5927f36 -- apps/fc/tests`),登记『有测试(文件:行号)/ 无』;另登记两个 handler.py 入口路径(GET 存活探针、POST 主流程、异常分支)在测试中被驱动的证据。结论行:补偿覆盖面的事实清单 + `→ HYP-23(04-09 回填锚点;充分性判断在回填时依此清单下,不质疑 DNF-03 豁免本身)`。
    3. 台账 7 行回填,反向映射相关行补证。
  </action>
  <verify>
    <automated>grep -c 'HYP-23' .planning/audit/TEST-AUDIT.md && grep -c 'DNF-03' .planning/audit/TEST-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - fc 侧 7 行台账已过面列全部 8/8
    - HYP-23 专项表逐错误码成行(行数 = errors.py 实际枚举数),每行覆盖判定非空
    - 专项结论行同时含 `HYP-23` 与 `DNF-03`(交叉引用条款落地)
  </acceptance_criteria>
  <done>fc 侧普审完毕;HYP-23 的补偿事实清单可机械复核,回填判断材料齐备</done>
</task>

<task type="auto">
  <name>Task 3: node:test 10 文件逐面普审、HYP-24 专项与反向映射收敛</name>
  <files>.planning/audit/TEST-AUDIT.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(HYP-24 假设原文 :232-237——pages 胶水层 vs 纯 utils 的测试边界)
    - .planning/audit/scans/coverage-node.md(若已产出:pages/ 覆盖数据佐证;未产出不阻塞,留指针由 04-08 补引)
  </read_first>
  <action>
    1. 对 `apps/miniprogram/test/` 全部 10 个 *.test.js 按 8 面过账:面④重点核对 JS 侧镜像常量断言(RETRY_DELAYS_MS、MAX_UPLOAD_RETRIES、错误码字符串与 Python 侧的字面一致性锁定);面③核对 mock wx 注入与 storage 状态隔离惯例。
    2. HYP-24 专项:`git grep -n "require.*pages/" 5927f36 -- apps/miniprogram/test` 全量命中——登记哪些页面文件被测试真实加载(TESTING.md 已知:uploader.test.js 加载 pages/uploads/uploads.js 的 Page harness 模式)、`pages/index/index.js`(796 行,`git show 5927f36:apps/miniprogram/pages/index/index.js | wc -l` 核实)是否被任何测试加载。结论行:页面级加载边界事实 + `→ HYP-24(04-09 回填锚点;coverage-node.md 的 pages/ 数据为实测佐证)`。
    3. 反向映射收敛:把 04-06 留下的全部占位态行改为终态判定(兜底证据或『无』+ 定级参照);节尾机械对账行更新(终态 = 行总数,占位 = 0)。
    4. 台账全表核查:41 行已过面列全部 8/8,产出/备注列无空格。
  </action>
  <verify>
    <automated>grep -c '8/8' .planning/audit/TEST-AUDIT.md | grep -cx '41' && grep -c '补证中' .planning/audit/TEST-AUDIT.md | grep -cx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '8/8' .planning/audit/TEST-AUDIT.md` = 41(全表过面;若 04-06 头部注记的实测对象数非 41,以该数为准)
    - `grep -c '补证中' .planning/audit/TEST-AUDIT.md` 输出 0(反向映射无占位残留)
    - `grep -c 'HYP-24' 该文件` ≥ 1 且结论行含 index.js 是否被加载的明确判定
    - `grep -c 'RETRY_DELAYS_MS' 该文件` ≥ 1(JS 侧镜像常量核对在档)
  </acceptance_criteria>
  <done>双语言 41 模块逐面普审闭合;反向映射全终态;HYP-23/24 回填材料齐备,判断层交 04-08</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计产物 → 读者 | 测试文件中若含 fixture 凭证模式,引用须走秘密红线 |
| 审计写入 → 封版产物/被审源码 | 只读取证,唯一写入点 TEST-AUDIT.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Information Disclosure | 台账备注列证据 | medium | mitigate | fixture/样本中的凭证模式只记位置+模式名(CHARTER 秘密红线) |
| T-04-16 | Tampering | apps/ 测试源码与封版产物 | high | mitigate | 纯 `git show`/`git grep` 静读;files_modified 仅 TEST-AUDIT.md;不执行任何测试(实跑证据已由 04-01/04-02 归档) |
</threat_model>

<verification>
- TEST-AUDIT.md 41 行台账全量 8/8,反向映射终态,机械对账行闭合
- `git diff --stat 5927f36 -- apps/ scripts/ docs/` 为空
</verification>

<success_criteria>
- D-10 逐模块逐面普审可机械验收(41 × 8/8,零占位)
- 04-08 立条所需的全部线索(缺口候选、兜底证据、专项事实清单)集齐
</success_criteria>

<output>
完成后创建 `.planning/phases/04-docs-config-test-audit/04-07-SUMMARY.md`(汇总缺口候选面清单,供 04-08 聚合)
</output>
