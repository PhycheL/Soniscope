---
phase: 04-docs-config-test-audit
plan: 2
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - .planning/audit/scans/coverage-pytest.md
  - .planning/audit/scans/coverage-node.md
autonomous: false
requirements: [AUDIT-04]
user_setup:
  - service: pypi
    why: "pytest-cov 临时注入(D-03)在包合法性协议下判 [SUS](数据缺口所致),首次注入前需人工确认包合法性"
    env_vars: []
    dashboard_config:
      - task: "在 https://pypi.org/project/pytest-cov/ 目视确认包归属 pytest-dev 官方组织"
        location: "PyPI 包页面"
must_haves:
  truths:
    - "Python 侧覆盖率经 pytest-cov 临时注入实测完成,数字与命令、工具版本一起归档,仓库零配置写入"
    - "JS 侧覆盖率经 node 内置 --experimental-test-coverage 实测完成,数字标注 experimental 来源,与 Python 侧同格式归档"
    - "两份归档均为纯输入证据:仅按模块/文件罗列数字,无任何阈值判断或质量结论"
    - "worktree 专区已拆除,git worktree list 无残留"
  artifacts:
    - ".planning/audit/scans/coverage-pytest.md — Python 覆盖率归档(命令+版本+模块级数字+口径备注)"
    - ".planning/audit/scans/coverage-node.md — JS 覆盖率归档(experimental 标注+文件级数字+pages/ 出现注记)"
  key_links:
    - "coverage-pytest.md / coverage-node.md → 04-08 反向映射兜底证据与 F-TEST 条目的证据字段"
    - "coverage-node.md 的 pages/ 覆盖数据 → HYP-24 证据点(04-08/04-09 消费)"
---

<objective>
按 D-03(pytest-cov 命令行临时注入,零仓库写入)与 D-04(node 内置 experimental 覆盖率,绕过 pytest 桥)在 04-01 建立的 worktree 基线专区内完成双语言覆盖率实测,归档至 `.planning/audit/scans/`,随后拆除专区。

Purpose: 覆盖率测量结果作为 AUDIT-04 的输入证据归档(成功判据 3:仅证据,不作质量结论);双侧数字对称,供 04-08 反向映射与 F-TEST 条目引用。
Output: scans/coverage-pytest.md、scans/coverage-node.md;worktree 专区拆除。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-docs-config-test-audit/04-RESEARCH.md
@.planning/audit/scans/gate-run-worktree.md
@.planning/phases/04-docs-config-test-audit/04-01-SUMMARY.md
</context>

## Artifacts this phase produces

见 04-01-PLAN.md 同名节(阶段全量清单)。本计划创建:`.planning/audit/scans/coverage-pytest.md`、`.planning/audit/scans/coverage-node.md`;本计划拆除 worktree 专区。

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: pytest-cov 包合法性确认(首次注入前)</name>
  <files>无文件产出(人工确认门,阻塞 Task 2 的首次 `uv run --with pytest-cov` 注入)</files>
  <action>暂停并请人工确认 pytest-cov 包合法性,获确认前不得执行任何 pytest-cov 注入命令(包合法性协议:[SUS] 包安装前置 blocking-human 检查点,不可自动通过)。</action>
  <what-built>尚未执行任何 pytest-cov 注入。04-RESEARCH Package Legitimacy Audit 对 pytest-cov 判 [SUS],判定原因为 seam 数据缺口(unknown-downloads / no-repository),非包本身可疑信号。缓解证据已在案:`pip index versions pytest-cov` 实测确认 54 个历史版本、最新 7.1.0、版本链自 0.6 起 15+ 年;D-03 为用户锁定决策且点名该包。</what-built>
  <how-to-verify>
    1. 打开 https://pypi.org/project/pytest-cov/
    2. 确认维护者/主页指向 pytest-dev 官方组织(github.com/pytest-dev/pytest-cov)
    3. 确认最新版本号与研究记录(7.1.0 量级)一致
  </how-to-verify>
  <verify>人工回复 "approved"(或提出疑点)后方可进入 Task 2</verify>
  <done>用户已确认包合法性(approved),或已选定回退方案 `--with coverage` + coverage run 并在归档中注记</done>
  <resume-signal>输入 "approved" 继续覆盖率实测;或描述疑点(将改用回退方案 `--with coverage` + coverage run)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Python 覆盖率实测与归档(D-03)</name>
  <files>.planning/audit/scans/coverage-pytest.md</files>
  <read_first>
    - .planning/audit/scans/gate-run-worktree.md(worktree 专区路径备注——路径失效则按备注命令重建)
    - .planning/audit/scans/gates-baseline.md(归档格式先例:头部 per D-NN 依据 + 工具版本行 + 命令/结果/注记)
    - .planning/phases/04-docs-config-test-audit/04-RESEARCH.md(§Code Examples『Python 覆盖率』与 Assumptions Log A1 冒烟配方)
  </read_first>
  <action>
    全部命令在 worktree 专区内执行(专区路径读自 gate-run-worktree.md 备注;若失效按 `git worktree add <scratchpad>/wt-5927f36 5927f36` + `uv sync --frozen` 重建):

    1. A1 冒烟(先单文件验证 `--cov=fc_shared` 对 pythonpath 导入包的统计口径):`uv run --frozen --with pytest-cov pytest apps/fc/tests/test_sts.py --cov=fc_shared --cov-report=term`(若该文件名在基线不存在,取 `apps/fc/tests/` 下任一测试文件)。若输出的覆盖表中 fc_shared 无数据行,按 A1 回退改用路径形式 `--cov=apps/fc/shared` 并在归档注记该口径替换。
    2. 全量实测:`uv run --frozen --with pytest-cov pytest --cov=soniscope_worker --cov=fc_shared --cov-report=term-missing 2>&1 | tee <scratchpad>/coverage-pytest-raw.txt`(fc_shared 参数按冒烟结论取定)。`--with` 为 ephemeral overlay,不改 .venv 声明、不改 pyproject/uv.lock(D-03 零仓库写入)。
    3. 版本采集:`uv --version`;`uv run --frozen --with pytest-cov pytest --version`;`uv run --frozen --with pytest-cov python -c "import importlib.metadata as m; print(m.version('pytest-cov'))"`。
    4. 创建 `.planning/audit/scans/coverage-pytest.md`,结构仿 gates-baseline.md:文件头(标题『扫描档案:Python 覆盖率实测(worktree 专区,pytest-cov 临时注入)』+ Created/基线行 + 依据句 per D-01/D-02/D-03 + Task 1 检查点确认记录一句 + 工具版本行)→ 冒烟节(命令+结论+口径取定)→ 全量节(命令原文 + 按模块/文件的行覆盖数字表,照抄 term-missing 输出的 Stmts/Miss/Cover 列)→ 口径备注节:①pytest-cov 对 `test_miniprogram_js.py` 子进程内的 node 完全盲,JS 侧数字见 coverage-node.md,两档口径互不重叠(Pitfall 3,D-04 双侧分测原因);②本档为纯输入证据,数字仅按模块罗列,不附带任何阈值判断或质量结论(成功判据 3);③归档措辞不得使用会被阶段机械验收负向检索的定性字样(以 04-08 acceptance 的负向 grep 词为准)→ 尾注落款(含模块行数计数)。
  </action>
  <verify>
    <automated>test -f .planning/audit/scans/coverage-pytest.md && grep -c 'pytest-cov' .planning/audit/scans/coverage-pytest.md && grep -c 'soniscope_worker' .planning/audit/scans/coverage-pytest.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audit/scans/coverage-pytest.md` 存在,含冒烟节与全量节各一
    - `grep -c 'D-03' 该文件` ≥ 1 且 `grep -c '--with pytest-cov' 该文件` ≥ 1(依据与命令原文在档)
    - 覆盖数字表含 `soniscope_worker` 与 fc_shared(或注记后的路径口径)两个包的模块行
    - 主仓 `git status --porcelain -- pyproject.toml uv.lock apps/worker/pyproject.toml` 输出为空(零配置写入)
  </acceptance_criteria>
  <done>Python 覆盖率数字与命令、版本、口径备注完整归档;冒烟先行,A1 回退路径留痕;仓库零写入</done>
</task>

<task type="auto">
  <name>Task 3: JS 覆盖率实测归档与专区拆除(D-04)</name>
  <files>.planning/audit/scans/coverage-node.md</files>
  <read_first>
    - .planning/audit/scans/gate-run-worktree.md(专区路径与拆除说明)
    - .planning/phases/04-docs-config-test-audit/04-RESEARCH.md(§Code Examples『JS 覆盖率』与 Pitfall 4:排除测试文件自身)
    - .planning/audit/scans/vulture.md(对账等式收尾写法)
  </read_first>
  <action>
    1. worktree 专区根目录内执行:`node --test --experimental-test-coverage --test-coverage-exclude='apps/miniprogram/test/**' apps/miniprogram/test/*.test.js 2>&1 | tee <scratchpad>/coverage-node-raw.txt`(绕过 pytest 桥直跑,不改桥代码,D-04;`--test-coverage-exclude` 控噪,Pitfall 4)。记录 `node --version`。
    2. 创建 `.planning/audit/scans/coverage-node.md`,结构同 coverage-pytest.md:文件头(标题『扫描档案:JS 覆盖率实测(worktree 专区,node 内置 experimental)』+ Created/基线行 + 依据句 per D-01/D-02/D-04 + 工具版本行)→ 命令节(命令原文 + 按文件的行覆盖数字表,照抄 node coverage 报告)→ 口径备注节:①数字来源为 node `--experimental-test-coverage`,**experimental 标注为硬要求(D-04)**;②报告中出现 `apps/miniprogram/pages/uploads/uploads.js` 系 uploader.test.js 加载真实页面文件所致——这是 HYP-24(pages 胶水层测试边界)的有价值证据点,登记指针 `→ HYP-24(04-08/04-09 消费)`;③确认报告无 `*.test.js` 行(排除已生效;若仍出现则分行显式标注);④与 coverage-pytest.md 口径互不重叠备注(双语言证据对称);⑤纯输入证据、无阈值判断,措辞约束同 coverage-pytest.md 口径备注第③点 → 尾注落款(文件行数计数)。
    3. 拆区(D-02 用完即拆):主仓内执行 `git -C /Volumes/Data/ProjectCode/my_soniscope worktree remove --force "<专区路径>"`(`--force` 因 .venv/.pytest_cache 等未跟踪文件,Pitfall 1),随后 `git worktree list` 确认无 wt-5927f36 残留、`git worktree prune` 兜底。
    4. 零 diff 快查:`git diff --stat 5927f36 -- apps/ scripts/ docs/`(期望空),结果记入 coverage-node.md 尾部一行。
  </action>
  <verify>
    <automated>test -f .planning/audit/scans/coverage-node.md && grep -ci 'experimental' .planning/audit/scans/coverage-node.md && git -C /Volumes/Data/ProjectCode/my_soniscope worktree list | grep -c 'wt-5927f36' | grep -cx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audit/scans/coverage-node.md` 存在,`grep -ci 'experimental' 该文件` ≥ 2(标题/备注双处标注)
    - `grep -c 'HYP-24' 该文件` ≥ 1(pages/ 证据指针在档)
    - `grep -c 'test-coverage-exclude' 该文件` ≥ 1(控噪命令原文在档)
    - `git worktree list | grep -c 'wt-5927f36'` 输出 0(专区已拆除)
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>JS 覆盖率带 experimental 标注归档,pages/ 证据点登记 → HYP-24;worktree 专区拆除无残留;零 diff 通过</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI → 专区 venv | pytest-cov 临时注入引入外部包代码 |
| 审计执行 → 云端 | 覆盖率命令面若越界会触真云 IO |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC | Tampering | pip/uv 包安装(pytest-cov [SUS]) | high | mitigate | Task 1 blocking-human 检查点(不可自动通过);仅 ephemeral `--with` 注入,零仓库配置写入;疑点时回退 `--with coverage` |
| T-04-04 | Elevation of Privilege | 覆盖率实跑命令面 | high | mitigate | 白名单仅 pytest(全 fake 零云 IO,TESTING.md 核实)与 node --test;绝不执行 test-*/verify-*/被审脚本(D-01) |
| T-04-05 | Information Disclosure | scans 归档内容 | medium | mitigate | 归档仅含覆盖数字与文件路径;凭证模式仅记位置+模式名(CHARTER 秘密红线) |
</threat_model>

<verification>
- 两份覆盖率归档成档,双语言证据对称,均为纯输入证据(无定性结论语言)
- worktree 专区拆除,`git worktree list` 无残留
- 主仓零 diff、零配置写入
</verification>

<success_criteria>
- 成功判据 3 落实:覆盖率作为证据归档,数字未被用作质量结论
- 04-08 所需覆盖率证据(含 HYP-24 的 pages/ 数据点)全部就位
</success_criteria>

<output>
完成后创建 `.planning/phases/04-docs-config-test-audit/04-02-SUMMARY.md`
</output>
