---
phase: 04-docs-config-test-audit
plan: 4
type: execute
wave: 2
depends_on: ["04-03"]
files_modified:
  - .planning/audit/DOC-CLAIMS.md
  - .planning/audit/findings/docs-config.md
autonomous: true
requirements: [AUDIT-03]
must_haves:
  truths:
    - "runbook 4 份(cloud-setup/deployment-guide/fc-deploy/mvp-acceptance)的可核声明全部四态销号,每条双行号证据 @ 5927f36"
    - "HYP-14 核对结论在档:发布文档是否覆盖 config.js ENV 常量的生产翻转步骤有明确判定,HANDOFF DOC 第 2/3 条移交证据被显式引用"
    - "纯控制台/云端事实一律标『无法静态核实』,无猜测判定"
  artifacts:
    - ".planning/audit/DOC-CLAIMS.md — runbook 4 节销号清单"
    - ".planning/audit/findings/docs-config.md — 本计划批次 F-DOC 条目(runbook 侧 MEDIUM 锚点适用)"
  key_links:
    - "HYP-14 结论行 → 04-09 回填锚点 + HANDOFF-PHASE4.md DOC 第 2/3 条销号"
    - "runbook 步骤 ↔ fc_deploy.py 能力面对照 → HYP-04 既有回填的『runbook 保真度』口径闭环"
---

<objective>
按 D-05/D-07 完成 runbook 4 份深核对象的声明抽取与四态销号:`docs/runbook/cloud-setup.md`(461 行)、`docs/runbook/mvp-acceptance.md`(217 行)、`docs/runbook/deployment-guide.md`(487 行)、`docs/runbook/fc-deploy.md`(670 行);完成 HYP-14(ENV 发布翻转口径)的全文档检索核对(HANDOFF DOC ×2 销号);发现级 drift 立 F-DOC 条目(AUDIT-03)。

Purpose: runbook 是『可诱发高危误操作的误导性文档』MEDIUM 锚点的主战场——环境重建完全依赖 runbook 保真度(HYP-04 已证实),步骤与实态的漂移直接构成上线风险。
Output: DOC-CLAIMS.md runbook 4 节;findings/docs-config.md 本批次条目与批次导语。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-docs-config-test-audit/04-PATTERNS.md
@.planning/audit/HANDOFF-PHASE4.md
@.planning/phases/04-docs-config-test-audit/04-03-SUMMARY.md
</context>

## Artifacts this phase produces

见 04-01-PLAN.md 同名节(阶段全量清单)。本计划续写:`.planning/audit/DOC-CLAIMS.md`(runbook 4 节)、`.planning/audit/findings/docs-config.md` `## 发现` 节。

<tasks>

<task type="auto">
  <name>Task 1: cloud-setup.md 与 mvp-acceptance.md 深核销号</name>
  <files>.planning/audit/DOC-CLAIMS.md</files>
  <read_first>
    - .planning/audit/DOC-CLAIMS.md(04-03 骨架与 PRD/tech-spec 两节——沿用同一清单行格式与编号风格)
    - .planning/audit/CHARTER.md(证据提取命令与秘密红线)
  </read_first>
  <action>
    1. `git show 5927f36:docs/runbook/cloud-setup.md` 抽取声明清单(编号 CS-01 起)并四态销号:
       - §3-4(FC 函数配置、OSS 桶设置)与静态可核对象对照:FC 运行时 env 变量名清单 ↔ `apps/fc/shared/fc_shared/env.py:16-38`(OSS_BUCKET/OSS_REGION/OSS_ENDPOINT/WX_APPID/WX_APP_SECRET/OPENID_ALLOWLIST/RAM_ROLE_ARN/ALIYUN_AK_ID/ALIYUN_AK_SECRET/MAX_UPLOAD_BYTES);URL/域名声明 ↔ `apps/miniprogram/config.js`(config.js 头注自证与 cloud-setup §3-4 一致——本任务销文档侧行并与 04-05 config.js 节互指);桶名 soniscope-audio、区域 cn-beijing、object key 前缀 `recordings/` ↔ `fc_shared/sts.py` 与 `oss_admin.py`。
       - 纯控制台操作事实(RAM 角色创建、触发器配置、控制台截图步骤、实际线上资源规格)标『无法静态核实』,不猜测(D-07)。
    2. `git show 5927f36:docs/runbook/mvp-acceptance.md` 抽取声明清单(编号 MA-01 起)并销号:验收步骤中引用的 make 目标存在性 ↔ `git show 5927f36:Makefile`(目标名逐一 grep 核对);引用的路径/文档 dead-ref 检查(`git ls-tree -r --name-only 5927f36`);验收判据中的常量声明(计数、文件名)↔ 代码实态。注意:本任务只核对『目标是否存在与声明是否一致』,绝不执行任何 make 验收目标(D-01 红线——mvp-acceptance 引用的多为真云目标)。
    3. 两节各以四态计数等式收尾;覆盖总表两行状态列改终态。
  </action>
  <verify>
    <automated>grep -c '^| CS-' .planning/audit/DOC-CLAIMS.md && grep -c '^| MA-' .planning/audit/DOC-CLAIMS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| CS-' .planning/audit/DOC-CLAIMS.md` ≥ 8 且 `grep -c '^| MA-' 该文件` ≥ 5
    - `grep -c '无法静态核实' 该文件` ≥ 1(控制台事实至少一处如实标注)
    - 两节尾均有四态计数等式行
    - 执行历史无任何 `make test-`/`make verify-`/`make deploy-` 调用
  </acceptance_criteria>
  <done>cloud-setup 与 mvp-acceptance 全部可核声明销号完毕,云端事实无猜测</done>
</task>

<task type="auto">
  <name>Task 2: deployment-guide.md 与 fc-deploy.md 深核销号 + HYP-14 全文档核对</name>
  <files>.planning/audit/DOC-CLAIMS.md</files>
  <read_first>
    - .planning/audit/HANDOFF-PHASE4.md(DOC 节第 2/3 条:HYP-14 ×2 移交原文——config.js:29 ENV 现值与 dev 门控实现证据)
    - .planning/audit/HYPOTHESES.md(HYP-14 假设原文 :141-146;HYP-04 已回填条目 :46-53——fc_deploy 能力面五项证据,runbook 对照的代码侧靠山)
  </read_first>
  <action>
    1. `git show 5927f36:docs/runbook/deployment-guide.md` 抽取声明清单(编号 DG-01 起)并销号:部署步骤中的命令原文 ↔ Makefile 目标实态;部署前检查清单声明 ↔ 代码实态;引用路径 dead-ref 检查。
    2. `git show 5927f36:docs/runbook/fc-deploy.md` 抽取声明清单(编号 FD-01 起)并销号:runbook 步骤 ↔ `fc_deploy.py` 能力面(直引 HYPOTHESES.md HYP-04 证据行:能力面确为 backup/package/update_code/rollback/logs 五项,`fc_deploy.py:13,106-119,667-672,611 @ 5927f36`,不重复采证)——runbook 是否声称了工具不具备的能力(如函数创建/env 配置/触发器)即 drift 候选;`make deploy-fc FUNCTION=...` 命令口径 ↔ Makefile;`ALIYUN_DEPLOY_AK_ID`/`ALIYUN_DEPLOY_AK_SECRET` 环境变量名 ↔ fc_deploy.py 实际读取名。
    3. HYP-14 专项核对(HANDOFF DOC 第 2/3 条销号):`git grep -n 'ENV' 5927f36 -- docs/ AGENTS.md` 与 `git grep -n 'production' 5927f36 -- docs/ AGENTS.md` 全量检索,判定:发布清单/runbook/任何文档是否覆盖『发布前把 `apps/miniprogram/config.js:29` 的 ENV 常量由 development 翻转为 production』这一步骤(带 development 上线的后果证据直引 HANDOFF 第 3 条:`pages/dev/dev.js:18,28,52`、`utils/fault_injection.js:38-40,82-107 @ 5927f36` 三重门控全系 ENV 取值)。结论行登记于 DOC-CLAIMS(判定态 + 命中/未命中行号全集 + `→ HYP-14(04-09 回填锚点);销号引 HANDOFF-PHASE4.md DOC 节第 2、3 条`)。
    4. 两节四态计数等式收尾;覆盖总表两行状态列改终态。
  </action>
  <verify>
    <automated>grep -c '^| DG-' .planning/audit/DOC-CLAIMS.md && grep -c '^| FD-' .planning/audit/DOC-CLAIMS.md && grep -c 'HYP-14' .planning/audit/DOC-CLAIMS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^| DG-' .planning/audit/DOC-CLAIMS.md` ≥ 6 且 `grep -c '^| FD-' 该文件` ≥ 8
    - `grep -c 'HYP-14' 该文件` ≥ 1 且该结论行含 `HANDOFF-PHASE4.md` 显式引用(DOC 第 2、3 条销号)
    - `grep -c 'HYP-04' 该文件` ≥ 1(fc_deploy 能力面对照走引用不重复采证)
    - 两节尾均有四态计数等式行
  </acceptance_criteria>
  <done>部署双 runbook 销号完毕;HYP-14 的文档覆盖判定有全量检索证据,HANDOFF 两条移交显式销号</done>
</task>

<task type="auto">
  <name>Task 3: runbook 批次 F-DOC 立条与机械对账</name>
  <files>.planning/audit/findings/docs-config.md, .planning/audit/DOC-CLAIMS.md</files>
  <read_first>
    - .planning/audit/findings/docs-config.md(既有条目与 F-DOC 编号现状——续接编号)
    - .planning/audit/CHARTER.md(MEDIUM 锚点『可诱发高危误操作的误导性文档(如 runbook 步骤与实态不符)』;LOW 锚点『文档死链/路径失效』)
  </read_first>
  <action>
    1. 汇总本计划 4 节销号结果,把达发现级的 drift/dead-ref 立为 F-DOC 条目(九字段,编号续接 04-03,先 `grep -c '^### F-DOC-'` 确认基数):
       - runbook 步骤与实态不符且照做会误操作 → MEDIUM 锚点;仅口径过期/描述陈旧不致误操作 → LOW;
       - HYP-14 判定若为『发布文档未覆盖 ENV 翻转步骤』→ 立条候选(影响:带 development 上线向最终用户暴露开发者菜单与故障注入开关;可能性:每次发布都依赖记忆而非清单——按 CHARTER 影响×可能性据实定级),关联字段链 HYP-14 与 HANDOFF 两条;若文档已覆盖则显式记『已检查,无发现』;
       - dead-ref 命中仍只登记 `→ HYP-02` 指针,聚合立条留 04-05(避免拆散聚合条)。
    2. 批次导语 blockquote(『04-04 判定产物(runbook 4 份深核):共 N 条发现——…;无法静态核实 M 条如实标注;dead-ref 登记移交 04-05 聚合』)。
    3. DOC-CLAIMS 本批次机械对账小节:4 节条目计数 + 四态分布合计等式。
  </action>
  <verify>
    <automated>grep -c '04-04 判定产物' .planning/audit/findings/docs-config.md</automated>
  </verify>
  <acceptance_criteria>
    - findings/docs-config.md 含 `04-04 判定产物` 批次导语(0 条发现时亦有显式记录)
    - 新立 F-DOC 条目九字段齐全且编号与 04-03 无冲突(`grep -c '^### F-DOC-'` 计数 = 既有 + 新增)
    - HYP-14 的去向在 findings 或 DOC-CLAIMS 中明确二选一落地(立条 或 显式无发现)
    - DOC-CLAIMS 覆盖总表 runbook 4 行状态列均为终态
  </acceptance_criteria>
  <done>runbook 维度判断层落账完毕,MEDIUM 锚点适用性逐条有理由;账面可机械复核</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计产物 → 读者 | runbook 含部署凭证变量名与云资源标识,证据引用须防二次泄露 |
| 审计执行 → 云端 | mvp-acceptance/runbook 引用大量真云 make 目标,误执行即触云 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-08 | Elevation of Privilege | runbook 引用的 make 真云目标 | high | mitigate | 本计划纯静读(git show/git grep);任务 action 显式禁令,acceptance 断言执行历史无 test-*/verify-*/deploy- 调用 |
| T-04-09 | Information Disclosure | 证据字段引用 runbook 凭证段落 | medium | mitigate | CHARTER 秘密红线:只记 `path:line @ 5927f36` + 模式名 |
| T-04-10 | Tampering | docs/ 与封版产物 | high | mitigate | files_modified 仅两个 .planning 写入点;零 diff 由阶段收尾统一验证 |
</threat_model>

<verification>
- runbook 4 节销号清单成档,每节四态计数等式闭合
- HYP-14 结论行与 HANDOFF 两条销号引用在档
- findings 批次导语与条目(或显式无发现)在档
</verification>

<success_criteria>
- AUDIT-03 runbook 侧完成:步骤与实态一致性有逐条证据,误导性步骤(若有)按 MEDIUM 锚点入台账
- 04-09 回填 HYP-14 所需的判定与证据全部就位
</success_criteria>

<output>
完成后创建 `.planning/phases/04-docs-config-test-audit/04-04-SUMMARY.md`(记录 F-DOC 已用最大编号)
</output>
