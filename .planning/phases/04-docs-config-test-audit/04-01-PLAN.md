---
phase: 04-docs-config-test-audit
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/scans/gate-run-worktree.md
autonomous: true
requirements: [AUDIT-04]
must_haves:
  truths:
    - "离线门禁 make test 在基线 5927f36 的仓库外 worktree 专区内实跑过,exit code 与 collected/passed/skipped 三计数已归档"
    - "node 缺失时 JS 桥测试静默 SKIP 且 exit 0 的行为有受控反事实实跑证据"
    - "实跑期间主工作区零触碰(零 diff 快查通过),真云目标 test-*/verify-* 与被审脚本一律未执行"
  artifacts:
    - ".planning/audit/scans/gate-run-worktree.md — 门禁实跑归档(命令+工具版本+三计数+反事实观测+worktree 专区备注)"
  key_links:
    - "gate-run-worktree.md 三计数 → 04-08 D-11 门禁三方对照的『实跑观测』方"
    - "worktree 专区路径备注 → 04-02 覆盖率实测复用(路径失效可按命令重建)"
---

<objective>
按 D-01(分级执行口径)与 D-02(worktree 基线专区)建立仓库外基线专区,实跑离线门禁 `make test` 并采集 D-11 三方对照所需的实跑观测证据(collected/passed/skipped 三计数、JS 桥实跑情况、node 缺失反事实 SKIP 观测),归档至 `.planning/audit/scans/gate-run-worktree.md`。

Purpose: AUDIT-04 的『make test 门禁完整性』审计需要实跑观测作为三方对照(D-11)的第三方证据——静态配置与文档声称在 Phase 3/HANDOFF 已有线索,唯独实跑观测缺席。
Output: scans/gate-run-worktree.md(只存档不判断);存续的 worktree 专区(供 04-02 复用,本计划不拆区)。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-docs-config-test-audit/04-RESEARCH.md
@.planning/audit/CHARTER.md
@.planning/audit/scans/gates-baseline.md
</context>

## Artifacts this phase produces

本阶段(Phase 4)全部新增符号——drift 校验时应排除:

- `.planning/audit/scans/gate-run-worktree.md`(本计划创建)
- `.planning/audit/scans/coverage-pytest.md`、`.planning/audit/scans/coverage-node.md`(04-02)
- `.planning/audit/DOC-CLAIMS.md`(04-03 起)
- `.planning/audit/TEST-AUDIT.md`(04-06 起)
- `.planning/audit/findings/docs-config.md` 的 `## 发现` 节新增 F-DOC-NN 条目(04-03/04/05)
- `.planning/audit/findings/test.md` 的 `## 发现` 节新增 F-TEST-NN 条目(04-08)
- `.planning/audit/HYPOTHESES.md` 的 11 条状态回填与尾部总对账新章节(04-09)
- 仓库外 worktree 专区 `wt-5927f36`(临时,04-02 拆除,不入仓)

<tasks>

<task type="auto">
  <name>Task 1: worktree 基线专区建立与离线门禁实跑观测</name>
  <files>(仓库外)会话 scratchpad 下 `wt-5927f36/` worktree 专区与 `gate-run-*.txt` 原始输出(均不入仓)</files>
  <read_first>
    - .planning/phases/04-docs-config-test-audit/04-RESEARCH.md(§Code Examples『Worktree 基线专区』与 §Common Pitfalls 1/2/5——命令已实测,照用不发明)
    - .planning/audit/scans/gates-baseline.md(Phase 3 实跑归档先例:命令+exit+计数+注记格式)
  </read_first>
  <action>
    按 D-02 在仓库外建立基线专区并实跑离线门禁(D-01),全程只执行以下白名单命令,绝不执行任何 `make test-*`/`verify-*` 真云目标或 `scripts/` 下被审脚本:

    1. 建区(主仓 `/Volumes/Data/ProjectCode/my_soniscope` 内执行):`WT=<会话 scratchpad>/wt-5927f36`,`git worktree add "$WT" 5927f36`。确认 `git worktree list` 出现该条目且 HEAD 为 5927f36(detached)。
    2. 装依赖(专区内,cd "$WT"):`uv sync --frozen`——这是 `make install` 的钉版等价形式(Pitfall 2:防 uv.lock 漂移,Phase 3 gates-baseline.md 先例同款)。记录 exit code。
    3. 门禁声称面原样实跑一次:专区内执行 `make test`(即 Makefile:170-171 声称的门禁入口),完整输出 tee 到 scratchpad `gate-run-make-test.txt`。记录 exit code——若非绿照记不阻塞(CONTEXT 裁量条款:非绿结果按 CHARTER 正常定级,判断留 04-08)。
    4. 计数观测(D-11 证据):专区内 `uv run --frozen pytest -rs 2>&1 | tee <scratchpad>/gate-run-rs.txt`(保留 skip 原因,Pitfall 5)与 `uv run --frozen pytest --collect-only -q 2>&1 | tail -5 | tee <scratchpad>/gate-run-collect.txt`(collected 底数)。从输出摘录 collected/passed/skipped/failed 四个计数与全部 skip 原因行。特别记录 `apps/worker/tests/test_miniprogram_js.py`(JS 桥)在本机(node 存在)下的实际执行情况(passed 还是 skipped)。
    5. lock 漂移观测:专区内 `git status --porcelain` 输出摘录(uv.lock 是否被 make test 改动——该观测本身是门禁行为证据,worktree 为一次性副本,漂移不污染主仓)。
    6. 工具版本采集:`uv --version`、`git --version`、`node --version`、专区内 `uv run --frozen pytest --version`,供 Task 2 归档头部。
    7. 本计划不拆区:worktree 留给 04-02 覆盖率实测复用。
  </action>
  <verify>
    <automated>git -C /Volumes/Data/ProjectCode/my_soniscope worktree list | grep -c 'wt-5927f36'</automated>
  </verify>
  <acceptance_criteria>
    - `git worktree list` 输出恰含 1 条 `wt-5927f36` 条目,commit 列为 5927f36
    - scratchpad 下存在 `gate-run-make-test.txt`、`gate-run-rs.txt`、`gate-run-collect.txt` 三份原始输出且非空
    - `gate-run-rs.txt` 内含 pytest 末行汇总(形如 `N passed` 且 skipped 计数若有则可见)
    - 执行历史中不存在任何 `make test-`、`make verify-`、`scripts/test_asr.py`、`scripts/fetch_test_fixtures.py` 调用
  </acceptance_criteria>
  <done>基线专区就绪,make test 原样实跑一次,三计数与 skip 原因、lock 漂移观测、工具版本全部落在 scratchpad 原始输出中</done>
</task>

<task type="auto">
  <name>Task 2: node 缺失反事实 SKIP 观测与 scans 归档</name>
  <files>.planning/audit/scans/gate-run-worktree.md</files>
  <read_first>
    - .planning/audit/scans/gates-baseline.md(归档文件头/命令+结果+注记/零 diff 快查四段式——逐段仿写)
    - .planning/audit/scans/vulture.md(对账等式收尾写法)
  </read_first>
  <action>
    1. 受控反事实观测(D-11『全绿≠全跑』证据,RESEARCH Pitfall 5 配方):本机 node 存在,skip 不会自然发生,须人工剔除 node 复现。专区内先解析绝对路径 `UV_BIN=$(command -v uv)`、`NODE_DIR=$(dirname "$(command -v node)")`(若 uv 与 node 同目录,后续必须经 `$UV_BIN` 绝对路径调用以免连带失效);再以剔除 `$NODE_DIR` 后的 PATH 执行 `"$UV_BIN" run --frozen pytest apps/worker/tests/test_miniprogram_js.py -rs`。预期观测:测试被 SKIPPED(skipif 依据 `apps/worker/tests/test_miniprogram_js.py:24 @ 5927f36` 的 `shutil.which("node") is None`)且进程 exit 0。原样记录实际观测(若与预期不符照记,判断留 04-08)。
    2. 创建 `.planning/audit/scans/gate-run-worktree.md`,结构仿 gates-baseline.md:
       - 文件头:标题『扫描档案:离线门禁基线实跑(worktree 专区)』+ `**Created:** 2026-07-05` + `**基线:** \`5927f36\`(全 SHA 见 .planning/audit/CHARTER.md 审计基线章节)` + 依据句 per D-01(离线门禁可跑,真云目标绝不执行)/ D-02(worktree 基线专区)/ D-11(三计数供三方对照)+ 工具版本行(Task 1 第 6 步采集值)。
       - worktree 专区备注(仿 COVERAGE.md 基线导出备注):建区命令原文、专区绝对路径、`uv sync --frozen` 钉版说明、『若会话更替致路径失效,按 `git worktree add <新路径> 5927f36` 重建即可(内容由基线 SHA 唯一决定);本专区由 04-02 用毕后 `git worktree remove --force` 拆除』。
       - 正文逐命令一节:命令原文 + `**结果:exit=N,collected X / passed Y / skipped Z**` + 输出摘要 fenced block + 注记(skip 原因行、JS 桥实跑情况、uv.lock 漂移观测)。每节注记末尾写明『此处只存档不判断,销号判断由 04-08 D-11 对照完成』。
       - 反事实观测节:PATH 剔除配方原文 + 观测结果(SKIPPED 计数与 exit code)+ 注记(skipif 静态依据行号)。
       - 零 diff 快查节:主仓执行 `git diff --stat 5927f36 -- apps/ scripts/ docs/`,记录实际输出(期望空)。
       - 尾注落款:`*离线门禁实跑归档: 2026-07-05(命令 N 条;三计数 collected/passed/skipped 已采;反事实 SKIP 观测 1 次;worktree 专区存续待 04-02)*`。
    3. 全部计数照抄原始输出,不做任何质量结论——本档为纯输入证据(成功判据 3 口径)。
  </action>
  <verify>
    <automated>test -f .planning/audit/scans/gate-run-worktree.md && grep -c 'exit=' .planning/audit/scans/gate-run-worktree.md && git diff --stat 5927f36 -- apps/ scripts/ docs/ | wc -l | grep -cx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audit/scans/gate-run-worktree.md` 存在,`grep -c 'exit=' 该文件` ≥ 3(make test / pytest -rs / 反事实观测各至少一处结果行)
    - `grep -c '5927f36' 该文件` ≥ 2(基线头 + worktree 备注)
    - `grep -ci 'SKIP' 该文件` ≥ 1(反事实观测结果在档)
    - `grep -c 'D-11' 该文件` ≥ 1(三方对照去向指针在档)
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空(主仓零触碰)
  </acceptance_criteria>
  <done>门禁实跑与反事实观测全部按 gates-baseline 范式归档,只存档不判断;worktree 专区存续并在归档中留有重建与拆除说明</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计执行 → 云端 | 实跑命令面若越界(test-*/verify-*/被审脚本)会触真云 IO |
| 审计执行 → 主工作区 | 实跑若在主仓进行会污染零 diff 基线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | make/pytest 实跑命令面 | high | mitigate | D-01 白名单硬编码于任务:仅 `uv sync --frozen`/`make test`/`uv run --frozen pytest ...`;acceptance 显式断言无 test-*/verify-*/scripts 调用 |
| T-04-02 | Tampering | 主工作区 apps/scripts/docs | high | mitigate | D-02 结构性隔离:全部执行在仓库外 worktree;Task 2 收尾跑零 diff 快查并归档结果 |
| T-04-03 | Information Disclosure | scans 归档内容 | medium | mitigate | 归档只含计数与 skip 原因行;任何输出若含凭证模式仅记 `path:line @ 5927f36` + 模式名(CHARTER 秘密红线) |
</threat_model>

<verification>
- `git worktree list` 含 wt-5927f36(专区存续,供 04-02)
- scans/gate-run-worktree.md 按 gates-baseline 四段式成档,三计数与反事实观测在档
- 零 diff 快查通过并记录
</verification>

<success_criteria>
- AUDIT-04 的实跑观测证据链(D-11 第三方)完整:make test exit + 三计数 + JS 桥实跑情况 + 反事实 SKIP + lock 漂移观测
- 全程未执行任何真云目标或被审脚本;主工作区零触碰
</success_criteria>

<output>
完成后创建 `.planning/phases/04-docs-config-test-audit/04-01-SUMMARY.md`(记录 worktree 专区绝对路径,供 04-02 接续)
</output>
