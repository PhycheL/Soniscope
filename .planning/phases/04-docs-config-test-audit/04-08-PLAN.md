---
phase: 04-docs-config-test-audit
plan: 8
type: execute
wave: 3
depends_on: ["04-02", "04-07"]
files_modified:
  - .planning/audit/TEST-AUDIT.md
  - .planning/audit/findings/test.md
autonomous: true
requirements: [AUDIT-04]
must_haves:
  truths:
    - "make test 门禁完整性有三方对照结论:声称 × 静态配置 × worktree 实跑观测逐项对照,任一不一致均成缺口候选并有去向"
    - "TEST 缺口发现按面聚合入台账(一面一条),严重度参照 Phase 2/3 脆弱区发现定级,关联字段链 F-*/HYP"
    - "覆盖率数字在发现中仅作证据引用,无任何质量定性语言(成功判据 3 机械验收通过)"
    - "HANDOFF-PHASE4.md TEST 3 条移交(HYP-22 / HYP-25 ×2)被显式引用销号"
  artifacts:
    - ".planning/audit/TEST-AUDIT.md — D-11 三方对照节 + 收口机械对账"
    - ".planning/audit/findings/test.md — F-TEST-NN 条目与批次导语"
  key_links:
    - "三方对照结论与 F-TEST 条目 → 04-09 的 HYP-22/23/24/25 回填锚点"
    - "反向映射定级规则 → F-TEST 严重度字段(参照原 F-* 严重度)"
---

<objective>
按 D-11(门禁完整性三方对照)与 D-12(缺口按面聚合立条)完成 TEST 维度判断层:在 TEST-AUDIT.md 落三方对照表,把 04-01/04-02 实跑证据、04-06 反向映射、04-07 普审线索聚合为 F-TEST 发现写入 findings/test.md,并收口 TEST-AUDIT.md(AUDIT-04;ROADMAP 成功判据 2/3)。

Purpose: 『全绿 ≠ 全跑』的门禁判断与全部测试缺口在此定案;覆盖率仅证据的红线在判断层落实。
Output: TEST-AUDIT.md 三方对照节 + 收口;findings/test.md F-TEST 条目。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/audit/TEST-AUDIT.md
@.planning/audit/scans/gate-run-worktree.md
@.planning/audit/scans/coverage-pytest.md
@.planning/audit/scans/coverage-node.md
@.planning/audit/HANDOFF-PHASE4.md
</context>

## Artifacts this phase produces

见 04-01-PLAN.md 同名节(阶段全量清单)。本计划续写:`.planning/audit/TEST-AUDIT.md`(三方对照节 + 收口)、`.planning/audit/findings/test.md` `## 发现` 节。

<tasks>

<task type="auto">
  <name>Task 1: D-11 门禁完整性三方对照表</name>
  <files>.planning/audit/TEST-AUDIT.md</files>
  <read_first>
    - .planning/audit/scans/gate-run-worktree.md(实跑三计数、JS 桥实跑情况、反事实 SKIP 观测——『实跑观测』方的全部证据)
    - .planning/audit/HANDOFF-PHASE4.md(TEST 节 3 条:HYP-22 fc_live 手工 code 依赖;HYP-25 ×2 静态门禁范围证据 `pyproject.toml:32,50 @ 5927f36`、`Makefile:166-167 @ 5927f36` 与 test_asr.py 实害样本)
    - .planning/audit/CONTRACT-MATRIX.md(对照表结构先例)
  </read_first>
  <action>
    在 TEST-AUDIT.md 新增『门禁完整性三方对照(D-11)』节,表列:对照项 | 声称 | 静态配置 | 实跑观测 | 判定(一致 / 缺口候选 → 去向)。至少覆盖以下对照项,逐项三方取证:

    1. pytest 套件范围:声称(Makefile:170-171 test 目标、AGENTS/README 对 make test 的描述——引 DOC-CLAIMS 相关销号行若已有)× 静态(根 pyproject.toml testpaths = apps/worker/tests + apps/fc/tests、pythonpath = apps/fc/shared)× 实跑(gate-run-worktree.md collected 计数)。
    2. JS 测试进门禁的路径:声称(文档是否声称 make test 含 JS 测试)× 静态(test_miniprogram_js.py:24 skipif node 缺失即 SKIP、exit 0)× 实跑(本机 node 存在时 JS 桥 passed;反事实观测 node 剔除后 SKIPPED + exit 0)——『全绿 ≠ 全跑』的结构性缺口候选,已知线索定案点。
    3. 静态门禁(lint/typecheck)范围:声称(make lint/typecheck 全仓印象)× 静态(mypy files 与 ruff src 均仅 apps/ 四路径、Makefile:166-167 lint 仅 `ruff check apps/`)× 实害样本(HANDOFF HYP-25 第 2 条:test_asr.py 六条违例与已提交签名 URL 在 make lint 全绿下入库;签名 URL 只引位置 `scripts/test_asr.py:80 @ 5927f36` + 模式名,绝不复制值本体)。
    4. 覆盖率门禁:声称(无)× 静态(仓库无任何覆盖率配置,TESTING.md『None enforced』)× 实跑(coverage-pytest.md/coverage-node.md 系本审计临时注入所得)——『无覆盖率门禁』事实行。
    5. 活体路径:声称(make test-fc-live 等目标存在)× 静态(fc_live.py:15-16、verify_upload_live.py:14 手工 code 依赖,缺 code 即 SKIP)× 实跑(不适用——真云目标绝不执行,D-01,标注『静态+移交证据判定』)。

    每行判定列落终态;缺口候选行注去向 `→ Task 2 立条` 或『不立条理由』。HANDOFF TEST 3 条在对应行显式引用(销号写法:『销号引 HANDOFF-PHASE4.md TEST 节第 N 条』)。节尾机械对账行(对照项计数、缺口候选计数)。
  </action>
  <verify>
    <automated>grep -c 'D-11' .planning/audit/TEST-AUDIT.md && grep -c 'HANDOFF-PHASE4.md TEST' .planning/audit/TEST-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 三方对照表 ≥ 5 行,每行三列证据俱全(声称/静态/实跑各有行号或归档引用)且判定列非空
    - `grep -c '销号引 HANDOFF-PHASE4.md TEST' .planning/audit/TEST-AUDIT.md` ≥ 3(3 条移交逐条显式销号)
    - JS 桥行同时引用 skipif 静态证据(test_miniprogram_js.py:24)与反事实实跑观测(gate-run-worktree.md)
    - test_asr.py 相关证据只含位置与模式名,无任何 URL 参数值
  </acceptance_criteria>
  <done>门禁完整性判断有三方证据支撑,缺口候选全部有去向;HANDOFF TEST 3 条销号</done>
</task>

<task type="auto">
  <name>Task 2: F-TEST 按面聚合立条(D-12)</name>
  <files>.planning/audit/findings/test.md</files>
  <read_first>
    - .planning/audit/findings/test.md(F-TEST-00 schema 示例与 `## 发现` 写入位置 :21)
    - .planning/audit/findings/code.md(:23 批次导语与九字段实战格式)
    - .planning/audit/TEST-AUDIT.md(反向映射终态、41 行台账线索、三方对照缺口候选、HYP-23/24 专项事实清单——立条的全部输入)
  </read_first>
  <action>
    把 TEST 维度全部缺口按面聚合立条(一个缺口面一条,编号 F-TEST-01 起续接,九字段,维度行固定 `- **维度:** 测试质量与覆盖 (TEST)`,状态 draft,上线判定留空),先写批次导语 blockquote(『04-08 判定产物(D-11 三方对照 + D-09/D-10 聚合):共 N 条发现——…;显式无发现清单;HANDOFF 3 条销号去向』)。候选面(据实取舍,每面证据字段内列具体模块/行号清单):

    1. 活体路径零自动化覆盖(证据:HANDOFF HYP-22 条 fc_live/verify_upload_live 手工 code 依赖 + 三方对照第 5 行;关联 HYP-22);
    2. pages 胶水层无自动化测试(证据:04-07 HYP-24 专项的加载边界事实 + coverage-node.md 的 pages/ 覆盖数据行;index.js 796 行量级;关联 HYP-24);
    3. scripts/ 在全部静态门禁之外且已有实害样本(证据:HANDOFF HYP-25 ×2 + 三方对照第 3 行;关联 HYP-25、F-TOOL-05;test_asr.py:80 只引位置+模式名);
    4. JS 桥静默 skip 使 make test 全绿不代表全跑(证据:skipif 静态行 + 反事实实跑观测;关联 D-11 对照行);
    5. 反向映射查出的脆弱区无兜底项(每个独立缺口面一条或按共同根因合并;严重度参照原 F-* 发现定级——定级规则引 TEST-AUDIT 反向映射节首;关联字段链对应 F-CON/F-CODE/F-TOOL);
    6. 04-07 普审 8 面聚合出的其他缺口面(若有;无关联脆弱区的一般缺口按 CHARTER LOW 锚点)。

    硬约束:①覆盖率数字只能出现在证据字段并指向 scans/ 归档行(写法示例:『模块 M 行覆盖 0%(scans/coverage-pytest.md),该模块承载 F-CODE-02 脆弱区』),不得据数字下质量定性结论(成功判据 3);②条目与导语的措辞避开阶段机械验收的负向检索词(以本任务 acceptance 的 grep 词为准);③候选面若判定不成立,批次导语中显式记『已检查,无发现』。
  </action>
  <verify>
    <automated>grep -c '04-08 判定产物' .planning/audit/findings/test.md && grep -ci '评分\|score' .planning/audit/findings/test.md | grep -cx '0'</automated>
  </verify>
  <acceptance_criteria>
    - findings/test.md 含 `04-08 判定产物` 批次导语;`grep -c '^### F-TEST-' 该文件` ≥ 4(示例 00 外至少 3 条实条——候选面 1/3/4 证据已足)
    - `grep -ci '评分\|score' .planning/audit/findings/test.md` 输出 0(成功判据 3 机械验收)
    - 每条九字段齐全,严重度行为『严重度 — 影响:…;可能性:…』格式;参照定级的条目在严重度理由或关联字段中点名原 F-* 发现
    - `grep -c 'scans/coverage' 该文件` ≥ 1(覆盖率作为证据引用的写法落地)
  </acceptance_criteria>
  <done>TEST 缺口全部按面聚合入台账,定级可追溯到脆弱区,覆盖率仅证据红线通过机械验收</done>
</task>

<task type="auto">
  <name>Task 3: TEST-AUDIT.md 收口与机械对账</name>
  <files>.planning/audit/TEST-AUDIT.md</files>
  <read_first>
    - .planning/audit/TEST-AUDIT.md(全量现状——收口对象)
    - .planning/audit/findings/test.md(本计划立条结果——产出列反填)
  </read_first>
  <action>
    1. 台账产出列反填:41 行中与 F-TEST 条目对应的行把产出列改为 `F-TEST-NN`(或保持『无缺口线索』);反向映射行的缺口判定列反填最终 F-TEST 编号。
    2. 总机械对账节:台账 41 行 × 8/8 等式;反向映射行总数 = 终态数(占位 0);三方对照行数与缺口候选去向计数;F-TEST 立条数与候选面处置等式(立条 + 显式无发现 = 候选面总数)✓。
    3. 尾注落款:`*TEST 覆盖台账: 2026-07-05(41 模块 × 8 面;反向映射 22+N 行终态;三方对照 M 项;F-TEST K 条;HYP-22/23/24/25 锚点齐备)*`。
  </action>
  <verify>
    <automated>grep -c 'F-TEST-' .planning/audit/TEST-AUDIT.md && grep -c '✓' .planning/audit/TEST-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - 反向映射节判定为缺口的行均含 F-TEST 编号反填
    - 总机械对账节各等式闭合且带 ✓ 标记
    - 尾注落款含四个计数(模块×面/反向映射/对照项/立条数)
  </acceptance_criteria>
  <done>TEST 维度证据层与判断层双向指针闭环,账面可机械复核;4 条 TEST HYP 回填材料齐备</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计产物 → 读者 | F-TEST 证据引用 test_asr.py 签名 URL 位置时须防二次泄露 |
| 判断层 → 证据层 | 覆盖率数字若被转写为质量结论即违反成功判据 3 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-17 | Information Disclosure | findings/test.md 证据字段 | medium | mitigate | CHARTER 秘密红线:test_asr.py:80 只引位置+模式名(HANDOFF 已示范) |
| T-04-18 | Repudiation | 覆盖率数字的用途边界 | medium | mitigate | 机械验收负向 grep(Task 2 acceptance)+ 证据字段引用写法钉定(指向 scans/ 归档行) |
| T-04-19 | Tampering | 封版产物与被审源码 | high | mitigate | files_modified 仅两个 .planning 写入点;纯静读,不执行任何目标 |
</threat_model>

<verification>
- 三方对照、F-TEST 条目、收口对账三件套成档且互相指针闭环
- `grep -ci '评分\|score' .planning/audit/findings/test.md` = 0
- `git diff --stat 5927f36 -- apps/ scripts/ docs/` 为空
</verification>

<success_criteria>
- ROADMAP 成功判据 2 达成:双侧测试质量与覆盖缺口入台账,含 make test 门禁完整性,定级参照 Phase 2/3 脆弱区
- ROADMAP 成功判据 3 达成:覆盖率仅证据,机械验收通过
</success_criteria>

<output>
完成后创建 `.planning/phases/04-docs-config-test-audit/04-08-SUMMARY.md`(记录 F-TEST 最终编号与 4 条 TEST HYP 锚点位置)
</output>
