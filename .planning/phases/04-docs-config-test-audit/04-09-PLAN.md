---
phase: 04-docs-config-test-audit
plan: 9
type: execute
wave: 4
depends_on: ["04-05", "04-08"]
files_modified:
  - .planning/audit/HYPOTHESES.md
autonomous: true
requirements: [AUDIT-05]
must_haves:
  truths:
    - "HYPOTHESES.md 25 条状态全部为证实/证伪/细化之一,均附新鲜 file:line @ 5927f36 证据,机械 grep 验证零残留"
    - "总对账新章节在档:25/25 状态分布表 + 机械验证命令 + 29 条(25 HYP + 4 DNF)溯源闭环声明 + HANDOFF 6 条销号声明"
    - "阶段收尾验证通过并记录:apps/scripts/docs 相对 5927f36 零 diff、git worktree list 无残留"
  artifacts:
    - ".planning/audit/HYPOTHESES.md — 11 条回填(DOC 6 + TEST 4 + CON 1)+ 尾部总对账章节 + 尾注更新"
  key_links:
    - "总对账章节 → Phase 5 RPT-08 可追溯映射表直接输入"
    - "每条回填的去向闭环(F-*/RPT 候选/DNF/章程范围外)→ RPT-07 分维度置信声明"
---

<objective>
按 D-13/D-14/D-15 完成 AUDIT-05 收官:回填 HYPOTHESES.md 余 11 条(DOC 6:HYP-02/05/06/11/14/21;TEST 4:HYP-22/23/24/25;CON 1:HYP-13),机械对账已回填 14 条,新增 25/25 总对账章节,销号 HANDOFF 6 条移交,执行阶段零 diff 收尾验证(ROADMAP 成功判据 4)。

Purpose: CONCERNS.md 假设清单全部关闭是本阶段的终态判据;总对账直接喂 Phase 5 RPT-08。
Output: HYPOTHESES.md 全量闭环;阶段收尾验证记录。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/audit/HYPOTHESES.md
@.planning/audit/DOC-CLAIMS.md
@.planning/audit/TEST-AUDIT.md
@.planning/phases/04-docs-config-test-audit/04-05-SUMMARY.md
@.planning/phases/04-docs-config-test-audit/04-08-SUMMARY.md
</context>

## Artifacts this phase produces

见 04-01-PLAN.md 同名节(阶段全量清单)。本计划续写:`.planning/audit/HYPOTHESES.md`(11 条回填 + 总对账新章节;HYPOTHESES.md 是唯一可续写的既有产物,D-16)。

<tasks>

<task type="auto">
  <name>Task 1: 余 11 条 HYP 回填与 HANDOFF 6 条销号</name>
  <files>.planning/audit/HYPOTHESES.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(11 条待回填条目原文与 14 条已回填条目的三种状态写法先例:证实 HYP-01 :25-27、细化 HYP-15 :153、引用回填 HYP-03 :43)
    - .planning/audit/DOC-CLAIMS.md(HYP-02/05/06/14/16/21 结论锚点行)
    - .planning/audit/TEST-AUDIT.md(HYP-22/23/24/25 专项结论与三方对照行)
    - .planning/audit/HANDOFF-PHASE4.md(6 条移交原文——销号引用的对象)
    - .planning/audit/CONTRACT-MATRIX.md(§往返校验结论 :276 起与总结论 :307-309——HYP-13 引用回填证据源)
  </read_first>
  <action>
    逐条回填 11 条:操作模式为把该条现有状态行(`- **状态:**` 行)替换为结论行,其后补 `- **证据:**` 行、改写/追加 `- **备注:**` 行(含去向闭环 + 『04-09 回填』落款),严格沿用已回填 14 条的格式。逐条要点:

    - **HYP-02**(预期证实——『引用失效』半句;deletions uncommitted 半句已被基线推翻,备注保留该边界):证据引 DOC-CLAIMS AGENTS.md 节 AG 行全集(17 处)+ 设计文档 3 处 + `git ls-tree` 存在性佐证;去向 → F-DOC 聚合条编号(04-05 立)。
    - **HYP-05**(vendored 仓):证据引 DOC-CLAIMS 存在级登记(ls-tree 计数 ≈1003 文件);去向 → 04-05 所立 INFO 条编号。
    - **HYP-06**(四套 AI 工具目录):证据引 DOC-CLAIMS 四目录登记与抽样佐证;去向 → 04-05 所立 INFO 条编号。
    - **HYP-11**(D-14 引用回填):状态『细化 — 章程范围外』,证据引 CHARTER 排除项表首行(FC 直转目标态对照不引入)具体行号;备注注明不占发现 ID、留 RPT 范围声明呈现。
    - **HYP-14**:结论按 04-04 文档覆盖判定 + 04-05 config.js:29 证据行定(证实/细化据实);证据双引 DOC-CLAIMS 两处锚点行 + HANDOFF DOC 第 2、3 条(销号显式引用);去向 → F-DOC 条编号或显式无发现记录。
    - **HYP-21**:结论按 04-03 PRD 范围声明核对行定;证据引 DOC-CLAIMS P 行(PRD 具体行号)+ 代码实态(无读取 UI);去向按 D-12 存在级口径(与 HYP-01/20 同款,预计不占发现 ID,记 RPT 呈现)。
    - **HYP-22**:证据引 TEST-AUDIT 三方对照第 5 行 + HANDOFF TEST 第 1 条(销号引用)+ fc_live.py:15-16/verify_upload_live.py:14;去向 → F-TEST 条编号(04-08 立)。
    - **HYP-23**:结论依 04-07 逐错误码补偿事实清单下判(充分性判断只此一处,回填时基于清单;不质疑 DNF-03 豁免本身,备注保留交叉引用);证据引 TEST-AUDIT 专项表;去向据实(缺口达标则链 F-TEST,否则显式无发现)。
    - **HYP-24**:证据引 TEST-AUDIT HYP-24 专项行(index.js 加载判定)+ scans/coverage-node.md 的 pages/ 数据行;去向 → F-TEST 条编号。
    - **HYP-25**:证据引 TEST-AUDIT 三方对照第 3 行 + HANDOFF TEST 第 2、3 条(销号引用;test_asr.py:80 只引位置+模式名);去向 → F-TEST 条编号。
    - **HYP-13**(D-14 引用回填,Pitfall 7:不立任何新 F-*、不新开台账文件):证据明引 CONTRACT-MATRIX.md §往返校验结论(:276 起;总结论 :307-309『FC↔Worker 主链在样本域内无漂移,分叉全部位于小程序声部』)+ 组① 行 2/4/5 判定行;备注注明小程序侧三条已立 F-CON-01/02/03,同一基线 SHA 证据仍新鲜,不重复采证。

    另:HYP-16 状态与既有结论不动(D-13 不复判),仅在其备注行末追加一句指针『文档口径半句已于 Phase 4 核对(DOC-CLAIMS.md PRD/tech-spec/runbook 各节 HYP-16 结论行),销号 HANDOFF-PHASE4.md DOC 节第 1 条。04-09 补注』。
  </action>
  <verify>
    <automated>grep -c '^- \*\*状态:\*\* 未验证' .planning/audit/HYPOTHESES.md | grep -cx '0' && grep -c '04-09 回填' .planning/audit/HYPOTHESES.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \*\*状态:\*\* 未验证' .planning/audit/HYPOTHESES.md` 输出 0
    - `grep -c '^- \*\*状态:\*\*' 该文件` = 25(每条恰一行状态,无重复无遗漏)
    - `grep -c '04-09 回填' 该文件` = 11(11 条新回填逐条落款)
    - 11 条每条均有 `- **证据:**` 行且含 `@ 5927f36` 或对基线锚定产物(DOC-CLAIMS/TEST-AUDIT/CONTRACT-MATRIX/scans)的行级引用
    - `grep -c 'HANDOFF-PHASE4.md' 该文件` ≥ 5(6 条移交在回填/补注中显式引用;HYP-14 双条可合并引用)
  </acceptance_criteria>
  <done>25 条 HYP 全部闭环,HANDOFF 6 条移交销号,引用回填不重复采证</done>
</task>

<task type="auto">
  <name>Task 2: D-13 机械对账与 D-15 总对账章节</name>
  <files>.planning/audit/HYPOTHESES.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(:6-14 头部『转换对账』节排版先例与 :12 勘误行先例;尾注现状 :248)
    - .planning/audit/DO-NOT-FIX.md(DNF-01~04——29 条溯源的另一半)
  </read_first>
  <action>
    1. D-13 机械对账(14 条已回填:HYP-01/03/04/07/08/09/10/12/15/16/17/18/19/20):逐条只验形式合规——状态行存在且为三态之一、证据行含 `@ 5927f36`、备注去向闭环到 F-* 编号 / RPT 候选标注 / DNF 候选 / HANDOFF 引用之一。不复判内容。若发现历史计数或格式出入,按 :12 先例加勘误行处理,不改史。
    2. 在文件尾注之前新增『## 总对账(Phase 4 收官,per D-15)』章节,含四部分:
       - 25/25 状态分布表:证实/证伪/细化三态计数(回填后实际统计)+ 合计 = 25 等式;
       - 机械验证命令(fenced bash,照 04-RESEARCH 实测格式):`grep -c '^### HYP-' .planning/audit/HYPOTHESES.md`(期望 25)、`grep -c '^- \*\*状态:\*\*' ...`(期望 25)、状态残留负向检查(期望 0)、`grep -c '^### DNF-' .planning/audit/DO-NOT-FIX.md`(期望 4;25+4=29 对账);
       - 29 条溯源闭环声明:25 HYP 逐条一行(ID | 状态 | 去向:F-* / RPT-06 候选 / DNF 候选 / 章程范围外 / 无发现显式记录)+ 4 DNF 引用行(DNF-01~04 与 DOC 核对闭环位置,DNF-02 即 issue-cedential 线索闭环)+ Known Bugs 显式无线索行照录(不计入 29);
       - HANDOFF 6 条销号声明:每条 → 消费位置(DOC-CLAIMS 节/HYP 回填/F-* 条目)。
    3. 尾注更新:在既有斜体尾注后追加(或按其格式续写)Phase 4 回填集说明——『04-09:11 条(DOC 6 + TEST 4 + CON 1)……累计 25/25 全部闭环 ✓』。
  </action>
  <verify>
    <automated>grep -c '总对账' .planning/audit/HYPOTHESES.md && grep -c '^### HYP-' .planning/audit/HYPOTHESES.md | grep -cx '25'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '总对账' .planning/audit/HYPOTHESES.md` ≥ 1(新章节在档)
    - 分布表三态计数之和 = 25,与逐条状态行 grep 实测一致
    - 溯源声明 25 行 HYP 逐条有去向;`grep -c 'DNF-0' 该文件` ≥ 4(4 条 DNF 溯源在列)
    - 尾注含『25/25』与 ✓ 标记
  </acceptance_criteria>
  <done>D-13 形式对账完毕(出入走勘误行),D-15 总对账章节成型,RPT-08 输入就绪</done>
</task>

<task type="auto">
  <name>Task 3: 阶段机械收尾验证与记录</name>
  <files>.planning/audit/HYPOTHESES.md</files>
  <read_first>
    - .planning/audit/CHARTER.md(零 diff 验证命令写定处)
    - .planning/audit/HYPOTHESES.md(总对账章节——收尾记录写入点)
  </read_first>
  <action>
    执行阶段收尾全套机械验收并把结果记入总对账章节末尾『阶段收尾验证』小节(命令 + 实际输出逐条):

    1. 零 diff 验证:`git diff --stat 5927f36 -- apps/ scripts/ docs/`——期望空输出,原样记录(里程碑硬约束,ROADMAP 全阶段口径)。
    2. worktree 清理确认:`git worktree list`——期望无 wt-5927f36 残留(04-02 已拆;若有残留,主仓内 `git worktree remove --force <路径>` 后复验并记录)。
    3. HYP 全套 grep 复验(Task 2 机械验证命令逐条实跑,期望值 25/25/0/4)。
    4. 台账存在性复验:`ls .planning/audit/DOC-CLAIMS.md .planning/audit/TEST-AUDIT.md .planning/audit/scans/coverage-pytest.md .planning/audit/scans/coverage-node.md .planning/audit/scans/gate-run-worktree.md` 全部存在。
    5. 发现台账批次复验:`grep -c '^### F-DOC-' .planning/audit/findings/docs-config.md` 与 `grep -c '^### F-TEST-' .planning/audit/findings/test.md` 实际计数照录(含示例 00)。
  </action>
  <verify>
    <automated>git diff --stat 5927f36 -- apps/ scripts/ docs/ | wc -l | grep -cx '0' && git worktree list | grep -c 'wt-5927f36' | grep -cx '0' && grep -c '阶段收尾验证' .planning/audit/HYPOTHESES.md</automated>
  </verify>
  <acceptance_criteria>
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空且该事实记录在档
    - `git worktree list | grep -c 'wt-5927f36'` 输出 0 且记录在档
    - 『阶段收尾验证』小节含 5 组命令与实际输出
    - 全部期望值命中(25/25/0/4 与文件存在性)
  </acceptance_criteria>
  <done>ROADMAP 成功判据 4 与里程碑零 diff 硬约束双达成,证据在档;Phase 4 可交 /gsd-verify-work</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计产物 → 读者 | 回填证据若复制秘密类内容会二次泄露 |
| 回填写入 → 既有回填条目 | 误改已回填 14 条内容会破坏 Phase 3 封版判断 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-20 | Information Disclosure | HYP 回填证据字段 | medium | mitigate | CHARTER 秘密红线;HYP-25 回填只引位置+模式名(HYP-07 先例同款) |
| T-04-21 | Tampering | 已回填 14 条与封版产物 | high | mitigate | D-13 只验形式不改内容,出入走勘误行不改史;HYP-16 仅备注追加指针不动状态 |
| T-04-22 | Repudiation | 阶段收尾判据 | low | accept | 全部验收为可重放机械命令并连输出记录在档,可任意复核 |
</threat_model>

<verification>
- 25/25 全闭环、总对账章节、收尾验证记录三件套在档
- 零 diff 与 worktree 清理机械验证通过
</verification>

<success_criteria>
- AUDIT-05 达成:CONCERNS.md 假设清单每条为证实/证伪/细化之一,附新鲜 @ 5927f36 证据,零『未验证』残留(机械 grep 证明)
- ROADMAP 成功判据 4 与零 diff 硬约束达成;Phase 5 RPT-07/08 输入齐备
</success_criteria>

<output>
完成后创建 `.planning/phases/04-docs-config-test-audit/04-09-SUMMARY.md`
</output>
