---
phase: 01-audit-charter-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/CHARTER.md
  - .planning/audit/findings/contract.md
  - .planning/audit/findings/code.md
  - .planning/audit/findings/toolchain.md
  - .planning/audit/findings/docs-config.md
  - .planning/audit/findings/test.md
autonomous: true
requirements: [CHARTER-01, CHARTER-02, CHARTER-03, CHARTER-04, CHARTER-05]
must_haves:
  truths:
    - "CHARTER.md 开头声明完整基线 SHA `5927f362785d44b085a791ca387732991012ce5a` 恰一次,正文证据引用统一为 `path:line @ 5927f36` 短格式(D-02)"
    - "五级严重度 CRITICAL/HIGH/MEDIUM/LOW/INFO 每级绑定 SoniScope 场景锚点(数据丢失/静默转写失败/凭证泄漏/存在级观察等),不含'视情况''可酌情'类裁量措辞"
    - "S/M/L/XL 四档判定标准成文(S ≤单文件、M=同组件多文件、L=跨组件、XL=需独立阶段)且全文无'数字+小时'形式的工时表达"
    - "扫描排除清单九条路径、D-06 scripts/ 缩窄范围、D-07 秘密扫描穿透规则(5 类模式)、D-03 零 diff 命令、D-04 无例外协议全部在章程成文"
    - "发现记录 schema 九字段与 ID 规则(F-<维度码>-NN / HYP-NN / DNF-NN)定稿,findings/ 五个维度台账骨架存在且 schema 示例字段与章程一致"
  artifacts:
    - path: ".planning/audit/CHARTER.md"
      provides: "CHARTER-01~05 全部规则:基线声明、范围与方法、排除清单、秘密扫描规则、严重度体系、工作量分档、发现 schema"
      contains: "5927f362785d44b085a791ca387732991012ce5a"
    - path: ".planning/audit/findings/contract.md"
      provides: "CON 维度台账骨架(表头 + 1 条 F-CON-00 schema 示例)"
      contains: "F-CON"
    - path: ".planning/audit/findings/code.md"
      provides: "CODE 维度台账骨架"
    - path: ".planning/audit/findings/toolchain.md"
      provides: "TOOL 维度台账骨架"
    - path: ".planning/audit/findings/docs-config.md"
      provides: "DOC 维度台账骨架"
    - path: ".planning/audit/findings/test.md"
      provides: "TEST 维度台账骨架"
  key_links:
    - from: ".planning/audit/findings/contract.md"
      to: ".planning/audit/CHARTER.md"
      via: "schema 示例字段顺序与章程 schema 章节九字段完全一致"
      pattern: "维度.*严重度.*证据"
    - from: ".planning/audit/CHARTER.md"
      to: ".planning/ROADMAP.md"
      via: "五维度表与 Phase 2-5 执行阶段划分一一对应(CON→P2, CODE/TOOL→P3, DOC/TEST→P4)"
      pattern: "CON|CODE|TOOL|DOC|TEST"
---

<objective>
产出审计章程 `.planning/audit/CHARTER.md`(覆盖 CHARTER-01~05 全部标尺)与 `findings/` 五个维度发现台账骨架,使 Phase 2–5 的任何审计者拿到章程后可直接套用而无需再作解释。落实锁定决策 D-01(基线 SHA)、D-02(证据格式)、D-03(零 diff 验证)、D-04(无例外协议)、D-05(排除清单)、D-06(scripts/ 缩窄)、D-07(秘密扫描穿透)、D-09(存在级问题处置)。

Purpose: 审计的所有标尺与边界在任何证据收集开始前定稿(ROADMAP Phase 1 目标);章程条款模糊会造成 Phase 2/3 并行执行者口径漂移,Phase 5 校准成本剧增。
Output: `.planning/audit/CHARTER.md` + `.planning/audit/findings/{contract,code,toolchain,docs-config,test}.md` 五个台账骨架。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-audit-charter-baseline/01-CONTEXT.md
@.planning/phases/01-audit-charter-baseline/01-RESEARCH.md
@.planning/phases/01-audit-charter-baseline/01-PATTERNS.md
</context>

## Artifacts this phase produces

本阶段(两个 plan 合计)创建的全部符号与文件:

- 文件:`.planning/audit/CHARTER.md`、`.planning/audit/HYPOTHESES.md`(plan 02)、`.planning/audit/DO-NOT-FIX.md`(plan 02)、`.planning/audit/findings/contract.md`、`.planning/audit/findings/code.md`、`.planning/audit/findings/toolchain.md`、`.planning/audit/findings/docs-config.md`、`.planning/audit/findings/test.md`
- ID 体系(本 plan 在章程中定义):发现 ID `F-<维度码>-NN`(F-CON-NN / F-CODE-NN / F-TOOL-NN / F-DOC-NN / F-TEST-NN)、假设 ID `HYP-NN`、Do-NOT-fix ID `DNF-NN`
- 维度短码:`CON`(契约一致性)、`CODE`(组件代码)、`TOOL`(部署与验证工具链)、`DOC`(文档配置一致性)、`TEST`(测试质量与覆盖)
- 证据引用格式:`path:line @ 5927f36` / `path:10-25 @ 5927f36`
- 严重度术语:CRITICAL / HIGH / MEDIUM / LOW / INFO(定性,影响×可能性一行理由)
- 工作量档位:S / M / L / XL
- 发现状态值:`draft` / `calibrated`;上线判定值:BLOCKER / PRE-LAUNCH / POST-LAUNCH(Phase 5 填)
- 无新代码符号——本阶段为纯文档阶段,零 diff 保护区(apps/、scripts/、docs/)不产生任何变更

<tasks>

<task type="auto">
  <name>Task 1: 创建 CHARTER.md — 基线声明、范围与方法、排除清单、秘密扫描规则</name>
  <files>.planning/audit/CHARTER.md</files>
  <read_first>
    - .planning/phases/01-audit-charter-baseline/01-RESEARCH.md(基线声明区块模板 §Code Examples、五维度表 §Pattern 1、取证命令 §Pattern 2、秘密扫描模式清单 §"秘密扫描模式清单建议"、Pitfall 6 的零 diff 覆盖面说明)
    - .planning/phases/01-audit-charter-baseline/01-PATTERNS.md(头部元数据/页脚/排除项表格/中英混排等文档结构惯例)
    - .planning/phases/01-audit-charter-baseline/01-CONTEXT.md(D-01~D-09 原文措辞,尤其 D-04 的绝对性措辞与 specifics 节)
    - .planning/REQUIREMENTS.md(CHARTER-01/04 需求原文、Out of Scope 表)
    - .planning/codebase/STRUCTURE.md(排除清单九条路径的目录用途依据)
  </read_first>
  <action>
    创建 `.planning/audit/CHARTER.md`(先建 `.planning/audit/` 目录),中文正文 + 英文 ID/术语,含以下章节(本 task 只写前半部,严重度/工作量/schema 章节由 Task 2 追加):

    1. 头部:`# 审计章程: SoniScope — 上线前代码审计` + `**Defined:** 2026-07-04` + `**基线 commit:** 5927f362785d44b085a791ca387732991012ce5a`。完整 SHA 全文只在此声明一次,正文其余处一律用短 SHA `5927f36`(per D-02)。

    2. `## 审计基线`(per D-01/D-02/D-03/D-04):直接采用 01-RESEARCH.md §Code Examples 已验证的基线声明区块——分支 `ralph/soniscope-mvp-claude`(main 落后 53 提交且无独立提交)、钉定时工作树干净、旧权威文档迁移(docs/PRD_v1.md、docs/tech-spec.md、docs/deployment-guide.md → `docs/v1.0.0 prd/` 与 `docs/runbook/`)已随提交入库(此即 CHARTER-01 要求的 dirty-tree 处置成文:记录事实,阻塞已自行解除)。证据格式 `path:line @ 5927f36`,多行 `path:10-25 @ 5927f36`;证据一律提取自 `git show 5927f36:<path>`,禁止以工作树文件充当行号证据(Pitfall 5 对策)。零 diff 验证条款(per D-03):写定命令 `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出必须为空,Phase 2–5 每阶段收尾执行一次并记录结果;并加一句说明:该命令只保护三个目录,Makefile/AGENTS.md/pyproject.toml 等根文件审计对象靠"证据一律出自 5927f36"兜底(Pitfall 6)。无例外协议(per D-04,保留 CONTEXT.md specifics 的绝对性措辞):任何发现(含 CRITICAL,如泄露的有效凭证)一律只进台账并标 BLOCKER,不中断审计、不重钉基线;云端操作(账号中删凭证、改环境变量等)绝不由审计者动手,同样只进台账——无例外、无自由裁量。

    3. `## 范围与方法`(CHARTER-04):五个审计维度表(照 01-RESEARCH.md §Pattern 1:CON=契约一致性/CONTRACT-01~04/Phase 2,CODE=组件代码/AUDIT-01/Phase 3,TOOL=部署与验证工具链/AUDIT-02/Phase 3,DOC=文档配置一致性/AUDIT-03/Phase 4,TEST=测试质量与覆盖/AUDIT-04/Phase 4);审计 SHA 引用;明确排除项(REQUIREMENTS.md Out of Scope 原文对应):FC 直转目标态对照(契约一致性以三处实现现状互相对照为准)、渗透测试深度(顺带安全发现仍记录并标注"顺带发现")、vendored `docs/example/start-fc-main/` 逐行审计、数值化质量评分、精确工时估计;双语言适配声明(Python 3.11+ mypy-strict/ruff 与小程序 CommonJS JS 的审计判断标准分别适配);证据提取方法(`git show`/`git grep` 按基线 SHA 取证,命令示例引 01-RESEARCH.md §Pattern 2 三条已实测命令)。

    4. `## 扫描排除清单`(per D-05/D-06/D-09):表格列出九条排除路径及理由——`docs/example/start-fc-main/`(vendored,29MB)、`scripts/ralph/`(agent 元工具)、`.claude/`、`.cursor/`、`.codex/`、`.agents/`(四套 AI 工具目录)、`openspec/`(工作流状态)、`build/`、`tests/audio/`(产物与二进制;fixture manifest/描述文件仍纳入 DOC 维度文档一致性审计)。AUDIT-02 的 scripts/ 审计范围缩窄声明(per D-06):仅 `scripts/test_asr.py`、`scripts/fetch_test_fixtures.py`、`scripts/gen_worker_config.sh`。存在级问题处置条款(per D-09):被排除目录的存在级问题(vendored 仓库膨胀、四套工具目录漂移、scripts/ralph/ 在仓)照常进台账,严重度预期 LOW/INFO,但不逐文件审计。

    5. `## 秘密扫描穿透规则`(per D-07):声明秘密/凭证扫描穿透所有排除目录,对基线 commit 全量扫描(`git grep <pattern> 5927f36 -- .` 天然覆盖该 commit 全部已跟踪文件);列出 01-RESEARCH.md §"秘密扫描模式清单建议" 的五类模式命令(LTAI 长期 AK ID、`OSSAccessKeyId=` 签名 URL、`Signature=` 签名参数、appsecret 字面量、SecurityToken/STS token);写明"命中 ≠ 发现,人工核实后才进台账";引用 `scripts/test_asr.py` 曾提交过期预签名 URL 的先例说明穿透理由(CONTEXT.md specifics 要求保留此例);写明秘密类证据红线:引用只写 `path:line @ 5927f36` + 模式名,绝不复制值本体,哪怕已过期。

    禁止事项:全文不得出现"视情况""可酌情""一般来说"类裁量措辞;不得对 apps/、scripts/、docs/ 做任何 Edit/Write;工期一律以 S/M/L/XL 表达,任何时长不得以"数字+小时"形式书写(时长示例用秒/分钟,如 STS ≤900 秒)。
  </action>
  <verify>
    <automated>test -f .planning/audit/CHARTER.md && test "$(grep -c '5927f362785d44b085a791ca387732991012ce5a' .planning/audit/CHARTER.md)" -eq 1 && grep -q '@ 5927f36' .planning/audit/CHARTER.md && grep -q 'git diff --stat 5927f36 -- apps/ scripts/ docs/' .planning/audit/CHARTER.md && for p in start-fc-main scripts/ralph .claude/ .cursor/ .codex/ .agents/ openspec build tests/audio; do grep -qF "$p" .planning/audit/CHARTER.md || { echo "MISSING $p"; exit 1; }; done && grep -q 'LTAI' .planning/audit/CHARTER.md && grep -q 'test_asr.py' .planning/audit/CHARTER.md && ! grep -qE '视情况|可酌情' .planning/audit/CHARTER.md && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audit/CHARTER.md` 存在;完整 SHA `5927f362785d44b085a791ca387732991012ce5a` 在全文恰出现 1 次(`grep -c` = 1),短 SHA 证据格式 `@ 5927f36` 至少出现 1 次
    - 零 diff 命令原文 `git diff --stat 5927f36 -- apps/ scripts/ docs/` 在章程中逐字可 grep 到
    - 九条排除路径(start-fc-main、scripts/ralph、.claude/、.cursor/、.codex/、.agents/、openspec、build、tests/audio)全部可 grep 到
    - 秘密扫描章节含 `LTAI` 模式与 `test_asr.py` 先例引用
    - 全文 grep 不到"视情况"与"可酌情"
    - 零 diff 保持:`git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>CHARTER.md 前半部(基线、范围与方法、排除清单、秘密扫描)成文,D-01~D-07、D-09 全部条款落地,零 diff 保护区未被触碰</done>
</task>

<task type="auto">
  <name>Task 2: CHARTER.md 追加严重度体系、工作量分档、发现记录 schema 与 ID 规则</name>
  <files>.planning/audit/CHARTER.md</files>
  <read_first>
    - .planning/audit/CHARTER.md(Task 1 产出的当前状态,追加须保持章节风格一致)
    - .planning/phases/01-audit-charter-baseline/01-RESEARCH.md(§"严重度锚点建议" 五级锚点表与顺带安全发现处理、§Pattern 3 发现 schema 九字段模板与 ID 规则论证、§"CONCERNS.md 线索分流盘点")
    - .planning/REQUIREMENTS.md(CHARTER-02/03/05 需求原文:五级术语、影响×可能性格式、S/M/L/XL 判定标准原文)
  </read_first>
  <action>
    向 `.planning/audit/CHARTER.md` 追加三个章节(置于 Task 1 各章节之后、页脚之前):

    1. `## 严重度体系`(CHARTER-02):五级表,每级用 SoniScope 场景术语锚定,采纳 01-RESEARCH.md §"严重度锚点建议" 的锚点——CRITICAL=用户录音数据丢失或不可恢复(OSS 对象被删、`.done` 早写致片段永久跳过)/有效长期凭证泄露(在库 LTAI AK、WX_APP_SECRET 明文且仍有效)/认证绕过(allowlist 失效);HIGH=静默转写失败(契约活跃失配使上传对 Worker 永久不可见)/STS 权限越界(单键策略失效)/崩溃恢复产出损坏工件;MEDIUM=潜伏失配(当前参数下不触发,变更即爆)/可诱发高危误操作的误导性文档/已过期凭证曾入库(泄露习惯风险);LOW=技术债与非关键路径重复实现/文档死链/lint-typecheck 覆盖缺口/非热路径性能;INFO=存在级观察(vendored 膨胀、工具目录漂移,呼应 D-09 预期定级)/风格不一致/无行动必要的事实。评级理由格式条款:每个评级必须附一行"影响:…;可能性:…"定性理由,禁止任何数值化评分形式(呼应 REQUIREMENTS.md Out of Scope 对数值评分的禁令,描述该禁令时以文字表述、不引数字示例)。顺带安全发现条款:不设自动升级规则,与其他发现同用影响×可能性定级,仅在台账加"顺带发现(out-of-dimension)"标注。边界情况以锚点示例封死,不写授权裁量的措辞。

    2. `## 工作量分档`(CHARTER-03):S/M/L/XL 判定标准照 REQUIREMENTS.md 原文成文——S ≤单文件、M=同组件多文件、L=跨组件、XL=需独立阶段;明文禁止精确工时估计(表述为"禁止小时级精确估计",不写数字示例);每档配一个 SoniScope 示例:改 `apps/miniprogram/config.js` 一处常量=S;`fc_shared` 内多文件调整=M;fragment_id 格式变更需 FC+Worker+小程序三处同步=L;实现 `transcribe-audio` FC 函数=XL。

    3. `## 发现记录 schema 与台账布局`(CHARTER-05):照 01-RESEARCH.md §Pattern 3 定稿九字段固定顺序——ID(`### F-<维度码>-NN: 一行标题`)、维度、严重度(含影响×可能性一行理由)、证据(`path:line @ 5927f36` + git show 提取的引用片段)、修复建议、工作量、关联发现(承载发现↔发现与发现↔HYP 链接,喂 RPT-08)、上线判定(Phase 5 填,建槽留空,取值 BLOCKER/PRE-LAUNCH/POST-LAUNCH)、状态(draft/calibrated,Phase 5 校准留痕)。ID 规则:发现 `F-CON-NN`/`F-CODE-NN`/`F-TOOL-NN`/`F-DOC-NN`/`F-TEST-NN`(加 `F-` 前缀以区别于需求 ID `CONTRACT-NN`)、假设 `HYP-NN`、Do-NOT-fix `DNF-NN`。台账布局:`findings/` 按维度分 5 文件(contract/code/toolchain/docs-config/test),分文件理由=Phase 2 与 Phase 3 并行写入防冲突,每阶段只写自己维度的文件,Phase 5 汇总合并;台账与报告一律落在 `.planning/audit/`,严禁写入 apps/、scripts/、docs/(零 diff 硬约束);另注明配套文件 `HYPOTHESES.md`(假设清单)与 `DO-NOT-FIX.md`(RPT-05 预录入)由本阶段 plan 02 产出。

    最后补页脚:`---` + `*审计章程定稿: 2026-07-04*`(照 01-PATTERNS.md 页脚惯例)。
  </action>
  <verify>
    <automated>for s in CRITICAL HIGH MEDIUM LOW INFO; do grep -q "$s" .planning/audit/CHARTER.md || { echo "MISSING $s"; exit 1; }; done && grep -q '影响' .planning/audit/CHARTER.md && grep -q '可能性' .planning/audit/CHARTER.md && grep -q 'XL' .planning/audit/CHARTER.md && ! grep -qE '[0-9]+(\.[0-9]+)?[[:space:]]*小时' .planning/audit/CHARTER.md && grep -q 'F-CON' .planning/audit/CHARTER.md && grep -q 'F-CODE' .planning/audit/CHARTER.md && grep -q 'F-TOOL' .planning/audit/CHARTER.md && grep -q 'F-DOC' .planning/audit/CHARTER.md && grep -q 'F-TEST' .planning/audit/CHARTER.md && grep -q 'HYP-' .planning/audit/CHARTER.md && grep -q 'DNF-' .planning/audit/CHARTER.md && grep -q '上线判定' .planning/audit/CHARTER.md && grep -q 'calibrated' .planning/audit/CHARTER.md && ! grep -qE '视情况|可酌情' .planning/audit/CHARTER.md && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)"</automated>
  </verify>
  <acceptance_criteria>
    - CRITICAL、HIGH、MEDIUM、LOW、INFO 五个术语全部在 CHARTER.md 可 grep 到,且"影响"与"可能性"作为理由格式关键词出现
    - `XL` 可 grep 到,全文 grep 不到"数字+小时"形式的工时表达(正则 `[0-9]+(\.[0-9]+)?[[:space:]]*小时` 零命中)
    - 五个发现 ID 前缀(F-CON/F-CODE/F-TOOL/F-DOC/F-TEST)与 HYP-、DNF- 前缀全部在章程中定义
    - schema 章节含"上线判定"槽位与 `calibrated` 状态值
    - 全文 grep 不到"视情况"与"可酌情";零 diff 保持
  </acceptance_criteria>
  <done>CHARTER.md 完整定稿:严重度五级锚点、S/M/L/XL、九字段 schema、三套 ID 规则、台账布局全部成文,CHARTER-02/03/05 章程侧条款闭合</done>
</task>

<task type="auto">
  <name>Task 3: 创建 findings/ 五个维度台账骨架</name>
  <files>.planning/audit/findings/contract.md, .planning/audit/findings/code.md, .planning/audit/findings/toolchain.md, .planning/audit/findings/docs-config.md, .planning/audit/findings/test.md</files>
  <read_first>
    - .planning/audit/CHARTER.md(Task 2 定稿的 schema 九字段与 ID 规则——骨架示例必须与之逐字段一致)
    - .planning/phases/01-audit-charter-baseline/01-PATTERNS.md(§findings 骨架说明:仅表头 + 1 条 schema 示例,不写真实发现)
  </read_first>
  <action>
    创建 `.planning/audit/findings/` 目录及五个文件:`contract.md`(CON,Phase 2 写入)、`code.md`(CODE,Phase 3)、`toolchain.md`(TOOL,Phase 3)、`docs-config.md`(DOC,Phase 4)、`test.md`(TEST,Phase 4)。每个文件结构相同:

    1. 头部:`# 发现台账: <维度中文名> (<维度码>)` + `**Created:** 2026-07-04` + 一行说明(本文件由哪个 Phase 写入、ID 前缀是什么、schema 以 `.planning/audit/CHARTER.md` 为准)。
    2. 一条编号为 00 的 schema 示例条目(如 `### F-CON-00: (schema 示例,非真实发现)`),九字段与 CHARTER.md schema 章节完全一致的占位内容:维度、严重度(含"影响:…;可能性:…"占位)、证据(`path:line @ 5927f36` 格式占位)、修复建议、工作量、关联发现、上线判定(留空)、状态(draft)。示例条目正文须明确标注"示例,Phase 5 汇总时剔除"。
    3. `## 发现` 空节,供后续阶段 append。

    五个文件的示例条目 ID 分别为 F-CON-00、F-CODE-00、F-TOOL-00、F-DOC-00、F-TEST-00。不写任何真实发现(本阶段不收集证据)。
  </action>
  <verify>
    <automated>for f in contract:F-CON code:F-CODE toolchain:F-TOOL docs-config:F-DOC test:F-TEST; do file=".planning/audit/findings/${f%%:*}.md"; pfx="${f##*:}"; test -f "$file" || { echo "MISSING $file"; exit 1; }; grep -q "${pfx}-00" "$file" || { echo "NO SCHEMA EXAMPLE in $file"; exit 1; }; done && for fld in 维度 严重度 证据 修复建议 工作量 关联发现 上线判定 状态; do grep -q "$fld" .planning/audit/findings/contract.md || { echo "MISSING FIELD $fld"; exit 1; }; done && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)"</automated>
  </verify>
  <acceptance_criteria>
    - 五个文件全部存在于 `.planning/audit/findings/`,文件名为 contract.md、code.md、toolchain.md、docs-config.md、test.md
    - 每个文件含对应前缀的 00 号 schema 示例(F-CON-00、F-CODE-00、F-TOOL-00、F-DOC-00、F-TEST-00)
    - contract.md 的示例条目含全部八个中文字段名:维度、严重度、证据、修复建议、工作量、关联发现、上线判定、状态(第九要素 ID 即 `###` 标题本身)
    - 零 diff 保持:`git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>五个维度台账骨架就位,Phase 2/3 可并行 append 各自维度文件而互不冲突,schema 示例与章程一致</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 审计产物 → git 历史 | 章程/台账一旦提交即永久入库;若复制了秘密值本体则构成二次泄露 |
| 审计者 → 零 diff 保护区(apps/、scripts/、docs/)与云端资源 | 审计流程本身不得篡改被审对象与生产资源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Information Disclosure | CHARTER.md 秘密扫描章节引用先例时复制秘密值本体 | high | mitigate | 章程写死"只引 `path:line @ SHA` + 模式名,不复制值本体"红线(Task 1 第 5 节);先例引用仅提文件与模式名 |
| T-01-02 | Tampering | 审计者顺手修改 apps/、scripts/、docs/ 或执行云端操作 | high | mitigate | D-04 无例外协议以绝对措辞写入章程;每 task 的 automated verify 均含零 diff 空输出检查 |
| T-01-03 | Information Disclosure | 秘密扫描规则遗漏 vendored/工具目录 | medium | mitigate | D-07 穿透条款 + `git grep <pattern> 5927f36 -- .` 对 commit 全量扫描写入章程 |

供应链说明:本阶段零包安装(npm/pip/cargo 均无),无供应链威胁面,无需 T-01-SC 行(01-RESEARCH.md Package Legitimacy Audit 判定"不适用")。
</threat_model>

<verification>
- `.planning/audit/CHARTER.md` 通过 01-RESEARCH.md §Validation Architecture 的 CHARTER-01~04 全部 smoke 检查(完整 SHA 恰 1 次、五级术语齐、无小时数字、九排除路径齐)
- `findings/` 五文件存在且 schema 示例字段与章程一致
- 零 diff 不变量:`test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)"` 通过
- 全文无裁量措辞("视情况"/"可酌情"零命中)
</verification>

<success_criteria>
- CHARTER-01:基线 SHA 钉住并成文,dirty-tree 处置(记录事实)成文,证据格式 `path:line @ 5927f36` 定稿
- CHARTER-02:五级严重度 SoniScope 锚点 + 影响×可能性理由格式定稿,任何审计者可直接套用
- CHARTER-03:S/M/L/XL 判定标准 + SoniScope 示例定稿,无小时估计
- CHARTER-04:五维度、审计 SHA、排除项、零 diff 规则、无例外协议全部成文
- CHARTER-05(章程侧):九字段 schema、三套 ID 规则、排除清单、台账布局定稿,findings/ 骨架就位
</success_criteria>

<output>
Create `.planning/phases/01-audit-charter-baseline/01-01-SUMMARY.md` when done
</output>
