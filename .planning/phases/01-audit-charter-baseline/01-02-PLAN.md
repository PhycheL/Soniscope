---
phase: 01-audit-charter-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/DO-NOT-FIX.md
  - .planning/audit/HYPOTHESES.md
  - .planning/STATE.md
autonomous: true
requirements: [CHARTER-05]
must_haves:
  truths:
    - "HYP 条目数 + DNF 条目数 = CONCERNS.md 粗体线索条目数(机械计数 29),对账等式写在 HYPOTHESES.md 头部且可 grep 复核"
    - "每条 HYP 恰标注一个待验证维度(CON/CODE/TOOL/DOC/TEST)并注明 CONCERNS.md 来源节名与条目标题,可追溯性无断链"
    - "DO-NOT-FIX.md 含 4 条 DNF,每条带 `⚠ intentional` 标注与从 git show 5927f36 提取的 `path:line @ 5927f36` 证据"
    - "Known Bugs 节的显式'已检查,无已知 bug 线索'记录存在于 HYPOTHESES.md(喂 RPT-08 负向记录)"
    - "任何秘密值本体未被复制进 HYPOTHESES.md / DO-NOT-FIX.md(只引位置与模式名)"
    - "STATE.md 中 Phase 1 dirty-tree 阻塞条目已改写为已解除记录"
  artifacts:
    - path: ".planning/audit/DO-NOT-FIX.md"
      provides: "RPT-05 登记表初稿:4 条 DNF 预录入(D-08)"
      contains: "DNF-01"
    - path: ".planning/audit/HYPOTHESES.md"
      provides: "CONCERNS.md 全部线索的未验证假设清单(HYP-NN,标注待验证维度),Phase 4 AUDIT-05 关闭线索的工作底稿"
      contains: "HYP-01"
  key_links:
    - from: ".planning/audit/HYPOTHESES.md"
      to: ".planning/codebase/CONCERNS.md"
      via: "每条 HYP 的来源字段指向 CONCERNS.md 原节名+条目标题,头部对账等式核对总数"
      pattern: "CONCERNS.md"
    - from: ".planning/audit/DO-NOT-FIX.md"
      to: ".planning/audit/HYPOTHESES.md"
      via: "分流互补:同一 CONCERNS 条目只出现在一侧;HYPOTHESES.md 头部对账等式引用 DNF 计数"
      pattern: "DNF-"
---

<objective>
将 `.planning/codebase/CONCERNS.md` 全部 29 条线索按 D-08 分流:4 条"故意设计"条目直接预录入 `.planning/audit/DO-NOT-FIX.md`(RPT-05 登记表初稿),其余 25 条转为 `.planning/audit/HYPOTHESES.md` 未验证假设清单(每条标注待验证维度),另为 Known Bugs 节保留 1 条显式"无线索"记录;同时清理 `.planning/STATE.md` 中已过时的 dirty-tree 阻塞。实现 ROADMAP Phase 1 成功判据 5 与 CHARTER-05 的假设清单侧交付,落实锁定决策 D-08(Do-NOT-fix 预录入分流)。

Purpose: 假设清单是 Phase 4(AUDIT-05)逐条关闭线索的唯一工作底稿,可追溯性断链会使 RPT-08 映射表无法组装;DNF 预录入直接流入 Phase 5 的 RPT-05 登记表。
Output: `.planning/audit/DO-NOT-FIX.md`、`.planning/audit/HYPOTHESES.md`,更新后的 `.planning/STATE.md`。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/codebase/CONCERNS.md
@.planning/phases/01-audit-charter-baseline/01-CONTEXT.md
@.planning/phases/01-audit-charter-baseline/01-RESEARCH.md
@.planning/phases/01-audit-charter-baseline/01-PATTERNS.md
</context>

## Artifacts this phase produces

本 plan 创建的符号与文件(phase 全量清单见 01-01-PLAN.md 同名章节):

- 文件:`.planning/audit/DO-NOT-FIX.md`、`.planning/audit/HYPOTHESES.md`
- ID 实例:`DNF-01` ~ `DNF-04`(Do-NOT-fix 登记条目)、`HYP-01` ~ `HYP-25`(未验证假设条目)
- 标注措辞:`⚠ intentional — do not "fix"`(取 REQUIREMENTS.md RPT-05 原文)
- HYP 条目字段:来源 / 假设 / 待验证维度 / 状态(初始值"未验证")/ 备注(可选)
- 修改文件:`.planning/STATE.md`(Blockers/Concerns 节过时条目改写)
- 无新代码符号——纯文档产物,零 diff 保护区不产生任何变更

<tasks>

<task type="auto">
  <name>Task 1: 创建 DO-NOT-FIX.md — D-08 四条预录入(RPT-05 初稿)</name>
  <files>.planning/audit/DO-NOT-FIX.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md(四条来源条目原文:Tech Debt 节 whisper-local 桩、Fragile Areas 节 issue-cedential 域名、Fragile Areas 节 FC handlers mypy 豁免、Security 节小程序接收原始 STS 秘密)
    - .planning/phases/01-audit-charter-baseline/01-RESEARCH.md(§"CONCERNS.md 线索分流盘点" 的 DNF 预录入清单与 A3 假设说明)
    - .planning/phases/01-audit-charter-baseline/01-PATTERNS.md(§DO-NOT-FIX.md 的四条来源行号与标注措辞出处)
  </read_first>
  <action>
    创建 `.planning/audit/DO-NOT-FIX.md`(目录 `.planning/audit/` 若不存在则先建;与 plan 01 并行执行时该目录可能尚未创建,mkdir -p 幂等处理)。头部:`# Do-NOT-fix 登记表(RPT-05 初稿)` + `**Created:** 2026-07-04` + 说明(per D-08 预录入,Phase 5 组装 RPT-05 时用户最终裁定;本表条目不转假设、后续阶段不再验证)。

    四条条目,每条格式 `### DNF-NN: <一行标题>`,字段:标注(逐字使用 REQUIREMENTS.md RPT-05 的原文标注 `⚠ intentional — do not "fix"`)、来源(CONCERNS.md §节名 / 条目标题)、证据(`path:line @ 5927f36`,行号必须先用 `git show 5927f36:<path>` 核实后填写,不读工作树)、理由(为何是故意设计,引 CONCERNS.md 原文要点)、分流依据(D-08 点名 / D-08 "等"字延伸即 RESEARCH A3):

    1. DNF-01 `whisper-local` 转写器为故意桩 — 证据定位 `apps/worker/src/soniscope_worker/transcriber.py`(WhisperLocalTranscriber.transcribe 抛错处,约 145 行,以 git show 核实为准);D-08 点名。
    2. DNF-02 `issue-cedential` 拼写域名为 Aliyun 真实分配值 — 证据定位 `apps/miniprogram/config.js`(URL 常量及其警告注释,约 8-10 行);D-08 点名。
    3. DNF-03 FC handler.py 的 mypy strict 豁免 — 证据定位根 `pyproject.toml`([tool.mypy] 中说明模块名冲突缘由的注释行);D-08 点名。备注:CONCERNS.md Test Coverage Gaps 节另有同主题条目,该条不入本表、由 HYPOTHESES.md 以 TEST 维度承接(仅验证"行为测试补偿充分"这一判断,不质疑豁免本身),两侧互相以 ID 交叉引用。
    4. DNF-04 小程序接收原始 STS 秘密(by design)— 证据定位 `apps/fc/shared/fc_shared/sts.py`(credential_response 返回 access_key_secret/security_token 处,约 102 行);分流依据为 D-08 "等"字延伸(RESEARCH.md 假设 A3),在条目内写明此依据以便用户 Phase 5 裁定。

    秘密红线:DNF-04 涉及凭证机制,证据只写位置与字段名(access_key_secret、security_token 为代码标识符,可写),绝不复制任何真实密钥/token/签名值。页脚照 01-PATTERNS.md 惯例。
  </action>
  <verify>
    <automated>test -f .planning/audit/DO-NOT-FIX.md && test "$(grep -c '^### DNF-' .planning/audit/DO-NOT-FIX.md)" -eq 4 && test "$(grep -c 'intentional' .planning/audit/DO-NOT-FIX.md)" -ge 4 && test "$(grep -c '@ 5927f36' .planning/audit/DO-NOT-FIX.md)" -ge 4 && grep -q 'transcriber.py' .planning/audit/DO-NOT-FIX.md && grep -q 'config.js' .planning/audit/DO-NOT-FIX.md && grep -q 'pyproject.toml' .planning/audit/DO-NOT-FIX.md && grep -q 'sts.py' .planning/audit/DO-NOT-FIX.md && ! grep -qE 'OSSAccessKeyId=TMP\.[A-Za-z0-9]{4,}|Signature=[0-9A-Za-z%+]{16,}' .planning/audit/DO-NOT-FIX.md && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^### DNF-' .planning/audit/DO-NOT-FIX.md` 恰为 4
    - 每条含 `⚠ intentional — do not "fix"` 标注(`grep -c 'intentional'` ≥ 4)与 `@ 5927f36` 证据引用(≥ 4 处)
    - 四个证据文件名(transcriber.py、config.js、pyproject.toml、sts.py)全部可 grep 到
    - 秘密值本体零命中:正则 `OSSAccessKeyId=TMP\.[A-Za-z0-9]{4,}` 与 `Signature=[0-9A-Za-z%+]{16,}` 在本文件均无匹配
    - 零 diff 保持:`git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>RPT-05 登记表初稿就位:4 条 DNF 带标注、SHA 钉定证据与分流依据,DNF-04 的 A3 假设性质已写明供 Phase 5 裁定</done>
</task>

<task type="auto">
  <name>Task 2: 创建 HYPOTHESES.md — CONCERNS.md 其余 25 条线索转未验证假设</name>
  <files>.planning/audit/HYPOTHESES.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md(全部 9 个节、29 条粗体条目——逐条转写的唯一数据源)
    - .planning/audit/DO-NOT-FIX.md(Task 1 产出——确认哪 4 条已分流,避免重复入账)
    - .planning/phases/01-audit-charter-baseline/01-RESEARCH.md(§"CONCERNS.md 线索分流盘点" 的维度归属示例与 A4 说明、§Pitfall 4 可追溯性规则)
    - .planning/phases/01-audit-charter-baseline/01-PATTERNS.md(§HYPOTHESES.md 的条目字段布局、计数对账 pattern、显式无发现记录 pattern)
  </read_first>
  <action>
    创建 `.planning/audit/HYPOTHESES.md`。头部:`# 未验证假设清单` + `**Created:** 2026-07-04` + 转换对账块(照 01-PATTERNS.md 计数对账 pattern):`29 条 CONCERNS.md 粗体线索 = 4 条 DNF 预录入(见 DO-NOT-FIX.md)+ 25 条 HYP;另加 1 条 Known Bugs 显式"无线索"记录(不计入 29)`。注明:01-RESEARCH.md 曾统计为 30 条,系 Fragile Areas 节误计(实为 5 条),以机械计数命令 `grep -cE '^\*\*[^*]+:\*\*$' .planning/codebase/CONCERNS.md` 输出 29 为准。

    正文按 CONCERNS.md 原节顺序分节(Tech Debt / Known Bugs / Security / Performance / Fragile Areas / Scaling Limits / Dependencies at Risk / Missing Critical Features / Test Coverage Gaps)。除 Task 1 已分流的 4 条 DNF 外,其余 25 条粗体条目逐条转写为 `### HYP-NN: <沿用 CONCERNS.md 条目标题>`(NN 从 01 连续编号到 25),字段:
    - **来源:** CONCERNS.md §原节名 / 原条目标题(一一对应,禁止合并或改写标题——Pitfall 4 可追溯性规则)
    - **假设:** 把 CONCERNS.md 的叙述改写为一句可证实/证伪的陈述(线索是假设不是答案,不下结论)
    - **待验证维度:** 恰一个短码。建议标注(执行者可按更合理判断调整,但每条必须恰一个):FC 直转未实现→CODE;权威文档迁移引用失效(含 AGENTS.md 旧路径)→DOC;纯 JS sha256→CODE;fc_deploy 仅 update_code→TOOL;vendored 仓库在库→DOC;四套工具目录漂移→DOC;已提交过期预签名 URL→TOOL;FC 环境变量/config.yaml 长期凭证→CODE;单用户 allowlist→CODE;顺序处理 Worker→CODE;FC 直转 NLS 轮询计费→DOC(备注:涉 FC 直转目标态,属章程排除项,预计 Phase 4 以"细化:范围外"关闭);wsgiref 服务器→CODE;fragment_id 三处重复→CON;ENV 硬编码 development→DOC;自研 miniprogram lint→TOOL;单机单用户轮询→CODE;无 FC 限流→CODE;legacy SDK in test_asr→TOOL;NLS 2018 API 依赖→CODE;transcribe-audio 未建(Missing Features 节)→CODE(备注:与 Tech Debt 首条同根,互相以 HYP ID 关联);转写结果无消费展示→DOC;E2E 需手工微信 code→TEST;handler.py 在 mypy strict 之外(Test Coverage Gaps 节)→TEST(备注:豁免本身系故意设计见 DNF-03,本条仅验证行为测试补偿是否充分);页面级代码仅经纯模块测试→TEST;scripts/ 不在 lint/typecheck 范围→TEST
    - **状态:** 未验证(Phase 4 回填证实/证伪/细化)
    - **备注:** 可选;"acceptable for MVP" 类条目(fc_deploy 仅 update_code、单用户 allowlist、wsgiref、Worker 串行)在备注写明"CONCERNS.md 自评可接受,待 Phase 3/4 核实该判断是否成立"(RESEARCH A4 分流依据)

    Known Bugs 节:不设 HYP 编号,写一条显式记录,正文含原句要义"已检查,无已知 bug 线索"(CONCERNS.md 原文 None detected in application code,apps/ 无 TODO/FIXME/HACK 标记),注明该记录喂 RPT-08 的"已检查,无发现"显式行。

    秘密红线:转写 Security 节条目时只写位置与模式名(如 `OSSAccessKeyId=TMP.*` 模式),绝不复制真实签名/token 值。页脚照 01-PATTERNS.md 惯例。
  </action>
  <verify>
    <automated>HYP=$(grep -c '^### HYP-' .planning/audit/HYPOTHESES.md); DNF=$(grep -c '^### DNF-' .planning/audit/DO-NOT-FIX.md); SRC=$(grep -cE '^\*\*[^*]+:\*\*$' .planning/codebase/CONCERNS.md); test $((HYP + DNF)) -eq "$SRC" && test "$HYP" -eq 25 && for d in CON CODE TOOL DOC TEST; do grep -q "待验证维度.*$d" .planning/audit/HYPOTHESES.md || grep -qE "\*\*待验证维度[:：]\*\*.*$d" .planning/audit/HYPOTHESES.md || { echo "MISSING DIM $d"; exit 1; }; done && grep -q '无已知 bug 线索' .planning/audit/HYPOTHESES.md && test "$(grep -c '未验证' .planning/audit/HYPOTHESES.md)" -ge 25 && ! grep -qE 'OSSAccessKeyId=TMP\.[A-Za-z0-9]{4,}|Signature=[0-9A-Za-z%+]{16,}' .planning/audit/HYPOTHESES.md && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)"</automated>
  </verify>
  <acceptance_criteria>
    - 对账等式机械成立:`grep -c '^### HYP-' HYPOTHESES.md`(=25)+ `grep -c '^### DNF-' DO-NOT-FIX.md`(=4)= `grep -cE '^\*\*[^*]+:\*\*$' .planning/codebase/CONCERNS.md`(=29)
    - 五个维度短码 CON/CODE/TOOL/DOC/TEST 均至少出现在一条 HYP 的待验证维度字段
    - "无已知 bug 线索" 显式记录可 grep 到;"未验证" 状态出现 ≥ 25 次
    - 秘密值本体零命中(与 Task 1 同两条正则在本文件无匹配)
    - 零 diff 保持:`git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>25 条 HYP 一一对应 CONCERNS.md 未分流条目,每条恰一个待验证维度与"未验证"初始状态,头部对账等式成立,Phase 4 可逐条对账关闭</done>
</task>

<task type="auto">
  <name>Task 3: 清理 STATE.md 过时的 dirty-tree 阻塞条目</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md(当前 Blockers/Concerns 节,目标条目在 77 行附近)
    - .planning/phases/01-audit-charter-baseline/01-RESEARCH.md(§"关键事实验证" 的工作树核实结论与 §State of the Art 的 Deprecated 条目说明)
  </read_first>
  <action>
    编辑 `.planning/STATE.md` 的 `### Blockers/Concerns` 节:将其中第一条(`[Phase 1]` 开头、关于 3 份 docs 已删未提交的 dirty-tree 条目,见 `.planning/STATE.md:77`)改写为一条已解除记录,内容要点:该阻塞已于 2026-07-04 经 CONTEXT 讨论与 RESEARCH 双重核实解除——工作树干净,`docs/PRD_v1.md`、`docs/tech-spec.md`、`docs/deployment-guide.md` 的删除已随提交入库,内容迁至 `docs/v1.0.0 prd/` 与 `docs/runbook/`;基线事实以 `.planning/audit/CHARTER.md` 审计基线章节为准。改写后的条目须含"已解除"字样,且不得保留原条目中"阻塞"与"决定"连用的原措辞。保留同节第二条(REQUIREMENTS 计数修正记录)不动。不修改 frontmatter(last_updated 等由执行流程统一处理)。仅用 Edit 做定点替换,禁止整文件重写。
  </action>
  <verify>
    <automated>! grep -q '决定阻塞' .planning/STATE.md && grep -q '已解除' .planning/STATE.md && grep -q 'REQUIREMENTS.md 原统计' .planning/STATE.md && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '已解除' .planning/STATE.md` 命中;原条目的"决定阻塞"连用措辞在全文零命中
    - 同节第二条(REQUIREMENTS 计数修正)原文保留,`grep -q 'REQUIREMENTS.md 原统计'` 仍命中
    - 零 diff 保持:`git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>STATE.md 不再宣示已过时的 Phase 1 阻塞,后续阶段读到的项目状态与基线事实一致</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CONCERNS.md 秘密相关线索 → 审计台账 | Security 节条目描述过已提交的签名 URL 等;转写时若复制值本体即构成二次泄露入库 |
| 审计者 → 零 diff 保护区(apps/、scripts/、docs/) | 转写与取证过程不得改动被审对象 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Information Disclosure | HYPOTHESES.md / DO-NOT-FIX.md 转写 Security 类条目时复制签名/token 值本体 | high | mitigate | 条目只写 `path:line @ 5927f36` + 模式名;两文件的 automated verify 均含秘密模式负向 grep 门 |
| T-01-05 | Tampering | 转写/取证时"顺手修复"CONCERNS 指出的问题(如 AGENTS.md 死链) | high | mitigate | 全部产物落 `.planning/audit/`;每 task verify 含零 diff 空输出检查;AGENTS.md 死链仅入 HYP(DOC 维度) |
| T-01-06 | Repudiation | HYP 转写合并/改写标题导致 Phase 4 无法逐条对账(RPT-08 断链) | medium | mitigate | 1:1 转写规则 + 头部对账等式 + verify 的计数等式门(HYP+DNF=源条目数) |

供应链说明:零包安装,无供应链威胁面(01-RESEARCH.md Package Legitimacy Audit 判定"不适用")。
</threat_model>

<verification>
- 对账等式机械通过:HYP(25) + DNF(4) = CONCERNS.md 粗体条目数(29)
- 五维度短码均出现在待验证维度字段;显式"无线索"记录存在
- 两个台账文件秘密模式负向 grep 零命中
- STATE.md 过时阻塞已改写;零 diff 不变量通过
</verification>

<success_criteria>
- ROADMAP Phase 1 成功判据 5 达成:CONCERNS.md 全部线索已转为未验证假设清单,每条标注待验证维度,等待后续阶段证实/证伪
- D-08 落地:故意设计条目直接预录入 RPT-05 初稿,不占用后续验证工作量
- 可追溯链完整:CONCERNS.md 条目 ↔ HYP/DNF ID 一一对应,Phase 4 与 RPT-08 可逐条对账
</success_criteria>

<output>
Create `.planning/phases/01-audit-charter-baseline/01-02-SUMMARY.md` when done
</output>
