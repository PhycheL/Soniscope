---
phase: 03-component-toolchain-deep-dive
plan: 05
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - .planning/audit/findings/toolchain.md
  - .planning/audit/COVERAGE.md
  - .planning/audit/HYPOTHESES.md
  - .planning/audit/HANDOFF-PHASE4.md
  - .planning/audit/scans/ruff-extended.md
  - .planning/audit/scans/vulture.md
autonomous: true
requirements: [AUDIT-02]

must_haves:
  truths:
    - "Worker 包内 12 个验证/运维模块每个完整读过并逐面(9/9)过普审,COVERAGE.md TOOL 对应 12 行无'待审'残留"
    - "TOOL 侧发现按九字段 schema 入 findings/toolchain.md,严重度按工具级影响定级(D-03),不套主链锚点"
    - "HYP-04/15 状态回填完毕并附 file:line @ 5927f36 证据"
    - "D14-3 联调工具契约镜像集群的证据(fc_live/verify_upload_live/e2e_scenarios 各落点行号)已采集,未下裁定"
  artifacts:
    - ".planning/audit/findings/toolchain.md(F-TOOL 条目)"
    - ".planning/audit/COVERAGE.md(TOOL 维度 worker 模块 12 行已填)"
    - ".planning/audit/HYPOTHESES.md(HYP-04/15 已回填)"
  key_links:
    - "HYP-15 回填 → 引用 scans/eslint.md 的量化小结(漏报面底数)+ miniprogram_lint.py 规则清单行号"
    - "D14-3 证据 → 03-07 裁定输入(关联 F-CON 与 CONTRACT-MATRIX 移交记录)"
---

<objective>
对 Worker 包内 12 个验证/运维模块(4,982 行,D-03 锁定归 TOOL 维度)执行普审+深挖:fc_deploy 的 HYP-04(仅 update_code)、miniprogram_lint 的 HYP-15(规则覆盖面,以 ESLint 量化底数对照)、fc_live/verify_upload_live/e2e_scenarios 的 D14-3 证据采集;发现入 findings/toolchain.md,严重度按工具级影响定级。

Purpose: AUDIT-02 的 Worker 工具链份额(成功判据 2);D-03 明确这些模块按功能归"部署与验证工具链"。
Output: findings/toolchain.md 条目、COVERAGE.md TOOL 12 行、HYPOTHESES.md 2 条回填。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-component-toolchain-deep-dive/03-CONTEXT.md
@.planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md
@.planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md
@.planning/audit/CHARTER.md
@.planning/audit/HYPOTHESES.md
@.planning/audit/CONTRACT-MATRIX.md
</context>

## Artifacts this phase produces

见 03-01-PLAN.md 同名节。本计划新增符号:findings/toolchain.md 中 `F-TOOL-01` 起条目;HYPOTHESES.md 中 HYP-04/15 的证据行与状态值;COVERAGE.md TOOL 维度 worker 模块 12 行填值。

<tasks>

<task type="auto">
  <name>Task 1: 四大工具模块深挖普审(verify_prep / fc_deploy / retranscribe / fc_live,2,777 行)</name>
  <files>.planning/audit/findings/toolchain.md, .planning/audit/COVERAGE.md</files>
  <read_first>
    - .planning/audit/findings/toolchain.md(F-TOOL-00 schema 示例——新条目从最大现有编号 +1 起)
    - .planning/audit/CONTRACT-MATRIX.md(§③债务移交记录 D14-3 原文:fc_live 7 字段清单 :47-55、3 错误码 :42-44、50MB 隐式假设 :57-59、合成 fragment_id :256)
    - .planning/audit/scans/ruff-extended.md 与 .planning/audit/scans/vulture.md(销号确认项中属这 4 模块的行)
    - .planning/audit/COVERAGE.md(本任务要填的 4 行;9 面清单定义)
    - .planning/audit/CHARTER.md(严重度体系——本计划按 D-03 用工具级影响定级)
  </read_first>
  <action>
    逐模块 `git show 5927f36:<path>` 完整读 + 9 面过。严重度口径(D-03):工具失准/误导操作者/危险操作防护缺失等工具级影响,不套主链数据丢失锚点;但工具若能触发真云破坏性操作(如误删、误部署),按其后果如实定级:
    - `verify_prep.py`(924):REQUIRED_TOOLS/MIN_DISK_BYTES 等前置检查的完整性与误报面;报告生成路径的异常处理。
    - `fc_deploy.py`(707,深挖 HYP-04):package/backup/deploy/rollback 流程——核实是否仅支持代码更新(配置/env 变更不覆盖)、备份与回滚路径的失败分支、SHARED_PARENT vendoring 逻辑的脆弱点、部署凭证(ALIYUN_DEPLOY_AK_ID/SECRET)读取面是否可能入日志。
    - `retranscribe.py`(590,D-03 点名):显式重转写对 `.done` 状态机的绕行边界——误触面(是否可能波及非目标 fragment)。
    - `fc_live.py`(556,D14-3 证据点):对照 CONTRACT-MATRIX 移交记录逐处核实契约镜像(字段清单/错误码/50MB 假设/合成 fragment_id)现状与"与 fc_shared 保持一致"注释锚点的有无——只采证据与行号,裁定留 03-07(D-15)。
    落格规则:九字段 schema(同 findings/contract.md 范本),维度字段一律写 `部署与验证工具链 (TOOL)`,ID 从 F-TOOL-01 顺延,DNF 对照跳过,深挖无发现记显式"无发现",契约观察只移交(成功判据 4)。
  </action>
  <verify>
    <automated>awk -F'|' '$2 ~ /verify_prep\.py|fc_deploy\.py|retranscribe\.py|fc_live\.py/ && $0 ~ /待审/' .planning/audit/COVERAGE.md | grep -c . | grep -q '^0$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 中四模块行全部落格(已过面 9/9)
    - findings/toolchain.md 新增条目(如有)维度字段为 `部署与验证工具链 (TOOL)`,九字段齐备,证据含 `@ 5927f36` 引用片段
    - fc_live.py 的 D14-3 四处证据(字段清单/错误码/大小假设/合成 ID)行号记入 COVERAGE 深挖点登记节或行备注,未下裁定
    - HYP-04 的核实证据(fc_deploy 能力边界的具体行号)已采集(回填在 Task 3)
  </acceptance_criteria>
  <done>四大工具模块深挖完毕,HYP-04 证据与 D14-3 证据就位。</done>
</task>

<task type="auto">
  <name>Task 2: 其余 8 个工具模块普审(2,205 行)</name>
  <files>.planning/audit/findings/toolchain.md, .planning/audit/COVERAGE.md, .planning/audit/HANDOFF-PHASE4.md</files>
  <read_first>
    - .planning/audit/findings/toolchain.md(编号状态)
    - .planning/audit/scans/eslint.md(尾部 HYP-15 量化小结——miniprogram_lint 漏报面底数)
    - .planning/audit/scans/vulture.md(销号确认项中属这 8 模块的行)
    - .planning/audit/COVERAGE.md(本任务要填的 8 行)
  </read_first>
  <action>
    逐模块 `git show 5927f36:<path>` 完整读 + 9 面过:
    - `verify_upload_live.py`(464,D14-3 证据点 :34-35 REASON_* 第三份字面定义、:201 合成 fragment_id):采证不裁定。
    - `ops.py`(380):OSS 运维辅助命令——重点核实是否存在删除类操作入口(CLAUDE.md 反模式:Worker 业务代码禁 DeleteObject;运维工具若有删除入口,核实其防误触设计并按工具级影响定级)。
    - `e2e.py`(295)/`e2e_scenarios.py`(268,D14-3 导入消费端——核实其确为导入而非第四份字面副本)/`sts_escape.py`(268,STS 越权探测工具——凭证处理面)/`fixtures.py`(232,D14-1 stdlib hashlib 侧证据 :21,118)/`miniprogram_lint.py`(218,深挖 HYP-15:逐条列出其现有检查规则清单与行号,对照 scans/eslint.md 量化小结评估漏报面——注意 CHARTER 双语言适配:ESLint 是线索底数不是标准)/`latency.py`(80)。
    落格规则同 Task 1;TEST 维度顺带证据(如工具模块暴露的测试缺口观察)按 D-11 入 HANDOFF-PHASE4.md TEST 节,不立发现。
  </action>
  <verify>
    <automated>awk -F'|' '$4 ~ /TOOL/ && $2 ~ /soniscope_worker/ && $0 ~ /待审/' .planning/audit/COVERAGE.md | grep -c . | grep -q '^0$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 中 TOOL 维度、路径含 soniscope_worker 的全部 12 行(含 Task 1 的 4 行)均无"待审"残留
    - miniprogram_lint.py 行产出列有明确下落,其规则清单证据(行号)已采集供 HYP-15 回填
    - fixtures.py 的 D14-1 证据与 verify_upload_live.py/e2e_scenarios.py 的 D14-3 证据记入备注,未下裁定
    - ops.py 的删除类操作核实结论(有/无 + 行号)出现在 COVERAGE 备注或 F-TOOL 条目中
  </acceptance_criteria>
  <done>TOOL 维度 worker 模块 12/12 覆盖落格。</done>
</task>

<task type="auto">
  <name>Task 3: HYP-04/15 回填与销号去向反填</name>
  <files>.planning/audit/HYPOTHESES.md, .planning/audit/scans/ruff-extended.md, .planning/audit/scans/vulture.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(HYP-04/15 条目现状)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§HYPOTHESES.md 回填格式)
  </read_first>
  <action>
    1. HYP-04(fc_deploy 仅代码更新)回填:以 Task 1 证据定状态;该条属"MVP 可接受"自评类,按 D-10 以上线语境裁定——成立则备注标 RPT-06/DNF 候选(D-12),不成立则 findings/toolchain.md 立发现并引 F-ID。
    2. HYP-15(自研 miniprogram lint 替代 ESLint)回填:证据 = miniprogram_lint.py 规则清单行号 + scans/eslint.md 量化小结引用(漏报面底数);状态与结论按核实事实定。
    3. 销号表去向反填:scans/ 档案中去向为"深挖线索(03-05…)"的确认行回填 F-TOOL ID 或降级理由。
    4. 尾部统计行更新(累计回填 11 条);收尾零 diff 验证。
  </action>
  <verify>
    <automated>for id in HYP-04 HYP-15; do grep -A9 "^### ${id}:" .planning/audit/HYPOTHESES.md | grep -m1 '状态' | grep -q '未验证' && exit 1; grep -A9 "^### ${id}:" .planning/audit/HYPOTHESES.md | grep -q '5927f36' || exit 1; done && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - HYP-04/15 状态行为 证实/证伪/细化 之一 + 一句结论,各含 `file:line @ 5927f36` 证据行
    - HYP-15 证据行或备注引用 scans/eslint.md 量化小结
    - scans/ 档案确认行去向列不再含"03-05"占位
    - 零 diff 验证输出为空
  </acceptance_criteria>
  <done>TOOL 维度 worker 模块侧 HYP 闭环,仅余 scripts/Makefile(03-06)与裁定收尾(03-07)。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git show 引用片段 → findings/toolchain.md | fc_deploy/sts_escape 凭证处理代码引用可能截入敏感样例 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | findings/toolchain.md 证据片段 | high | mitigate | 引用只截标识符;部署凭证 env 名可引用,值样例一律不引 |
| T-03-02 | Tampering | 审计基线 | high | mitigate | 取证仅 git show;不执行任何被审工具(D-08:fc_deploy/make 目标/scripts 一律不运行);零 diff 验证 |
</threat_model>

<verification>
- COVERAGE.md TOOL 维度 soniscope_worker 12 行全部落格
- findings/toolchain.md 条目九字段齐备、维度为 TOOL、严重度按工具级影响
- HYP-04/15 回填完毕;零 diff 为空
</verification>

<success_criteria>
- AUDIT-02 的 Worker 工具链份额完成;D14-1/3 证据齐备待 03-07 裁定
- 被审对象零执行(D-08)全程成立
</success_criteria>

<output>
Create `.planning/phases/03-component-toolchain-deep-dive/03-05-SUMMARY.md` when done
</output>
