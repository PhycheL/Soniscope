---
phase: 03-component-toolchain-deep-dive
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audit/COVERAGE.md
  - .planning/audit/HANDOFF-PHASE4.md
  - .planning/audit/scans/gates-baseline.md
  - .planning/audit/scans/ruff-extended.md
  - .planning/audit/scans/vulture.md
  - .planning/audit/scans/eslint.md
  - .planning/audit/scans/secrets.md
autonomous: false
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "COVERAGE.md 存在,63 个审计对象(47 CODE + 16 TOOL)逐行登记路径/行数/维度,普审关注面清单(9 面)在文件头定稿"
    - "全部审计仪器(ruff 扩展集/vulture/ESLint/门禁直调/五类秘密扫描)已运行,输出存档于 .planning/audit/scans/,每档含命令原文+工具版本"
    - "秘密类扫描存档只含 path:line,不含任何匹配内容列(脱敏管道生效)"
    - "工作树相对基线 5927f36 在 apps/ scripts/ docs/ 三目录零 diff"
  artifacts:
    - ".planning/audit/COVERAGE.md"
    - ".planning/audit/HANDOFF-PHASE4.md"
    - ".planning/audit/scans/gates-baseline.md"
    - ".planning/audit/scans/ruff-extended.md"
    - ".planning/audit/scans/vulture.md"
    - ".planning/audit/scans/eslint.md"
    - ".planning/audit/scans/secrets.md"
  key_links:
    - "COVERAGE.md 对象清单 与 03-RESEARCH.md 审计对象全量清单 逐行一致(行数含)"
    - "scans/secrets.md 的五类命令 与 CHARTER.md 五类秘密扫描命令原文 一致"
---

<objective>
建立 Phase 3 的覆盖台账骨架与扫描档案:验证零 diff 前置条件、导出基线到 scratchpad、创建 COVERAGE.md(63 对象骨架 + D-04 普审关注面清单定稿)与 HANDOFF-PHASE4.md 移交清单骨架,经人工确认 vulture/eslint 包合法性后,运行全部审计仪器并按 D-05/D-06/D-07 存档到 `.planning/audit/scans/`。

Purpose: 后续所有普审/深挖计划(03-02~03-07)以本计划产出的线索档案与覆盖骨架为工作底稿;D-01 的"全模块普审"完成判定依赖本计划定稿的对象清单与关注面清单。
Output: COVERAGE.md 骨架、HANDOFF-PHASE4.md 骨架、scans/ 五份扫描档案(销号列留空,03-02 填)。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-component-toolchain-deep-dive/03-CONTEXT.md
@.planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md
@.planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md
@.planning/audit/CHARTER.md
</context>

## Artifacts this phase produces

本计划(及 Phase 3 整体)新建/修改的符号与文件——供 plan-review 源锚定排除:

- 新文件:`.planning/audit/COVERAGE.md`(覆盖台账,列结构:路径|行数|维度|深度|已过面|产出|备注)
- 新文件:`.planning/audit/HANDOFF-PHASE4.md`(跨维度移交清单,分 `## DOC 维度移交` / `## TEST 维度移交` 两节)
- 新目录+文件:`.planning/audit/scans/{gates-baseline,ruff-extended,vulture,eslint,secrets}.md`(扫描档案,含三态销号表骨架)
- 新发现 ID 系列:`F-CODE-01…NN`(findings/code.md,03-03/03-04/03-07 追加)、`F-TOOL-01…NN`(findings/toolchain.md,03-05/03-06/03-07 追加)
- 修改:`.planning/audit/HYPOTHESES.md` 14 条指定 HYP 的状态/证据/备注行(HYP-01/03/08/09/10/12/16/17/19/20 + HYP-04/07/15/18)
- 仓外 scratchpad 工件(不入库):`baseline-5927f36/` 基线导出、`eslint.config.mjs`、`bench_sha256.js`

<tasks>

<task type="auto">
  <name>Task 1: 零 diff 前置验证、基线导出与台账骨架创建</name>
  <files>.planning/audit/COVERAGE.md, .planning/audit/HANDOFF-PHASE4.md</files>
  <read_first>
    - .planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md(§审计对象全量清单——63 对象骨架的唯一数据源;§Architecture Patterns 普审关注面清单 9 面表;Code Examples #1/#2)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§COVERAGE.md 文件头格式、文档头三件套、cli.py 双维度备注写法、Phase 4 移交清单格式)
    - .planning/audit/CHARTER.md(审计基线章节——全 SHA;证据格式定义)
    - .planning/audit/CONTRACT-MATRIX.md(表格落格与"完成判定"节的排版先例,D-02 明示仿此)
  </read_first>
  <action>
    1. 前置验证(CHARTER 零 diff 规则):运行 `git diff --stat 5927f36 -- apps/ scripts/ docs/`,输出必须为空;非空则停止执行并上报,不得继续。
    2. 基线导出(per D-08 与 RESEARCH Code Examples #2):在会话 scratchpad(仓库外临时区,严禁指向仓库内)创建 `baseline-5927f36/` 目录,`git archive 5927f36 apps scripts | tar -x -C <该目录>`。记录导出路径供 Task 3 与后续计划引用(写入 COVERAGE.md 头部备注)。
    3. 创建 `.planning/audit/COVERAGE.md`(引用 D-01/D-02):
       - 文档头三件套(标题、Created 日期、基线 `5927f36` 行,格式仿 PATTERNS §文档头三件套),头部段落声明本文档角色(覆盖台账,证据与判断分离)并引用 D-01(全模块普审+线索深挖)/D-02(独立覆盖台账)/D-04(关注面清单)。
       - `## 普审关注面清单(D-04 定稿,9 面)` 节:逐字采用 03-RESEARCH.md §Architecture Patterns 的 9 面表(静默失败路径/数据丢失风险/秘密处理违规/硬编码云值/时区日期/死代码/注释与实态不符/纯逻辑+IO注入违反/跨端约定本端一致性),含 CHARTER 锚点列与仪器辅助信号列。此清单即全阶段"已过面 N/9"的分母定义。
       - `## CODE 维度` 与 `## TOOL 维度` 两节,各含一张表,列固定为:`| 路径 | 行数 | 维度 | 深度 | 已过面 | 产出 | 备注 |`。逐行预填 03-RESEARCH.md §审计对象全量清单的全部对象:CODE 47 行(worker 核心 14 + fc 12 + miniprogram 21,行数照抄清单实测值),TOOL 16 行(worker 验证/运维 12 + scripts 3 + Makefile 1)。每行路径用反引号包裹且行以 `| \`` 开头(机械对账依赖此格式)。深度/已过面/产出三列初始值一律填 `待审`。
       - cli.py 行备注预填:"TOOL 子命令入口,实体逻辑见 TOOL 侧对应模块,整体归 CODE 审一次"(RESEARCH Open Question 2 裁决)。`pages/dev/dev.js` 必须在列(Pitfall 8:小程序实为 3 页)。
       - `## 深挖点登记(20 处)` 节:列出 14 条 HYP(CODE:HYP-01/03/08/09/10/12/16/17/19/20;TOOL:HYP-04/07/15/18)与 6 条 D14(D14-1~6)及各自命中的模块路径(照抄 RESEARCH 清单"深挖线索"列),下落列初始 `待审`。
       - 文末预留 `## 完成判定` 节(占位,03-07 填)。
    4. 创建 `.planning/audit/HANDOFF-PHASE4.md`(引用 D-11):文档头三件套 + 角色段(跨维度顺带证据移交清单,延续 Phase 2 CONTRACT-MATRIX ③债务移交记录格式)+ `## DOC 维度移交` / `## TEST 维度移交` 两个空节,并注明逐条格式:`- **(移交 Phase 4 <维度>,HYP-NN):** <一句观察> — <file:line @ 5927f36>`。
    5. 创建 `.planning/audit/scans/` 目录(空目录即可,档案文件由 Task 3 写)。
  </action>
  <verify>
    <automated>test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && test "$(grep -cE '^\| `' .planning/audit/COVERAGE.md)" -eq 63 && grep -q '## 普审关注面清单' .planning/audit/COVERAGE.md && grep -q 'pages/dev/dev.js' .planning/audit/COVERAGE.md && grep -q '## TEST 维度移交' .planning/audit/HANDOFF-PHASE4.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| \`' .planning/audit/COVERAGE.md` 输出 63(47 CODE + 16 TOOL;= RESEARCH 清单 62 源文件 + Makefile)
    - COVERAGE.md 含字符串 `D-01`、`D-02`、`D-04` 与 9 行关注面表(`## 普审关注面清单` 节内表行数 = 9)
    - COVERAGE.md 含 `apps/miniprogram/pages/dev/dev.js` 与 `Makefile` 各一行;cli.py 行备注含"TOOL 子命令入口"
    - `## 深挖点登记` 节含 14 个 HYP ID 与 D14-1~6 共 20 行登记
    - HANDOFF-PHASE4.md 存在且含 `## DOC 维度移交` 与 `## TEST 维度移交` 两节及 D-11 引用
    - `git diff --stat 5927f36 -- apps/ scripts/ docs/` 输出为空
  </acceptance_criteria>
  <done>COVERAGE.md 63 对象骨架 + 9 面清单 + 20 深挖点登记就位;HANDOFF-PHASE4.md 骨架就位;基线已导出到 scratchpad;零 diff 验证通过。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: 临时仪器包合法性确认(vulture / eslint)</name>
  <files>(无文件改动——人工确认点)</files>
  <read_first>
    - .planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md(§Package Legitimacy Audit——两包核实证据全文)
  </read_first>
  <action>向用户呈现 03-RESEARCH.md §Package Legitimacy Audit 的两包核实证据(vulture: github.com/jendrikseipp/vulture,PyPI 10+ 年;eslint: 官方 org,1.35 亿周下载,无 postinstall,均已在研究会话实测运行),请求批准 Task 3 经 uvx/npx 临时调用两包。此检查点不可自动通过(包合法性检查点不受 auto_advance 影响)。用户批准前不得执行任何 uvx/npx 拉取。</action>Task 3 将经 `uvx vulture`(PyPI)与 `npx --yes eslint@9`(npm)临时拉取并运行两个审计仪器包。03-RESEARCH.md §Package Legitimacy Audit 已完成核实:vulture(github.com/jendrikseipp/vulture,10+ 年历史,2.16 已实测运行)与 eslint(官方 eslint org,1.35 亿周下载,9.39.4 已实测端到端、无 postinstall),两包的 [SUS] 判定均为机械性信号(PyPI 无下载量数据 / 最新版发布日期近),非身份可疑。</what-built>
  <how-to-verify>
    1. 打开 https://pypi.org/project/vulture 确认包主页指向 github.com/jendrikseipp/vulture
    2. 打开 https://www.npmjs.com/package/eslint 确认发布者为 eslint 官方组织
    3. 如认可 03-RESEARCH.md 的核实结论,可直接批准
  </how-to-verify>
  <acceptance_criteria>
    - 用户明确批准两包用于本阶段临时仪器调用(uvx/npx 临时获取,不写入仓库任何配置或依赖文件)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — 人工确认点,无自动验证;用户批准信号即验证</automated>
    <human-check>用户回复 "approved"(或指定兜底方案)</human-check>
  </verify>
  <done>用户批准(Task 3 照常执行)或选择兜底(Task 3 改用 ruff ARG/ERA + 人工普审替代 vulture、miniprogram_lint + 人工替代 ESLint,并在 COVERAGE.md 记"仪器缺席")。</done>
  <resume-signal>回复 "approved" 继续,或说明疑虑(将改用兜底:ruff ARG/ERA + 人工普审替代 vulture,miniprogram_lint + 人工替代 ESLint,COVERAGE.md 记"仪器缺席")</resume-signal>
</task>

<task type="auto">
  <name>Task 3: 全部审计仪器运行与扫描档案存档</name>
  <files>.planning/audit/scans/gates-baseline.md, .planning/audit/scans/ruff-extended.md, .planning/audit/scans/vulture.md, .planning/audit/scans/eslint.md, .planning/audit/scans/secrets.md</files>
  <read_first>
    - .planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md(Code Examples #3~#7 全部实测配方——命令逐字照用,Don't Hand-Roll 表禁止自拟;Pitfalls #1/#2/#3/#6)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§scans/ 命令存档格式、三态销号表格式、工具版本行增量要求)
    - .planning/audit/CHARTER.md(五类秘密扫描命令原文所在章节)
    - .planning/audit/CONTRACT-MATRIX.md(§重复逻辑普查 ②系统扫描存档——存档排版先例)
  </read_first>
  <action>
    按 D-05(现有门禁 + 临时扩展分析器)/D-07(命令+版本+输出存档)执行,全部命令取 03-RESEARCH.md Code Examples 实测配方原文,不得改动规则集(Pitfall 2 的降噪参数是定稿配方):
    1. `scans/gates-baseline.md`:仓内直调三条门禁(RESEARCH #6,per D-08 不经 make):`uv run mypy`、`uv run ruff check`、`uv run python -m soniscope_worker lint-miniprogram`。存档:每条命令原文 + 工具版本行(mypy 2.1.0 / ruff 0.15.20,以实际 `--version` 输出为准)+ 完整输出。直调前后各跑一次零 diff 快查并记录结果(Pitfall 6)。
    2. `scans/ruff-extended.md`:对 scratchpad 基线导出运行 RESEARCH #3 配方(`--isolated --target-version py311 --select S,BLE,TRY,DTZ,ARG,ERA,SIM,RET,PLC,PLE,PLW --ignore PLC0415,TRY003,S101`,路径 = 导出的 worker src + fc + scripts/test_asr.py + scripts/fetch_test_fixtures.py)。存档命令 + 版本 + 完整输出(输出中的路径统一改写为仓库相对路径,便于 path:line 引用)。
    3. `scans/vulture.md`:对导出运行 RESEARCH #4 配方(`uvx vulture <导出worker src> <导出fc> --min-confidence 80`)。存档同上,并预记已知误报类备注(Protocol 实现/typer 回调/WSGI 入口,RESEARCH A1)。
    4. `scans/eslint.md`:按 RESEARCH #5——`eslint.config.mjs` 写到导出根目录(仓库外,满足 D-05 零仓库写入),配置内容逐字取 RESEARCH #5 注释中的平面配置;`cd` 到导出根运行 `npx --yes eslint@9 --no-config-lookup -c eslint.config.mjs`,扫描路径限定 `apps/miniprogram/{utils,pages}/**/*.js` + `app.js` + `config.js`,不含 test/(Pitfall 3)。存档命令 + eslint/node 版本 + 完整输出(路径改写为仓库相对)。
    5. `scans/secrets.md`(per D-06,与 HYP-07 同批):CHARTER 五类秘密扫描命令逐条对基线运行(RESEARCH #7 形态),每条输出一律经 `cut -d: -f1,2` 脱敏管道只留 path:line(Pitfall 1 强制),另记每模式命中计数。档案中绝不出现任何匹配内容列。
    6. 五份档案统一结构(PATTERNS §scans/ 格式):文档头三件套 → 每扫描小节(命令原文 fenced bash 块 + 工具版本行 + 输出)→ 末尾放一张空的三态销号表骨架(列:# | 命中 path:line @ 5927f36 | 规则/模式 | 销号 | 理由/去向),销号列留空——03-02 计划填。
    7. 收尾:再跑零 diff 验证(`git diff --stat 5927f36 -- apps/ scripts/ docs/` 为空)并将结果记入 gates-baseline.md 尾部。
  </action>
  <verify>
    <automated>ls .planning/audit/scans/gates-baseline.md .planning/audit/scans/ruff-extended.md .planning/audit/scans/vulture.md .planning/audit/scans/eslint.md .planning/audit/scans/secrets.md >/dev/null && grep -l '版本' .planning/audit/scans/*.md | wc -l | grep -q 5 && ! grep -rE 'OSSAccessKeyId=[0-9A-Za-z]{8,}|Signature=[0-9A-Za-z%+/=]{16,}|LTAI[0-9A-Za-z]{10,}' .planning/audit/scans/ && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 五份 scans/ 档案齐备,每份含工具版本行与命令原文
    - `grep -rE 'OSSAccessKeyId=[0-9A-Za-z]{8,}|Signature=[0-9A-Za-z%+/=]{16,}|LTAI[0-9A-Za-z]{10,}' .planning/audit/scans/` 零命中(exit 1)——秘密值本体未入档
    - scans/secrets.md 含五类模式各自的命中计数行(合计应与实际输出一致;RESEARCH 实测参考值 10/4/1/3/51)
    - 每份档案末尾含空销号表骨架(表头行含"销号"列)
    - 仓内直调门禁前后零 diff 快查记录在 gates-baseline.md 中,阶段收尾零 diff 输出为空
  </acceptance_criteria>
  <done>五份扫描档案(命令+版本+输出+空销号表)入库;秘密类输出已脱敏;工作树相对基线零 diff。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 扫描输出 → git 仓库 | 秘密扫描原始输出含匹配值本体,入库即二次泄露(永久) |
| npm/PyPI → 本机 | uvx/npx 临时拉取仪器包,供应链投毒面 |
| 审计过程 → 被审基线 | 任何对 apps/ scripts/ docs/ 的写入污染审计基线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | scans/secrets.md 存档 | critical | mitigate | 脱敏管道 `cut -d: -f1,2` 强制 + Task 3 verify 反扫零命中门禁(D-06/Pitfall 1) |
| T-03-02 | Tampering | 审计基线(apps/ scripts/ docs/) | high | mitigate | 每任务收尾零 diff 验证;仪器只对 scratchpad 导出运行(仓内直调仅 mypy/ruff/miniprogram_lint 只读命令) |
| T-03-SC | Tampering | uvx vulture / npx eslint 临时获取 | high | mitigate | Task 2 blocking-human 合法性检查点 + RESEARCH Package Legitimacy Audit 证据(官方仓库、无 postinstall、已实测运行);固定 eslint@9 |
</threat_model>

<verification>
- `git diff --stat 5927f36 -- apps/ scripts/ docs/` 为空(零 diff 硬约束)
- COVERAGE.md 对象行数 = 63;关注面 9 面定稿;深挖点 20 处登记
- scans/ 五档齐备,秘密反扫零命中
- `git status --porcelain` 中新增/修改条目仅落 `.planning/` 路径
</verification>

<success_criteria>
- 后续计划可直接以 COVERAGE.md 为覆盖底稿、以 scans/ 为线索池开工
- 包合法性检查点获人工批准(或兜底方案已记录)
- 审计仪器全部运行完毕且档案可复核(命令+版本+输出三件齐)
</success_criteria>

<output>
Create `.planning/phases/03-component-toolchain-deep-dive/03-01-SUMMARY.md` when done
</output>
