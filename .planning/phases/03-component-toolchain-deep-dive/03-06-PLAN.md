---
phase: 03-component-toolchain-deep-dive
plan: 06
type: execute
wave: 6
depends_on: ["03-05"]
files_modified:
  - .planning/audit/findings/toolchain.md
  - .planning/audit/COVERAGE.md
  - .planning/audit/HYPOTHESES.md
  - .planning/audit/HANDOFF-PHASE4.md
  - .planning/audit/scans/secrets.md
  - .planning/audit/scans/ruff-extended.md
autonomous: true
requirements: [AUDIT-02]

must_haves:
  truths:
    - "scripts/ 三文件与 Makefile 均完整读过并逐面(9/9)过普审,COVERAGE.md TOOL 剩余 4 行无'待审'残留"
    - "scripts/test_asr.py 已提交过期预签名 URL 线索有明确核实结论(成功判据 2 点名),证据只写位置+模式名,不含值本体"
    - "HYP-07/18 状态回填完毕并附 file:line @ 5927f36 证据"
    - "Makefile 45 目标经静读审计(D-08 零执行),危险目标逐个细读"
  artifacts:
    - ".planning/audit/findings/toolchain.md(scripts/Makefile 侧 F-TOOL 条目,含 HYP-07 结论条目或显式无发现记录)"
    - ".planning/audit/COVERAGE.md(TOOL 维度 scripts 3 行 + Makefile 1 行已填)"
    - ".planning/audit/HYPOTHESES.md(HYP-07/18 已回填)"
  key_links:
    - "HYP-07 结论 → scans/secrets.md 中 test_asr.py 签名 URL 命中的销号去向闭环"
    - "HYP-25 顺带证据 → HANDOFF-PHASE4.md TEST 节(状态不动)"
---

<objective>
完成 AUDIT-02 剩余对象:scripts/ 缩窄清单三文件(test_asr.py 355 / fetch_test_fixtures.py 249 / gen_worker_config.sh 243)与 Makefile(171 行 45 目标)的普审+深挖;产出成功判据 2 点名的 `scripts/test_asr.py` 过期预签名 URL 核实结论(HYP-07),回填 HYP-07/18。

Purpose: 成功判据 2 的完整闭合——工具链发现全部进台账,含点名线索的核实结论。
Output: findings/toolchain.md 追加条目、COVERAGE.md 4 行、HYPOTHESES.md 2 条回填、HANDOFF TEST 节追加。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-component-toolchain-deep-dive/03-CONTEXT.md
@.planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md
@.planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md
@.planning/audit/CHARTER.md
@.planning/audit/HYPOTHESES.md
</context>

## Artifacts this phase produces

见 03-01-PLAN.md 同名节。本计划新增符号:findings/toolchain.md 中 scripts/Makefile 侧 F-TOOL 条目(编号顺延);HYPOTHESES.md 中 HYP-07/18 的证据行与状态值;COVERAGE.md scripts 3 行 + Makefile 1 行填值。

<tasks>

<task type="auto">
  <name>Task 1: scripts/ 三文件普审+深挖(847 行,含 HYP-07 核实结论)</name>
  <files>.planning/audit/findings/toolchain.md, .planning/audit/COVERAGE.md, .planning/audit/HANDOFF-PHASE4.md, .planning/audit/scans/secrets.md</files>
  <read_first>
    - .planning/audit/scans/secrets.md(test_asr.py 签名 URL 命中的销号行——本任务闭环其去向)
    - .planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md(Pitfall 7——HYP-07 证据红线全文;Pitfall 4——HYP-25 只移交不回填)
    - .planning/audit/findings/toolchain.md(编号状态)
    - .planning/audit/CHARTER.md(秘密值本体红线;严重度锚点——已提交凭证类命中对照凭证泄漏锚)
    - .planning/audit/COVERAGE.md(本任务要填的 3 行)
  </read_first>
  <action>
    逐文件 `git show 5927f36:<path>` 完整读 + 9 面过(D-08:三个脚本一律不执行):
    1. `scripts/test_asr.py`(355,深挖 HYP-07/HYP-18):
       - HYP-07 核实(成功判据 2 点名):定位 `DEFAULT_FILE_LINK`(约 :80)确认其是否为已提交的带签名 OSS URL、是否含 STS/签名参数、过期状态可否从 URL 参数静态判定。结论立 F-TOOL 发现(若确认)——证据字段只写 `scripts/test_asr.py:<行号> @ 5927f36` + 模式名(签名 URL 模式),引用片段可截变量名与赋值号,绝不截值本体(Pitfall 7 红线,含已过期值);严重度对照 CHARTER 凭证泄漏锚定级并在理由中说明过期因素。同步把 scans/secrets.md 对应销号行去向改为该 F-TOOL ID。
       - HYP-18(legacy AcsClient SDK):核实 aliyunsdkcore 用法与行号,采证供 Task 2 回填。
       - HYP-25 顺带证据(scripts/ 无 lint 门禁):只记 HANDOFF-PHASE4.md TEST 节(Pitfall 4:HYP-25 维度是 TEST,归 Phase 4 回填,状态不动)。
    2. `scripts/fetch_test_fixtures.py`(249):普审;复用 fixtures.py 的薄 CLI——核实两者职责边界与错误处理;HYP-25 顺带证据同上移交。
    3. `scripts/gen_worker_config.sh`(243):shell 脚本普审——生成 config.yaml 的秘密写入面(是否回显秘密到终端/日志)、chmod 600 设置有无、set -e 类失败保护有无。
    落格规则同 03-05(TOOL 维度、工具级影响定级、九字段)。
  </action>
  <verify>
    <automated>awk -F'|' '$2 ~ /scripts\// && $0 ~ /待审/' .planning/audit/COVERAGE.md | grep -c . | grep -q '^0$' && ! grep -rE 'OSSAccessKeyId=[0-9A-Za-z]{8,}|Signature=[0-9A-Za-z%+/=]{16,}' .planning/audit/findings/ && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 中 scripts/ 三行全部落格(已过面 9/9)
    - HYP-07 核实结论存在:findings/toolchain.md 有对应 F-TOOL 条目(或 COVERAGE 显式"无发现"+理由),证据字段含 `scripts/test_asr.py` 与 `@ 5927f36` 且不含 URL 值本体
    - `grep -rE 'OSSAccessKeyId=[0-9A-Za-z]{8,}|Signature=[0-9A-Za-z%+/=]{16,}' .planning/audit/findings/` 零命中
    - scans/secrets.md 中 test_asr.py 命中行去向为最终 F-TOOL ID(或降级理由)
    - HANDOFF-PHASE4.md TEST 节含 HYP-25 移交条目(含 `@ 5927f36` 证据);HYPOTHESES.md 中 HYP-25 状态未改动
  </acceptance_criteria>
  <done>scripts/ 三文件覆盖落格,HYP-07 点名线索有台账级核实结论且零值泄漏。</done>
</task>

<task type="auto">
  <name>Task 2: Makefile 静读审计(45 目标)与 HYP-07/18 回填</name>
  <files>.planning/audit/findings/toolchain.md, .planning/audit/COVERAGE.md, .planning/audit/HYPOTHESES.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(HYP-07/18 条目现状)
    - .planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md(Open Question 3 裁决——Makefile 按功能组过关注面、危险目标逐个细读)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§HYPOTHESES.md 回填格式)
  </read_first>
  <action>
    1. Makefile 审计(`git show 5927f36:Makefile`,D-08 零执行——静读为唯一取证方式,不以"跑通与否"作证据):按功能组(install/质量门禁/worker 运行/deploy/live 验证)过 9 面;危险目标(deploy-fc、rollback、任何含删除/覆写真云资源语义的目标)逐个细读,核实防误触设计(确认提示、参数强制、目标间依赖是否可能连带触发危险目标)、注释与实态一致性、.PHONY 声明完整性。COVERAGE.md Makefile 行落格,备注记功能分组。
    2. HYP-07 回填:状态 + 一句结论 + 证据行(位置+模式名,无值本体);若 Task 1 立了 F-TOOL,备注引 F-ID。
    3. HYP-18 回填:legacy SDK 使用证据(file:line @ 5927f36);该条评估以现状互审为基准,不引入弃用时间表判断(RESEARCH §State of the Art 口径)。
    4. 尾部统计行更新(累计回填 13 条,仅余 HYP-03);收尾零 diff 验证。
  </action>
  <verify>
    <automated>awk -F'|' '$2 ~ /Makefile/ && $0 ~ /待审/' .planning/audit/COVERAGE.md | grep -c . | grep -q '^0$' && for id in HYP-07 HYP-18; do grep -A9 "^### ${id}:" .planning/audit/HYPOTHESES.md | grep -m1 '状态' | grep -q '未验证' && exit 1; grep -A9 "^### ${id}:" .planning/audit/HYPOTHESES.md | grep -q '5927f36' || exit 1; done && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md Makefile 行落格,备注含功能分组说明与危险目标细读结论指向
    - HYP-07/18 状态行为 证实/证伪/细化 之一 + 一句结论,各含 `@ 5927f36` 证据行;HYP-07 证据不含 URL 值本体
    - HYPOTHESES.md 尾部统计行 = 累计回填 13 条
    - 零 diff 验证输出为空
  </acceptance_criteria>
  <done>AUDIT-02 对象全部覆盖(TOOL 16/16),TOOL 维度 HYP 四条全部闭环。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test_asr.py 签名 URL 核实 → 台账文本 | HYP-07 证据书写是本阶段值泄漏风险最高的单点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | HYP-07 发现条目与回填证据 | critical | mitigate | Pitfall 7 红线:只写位置+模式名;Task 1 verify 对 findings/ 反扫零命中 |
| T-03-02 | Tampering | 审计基线 | high | mitigate | scripts/Makefile 零执行(D-08);取证仅 git show;零 diff 验证 |
</threat_model>

<verification>
- COVERAGE.md TOOL 维度 16 行全部落格(12 模块 + scripts 3 + Makefile)
- HYP-07 核实结论在台账有明确下落;findings/ 秘密反扫零命中
- HYP 累计回填 13/14;零 diff 为空
</verification>

<success_criteria>
- 成功判据 2 完整闭合:scripts/、Makefile、fc_deploy 及验证模块的发现全部进台账,含 test_asr.py 点名线索核实结论
</success_criteria>

<output>
Create `.planning/phases/03-component-toolchain-deep-dive/03-06-SUMMARY.md` when done
</output>
