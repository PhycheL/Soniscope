---
phase: 03-component-toolchain-deep-dive
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - .planning/audit/findings/code.md
  - .planning/audit/COVERAGE.md
  - .planning/audit/HYPOTHESES.md
  - .planning/audit/HANDOFF-PHASE4.md
  - .planning/audit/scans/ruff-extended.md
  - .planning/audit/scans/vulture.md
  - .planning/audit/scans/eslint.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - "apps/fc 12 文件与 apps/miniprogram 21 文件每个完整读过并逐面(9/9)过普审,COVERAGE.md 对应 33 行无'待审'残留"
    - "fc/miniprogram 侧发现按九字段 schema 入 findings/code.md,每条附 git show 5927f36 人工引用片段"
    - "HYP-01/08/09/12/17/20 状态回填完毕并附 file:line @ 5927f36 证据"
    - "HYP-14 等 DOC 维度顺带证据已入 HANDOFF-PHASE4.md,HYP 状态未动;DNF-02/03/04 对照点未立发现"
  artifacts:
    - ".planning/audit/findings/code.md(fc/miniprogram 侧 F-CODE 条目)"
    - ".planning/audit/COVERAGE.md(CODE 维度 fc 12 行 + miniprogram 21 行已填)"
    - ".planning/audit/HYPOTHESES.md(HYP-01/08/09/12/17/20 已回填)"
  key_links:
    - "HYP-08 回填 → 同时引用 worker config.py(03-03 采证)与 fc env.py 两侧证据"
    - "D14-1/4/5/6 证据点(sha256 调用端/请求组装两份/config.js 硬编码/fragmentIdFromObjectKey)→ 记录待 03-07 裁定"
---

<objective>
对 apps/fc(12 文件,1,120 行)与 apps/miniprogram(21 文件,3,406 行)执行 D-01 全模块普审 + 线索深挖:FC 侧深挖 HYP-09/17(单键 STS 与无限流)、HYP-12(wsgiref 与 S104 命中)、HYP-08(env.py 凭证面);小程序侧采集 D14-1/4/5/6 证据、HYP-14 顺带证据移交;回填 HYP-01/08/09/12/17/20 六条(D-09/D-10)。

Purpose: AUDIT-01 的 FC 与小程序两层证据,补齐三层主体代码盘点(成功判据 1)。
Output: findings/code.md 追加条目、COVERAGE.md 33 行、HYPOTHESES.md 6 条回填、HANDOFF 移交追加。

上下文预算与阅读顺序(本计划约 4.5k 行全文精读,预计占单执行者上下文 60-75%,须严格控读):
1. 开工时把 <read_first> 上下文文档各读一遍并当场提炼工作要点(9 面清单、九字段 schema、销号确认项清单、DNF-02/03/04 对照点、D14 证据点行号),之后全程凭要点工作,禁止回头重读上下文文档。
2. 逐文件 `git show 5927f36:<path>` 取证,每个文件只读一遍;读完一个文件立即增量落格(findings/COVERAGE/HANDOFF),再读下一个——严禁攒到任务末尾统一落格。
3. 若上下文接近耗尽,优先保证已审文件的落格完整性并如实报告未审余量,不得压缩九面过审质量凑完成。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-component-toolchain-deep-dive/03-CONTEXT.md
@.planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md
@.planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md
@.planning/audit/CHARTER.md
@.planning/audit/HYPOTHESES.md
@.planning/audit/DO-NOT-FIX.md
</context>

## Artifacts this phase produces

见 03-01-PLAN.md 同名节。本计划新增符号:findings/code.md 中 fc/miniprogram 侧 F-CODE 条目(编号顺延);HYPOTHESES.md 中 HYP-01/08/09/12/17/20 的证据行与状态值;COVERAGE.md fc 12 行 + miniprogram 21 行填值。

<tasks>

<task type="auto">
  <name>Task 1: apps/fc 12 文件普审+深挖(1,120 行)</name>
  <files>.planning/audit/findings/code.md, .planning/audit/COVERAGE.md, .planning/audit/HANDOFF-PHASE4.md</files>
  <read_first>
    - .planning/audit/findings/code.md(现有编号状态与 03-03 已立条目——关联发现字段需互引)
    - .planning/audit/DO-NOT-FIX.md(DNF-03 handler mypy 豁免、DNF-04 小程序接收原始 STS——对照点不立发现)
    - .planning/audit/scans/ruff-extended.md(销号确认项中 fc 路径行——含 app.py 附近 S104 bind-all 命中)
    - .planning/audit/COVERAGE.md(本任务要填的 fc 12 行)
    - .planning/audit/CHARTER.md(严重度锚点——凭证泄漏 CRITICAL 锚在 FC 侧最相关)
  </read_first>
  <action>
    逐文件 `git show 5927f36:<path>` 完整读 + 9 面过(RESEARCH 清单顺序):
    - `fc_shared/sts.py`(176,深挖 HYP-09/17):单对象键 STS 策略的实际收窄程度(policy 范围、时效)、AssumeRole 失败路径是否泄漏细节;DNF-04 对照——"小程序接收原始 STS 秘密"系已裁定故意设计,不立发现,但策略实现缺陷(若有)仍可立。
    - `fc_shared/env.py`(150,HYP-08 证据):FC 长期凭证经环境变量装载的读取面、缺失时报错是否含值;此处采证,回填在 Task 3 与 worker config.py 侧合并。
    - `fc_shared/head.py`(141):HeadObject 校验逻辑的错误分支完整性(size/etag 不一致的处置)。
    - `issue_credential/handler.py`(110,深挖 HYP-17 无限流):allowlist 之外是否有任何频控/配额;DNF-03 对照——mypy 豁免不立发现(CHARTER 双语言适配声明明令)。
    - `verify_upload/handler.py`(106):同 DNF-03 对照;校验失败错误码路径。
    - `fc_shared/__init__.py`(106)/`http.py`(79)/`wechat.py`(52)/`auth.py`(52,HYP-09)/`errors.py`(51,D14 关联——错误码字面量真值源,记证据供 03-07 D14-3 裁定)/`audit.py`(62,HYP-08——is_sensitive 洗涤覆盖面核实:SENSITIVE_FIELD_NAMES 是否漏关键字段)/`app.py`(35,深挖 HYP-12——wsgiref ThreadingWSGIServer 生产适性 + 销号表 S104 bind-all 确认项人工核实)。
    落格规则同 03-03(九字段、维度 `组件代码 (CODE)`、主链锚点、DNF 对照跳过、深挖无发现记显式"无发现"、契约观察只移交)。FC 侧凭证相关发现的证据片段严禁截入任何示例/真实秘密值。
  </action>
  <verify>
    <automated>awk -F'|' '$2 ~ /apps\/fc/ && $0 ~ /待审/' .planning/audit/COVERAGE.md | grep -c . | grep -q '^0$' && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 中路径含 apps/fc 的 12 行全部落格(深度/已过面 9/9/产出 非"待审")
    - handler.py 两行与 sts.py 行的备注含 DNF-03/DNF-04 对照说明
    - app.py 的 S104 销号确认项有人工核实下落(F-ID 或降级理由,回填到 scans/ruff-extended.md 去向列)
    - errors.py 的错误码真值源证据(file:line @ 5927f36)记入 COVERAGE 备注,未下 D14 裁定
  </acceptance_criteria>
  <done>FC 层 12 文件覆盖落格,凭证/限流/运行时三个深挖点有明确下落。</done>
</task>

<task type="auto">
  <name>Task 2: apps/miniprogram 21 文件普审+深挖(3,406 行)</name>
  <files>.planning/audit/findings/code.md, .planning/audit/COVERAGE.md, .planning/audit/HANDOFF-PHASE4.md</files>
  <read_first>
    - .planning/audit/findings/code.md(编号状态)
    - .planning/audit/DO-NOT-FIX.md(DNF-02 issue-cedential 拼写域名——config.js 对照点,严禁当发现)
    - .planning/audit/scans/eslint.md(销号确认项 = 本任务深挖输入;HYP-15 量化小结留给 03-05 引用)
    - .planning/audit/CONTRACT-MATRIX.md(§③债务移交记录 D14-1/4/5/6 原文与行号证据——本任务采证对照基准)
    - .planning/audit/COVERAGE.md(本任务要填的 miniprogram 21 行)
  </read_first>
  <action>
    逐文件 `git show 5927f36:<path>` 完整读 + 9 面过。判断基准是仓库既有惯例(CHARTER 双语言适配),不引入外部 JS 风格标准:
    - `pages/index/index.js`(796,最大单文件):录音中断保护、草稿写入时序、wx storage IO 的失败分支;D14-1 调用端证据(:30,640 附近 sha256 调用)记录供 03-07。
    - `pages/uploads/uploads.js`(387)与 `utils/queue_runtime.js`(324):D14-4 两份同构请求组装证据(uploads.js :340,365 / queue_runtime.js :94-128)记录供 03-07;普审关注队列状态机变迁的漏态/死态。
    - `utils/uploads_view.js`(304)/`utils/audio.js`(185,契约观察→移交)/`utils/sha256.js`(171,HYP-03/D14-1:静态论证采证——算法实现形态、调用链、数据量级,微基准与回填在 03-07)/`utils/uploader.js`(164,D14-2 证据 RETRY_DELAYS_MS :28 附近)/`utils/verify.js`(138,D14-2 证据 :16 附近)/`utils/fault_injection.js`(124,HYP-14 顺带证据→移交 Phase 4 DOC)/`utils/oss_sign.js`(121,V4 签名实现的秘密处理面)/`utils/upload_queue.js`(119,D14-6 证据:fragmentIdFromObjectKey :38-44 无校验切割,关联 F-CON-03——采证不裁定)/`utils/ulid.js`(92)/`utils/chunking.js`(65)/`utils/hmac.js`(64)/`pages/dev/dev.js`(60,HYP-14 相关——ENV 开关暴露开发者菜单的顺带证据→移交)/`utils/logger.js`(60,日志字段是否可能带敏感值)/`utils/device.js`(60)/`utils/retention.js`(56)/`utils/draft.js`(52)/`config.js`(41,HYP-14/D14-5 顺带证据→移交;DNF-02 对照跳过拼写域名)/`app.js`(23)。
    落格规则同前任务;DOC 维度顺带证据(HYP-14 两处)逐条入 HANDOFF-PHASE4.md `## DOC 维度移交` 节(格式:去向 + 一句观察 + file:line @ 5927f36),HYP-14 状态不动(D-11)。
  </action>
  <verify>
    <automated>awk -F'|' '$2 ~ /apps\/miniprogram/ && $0 ~ /待审/' .planning/audit/COVERAGE.md | grep -c . | grep -q '^0$' && grep -q 'HYP-14' .planning/audit/HANDOFF-PHASE4.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 中路径含 apps/miniprogram 的 21 行全部落格(含 pages/dev/dev.js)
    - HANDOFF-PHASE4.md DOC 节含 HYP-14 相关移交条目(config.js 与 dev.js 两处证据,各含 `@ 5927f36`)
    - config.js 行备注含 DNF-02 对照说明;HYPOTHESES.md 中 HYP-14 状态本计划未改动
    - D14-1/4/5/6 的证据行号已记入 COVERAGE 深挖点登记节或对应行备注,均未下裁定
    - eslint.md 销号确认项(miniprogram 路径)去向列回填 F-ID 或降级理由
  </acceptance_criteria>
  <done>小程序层 21 文件覆盖落格,D14 四条证据与 HYP-14 移交就位。</done>
</task>

<task type="auto">
  <name>Task 3: HYP-01/08/09/12/17/20 回填</name>
  <files>.planning/audit/HYPOTHESES.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(六条目标条目现状;HYP-02 备注措辞先例)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§HYPOTHESES.md 回填格式)
    - .planning/audit/COVERAGE.md(03-03/本计划已采证据的行号引用来源)
  </read_first>
  <action>
    按 D-09 回填六条(格式同 03-03 Task 3:状态行 `证实/证伪/细化 — <一句结论>` + `- **证据:**` 行):
    - HYP-01(FC 直转已决未实现)与 HYP-20(transcribe-audio 函数未建):证据可引 `git ls-tree 5927f36 apps/fc` 仅两函数目录 + worker nls.py/transcriber.py 现役转写路径行号。
    - HYP-08(FC env 长期凭证 + 本地 config.yaml):合并 worker config.py(03-03 采证,见 COVERAGE 备注)与 fc env.py/audit.py 两侧证据。
    - HYP-09(openid allowlist 单用户认证)、HYP-12(wsgiref 运行时)、HYP-17(无限流/配额):以 Task 1 深挖证据回填。
    HYP-04/09/10/12 类"MVP 可接受"自评按 D-10 以上线语境裁定:成立→备注标 RPT-06 优点候选或 DNF 候选,不占发现 ID(D-12);不成立→findings/code.md 立发现并在备注引 F-ID(若 Task 1/2 已立则直接引用)。尾部统计行同步更新。收尾零 diff 验证。
  </action>
  <verify>
    <automated>for id in HYP-01 HYP-08 HYP-09 HYP-12 HYP-17 HYP-20; do grep -A9 "^### ${id}:" .planning/audit/HYPOTHESES.md | grep -m1 '状态' | grep -q '未验证' && exit 1; grep -A9 "^### ${id}:" .planning/audit/HYPOTHESES.md | grep -q '5927f36' || exit 1; done && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 六条 HYP 状态行均为 证实/证伪/细化 之一 + 一句结论,各含 `file:line @ 5927f36` 证据行
    - HYP-08 证据行同时含 worker 侧(config.py)与 FC 侧(env.py 或 audit.py)两处引用
    - "可接受成立"条目备注含 RPT-06/DNF 候选标注;"不成立"条目备注引用 F-CODE ID
    - 尾部统计行计数正确(累计回填 = 03-03 的 3 条 + 本计划 6 条 = 9 条)
    - 零 diff 验证输出为空
  </acceptance_criteria>
  <done>CODE 维度 10 条 HYP 中 9 条闭环(HYP-03 留待 03-07 微基准),三层主体代码盘点完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git show 引用片段 → findings/code.md | FC 凭证/STS/签名代码引用可能截入敏感样例 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | findings/code.md FC/签名侧证据片段 | high | mitigate | 引用只截标识符与结构;oss_sign.js/sts.py 证据不含任何密钥样例值 |
| T-03-02 | Tampering | 审计基线 | high | mitigate | 取证仅 git show;每任务零 diff 快查 |
</threat_model>

<verification>
- COVERAGE.md CODE 维度 47 行(worker 14 + fc 12 + miniprogram 21)全部落格
- findings/code.md 全部条目九字段齐备且证据含 @ 5927f36
- HYP 回填累计 9 条;HANDOFF 含 HYP-14 移交;零 diff 为空
</verification>

<success_criteria>
- 三层主体代码(成功判据 1)的 fc 与 miniprogram 份额完成,CODE 维度普审仅余 HYP-03/D14 裁定(03-07)
- DNF-02/03/04 对照零误立;契约观察零判断
</success_criteria>

<output>
Create `.planning/phases/03-component-toolchain-deep-dive/03-04-SUMMARY.md` when done
</output>
