---
phase: 03-component-toolchain-deep-dive
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - .planning/audit/scans/gates-baseline.md
  - .planning/audit/scans/ruff-extended.md
  - .planning/audit/scans/vulture.md
  - .planning/audit/scans/eslint.md
  - .planning/audit/scans/secrets.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "scans/ 五份档案的每条命中均已三态销号(确认/误报/移交),无一空置"
    - "每份档案末尾有对账等式:确认 X + 误报 Y + 移交 Z = 命中总数 ✓"
    - "确认项均经人工在 git show 5927f36 引用行核实后才标确认——原始工具输出不直接充当结论"
    - "scans/ 目录秘密反扫零命中,工作树相对基线零 diff"
  artifacts:
    - ".planning/audit/scans/gates-baseline.md(销号表已填)"
    - ".planning/audit/scans/ruff-extended.md(销号表已填)"
    - ".planning/audit/scans/vulture.md(销号表已填)"
    - ".planning/audit/scans/eslint.md(销号表已填)"
    - ".planning/audit/scans/secrets.md(销号表已填)"
  key_links:
    - "确认项 → COVERAGE.md 深挖点/后续普审计划的线索输入(去向列标注目标计划或后续回填 F-ID)"
    - "移交项 → HANDOFF-PHASE4.md 或 Phase 2 矩阵(去向列标注目标维度)"
---

<objective>
对 03-01 产出的全部扫描命中执行三态销号(D-07):每条命中经人工在 `git show 5927f36` 引用行核实后标 确认(成深挖线索,最终由普审计划立发现)/ 误报(记理由)/ 移交(标目标维度),并给出可复算对账等式。

Purpose: 成功判据 3 明令"原始 linter/工具输出不直接充当发现"——销号表是工具输出与人工判断之间的强制中间层;RPT-07 分维度置信声明直接引用销号档案。
Output: 五份 scans/ 档案的销号表全部填毕 + 对账等式。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-component-toolchain-deep-dive/03-CONTEXT.md
@.planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md
@.planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md
@.planning/audit/CHARTER.md
@.planning/audit/DO-NOT-FIX.md
</context>

## Artifacts this phase produces

见 03-01-PLAN.md 同名节(Phase 3 全量产物清单)。本计划新增符号:scans/ 五档中的已填三态销号表行、各档对账等式行(格式 `确认 X + 误报 Y + 移交 Z = N ✓`)。

<tasks>

<task type="auto">
  <name>Task 1: Python 侧线索销号(门禁基线 + ruff 扩展集 + vulture)</name>
  <files>.planning/audit/scans/gates-baseline.md, .planning/audit/scans/ruff-extended.md, .planning/audit/scans/vulture.md</files>
  <read_first>
    - .planning/audit/scans/gates-baseline.md(销号对象——当前状态)
    - .planning/audit/scans/ruff-extended.md(销号对象——当前状态)
    - .planning/audit/scans/vulture.md(销号对象——当前状态)
    - .planning/audit/DO-NOT-FIX.md(DNF-01~04 负面对照——DNF 覆盖的命中标误报并引 DNF 编号)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§三态销号表格式、三态分布对账句式)
    - .planning/audit/CHARTER.md(证据格式 path:line @ 5927f36;严重度锚点表——销号阶段只判三态不定级)
  </read_first>
  <action>
    对三份档案的每条命中逐条销号(D-07 三态)。核实方法:对每条命中运行 `git show 5927f36:<path>` 提取命中行及上下文,人工判断:
    - **确认**:命中反映真实可疑点 → 去向列写 `→ 深挖线索(03-0N <层名>普审)`,N 按命中路径归属(worker 核心 CODE→03-03;fc/miniprogram CODE→03-04;worker 验证运维模块 TOOL→03-05;scripts→03-06)。最终 F-ID 由对应计划回填本表。
    - **误报**:写一句理由(如"错误码常量非口令"、"Protocol 实现方法系动态引用"、"懒导入是项目故意模式")。DNF-01~04 已裁定的故意设计一律误报并引 DNF 编号。
    - **移交**:非 CODE/TOOL 维度证据(DOC/TEST/CON)→ 去向写目标(Phase 4 DOC/TEST + HYP 号,或 Phase 2 矩阵),并同步追加一条到 HANDOFF-PHASE4.md 对应节(格式见该文件头注)。
    已知误报类先例(RESEARCH):vulture 对 Protocol 实现/typer 回调/WSGI 入口的动态引用误报;ruff 扩展集探针信号 S110/S104/DTZ/S105/S106/ARG 须逐条核实不得批量定性。mypy/ruff 门禁基线若零输出,档案记"门禁通过,无命中需销号"。
    每份档案销号表填毕后追加对账等式行(PATTERNS 句式):`确认 X + 误报 Y + 移交 Z = 命中总数 N ✓`。
  </action>
  <verify>
    <automated>for f in gates-baseline ruff-extended vulture; do grep -q '= .*✓' .planning/audit/scans/$f.md || exit 1; done && ! grep -E '^\| [0-9]+ \|.*\|\s*\|\s*[^|]*\|$' .planning/audit/scans/ruff-extended.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 三份档案各含对账等式行(含 `✓`),等式左侧三数之和等于该档案记录的命中总数
    - 销号表中不存在销号列为空的行(每行三态取值为 确认/误报/移交 之一)
    - 每条"确认"行的去向列含目标计划编号(03-03/03-04/03-05/03-06 之一)
    - 每条"误报"行含一句中文理由;DNF 对照命中引用 DNF-01~04 编号
    - 移交项已同步写入 .planning/audit/HANDOFF-PHASE4.md(如有;无移交项则档案记"本档无移交项")
  </acceptance_criteria>
  <done>Python 侧全部命中三态销号完毕,对账等式可复算,确认项形成 03-03/03-05/03-06 的深挖线索输入。</done>
</task>

<task type="auto">
  <name>Task 2: JS 侧与秘密扫描销号 + scans/ 收尾反扫</name>
  <files>.planning/audit/scans/eslint.md, .planning/audit/scans/secrets.md, .planning/audit/HANDOFF-PHASE4.md</files>
  <read_first>
    - .planning/audit/scans/eslint.md(销号对象)
    - .planning/audit/scans/secrets.md(销号对象——只有 path:line,核实须回到 git show)
    - .planning/audit/HANDOFF-PHASE4.md(移交追加落点——当前状态)
    - .planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md(Pitfall 7——HYP-07 证据红线;§Anti-Patterns"对小程序 JS 引入外部 lint 标准作为判据")
  </read_first>
  <action>
    1. `scans/eslint.md` 销号:逐条核实(`git show 5927f36:<path>`)。注意 CHARTER 双语言适配:ESLint 结果只是线索与 HYP-15 漏报面量化底数,不是质量标准——命中是否"确认"以仓库既有惯例为基准判断,纯风格类(如 no-var 关闭项之外的偏好)一律误报。确认项去向:miniprogram 属 CODE → `03-04`;为 HYP-15(miniprogram_lint 覆盖面)提供的量化证据在档案尾部小结一段(warning/error 分布统计),供 03-05 审 miniprogram_lint.py 时引用。
    2. `scans/secrets.md` 销号(与 HYP-07 同批,D-06):对每条 path:line 命中运行 `git show 5927f36:<path>` 定位,判断:真实秘密类值(确认,去向按路径归 03-06 scripts 或对应计划)/ 变量名与模式字符串等非值命中(误报)/ 文档与测试引用(按维度移交或误报)。核实与销号书写全程遵守 Pitfall 7 红线:表中只写 path:line + 模式名,任何情况下不复制匹配值本体(含已过期值);`scripts/test_asr.py` 的签名 URL 命中预期为"确认 → 03-06 深挖(HYP-07)"。
    3. 两档各补对账等式行;移交项同步追加 HANDOFF-PHASE4.md。
    4. 收尾:对 scans/ 目录反扫秘密模式(收紧版三模式,见 verify 命令)确认零命中;跑零 diff 验证并在 secrets.md 尾部记录。
  </action>
  <verify>
    <automated>grep -q '= .*✓' .planning/audit/scans/eslint.md && grep -q '= .*✓' .planning/audit/scans/secrets.md && ! grep -rE 'OSSAccessKeyId=[0-9A-Za-z]{8,}|Signature=[0-9A-Za-z%+/=]{16,}|LTAI[0-9A-Za-z]{10,}' .planning/audit/scans/ && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - eslint.md 与 secrets.md 各含对账等式行,销号列无空值
    - secrets.md 销号表任何行均不含匹配值本体(仅 path:line + 模式名 + 三态 + 去向)
    - secrets.md 中 scripts/test_asr.py 的签名 URL 命中行存在且有明确三态与去向
    - eslint.md 尾部含 HYP-15 量化小结段(error/warning 计数与主导规则)
    - `grep -rE 'OSSAccessKeyId=[0-9A-Za-z]{8,}|Signature=[0-9A-Za-z%+/=]{16,}|LTAI[0-9A-Za-z]{10,}' .planning/audit/scans/` 零命中
    - 零 diff 验证输出为空并记录在案
  </acceptance_criteria>
  <done>全部五档销号完毕、对账可复算、秘密红线反扫通过;深挖线索输入就绪,03-03 可开工。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 销号核实(git show 输出)→ 销号表文本 | 核实秘密命中时引用片段可能截入值本体 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | scans/secrets.md 销号表 | critical | mitigate | 表中仅 path:line + 模式名(Pitfall 7);Task 2 verify 反扫门禁 |
| T-03-02 | Tampering | 审计基线 | high | mitigate | 本计划只写 .planning/;收尾零 diff 验证 |
| T-03-SC | Tampering | 无新增包安装 | low | accept | 本计划不拉取任何包,仅 git show 取证 |
</threat_model>

<verification>
- 五档对账等式全部成立(三数和 = 命中总数)
- scans/ 秘密反扫零命中;零 diff 为空
- 确认项去向全部指向具体后续计划;移交项在 HANDOFF-PHASE4.md 有对应条目
</verification>

<success_criteria>
- 每条工具命中都有人工核实结论,成功判据 3 的中间层建立完毕
- 03-03~03-06 各自的深挖线索清单可从销号表机械提取
</success_criteria>

<output>
Create `.planning/phases/03-component-toolchain-deep-dive/03-02-SUMMARY.md` when done
</output>
