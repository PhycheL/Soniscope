---
phase: 03-component-toolchain-deep-dive
plan: 07
type: execute
wave: 7
depends_on: ["03-06"]
files_modified:
  - .planning/audit/findings/code.md
  - .planning/audit/findings/toolchain.md
  - .planning/audit/COVERAGE.md
  - .planning/audit/HYPOTHESES.md
  - .planning/audit/HANDOFF-PHASE4.md
  - .planning/audit/scans/microbench-sha256.md
autonomous: true
requirements: [AUDIT-01, AUDIT-02]

must_haves:
  truths:
    - "D14-1~6 六条移交线索逐条经三要素框架(结构必要性/兜底机制/漂移后果)裁定,各有单独发现或单独'不构成债务'结论,互相以关联发现字段串联并反向引用 F-CON-01~06"
    - "HYP-03 以静态论证为主判据 + scratchpad 微基准辅助证据回填(标注'Mac 环境非真机,量级参考')"
    - "COVERAGE.md 完成判定节成立:63 对象全落格、20 深挖点全有下落、对账等式可复算"
    - "14 条指定 HYP 全部回填;HANDOFF-PHASE4.md 定稿封版;零 diff 与 scans/findings 秘密反扫通过"
  artifacts:
    - ".planning/audit/scans/microbench-sha256.md(微基准命令+环境+结果+复跑说明)"
    - ".planning/audit/findings/code.md(D14-1/2/4/5/6 裁定条目或结论)"
    - ".planning/audit/findings/toolchain.md(D14-3 裁定条目或结论)"
    - ".planning/audit/COVERAGE.md(完成判定节)"
    - ".planning/audit/HANDOFF-PHASE4.md(封版)"
  key_links:
    - "D14 裁定条目 → CONTRACT-MATRIX ③移交记录逐条销号(RPT-08 要求每条移交线索有明确下落)"
    - "HYP-03 回填 → 微基准档案 + sha256.js/index.js 静态证据双引用"
---

<objective>
Phase 3 裁定与收尾:D-16 微基准佐证 HYP-03 并回填;D14-1~6 按 D-13 三要素框架逐条独立裁定(D-15),严重度锚 D-14 口径;COVERAGE.md 完成判定、HANDOFF 封版、全套机械对账与零 diff/秘密反扫收尾。

Purpose: 关闭全部 20 处深挖点与 4 条阶段成功判据,交付 Phase 4/5 可直接消费的完整证据链。
Output: D14 六条裁定、HYP-03 回填、microbench 档案、COVERAGE 完成判定、HANDOFF 定稿。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-component-toolchain-deep-dive/03-CONTEXT.md
@.planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md
@.planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md
@.planning/audit/CHARTER.md
@.planning/audit/CONTRACT-MATRIX.md
@.planning/audit/findings/contract.md
</context>

## Artifacts this phase produces

见 03-01-PLAN.md 同名节。本计划新增符号:`.planning/audit/scans/microbench-sha256.md`(新文件);findings 两文件中的 D14 裁定条目(F-CODE/F-TOOL 编号顺延)或"不构成债务"结论段;COVERAGE.md `## 完成判定` 节内容;HYPOTHESES.md HYP-03 证据行;HANDOFF-PHASE4.md 尾部封版行;仓外 `bench_sha256.js`(不入库)。

<tasks>

<task type="auto">
  <name>Task 1: D-16 微基准与 HYP-03 回填</name>
  <files>.planning/audit/scans/microbench-sha256.md, .planning/audit/HYPOTHESES.md</files>
  <read_first>
    - .planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md(Code Examples #8——微基准配方;D-16 全文)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§scratchpad 工件——Phase 2 harness 复跑说明先例、来源断言要求)
    - .planning/audit/COVERAGE.md(03-04 已采的 sha256.js/index.js 静态证据——HYP-03 主判据来源)
    - .planning/audit/HYPOTHESES.md(HYP-03 条目现状)
  </read_first>
  <action>
    1. 微基准(D-16,本阶段唯一"对象执行"例外——基线导出的纯函数佐证执行,与 Phase 2 先例同构):`git show 5927f36:apps/miniprogram/utils/sha256.js` 导出到会话 scratchpad(严禁仓库内);在 scratchpad 写 `bench_sha256.js` 计时脚本——require 导出文件(脚本首部加来源断言:被 require 模块路径必须以 scratchpad 目录开头,仿 Phase 2 先例)、对典型体量(10 分钟分片 ≈10MB 的 Buffer/字符串)计时、多轮取中位数;`node` 运行。
    2. 建 `.planning/audit/scans/microbench-sha256.md`:文档头三件套 + 命令与环境(node 版本、机器为 Mac 非真机声明)+ 脚本要点与来源断言说明 + 结果(各轮与中位数)+ 复跑说明命令块(PATTERNS 存档义务)+ 结论一句(量级参考)。
    3. HYP-03 回填(D-09):静态论证为主判据(算法实现形态/调用链 index.js 调用点/数据量级——引 03-04 采证行号),微基准为辅助证据(引 microbench 档案,标注"Mac 环境非真机,量级参考")。状态与结论按事实定;若构成发现则入 findings/code.md(可与 Task 2 的 D14-1 裁定条目合并考量,避免同一事实双立——用关联发现字段区分性能问题(HYP-03)与重复实现债务(D14-1)两个判断)。尾部统计行更新(累计 14/14)。
  </action>
  <verify>
    <automated>test -f .planning/audit/scans/microbench-sha256.md && grep -q '非真机' .planning/audit/scans/microbench-sha256.md && grep -A9 '^### HYP-03:' .planning/audit/HYPOTHESES.md | grep -m1 '状态' | grep -qv '未验证' && grep -A12 '^### HYP-03:' .planning/audit/HYPOTHESES.md | grep -q '5927f36' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - microbench-sha256.md 含:命令+node 版本、来源断言说明、多轮结果与中位数、复跑说明、"Mac 环境非真机,量级参考"字样
    - 仓库内不存在 bench 脚本或导出副本(`git status --porcelain` 无 scratchpad 工件路径)
    - HYP-03 状态为 证实/证伪/细化 之一,证据行同时含静态证据(file:line @ 5927f36)与微基准档案引用
    - HYPOTHESES.md 尾部统计:14 条指定 HYP 全部非"未验证"
  </acceptance_criteria>
  <done>HYP-03 闭环,14/14 回填完成,微基准可复跑可复核。</done>
</task>

<task type="auto">
  <name>Task 2: D14-1~6 三要素逐条裁定</name>
  <files>.planning/audit/findings/code.md, .planning/audit/findings/toolchain.md, .planning/audit/COVERAGE.md</files>
  <read_first>
    - .planning/audit/CONTRACT-MATRIX.md(§③债务移交记录——D14-1~6 全文与行号证据;§普查表相关行)
    - .planning/audit/findings/contract.md(F-CON-01~06——关联发现字段须反向引用;F-CON-03 关联字段写法范本)
    - .planning/audit/COVERAGE.md(03-03/04/05 各计划采集的 D14 证据备注——裁定的证据输入)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§D14 裁定条目的增量要求)
  </read_first>
  <action>
    逐条独立裁定(D-15:六条各自走三要素、单独下落,聚类留 Phase 5),每条理由写足 D-13 三要素:① 结构必要性(跨部署单元无法共享 = 故意重复;同包/同端内重复 = 可疑)② 兜底机制(测试锁定/单一真值源注释锚点有无——引用具体行号)③ 漂移后果(静默丢数据 vs 仅工具失准)。严重度按 D-14:触及主链路数据可见性的(D14-6 fragmentIdFromObjectKey 无校验切割)参照 CHARTER 主链锚点;纯维护成本/工具失准类(D14-2/D14-3)默认 LOW~MEDIUM;重复落点多不自动拔高:
    - D14-1(sha256 跨语言双实现,关联 HYP-03)→ findings/code.md;与 Task 1 的 HYP-03 判断以关联发现字段串联。
    - D14-2(重试常量四落点,JS 侧 .length 派生 vs Worker 独立字面量)→ findings/code.md;注意 CLAUDE.md 声明重试表双语言镜像"有测试断言"——兜底机制要素须核实该断言存在与覆盖范围(git grep 测试文件仅作兜底核实用途,不算测试审计)。
    - D14-3(联调工具契约镜像集群,注释约定无测试兜底)→ findings/toolchain.md(D-03 落点);证据引 03-05 采集行号。
    - D14-4(小程序请求组装两份同构)→ findings/code.md。
    - D14-5(配置三机制并存 + config.js 唯一代码内硬编码云值)→ findings/code.md;与 HYP-14 移交(Phase 4 DOC)的边界在理由中写明——本条只裁重复实现债务,不裁文档配置漂移。
    - D14-6(key 反推第四处、无校验切割)→ findings/code.md;关联 F-CON-03。
    每条:立九字段发现(ID 顺延)或写"不构成债务"结论——结论不占 F-ID,落 COVERAGE 深挖点登记节对应行(附三要素理由与行号证据)。关联发现字段一律引 D14-N 编号 + 对应 F-CON ID(有则引)。六条裁定完成后,COVERAGE 深挖点登记节 20 行(14 HYP + 6 D14)下落列全部非"待审"。
  </action>
  <verify>
    <automated>for n in 1 2 3 4 5 6; do grep -rq "D14-$n" .planning/audit/findings/ .planning/audit/COVERAGE.md || exit 1; done && awk '/## 深挖点登记/,/## 完成判定/' .planning/audit/COVERAGE.md | grep -c '待审' | grep -q '^0$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - D14-1~6 每条在 findings/code.md、findings/toolchain.md 或 COVERAGE 深挖点登记节有唯一明确下落(F-ID 或"不构成债务"结论)
    - 每条裁定理由含三要素三个编号点(结构必要性/兜底机制/漂移后果)与行号证据
    - D14-3 下落在 findings/toolchain.md 侧(或 COVERAGE TOOL 侧结论);D14-6 关联字段引 F-CON-03
    - COVERAGE 深挖点登记 20 行下落列无"待审"
  </acceptance_criteria>
  <done>六条移交线索全部销号,RPT-08"每条移交线索有明确下落"在 Phase 3 侧成立。</done>
</task>

<task type="auto">
  <name>Task 3: 阶段机械收尾(完成判定、封版、对账、零 diff)</name>
  <files>.planning/audit/COVERAGE.md, .planning/audit/HANDOFF-PHASE4.md</files>
  <read_first>
    - .planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md(§Validation Architecture 机械验收 Map、Code Examples #9)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§机械对账收口、§零 diff 验证记录、§文档头三件套尾部封版行)
    - .planning/audit/COVERAGE.md(完成判定占位节)
  </read_first>
  <action>
    1. COVERAGE.md `## 完成判定` 节填写(仿 CONTRACT-MATRIX ④完成判定,逐条可复算命令+数字+✓):对象总数 63(`grep -cE '^\| \`' COVERAGE.md`)、"待审"残留 0、深挖点 20 处全有下落、已过面全 9/9。
    2. 全阶段对账:发现计数(`grep -c '^### F-CODE-'` 与 `'^### F-TOOL-'`,各扣 00 示例)、HYP 回填 14/14(逐 ID 列出)、scans/ 五档对账等式复核、销号确认项去向列零占位(不再含 03-0N 字样)、HANDOFF 移交条目计数。结果写入完成判定节。
    3. HANDOFF-PHASE4.md 定稿:检查每条格式(去向+观察+`@ 5927f36`),尾部加封版斜体行(条目计数)。
    4. COVERAGE.md 尾部加零 diff 验证记录节(PATTERNS 格式):`git diff --stat 5927f36 -- apps/ scripts/ docs/` 实际输出(空)+ `git status --porcelain` 结论(改动仅 .planning/)。
    5. 秘密反扫(RESEARCH #9 收紧版):对 .planning/audit/ 全目录反扫签名 URL/AK 模式,期望零命中;结果记入完成判定节。
    6. 全文件尾部封版斜体行补齐(COVERAGE/HANDOFF/microbench)。
  </action>
  <verify>
    <automated>grep -q '## 完成判定' .planning/audit/COVERAGE.md && awk '/## CODE 维度/,/## 完成判定/' .planning/audit/COVERAGE.md | grep -c '待审' | grep -q '^0$' && ! grep -rE 'OSSAccessKeyId=[0-9A-Za-z]{8,}|Signature=[0-9A-Za-z%+/=]{16,}|LTAI[0-9A-Za-z]{10,}' .planning/audit/scans/ .planning/audit/findings/ .planning/audit/COVERAGE.md .planning/audit/HANDOFF-PHASE4.md && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && grep -c '未验证' .planning/audit/HYPOTHESES.md | awk '{exit ($1<=11)?0:1}' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 完成判定节含:63 对象等式、深挖点 20 下落、发现计数、HYP 14/14 清单、秘密反扫结论、零 diff 记录——每条附可复算命令与 ✓
    - COVERAGE.md 全表(CODE+TOOL)零"待审"残留
    - HYPOTHESES.md 中"未验证"计数 ≤ 11(25 - 14;其余维度条目留 Phase 4)
    - `.planning/audit/` 秘密模式反扫零命中;零 diff 输出为空
    - COVERAGE/HANDOFF/microbench 三文件尾部含封版斜体行
  </acceptance_criteria>
  <done>Phase 3 四条成功判据全部机械可验:发现进台账、点名线索有结论、无原始输出充当发现、契约观察全移交。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scratchpad 微基准 → 仓库 | D-16 例外执行的工件若落仓库即违反零仓库写入 |
| 收尾对账 → 台账全集 | 最后一次秘密反扫是入库前的终防线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | .planning/audit/ 全集 | critical | mitigate | Task 3 全目录秘密反扫门禁(收紧三模式) |
| T-03-02 | Tampering | 审计基线 | high | mitigate | 微基准仅在 scratchpad 执行(来源断言);收尾零 diff + git status 双查 |
| T-03-03 | Tampering | 被审对象执行边界 | medium | mitigate | D-16 例外仅限基线导出纯函数;不运行任何 make 目标/脚本(D-08) |
</threat_model>

<verification>
- 四条阶段成功判据的机械验收命令全绿(RESEARCH §Validation Architecture Map)
- COVERAGE 完成判定、HYP 14/14、D14 6/6、HANDOFF 封版、零 diff、秘密反扫全部通过
</verification>

<success_criteria>
- Phase 3 goal 达成:三层主体代码与部署工具链的债务/脆弱区域全部经人工核实进入台账,Phase 4(测试/文档审计)与 Phase 5(报告)的代码实态基准就绪
</success_criteria>

<output>
Create `.planning/phases/03-component-toolchain-deep-dive/03-07-SUMMARY.md` when done
</output>
