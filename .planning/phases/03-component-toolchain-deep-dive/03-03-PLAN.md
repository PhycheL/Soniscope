---
phase: 03-component-toolchain-deep-dive
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - .planning/audit/findings/code.md
  - .planning/audit/COVERAGE.md
  - .planning/audit/HYPOTHESES.md
  - .planning/audit/HANDOFF-PHASE4.md
  - .planning/audit/scans/ruff-extended.md
  - .planning/audit/scans/vulture.md
  - .planning/audit/scans/gates-baseline.md
autonomous: true
requirements: [AUDIT-01]

must_haves:
  truths:
    - "apps/worker 核心 14 模块每个完整读过一遍并逐面(9/9)过普审关注面,COVERAGE.md 对应行无一残留'待审'"
    - "worker 侧发现按 CHARTER 九字段 schema 进入 findings/code.md,每条附人工在 git show 5927f36 提取的引用片段"
    - "HYP-10/16/19 状态回填为 证实/证伪/细化 之一并附 file:line @ 5927f36 证据"
    - "契约类观察未在本计划下判断,已记入 HANDOFF-PHASE4.md 或标注移交 Phase 2 矩阵"
  artifacts:
    - ".planning/audit/findings/code.md(worker 侧 F-CODE 条目)"
    - ".planning/audit/COVERAGE.md(CODE 维度 worker 14 行已填)"
    - ".planning/audit/HYPOTHESES.md(HYP-10/16/19 已回填)"
  key_links:
    - "每条 F-CODE 发现的证据字段 → git show 5927f36 可复现的 path:line 引用"
    - "销号表确认项(worker 路径)→ F-CODE ID 回填到 scans/ 去向列"
---

<objective>
对 apps/worker 核心 14 模块(4,871 行,CODE 维度)执行 D-01 全模块普审 + 线索深挖:每模块经 `git show 5927f36:<path>` 完整读一遍、逐面过 D-04 九面清单,深挖点(pipeline/poller 的 HYP-10/16、nls 的 HYP-19、poller 的 D14-1 证据、nls 的 D14-2 证据)逐行核实;发现按九字段 schema 入 findings/code.md,HYP-10/16/19 就地回填(D-09),COVERAGE.md worker 行落格。

Purpose: AUDIT-01 的 worker 层证据;Phase 4 测试审计与 Phase 5 报告的代码实态基准。
Output: findings/code.md worker 条目、COVERAGE.md 14 行、HYPOTHESES.md 3 条回填、HANDOFF 移交追加。

上下文预算与阅读顺序(本计划约 4.9k 行全文精读,预计占单执行者上下文 60-75%,须严格控读):
1. 开工时把 <read_first> 上下文文档各读一遍并当场提炼工作要点(9 面清单、九字段 schema、销号确认项清单、DNF 对照点),之后全程凭要点工作,禁止回头重读上下文文档。
2. 逐模块 `git show 5927f36:<path>` 取证,每个模块只读一遍;读完一个模块立即把发现/覆盖行/移交条目增量写入台账,再读下一个模块——严禁攒到任务末尾统一落格。
3. 若上下文接近耗尽,优先保证已审模块的落格完整性并如实报告未审余量,不得压缩九面过审质量凑完成。
</objective>

<execution_context>
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/workflows/execute-plan.md
@/Volumes/Data/ProjectCode/my_soniscope/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-component-toolchain-deep-dive/03-CONTEXT.md
@.planning/phases/03-component-toolchain-deep-dive/03-RESEARCH.md
@.planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md
@.planning/audit/CHARTER.md
@.planning/audit/HYPOTHESES.md
@.planning/audit/DO-NOT-FIX.md
@.planning/audit/findings/contract.md
</context>

## Artifacts this phase produces

见 03-01-PLAN.md 同名节。本计划新增符号:findings/code.md 中的 `F-CODE-01` 起 worker 侧条目;HYPOTHESES.md 中 HYP-10/16/19 的新增 `- **证据:**` 行与状态值;COVERAGE.md worker 14 行的 深度/已过面/产出 填值。

<tasks>

<task type="auto">
  <name>Task 1: 四大模块深挖普审(pipeline / nls / cli / poller,2,747 行)</name>
  <files>.planning/audit/findings/code.md, .planning/audit/COVERAGE.md</files>
  <read_first>
    - .planning/audit/findings/code.md(F-CODE-00 schema 示例与现有编号状态——新条目从最大现有编号 +1 起)
    - .planning/audit/findings/contract.md(F-CON-01~06 真实条目——证据字段三层结构写法范本,PATTERNS §发现台账)
    - .planning/audit/CHARTER.md(九字段 schema、严重度锚点表、S/M/L/XL 判定、证据格式)
    - .planning/audit/scans/ruff-extended.md 与 .planning/audit/scans/vulture.md(销号表中路径属 pipeline/nls/cli/poller 的确认项 = 本任务深挖输入)
    - .planning/audit/COVERAGE.md(§普审关注面清单 9 面定义;本任务要填的 4 行)
  </read_first>
  <action>
    逐模块审计(取证一律 `git show 5927f36:<path>`,禁读工作树):
    1. `apps/worker/src/soniscope_worker/pipeline.py`(875 行,深挖 HYP-10 串行吞吐):完整读,9 面逐面过。重点面:静默失败路径(各 STAGE_* 失败分支是否吞异常)、数据丢失风险(`.done` 写入时序、临时文件→原子 rename 三步协议是否处处成立——CLAUDE.md 反模式对照)、资源泄漏(失败分支的 .part/.tmp 清理)。
    2. `apps/worker/src/soniscope_worker/nls.py`(740 行,深挖 HYP-19 2018 版 NLS API;D14-2 证据点 RETRY_DELAYS_SECONDS 约 :45):记录重试常量与 MAX 上限的定义形态(独立字面量 vs 派生)供 03-07 D14-2 裁定引用——本任务只记证据不裁定(D-15)。核实 RESIGN_THRESHOLD_SECONDS 逻辑与轮询重试的静默失败面。
    3. `apps/worker/src/soniscope_worker/cli.py`(601 行):普审;COVERAGE 备注保持"TOOL 子命令入口"口径,TOOL 子命令实体不在此深审(03-05 审对应模块)。关注:CLI 边界异常→退出码转换的完整性、参数校验缺口。
    4. `apps/worker/src/soniscope_worker/poller.py`(531 行,深挖 HYP-10/HYP-16;D14-1 证据点 sha256 比对流程 :26,251):核实轮询循环的单线程假设、下载失败 .part 清理路径、`fragment_id_from_key` 往返校验调用(契约观察→只记移交,不判断,成功判据 4)。
    发现落格:每条按九字段 schema(格式逐字仿 PATTERNS §发现台账模板),ID 从 F-CODE-01 顺延(读现有文件确定起点),维度字段一律写 `组件代码 (CODE)`,严重度锚 CHARTER 主链锚点(数据丢失/静默转写失败/凭证泄漏),理由用"影响:…;可能性:…"一行定性、禁数值评分,工作量 S/M/L/XL,状态 draft,上线判定留空。DNF-01~04 对照命中不立发现。深挖点无发现时在 COVERAGE 产出列写显式"无发现"(D-12)。
    COVERAGE.md:4 行填 深度(深挖)/已过面 9/9/产出(F-ID 列表或"无发现")。
  </action>
  <verify>
    <automated>awk -F'|' '$2 ~ /pipeline\.py|nls\.py|cli\.py|poller\.py/ && $0 ~ /待审/' .planning/audit/COVERAGE.md | grep -c . | grep -q '^0$' && grep -q '9/9' .planning/audit/COVERAGE.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 中 pipeline.py/nls.py/cli.py/poller.py 四行的 深度/已过面/产出 列均非"待审",已过面 = 9/9
    - findings/code.md 新增条目(如有)每条含九字段(维度/严重度/证据/修复建议/工作量/关联发现/上线判定/状态 + 标题行),证据字段含 `@ 5927f36` 与 blockquote 引用片段
    - 严重度理由行匹配格式 `影响:.*;可能性:` 且不含任何数值评分
    - D14-1/D14-2 证据(定义形态与行号)已记入 COVERAGE 对应行备注或深挖点登记节,未在本计划内下债务裁定
  </acceptance_criteria>
  <done>四大模块深挖完毕,发现入台账、覆盖行落格、D14 证据就位待 03-07 裁定。</done>
</task>

<task type="auto">
  <name>Task 2: 其余 10 模块普审(manifest/recovery/audio/oss_admin/transcriber/config/paths/locks/__main__/__init__,2,124 行)</name>
  <files>.planning/audit/findings/code.md, .planning/audit/COVERAGE.md, .planning/audit/HANDOFF-PHASE4.md</files>
  <read_first>
    - .planning/audit/findings/code.md(当前编号状态)
    - .planning/audit/DO-NOT-FIX.md(DNF-01 whisper 桩——transcriber.py 对照点,撞见不立发现)
    - .planning/audit/COVERAGE.md(本任务要填的 10 行)
    - .planning/audit/scans/ruff-extended.md 与 .planning/audit/scans/vulture.md(销号确认项中属这 10 模块的行)
  </read_first>
  <action>
    逐模块 `git show 5927f36:<path>` 完整读 + 9 面过(方法同 Task 1):
    - `manifest.py`(473):manifest.json/transcript.json 组装的字段完整性、写入时序(先 tmp 后 rename)。
    - `recovery.py`(465):启动扫描的 .part/.tmp 清理与重转写判定——数据丢失面重点(误删/漏删分支)。
    - `audio.py`(412):ffprobe/ffmpeg 子进程调用的错误处理、临时文件泄漏、失败归档 inbox/failed/ 路径。
    - `oss_admin.py`(242):`object_key_for` 往返校验——契约类观察一律只记 HANDOFF 移交(→ Phase 2 矩阵已有覆盖则注明矩阵行号),不在本维度下判断(成功判据 4/D-11)。
    - `transcriber.py`(183):HYP-19 关联的 Protocol 隔离充分性;whisper-local 桩按 DNF-01 对照跳过不立发现。
    - `config.py`(150):HYP-08 证据点——MaskedSecret 实现、chmod 600 校验、`.get_secret_value()` 调用面的泄漏可能;此处只采证据,HYP-08 回填在 03-04(需 FC env.py 侧证据合并)。
    - `paths.py`(117)/`locks.py`(64)/`__main__.py`(11)/`__init__.py`(7):普审;paths 的 .env 向上搜索逻辑关注环境假设面。
    发现/覆盖/移交落格规则同 Task 1;DOC/TEST 维度顺带证据(如注释与文档漂移线索)按 D-11 记 HANDOFF-PHASE4.md,HYP 状态不动。
  </action>
  <verify>
    <automated>awk -F'|' '$4 ~ /CODE/ && $2 ~ /soniscope_worker/ && $0 ~ /待审/' .planning/audit/COVERAGE.md | grep -c . | grep -q '^0$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - COVERAGE.md 中 CODE 维度、路径含 soniscope_worker 的全部 14 行(含 Task 1 的 4 行)均无"待审"残留
    - transcriber.py 行备注含 DNF-01 对照说明(whisper 桩不立发现)
    - config.py 的 HYP-08 证据(file:line @ 5927f36)已记入 COVERAGE 备注或深挖点登记节,HYP-08 状态本计划未改动
    - oss_admin.py 的契约观察在 HANDOFF-PHASE4.md 或 COVERAGE 备注中标注移交去向(Phase 2 矩阵行号或 Phase 4)
  </acceptance_criteria>
  <done>worker 14 模块全部覆盖落格,worker 侧发现齐备,跨维度证据已移交。</done>
</task>

<task type="auto">
  <name>Task 3: HYP-10/16/19 回填与销号表去向反填</name>
  <files>.planning/audit/HYPOTHESES.md, .planning/audit/scans/ruff-extended.md, .planning/audit/scans/vulture.md, .planning/audit/scans/gates-baseline.md</files>
  <read_first>
    - .planning/audit/HYPOTHESES.md(HYP-10/16/19 现有条目结构——回填只动状态行并增补证据/备注行,不动来源与假设正文;HYP-02 备注是部分证伪措辞先例)
    - .planning/phases/03-component-toolchain-deep-dive/03-PATTERNS.md(§HYPOTHESES.md 回填格式建议:状态行 `证实/证伪/细化 — <一句结论>`,下增 `- **证据:**` 行)
    - .planning/audit/scans/ruff-extended.md(确认项中去向仍为"深挖线索(03-03…)"的行)
  </read_first>
  <action>
    1. HYP 回填(D-09,格式按 PATTERNS 建议定稿):HYP-10(Worker 单进程串行)、HYP-16(单机单用户轮询)、HYP-19(NLS 2018 API)三条,状态行改为 `证实/证伪/细化 — <一句结论>`,新增 `- **证据:**` 行(file:line @ 5927f36,可多处)。HYP-10/16 属 CONCERNS 自评"可接受"类:按 D-10 以上线语境裁定自评是否成立——成立则回填后在备注行标注 RPT-06 优点候选或 DNF 候选身份,不占发现 ID(D-12);不成立则正常在 findings/code.md 立发现并在 HYP 备注引用 F-ID。文件尾部斜体统计行同步更新回填计数。
    2. 销号表去向反填:scans/ 三份 Python 档案中去向为"深挖线索(03-03…)"的确认行,回填最终下落——立了发现的写 F-CODE-NN;核实后不构成发现的改写一句降级理由(此类改写要如实,不得为凑数硬立发现)。
    3. 收尾:零 diff 验证(`git diff --stat 5927f36 -- apps/ scripts/ docs/` 为空)。
  </action>
  <verify>
    <automated>for id in HYP-10 HYP-16 HYP-19; do grep -A9 "^### ${id}:" .planning/audit/HYPOTHESES.md | grep -m1 '状态' | grep -q '未验证' && exit 1; grep -A9 "^### ${id}:" .planning/audit/HYPOTHESES.md | grep -q '5927f36' || exit 1; done && test -z "$(git diff --stat 5927f36 -- apps/ scripts/ docs/)" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - HYP-10/16/19 状态行为 证实/证伪/细化 之一 + 一句结论,各含至少一处 `file:line @ 5927f36` 证据行
    - "可接受成立"的条目备注含 RPT-06 或 DNF 候选标注;"不成立"的条目备注引用对应 F-CODE ID
    - scans/ Python 三档确认行去向列不再含"03-03"占位(全部为 F-ID 或降级理由)
    - HYPOTHESES.md 尾部统计行反映新回填计数
    - 零 diff 验证输出为空
  </acceptance_criteria>
  <done>worker 层三条 HYP 闭环,销号表下落完整,基线零污染。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git show 引用片段 → findings/code.md | 引用 config.py 秘密处理代码时可能截入敏感样例值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Information Disclosure | findings/code.md 证据片段 | high | mitigate | 引用片段只截标识符与结构,不截任何真实/示例秘密值(CHARTER 红线,DO-NOT-FIX.md:41 写法先例) |
| T-03-02 | Tampering | 审计基线 | high | mitigate | 取证仅 git show(禁读改工作树);每任务零 diff 快查,收尾零 diff 验证 |
</threat_model>

<verification>
- COVERAGE.md CODE 维度 soniscope_worker 14 行全部落格(已过面 9/9)
- findings/code.md 每条新发现九字段齐备、证据含 @ 5927f36 引用片段
- HYP-10/16/19 回填完毕;零 diff 为空
</verification>

<success_criteria>
- apps/worker 层的债务与脆弱区域全部经人工核实进入台账(成功判据 1 的 worker 份额)
- 契约类观察零判断、全移交(成功判据 4)
</success_criteria>

<output>
Create `.planning/phases/03-component-toolchain-deep-dive/03-03-SUMMARY.md` when done
</output>
